# ATM Bug and Optimization Backlog

This backlog captures ordinary ATM product/framework bugs, dogfood failures, workflow friction, and optimization ideas. It is not the release incident deny-list. Use `docs/INCIDENT_RESPONSE.md` and `known-bad-versions.json` only for published releases that can cause data loss, corrupt governance state, violate licensing constraints, or ship critical release-trust defects.

AI routing note: use `.agents/skills/atm-bug-backlog/SKILL.md` before writing backlog entries. The skill must first classify whether the issue belongs to ATM or to another project repo.

2026-07-11 follow-up:

- `ATM-BUG-2026-07-07-048` is now fixed by running cached onefile dist in-process instead of spawning a second Node process for every invocation. `npm run validate:next-warm-run-latency -- --json` passed on Windows with `cliLogicMs: 39`, warm-run `p50Ms: 1693.09`, `p95Ms: 5195.47`, and `wrapperOverheadMs: 1998.98`, all within the declared budgets. `npm run typecheck`, runner entrypoint regression, and frozen release build also passed.
- `ATM-BUG-2026-07-11-091` records the route friction found while starting this closeout: backlog IDs were treated as missing ledger task IDs by prompt-scoped `next`, then misrouted to `create-atom` by `start`.

Do not record non-ATM project bugs here. For adopter/app/game/site/data bugs, create or update `docs/governance/project-bug-and-optimization-backlog.md` inside the affected repo.

2026-07-09 sync note:

- `ATM-BUG-2026-07-09-059` is fixed by adding a read-only `ATM_NEXT_TASK_SCOPE_AUDIT_ADVISORY` route for missing task-like scope when `requestedAction` is `audit` or `analyze`; mutation-oriented missing scope remains fail-closed.
- `ATM-BUG-2026-07-07-047` was already fixed before this sync; `npm run validate:prompt-scoped-next` now covers broad all/open/remaining task-card prompts and confirms they route to the discovered batch queue instead of `ATM_NEXT_TASK_SCOPE_NOT_FOUND`.
- `ATM-BUG-2026-07-08-056` remains observable: after releasing and re-claiming a clean `ATM-FRAMEWORK-TEMP-codex-captain` lock, `doctor` / `tasks audit` can still report it as `stale-completed` with an old linked task even though the lock JSON no longer contains `linkedTaskId`. This is advisory and did not block `hook pre-push`, but it should be fixed before relying on stale-lock audit as a precise signal.
- `ATM-BUG-2026-07-08-056` is fixed by moving framework lock auto-reconcile audit records to `.atm/runtime/framework-lock-auto-reconcile.jsonl` and by preventing active unlabeled temp locks from inferring stale linked-task context from older actor sessions. Regression coverage lives in `validate:framework-development-governance` and `validate:task-ledger-governance`.
- `ATM-BUG-2026-07-07-045` / `ATM-BUG-2026-07-07-053` are fixed by expanding `git commit` attempt status into a recoverable incident packet: every attempt now records HEAD before/after, whether HEAD advanced, `commit-status` and retry commands, raw copyable git command, and wrapper-staged residue rollback. The deadlock reason was that outer caller timeout or empty stdout/stderr left the operator unable to tell whether the commit had already landed, so retrying risked either duplicate work or manual index archaeology.
- `ATM-BUG-2026-07-09-060` is fixed by raising the governed `git commit` wrapper default timeout from 120s to 420s. The deadlock reason was that a normal framework pre-commit path took about 277s and passed, but the wrapper killed host git at 120s, reporting `ATM_GIT_COMMIT_TIMEOUT` even though the hook was not failing.
- `ATM-BUG-2026-07-07-043` is fixed at the commit-bundle preflight layer: a task with an active `closeout-only` claim now fails before auto-staging or pre-commit hooks when the bundle contains in-scope source mutations, returning `ATM_GIT_COMMIT_CLOSEOUT_ONLY_MUTATION` and the exact `next --claim --claim-intent write` recovery command.
- `ATM-BUG-2026-07-07-046`, `ATM-BUG-2026-07-07-049`, and `ATM-BUG-2026-07-07-050` were already fixed in the taskflow close atomicity lane: taskflow close commits now resolve actor-scoped git identity, commit from a temporary index, reset task-owned live-index residue after successful close commits, and share dry-run/write closeback-path readiness. The stale backlog rows were causing governance rework loops, so this sync marks the source-backed fixes as closed.
- `ATM-BUG-2026-07-08-056` is fixed by keeping framework lock auto-reconcile audit in `.atm/runtime/framework-lock-auto-reconcile.jsonl` instead of the protected history evidence singleton. This prevents `git commit --auto-stage` from picking up an uncommittable protected evidence file after same-actor stale temp-lock cleanup.
- `ATM-BUG-2026-07-07-047`, `ATM-BUG-2026-07-07-052`, and `ATM-BUG-2026-07-07-054` are fixed and verified: blanket all/open task-card prompts route to batch instead of `ATM_NEXT_TASK_SCOPE_NOT_FOUND`; protected override audit events survive/stage correctly; resumable validator runs expose status/resume behavior. The table rows were stale, so this sync removes three false High-priority governance blockers from the queue.
- `ATM-BUG-2026-06-15-008` is fixed by keeping cross-repo planning absolute paths out of Team Agents `file.write` leases: target repo write paths still require explicit target scope, while planning-repo absolute paths can route as read-only planning context without `ATM_TEAM_WRITE_SCOPE_TRAVERSAL`. Regression coverage lives in `node --strip-types scripts/validate-team-agents.ts --case permission-lease`.
- `ATM-BUG-2026-06-15-010` was already fixed by the historical-batch evidence lane: `evidence historical-batch` writes shared envelopes and task slices, and `taskflow close --historical-batch` consumes them with commit-range and close-readiness safeguards. The row was stale and is now synced to source reality.
- Dogfood note: this repair pass also showed a small workflow friction where the agent began a two-file quickfix after code localization before acquiring the framework temp claim. The claim was acquired immediately afterward and covered the touched files, but the root reason is that the fast playbook's "claim before edit" instruction is easy to miss once a bug is localized inside a large file. Keep improving `next`/skill UX so claim scope can be suggested from discovered candidate files before the first patch.
- Dogfood note: `ATM-BUG-2026-07-09-061` is fixed by adding `check:encoding:touched` and `check:encoding:staged` npm scripts backed by ATM's canonical `guard encoding` command. The touched wrapper includes untracked text files so newly created scripts/docs are checked before first staging.
- Dogfood note: `ATM-BUG-2026-07-09-062` is fixed by aligning the framework-maintenance fast playbook with the route command. The deadlock reason was that `next --prompt` correctly returned `framework-temp-claim-required` and a `framework-mode claim` command, but the embedded fast playbook still told agents to run `next --claim`, which fails with `ATM_NEXT_CLAIM_NO_TASK` when no imported task queue exists.
- Dogfood note: `ATM-BUG-2026-07-09-063` is fixed in this pass: `taskflow open --write` correctly fell back without a profile, but the recommended `tasks new --template aao-l2-split` path then failed because the framework runner had no bundled markdown template assets. The deadlock reason was that the documented low-level generator surface advertised a default template that could not be loaded in either frozen or source-first mode. The fix adds the default template asset to the markdown task-source package and locks source/frozen generation coverage in `tests/cli/tasks-new.test.ts`.
- Dogfood note: `ATM-BUG-2026-07-09-064` is fixed in this pass: `node atm.mjs git commit` can exceed a normal agent command timeout because its pre-commit hook runs heavyweight validators such as `validate:git-head-evidence`. The commit may still complete after the caller times out, so `git commit-status` now reports live branch commit queue diagnostics, including lock path, owner PID liveness, age, current HEAD, and wait-vs-inspect guidance.
- `ATM-BUG-2026-07-07-051` was already fixed in the emergency gate lane: `ATM_EMERGENCY_LANE_APPROVAL_REQUIRED` now includes required `--allowed-flag` entries for protected flags such as `--historical-delivery`, and the message warns that omitting them will later fail with `ATM_EMERGENCY_FLAG_NOT_APPROVED`. The stale row was keeping a resolved emergency-lane UX issue in the active queue.
- `ATM-BUG-2026-07-07-044` was already fixed in the prompt-scoped next lane: re-claim with escalated intent preserves scope-amended `claim.files` instead of rebuilding only from static task scope. The stale row was keeping a resolved claim lifecycle bug in the active queue.
- `ATM-BUG-2026-07-07-042` was already fixed in the prompt-scoped next validator: `teamPlanResult` is dumped only when assertions fail, so successful runs stay transcript-friendly. The stale row was keeping a resolved validator-noise issue in the active queue.
- `ATM-BUG-2026-07-09-059` was already fixed in the prompt-scoped next audit advisory lane: retrospective audit prompts that mention non-ledger historical labels now get `ATM_NEXT_TASK_SCOPE_AUDIT_ADVISORY` instead of the hard `ATM_NEXT_TASK_SCOPE_NOT_FOUND` blocker.
- `ATM-BUG-2026-06-15-004` is verified fixed in this pass: `taskflow close --historical-delivery` now supports the explicit operator waiver lane through `--waiver-out-of-scope-delivery --reason <text>` and the `--waive-out-of-scope` alias, while historical-delivery provenance records waived out-of-scope files and the waiver reason. The stale row kept a resolved closeback UX issue open; the proof path was briefly blocked by stale `taskflow-dryrun` assertions that still expected older commit-bundle/evidence behavior.
- `ATM-BUG-2026-07-07-055` is fixed by extending the taskflow close commit-bundle regression to cover stale staged bundle manifests: a same-task `.bundle-manifest.json` staged at an older version is reset out of the live index after the temp-index close commit lands the fresh version.
- `ATM-BUG-2026-06-16-014` is fixed by the historical-batch diagnostic disposition lane: partial or diagnostic-only slices can now be explicitly finalized as `keep-diagnostic`, `abandon`, or `remove-evidence`, while close-ready slices are refused and must still go through `taskflow close --historical-batch`.
- `ATM-BUG-2026-07-07-041` is fixed by deepening default `next` output compaction: non-verbose results now keep `evidence.nextAction` as the authoritative route payload, remove duplicated top-level aliases, keep playbook message echoes compact, and make `--verbose` a real parser-supported escape hatch for the full envelope.
- Dogfood note (2026-07-10 RFT batch `0002/0004/0005/0006/0007`): mechanical-split delivery friction, done-task import lockout, closeout exit-code ambiguity, and planning-index lag were recorded as `ATM-BUG-2026-07-10-076` through `ATM-BUG-2026-07-10-080`. Planning-mirror residue after successful closeback remains tracked in `ATM-BUG-2026-07-10-074`, with additional RFT cross-repo `source.planPath` evidence added in that row.

## Mandatory Global Hotfile Inventory

| Path | Kind | Owner | Authoring rule |
| --- | --- | --- | --- |
| docs/governance/atm-bug-and-optimization-backlog.md | generated-projection | atm.governance-backlog | Write item files under docs/governance/atm-bug-and-optimization-backlog.items; rebuild this projection. |
| atomic_workbench/atomization-coverage/path-to-atom-map.json | generated-projection | atm.atom-map | Write owner shard files under atomic_workbench/atomization-coverage/path-to-atom-map-shards; rebuild the projection. |
| docs/governance/team-agents/cross-vendor-handoff-ledger.md | generated-projection | atm.team-vendor-handoff | Write canonical JSON records; regenerate the Markdown projection. |
| .atm/history/tasks/*.json | ledger-records | atm.task-ledger | Use ATM CLI lifecycle commands; do not edit runtime ledger records by hand. |
| .atm/runtime/locks/*.json | runtime-locks | atm.lock-runtime | Use ATM claim, release, repair, and broker commands. |

## Generated Backlog Projection

<!-- ATM-GENERATED-BACKLOG-PROJECTION: items-dir=docs/governance/atm-bug-and-optimization-backlog.items -->

| ID | Date | Repo | Type | Severity | Status | Area | Finding | Expected behavior | Evidence / Repro | Follow-up |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| ATM-BUG-2026-06-15-001 | 2026-06-15 | AI-Atomic-Framework | Bug | High | Fixed in TASK-TEAM-0029 / 48d5b55a | Team Agents | `team start` could create a team runtime run even when normal task claim dependency gates would reject the task. | Team Agents start should share claim-admission parity with normal task lifecycle and fail closed before writing runtime state. | Dogfood on `TASK-APO-0029`: `next --claim` was blocked by missing dependency `TASK-APO-0028`, while `team start` still wrote `team-4d7bc0109ef7`. | Keep regression coverage in `scripts/validate-team-agents.ts --case claim-gate-parity`. |
| ATM-BUG-2026-06-15-002 | 2026-06-15 | AI-Atomic-Framework | Bug | High | Fixed in TASK-TEAM-0029 / 48d5b55a | Task lifecycle | `tasks close --status abandoned` attempted to stage `.atm/history/evidence/<task>.json` even when no evidence file exists, causing `git add` failure after the ledger had already mutated. | Abandoned close should require only a reason, write ledger/event state, and stage only artifacts that actually exist. | Reproduced while abandoning `TASK-TEAM-0028` and `TASK-APO-0029`; both close attempts mutated live ledger then failed on missing evidence staging. | Add a focused lifecycle regression test for reason-only abandoned close. |
| ATM-BUG-2026-06-15-003 | 2026-06-15 | AI-Atomic-Framework | Bug | Medium | Fixed in TASK-TEAM-0029 / 48d5b55a | Git governance | Abandoned close did not open a close-commit-window, so follow-up governance commit could require emergency/no-verify paths even after close mutation succeeded. | Abandoned close should open a short-lived commit window covering the ledger/event artifacts that exist. | `TASK-APO-0029` abandoned cleanup required native `git commit --no-verify` after the ATM wrapper rejected the commit due to missing active work session. | Add validation that abandoned close can be committed through the ATM git wrapper without a fresh claim. |
| ATM-BUG-2026-06-15-004 | 2026-06-15 | AI-Atomic-Framework | Optimization | Medium | Fixed in this cleanup commit | Closeback UX | `taskflow close --historical-delivery` used to detect out-of-scope historical delivery but could not complete the approved waiver path itself, routing the operator back to protected `tasks close` plus emergency approval. | Operator lane should support a governed, explicit out-of-scope historical-delivery waiver path without forcing direct backend usage when dry-run already identifies the exact blocker. | Verified current source supports `taskflow close --historical-delivery <ref> --waiver-out-of-scope-delivery --reason <text> --write --json` plus the `--waive-out-of-scope` alias, and `buildHistoricalDeliveryProvenance` records `waivedOutOfScopeFiles` / `waiverReason`. Validation: `node --experimental-strip-types packages/cli/src/commands/taskflow/__tests__/taskflow-dryrun.spec.ts`; `node --experimental-strip-types packages/cli/src/commands/tasks/__tests__/historical-delivery.test.ts`. | Keep the waiver and provenance coverage in the focused taskflow/historical-delivery tests; future closeback UX changes must not route approved out-of-scope historical deliveries back to emergency-only backend usage. |
| ATM-BUG-2026-06-15-005 | 2026-06-15 | AI-Atomic-Framework | Optimization | Medium | Fixed in 2026-06-28 onefile extraction lock lane | Runner / build UX | Running `npm run build` and a frozen-runner command in parallel can expose a transient onefile cache path failure. | Frozen runner cache refresh should be atomic or runner commands should wait/retry when the cache is being rebuilt. | Repaired on 2026-06-28 by moving the onefile launcher to an atomic extraction protocol: payload extraction now takes a per-payload lock, writes into a unique staging directory, publishes readiness only after the payload is complete, and lets concurrent runners wait/retry against the same cache root instead of crashing on a half-refreshed tree. The launcher also now honors `ATM_ONEFILE_CACHE_ROOT`, which makes isolated validation and cache hygiene deterministic. Regression coverage lives in `scripts/validate-onefile-release.ts`, and `scripts/validate-onefile-budget.ts --json` now also reports extraction-lock handoff results alongside startup timing. | Keep extraction-lock wait coverage in `scripts/validate-onefile-release.ts` and preserve the lock-handoff summary in `scripts/validate-onefile-budget.ts`; future onefile cache changes must not reintroduce partial-tree visibility during rebuild. |
| ATM-BUG-2026-06-15-006 | 2026-06-15 | AI-Atomic-Framework | Documentation / AI routing | High | Fixed by `.agents/skills/atm-bug-backlog` | Agent onboarding | The assistant could not find `ATM Bug and Optimization Backlog` when asked to update the bug record, because the backlog was not exposed through a dedicated skill or canonical repo path. | AI routing should trigger a skill that points directly to the correct backlog. | Initial searches found `docs/INCIDENT_RESPONSE.md` and `docs/governance/redteam-drift-defects.md`, but not this backlog. | Keep `.agents/skills/atm-bug-backlog/SKILL.md` as the canonical trigger surface for bug/optimization recording. |
| ATM-BUG-2026-06-15-007 | 2026-06-15 | AI-Atomic-Framework | Documentation / AI routing | High | Fixed by `.agents/skills/atm-bug-backlog` update | Backlog ownership | The first backlog skill treated all bug/optimization records as ATM backlog entries and did not distinguish ATM framework bugs from adopter/project bugs. | The skill should route ATM product bugs to the ATM backlog and non-ATM project bugs to a repo-local project backlog. | User correction: "這個 skill 應該是要區別紀錄的是 ATM 的 bug 還是 其他專案的 bug". | Preserve the owner classification table in the skill and use `docs/governance/project-bug-and-optimization-backlog.md` for non-ATM repos. |
| ATM-BUG-2026-06-15-008 | 2026-06-15 | AI-Atomic-Framework | Bug | High | Fixed in this cleanup commit | Team Agents | `team plan` / `team validate` reject imported cross-repo planning tasks whose `scopePaths` are absolute 3KLife paths, so Phase 0 planning tasks cannot start through Team Agents even after broker cleanup. | Team Agents should normalize or classify cross-repo planning paths before building `file.write` leases, or choose a read-only/planning recipe that does not request unsafe writes. | Fixed by deriving Team Agents write scope from target repo paths only: `targetAllowedFiles` is authoritative when present, target-root absolute paths normalize to repo-relative write leases, and planning-repo absolute paths become read-only planning context instead of `file.write` lease paths. `node --strip-types scripts/validate-team-agents.ts --case permission-lease` now creates a temp target/planning repo fixture and proves planning absolute paths do not raise `ATM_TEAM_WRITE_SCOPE_TRAVERSAL` or emit an empty `file.write` lease. | Keep the cross-repo planning fixture in `scripts/validate-team-agents.ts`; future Team Agents scope changes must preserve target-write fail-closed behavior while allowing planning-only absolute context. |
| ATM-BUG-2026-06-15-009 | 2026-06-15 | AI-Atomic-Framework | Optimization | High | Fixed in 2026-06-28 claim-readiness / latency lane | Claim / lifecycle routing | `tasks claim` and `taskflow close` for CID tasks took many rounds of reserve/promote/status probes and even a 300s `next --claim` timeout before surfacing the real blocker, so the operator path felt slow even though `path-to-atom-map.json` is only ~56 KB. | Claim should fail fast or converge quickly with one blocker summary when lifecycle state is already inconsistent; sharding should reduce shared-file contention, but it should not be needed to explain long claim latency. | Closed on 2026-06-28 by teaching `packages/cli/src/commands/next.ts` to emit a structured `claimReadiness` triage whenever `next --claim` cannot select a claimable task, including first blocker summaries for import-required, review closeout-only, dependency-closeout, and unscoped-prompt cases instead of a generic no-task answer. The same lane now emits `atm.claimLatencyTelemetry.v1` from both `next --claim` and `tasks claim`, so operators can see whether time was spent in parallel preflight, reserve/promote preparation, claim acquisition, or direction-lock writes. Focused regression coverage landed in `packages/cli/src/commands/next/__tests__/claim-readiness.test.ts`, while existing `packages/cli/src/commands/tasks/__tests__/dependency-gate.test.ts`, `packages/cli/src/commands/tasks/__tests__/lifecycle-state.test.ts`, `scripts/validate-prompt-scoped-next.ts`, and `scripts/validate-cli.ts --mode validate` continue to guard the broader claim route. | Keep the structured `claimReadiness` envelope and `atm.claimLatencyTelemetry.v1` attached to claim entrypoints; future claim / close routing changes must preserve first-blocker surfacing before operators fall back to manual reserve/promote/status probe loops. |
| ATM-BUG-2026-06-15-010 | 2026-06-15 | AI-Atomic-Framework | Optimization | High | Fixed in historical-batch evidence lane | Historical evidence / taskflow close | Completed multi-task delivery packages currently force operators to close or evidence tasks one by one, even when the real delivery landed as a coherent commit bundle with shared validators. This creates noisy after-the-fact evidence work and makes it hard to distinguish historical attestation from original live evidence. | ATM should support a governed historical batch evidence envelope: one delivery commit range, one validator set, one artifact manifest, and per-task scoped evidence slices that preserve each task acceptance boundary. The evidence must be marked historical, not fresh live execution. | Source reality now satisfies this contract: `packages/cli/src/commands/evidence.ts` implements `evidence historical-batch`, `packages/cli/src/commands/taskflow.ts` and `tasks close` consume `--historical-batch`, `docs/governance/historical-batch-evidence.md` documents the operator flow, and `packages/cli/src/commands/tasks/__tests__/historical-delivery.test.ts` plus `packages/cli/src/commands/taskflow/__tests__/taskflow-dryrun.spec.ts` cover complete slices, unmatched safeguards, envelope staging, and per-task close readiness. | Keep historical-batch command/spec/docs/tests aligned; remaining partial-slice disposition is separately tracked by `ATM-BUG-2026-06-16-014`. |
| ATM-BUG-2026-06-16-011 | 2026-06-16 | AI-Atomic-Framework | Product gap | High | Fixed in 2026-06-28 deferred-governance diagnosis lane | Pre-commit governance / framework-mode | Pre-commit governance blocks can fail a legitimate framework-repo commit because `framework claim` / direction-lock state is missing, even when the diff content, validators, and historical governance intent are otherwise correct. The hook currently treats governance timing gaps as a hard commit failure instead of distinguishing them from content-integrity failures. | Framework-repo pre-commit should surface a precise deferred-governance diagnosis, and historical-batch or closeback flows should have an explicit, governed path to satisfy or defer framework claim / direction-lock requirements without forcing blanket `--no-verify`. | Closed on 2026-06-28 by teaching `packages/cli/src/commands/hook.ts` to classify pre-commit failures into governance-state versus content-validation lanes instead of flattening everything into one generic block. `failureEnvelope` now exposes `governanceStateFailures`, `contentValidationFailures`, and `deferredGovernanceCandidate`, while governance-only failures surface the dedicated hook code `ATM_HOOK_PRE_COMMIT_DEFERRED_GOVERNANCE_REQUIRED` so operators can tell that the staged content is not failing validation and the remaining blocker is claim/governance timing. Regression coverage was added to `scripts/validate-git-hooks-enforcement.ts`, which now stages a framework-critical file in a fixture repo, forces validator success, and proves the hook reports the deferred-governance marker instead of a generic content-failure-only diagnosis. | Keep the deferred-governance split in `packages/cli/src/commands/hook.ts` and the fixture-backed regression in `scripts/validate-git-hooks-enforcement.ts`; future hook refactors must preserve a first-class governance-state diagnosis whenever the staged content is otherwise validator-clean. |
| ATM-BUG-2026-06-16-012 | 2026-06-16 | AI-Atomic-Framework | Optimization | High | Fixed in 2026-06-28 historical-batch validator attestation lane | Historical batch evidence / validators | `taskflow close --historical-batch` can use a batch envelope slice, but each task close still needs fresh per-task `evidence run` validator records. `evidence run --recent-run` records `historical-reference`, which is useful for diagnosis but not enough for close, so a 4-card batch still reruns expensive validators multiple times. | A batch close wave should support one fresh validator execution set with signed per-task validator slices, preserving task-specific acceptance while avoiding repeated `npm test` / `typecheck` runs for the same delivery commit. | Closed on 2026-06-28 by splitting historical-batch task evidence into two coordinated records inside `packages/cli/src/commands/evidence.ts`: the delivery provenance record stays `historical-reference`, while a second `atm.historicalBatchValidatorAttestation.v1` record carries the same proof-backed `commandRuns` plus reusable fresh validator passes for that task slice. This keeps historical delivery semantics intact but lets close/evidence gates consume one shared fresh validator execution wave without rerunning the same validator per task. Regression coverage now lives in `packages/cli/src/commands/tasks/__tests__/historical-delivery.test.ts`, which proves the historical slice is still historical-reference while `verifyTaskEvidence(..., gate: 'close')` passes from the fresh attestation slice alone. Validation also re-ran `scripts/validate-governance-commands.ts --mode validate` to keep the broader evidence/governance surface green. | Preserve the dual-record contract: historical delivery provenance must remain historical-reference, while reusable same-wave validator proof stays fresh and command-backed. Future evidence refactors must not collapse these back into one stale record or force per-task validator reruns after a batch envelope already executed them successfully. |
| ATM-BUG-2026-06-16-013 | 2026-06-16 | AI-Atomic-Framework | Bug | High | Fixed in `5f1a4548` | Historical batch evidence / commit bundle | `taskflow close --historical-batch` writes per-task closure bundles but does not stage the referenced historical batch envelope, leaving `.atm/history/evidence/historical-batches/<batch>.json` untracked while closure packets reference it. | The close commit bundle should include the referenced historical batch envelope, or create a governed batch-envelope commit before task-level closure packets can reference it. | After closing `TASK-CID-0103`, `git status --short` still showed `?? .atm/history/evidence/historical-batches/hist-batch-2026-06-16T01-40-43-634Z.json`; closure output referenced that same batch path as the source of the slice. Fast repair `5f1a4548` added envelope staging and dogfood dry-runs for `TASK-CID-0105` / `TASK-CID-0107` showed the batch envelope in `governedCommitBundle.targetRepo.stageFiles`. | Keep regression coverage in `packages/cli/src/commands/taskflow/__tests__/taskflow-dryrun.spec.ts`. |
| ATM-BUG-2026-06-16-014 | 2026-06-16 | AI-Atomic-Framework | Product gap | Medium | Fixed in historical-batch diagnostic disposition lane | Historical batch evidence / partial slices | Historical batch import can leave unclosed partial or diagnostic-only task slices as untracked `.atm/history/tasks`, evidence, and task-events, while completed slices close cleanly. The operator previously had no clear governed command to archive, abandon, or commit diagnostic-only slices without pretending they are done. | Batch evidence should provide an explicit partial-slice disposition: keep diagnostic evidence, abandon with reason, or remove generated diagnostic evidence through a governed cleanup path. Close-ready slices must remain fail-closed to taskflow close. | Fixed by `node atm.mjs evidence historical-batch-finalize`: it reads the batch envelope, refuses `okToCloseTask: true` slices, records `atm.historicalBatchTaskSliceDisposition.v1` in the envelope, appends audit evidence for `keep-diagnostic`/`abandon`, and supports `remove-evidence` that removes only task evidence records referencing that batch. | Keep `packages/cli/src/commands/tasks/__tests__/historical-delivery.test.ts` coverage for approved diagnostic batch finalization, `remove-evidence` cleanup, and close-ready finalize refusal. |
| ATM-BUG-2026-06-18-015 | 2026-06-18 | AI-Atomic-Framework | Bug | High | Fixed in 2026-06-28 branch queue validation lane | Git governance / multi-agent commit lane | Two Codex agents can both pass local governance checks, then collide at final `git commit` with `fatal: cannot lock ref 'HEAD'` because another agent moved `HEAD` between wrapper preflight and commit execution. The wrapper surfaces the raw failure, but ATM does not yet serialize the final branch-tip mutation through a steward lane or retry on safe fast-forward drift. | When governed commits target the same branch concurrently, ATM should funnel the final branch-tip mutation through a serialized steward lane or equivalent commit queue. At minimum, the wrapper should detect `HEAD` drift, re-evaluate branch state, and emit a first-class queue/retry path instead of a raw Git race. | The current source now satisfies that contract: `packages/cli/src/commands/git-governance.ts` serializes final commit mutation through `withBranchCommitQueueLock(...)`, emits `ATM_GIT_COMMIT_BRANCH_QUEUE_BUSY` / `ATM_GIT_COMMIT_BRANCH_QUEUE_RACE`, records `atm.branchCommitQueueEvidence.v1`, and explicitly detects raw `cannot lock ref 'HEAD'` races. Taskflow write-readiness already exposes the same guard via `branchCommitQueueGate`, and on 2026-06-28 this lane promoted the queue regression into a first-class validator surface with `scripts/validate-branch-commit-queue.ts`, `package.json`, and `scripts/validators.config.json`, so standard validation now proves both the taskflow busy gate and the governed commit retry contract remain wired in. | Keep the new branch queue validator in standard profiles; future git-governance refactors must preserve queue serialization, busy/race retry codes, and taskflow close write-readiness integration instead of falling back to raw Git `HEAD` race failures. |
| ATM-BUG-2026-06-19-016 | 2026-06-19 | AI-Atomic-Framework | Product gap | High | Fixed in 2026-06-28 residue-classifier hardening | Git governance / residue hygiene | ATM hook and governed commit flows can generate transient residue such as `git-head` evidence files or close-window snapshots, then leave them unstaged or out-of-scope after a failed or completed commit attempt. Agents rarely clean these files proactively, so the repo accumulates garbage and later governance checks become noisy. | ATM should classify auto-generated residue explicitly and either auto-clean safe byproducts or hard-block the commit with a precise diagnostic. User-authored dirt must never be silently deleted, but deterministic unreferenced byproducts should not be left behind for humans to clean manually. | On 2026-06-19, a focused governed commit added `packages/cli/src/commands/taskflow/__tests__/close-gates-focused.spec.ts`, then hook-generated `.atm/history/evidence/git-head.jsonl` remained as dirty residue after the final commit race. The same repo also shows recurring snapshot-style runtime residue under `.atm/runtime/snapshots/` from abandoned close/commit attempts. Reconfirmed on 2026-06-24 while dogfooding `TASK-CID-0120`: failed same-repo `taskflow close --write` attempts left stale close transition artifacts such as `.atm/history/task-events/TASK-CID-0120/2026-06-24T06-41-36-463Z-close-98d343f463cb.json`, which then became fresh blockers for later retries. Resolved on 2026-06-28 by hardening the shared residue classifier so `hook pre-commit` and the governed git wrapper now distinguish current-task governance artifacts from foreign closeback residue, auto-clean safe ATM byproducts such as transient task-import / protected-override reports, and fail closed on foreign closure packets or close/reconcile task-events with focused regression coverage. | Keep monitoring for any newly introduced ATM-generated artifact families and add them to the shared classifier only when ownership or reproducibility can be proven. |
| ATM-BUG-2026-06-22-017 | 2026-06-22 | AI-Atomic-Framework | Bug | High | Fixed in 2026-06-28 broker parity lane | Broker register / payload integrity | `broker register` trusts the intent-file payload over CLI `--task` and `--actor`, so a mismatched or cross-contaminated intent file can silently register another task's write intent under the wrong operator flow. | `broker register` should fail closed when CLI flags disagree with the payload, and the error should identify the mismatched fields before any registry mutation occurs. | Repaired on 2026-06-28 by hardening `packages/cli/src/commands/broker.ts`: `broker register` now enforces payload/flag parity before arbitration or registry writes, and throws `ATM_BROKER_REGISTER_PAYLOAD_FLAG_MISMATCH` with per-field mismatch details when `--task` and/or `--actor` disagree with the intent file. A new focused regression in `scripts/validate-broker-register.ts` proves mismatched task/actor pairs fail closed without mutating `.atm/runtime/write-broker.registry.json`, while matching payloads still register cleanly. The command surface in `packages/cli/src/commands/command-specs/broker.spec.ts` now also advertises `--actor` as a register parity input, not only a runtime-activate input. | Keep the payload/flag parity regression in `scripts/validate-broker-register.ts`; future broker register changes must preserve fail-closed behavior before any registry mutation when CLI authority and payload disagree. |
| ATM-BUG-2026-06-23-018 | 2026-06-23 | AI-Atomic-Framework | Bug | High | Fixed in 2026-06-28 hook runner routing lane | Governance lifecycle / framework hook lane | Framework source can fix pre-commit protected-state logic, but the repo-local Git hook still hard-codes `node atm.mjs hook pre-commit --json`, so batch `deliver-and-close` and pending commit windows continue to execute the stale frozen hook path until a release rebuild/install step updates the hook lane. | In the framework repo, source-first governance validation should have an explicit, governed way to run the matching hook logic for the same source tree being validated, or the hook installer should understand a framework-development mode instead of silently pinning every commit to the frozen runner. | The core `.atm/git-hooks/*` generator had already been repaired to arbitrate between `atm.mjs` and `atm.dev.mjs` in framework repositories, but the surrounding install surface still drifted: `templates/enforcement/pre-commit.sh` and `examples/git-hooks-enforcement/hooks/pre-commit` continued to hard-wire `node atm.mjs hook pre-commit --json`, leaving source-first framework installs and copied examples on the stale frozen lane. Closed on 2026-06-28 by lifting the same runner arbitration contract into the enforcement template and example hook, so framework repos now select `atm.dev.mjs` whenever source markers are present while adopters still fall back to `atm.mjs`. The repair also promoted direct hook-runner routing coverage into the validator surface via `scripts/validate-hook-runner-routing.ts`, `package.json`, and `scripts/validators.config.json`, while `scripts/validate-git-hooks-enforcement.ts` now verifies the template/example entrypoints use the same selected-runner contract instead of a hard-coded frozen call. Validation: `node --strip-types scripts/validate-hook-runner-routing.ts`, `node --strip-types scripts/validate-git-hooks-enforcement.ts --mode validate`, plus post-build frozen release verification. | Keep hook runner selection centralized around the shared framework marker contract, and preserve the new validator so future template/example or installer edits cannot drift back to a stale frozen-only hook lane. |
| ATM-BUG-2026-06-24-019 | 2026-06-24 | AI-Atomic-Framework | Bug | High | Fixed in 2026-06-28 hook-scope filtering lane | Taskflow close / pre-commit scope filtering | Same-repo `taskflow close` could be blocked by unrelated active planning-mirror / direction-lock findings because the pre-commit protected-state checks evaluated all active direction locks instead of only the locks relevant to the staged governed bundle. | Pre-commit scope checks for taskflow close should filter active direction locks down to the staged bundle so foreign planning mirrors do not raise false `ATM_PLANNING_MIRROR_DRIFT` or `ATM_TASK_DIRECTION_SCOPE_DRIFT` failures. | Verified on 2026-06-28 that the source fix is present in `packages/cli/src/commands/hook.ts` via `selectRelevantDirectionLocksForCommit(...)`, and the focused same-repo close regression passes in `packages/cli/src/commands/__tests__/framework-mode-staged-residue.spec.ts`. This repair lane promoted that regression into a first-class validator surface through `scripts/validate-framework-hook-scope-filtering.ts`, `package.json`, and `scripts/validators.config.json`, so standard ATM validation now guards the foreign-planning-lock case instead of leaving it as an ad hoc test file. Validation used `node --strip-types packages/cli/src/commands/__tests__/framework-mode-staged-residue.spec.ts`, `npm run validate:framework-hook-scope-filtering`, and post-build frozen release verification. | Keep `selectRelevantDirectionLocksForCommit(...)` as the common staged-bundle filter for direction-lock scope checks, and preserve the new validator in standard profiles so same-repo close bundles cannot regress back to foreign planning-lock false positives. |
| ATM-BUG-2026-06-24-020 | 2026-06-24 | AI-Atomic-Framework | Bug | High | Fixed in 2026-06-28 taskflow close atomicity lane | Taskflow close / planning mirror closeback | Same-repo `taskflow close` can mutate the planning card to `done` without a fully governed closeback packet for the planning mirror, causing task audit / protected-state failures such as manual-done metadata gaps, missing planning transition events, or stale planning close events that poison later retries. | Planning-mirror closeback should write a complete governed close transition for the mirror task, stage that transition event with the same close bundle, and roll it back cleanly if the governed commit fails. | Reproduced repeatedly on `TASK-CID-0120` with failures including `ATM_TASK_AUDIT_TRANSITION_EVIDENCE_MISSING`, `ATM_TASK_AUDIT_MANUAL_DONE`, and later `ATM_TASK_AUDIT_TRANSITION_EVENT_MISSING` / `ATM_PROTECTED_STATE_TASK_TRANSITION_COMMAND_INVALID`. Verified on 2026-06-28 that the governing source path now covers the whole close transaction: `applyPlanningCardCloseback(...)` writes frontmatter plus planning transition evidence, `buildTaskflowCommitBundle(...)` exact-stages that planning transition inside the same governed bundle, and `rollbackCloseWriteTransaction(...)` deletes the newly created planning transition artifact and restores the prior planning card snapshot if the governed commit fails. This lane elevated the end-to-end regression in `packages/cli/src/commands/taskflow/__tests__/taskflow-close-atomicity.test.ts` into the standard validator surface via `scripts/validate-taskflow-close-atomicity.ts`, `package.json`, and `scripts/validators.config.json`, so release validation now guards both the success path and the rollback path instead of leaving them as an ad hoc test file. Validation: `node --strip-types packages/cli/src/commands/taskflow/__tests__/taskflow-close-atomicity.test.ts`, `npm run validate:taskflow-close-atomicity`, plus post-build frozen release verification. | Keep the planning closeback atomicity validator in standard profiles; any future same-repo closeback refactor must preserve staged planning transition evidence and rollback cleanup as one governed transaction. |
| ATM-BUG-2026-06-27-021 | 2026-06-27 | AI-Atomic-Framework | Bug | Medium | Fixed in 2026-06-28 repair lane | Git governance / worktree hygiene | A normal framework worktree can carry repo-local `.git/config` state with `core.bare=true`, causing standard `git -C <repo> status` and Git-backed evidence commands to fail until the local config is repaired. | ATM preflight, doctor, or framework-mode status should detect a bare/worktree mismatch and report or repair the safe local setting before benchmark/evidence work begins. | Repaired on 2026-06-28 by adding a shared Git worktree-readiness probe used by `doctor`, `git-head-evidence`, and `framework-mode`. `doctor` now fails with `ATM_DOCTOR_GIT_WORKTREE_BARE_MISMATCH` and recommends `git config --local core.bare false`; `framework-mode status` surfaces a `git-worktree-readiness-failed` blocker; `framework-mode claim` fail-closes with `ATM_GIT_WORKTREE_READY_REQUIRED` before taking a temporary claim on a broken worktree. Regression coverage was added to `scripts/validate-git-head-evidence.ts` and `scripts/validate-framework-development-governance.ts`, and frozen-runner coverage was revalidated through `validate:root-drop-release` plus `validate:onefile-release`. | Keep the shared readiness probe as the single source of truth for worktree-backed Git diagnostics, and route future Git-backed preflight lanes through it instead of re-implementing ad hoc `git status` probes. |
| ATM-BUG-2026-06-27-022 | 2026-06-27 | AI-Atomic-Framework | Bug | High | Fixed in 2026-06-28 backlog follow-up lane | Task audit / completion report detector | `ATM_TASK_AUDIT_COMPLETION_REPORT_UNVERIFIED` can misclassify ordinary governance documents such as `docs/governance/atm-bug-and-optimization-backlog.md` as closeout reports, even when they are just bug-tracking prose and not closure evidence. | Completion-report detection should be path-aware and structure-aware: it should target explicit closeout/completion report documents instead of scanning every Markdown file in the repository for a few completion phrases. | Reproduced on 2026-06-27 when `node atm.mjs tasks audit --staged --json` and `node atm.mjs hook pre-commit --json` surfaced `docs/governance/atm-bug-and-optimization-backlog.md` as `ATM_TASK_AUDIT_COMPLETION_REPORT_UNVERIFIED`. The document is a long-lived bug backlog, not a closeout report, so asking humans or skills to avoid normal words like `done` / `completed` would be the wrong fix. Closed on 2026-06-28 by locking the path-aware behavior into `packages/cli/src/commands/framework-development/__tests__/closure-packet-schema.spec.ts`: the regression now proves a governance backlog file is ignored even if it contains `status: **all completed**`, while real `closeout-report.md` / `completion-report.md` files still trigger the audit error. | Keep the regression in `packages/cli/src/commands/framework-development/__tests__/closure-packet-schema.spec.ts`; future detector changes must preserve basename/path gating so governance backlog prose cannot regress into false closeout evidence. |
| ATM-BUG-2026-06-27-023 | 2026-06-27 | AI-Atomic-Framework | Bug | High | Fixed in 2026-06-28 backlog follow-up lane | Git governance / hook timing | The Git hook subprocess can rerun `validate:git-head-evidence` against stale or pre-refresh state after the parent governance flow has already generated fresh git-head evidence, causing a legitimate framework commit to fail on a false old-state verdict. | Hook and ATM git wrapper lanes should pass the same fresh git-head evidence context into the subprocess, or deterministically regenerate it inside the child before validation, so a successful parent preflight cannot be invalidated by stale child timing. | Reproduced on 2026-06-27 while backfilling meaningful fixes onto `origin/main`: `node atm.mjs hook pre-commit --json` passed after fresh evidence generation, but the later Git hook subprocess and governed commit lane fell back to old `validate:git-head-evidence` behavior and forced `git commit --no-verify` despite no real content failure. Related symptoms overlap the frozen-hook timing family tracked in `ATM-BUG-2026-06-23-018`, but this case is specifically about git-head evidence freshness crossing the hook subprocess boundary. Closed on 2026-06-28 after adding a dedicated regression in `scripts/validate-git-hooks-enforcement.ts` that uses the governed reconcile close-window lane to run an explicit parent `hook pre-commit`, verify the staged close packet plus `.atm/history/evidence/git-head.jsonl`, then complete a real child `git commit` through the installed hook path without reintroducing a stale evidence verdict. Validation: `node --strip-types scripts/validate-git-hooks-enforcement.ts --mode validate`. | Keep the parent/child freshness regression in `scripts/validate-git-hooks-enforcement.ts`; if this regresses again, treat it as a release-critical git-head evidence handoff failure rather than a docs-only backlog discrepancy. |
| ATM-BUG-2026-06-28-024 | 2026-06-28 | AI-Atomic-Framework | Bug | High | Fixed in 2026-06-28 repair lane (`c589f1250`) | Framework governance / temp claim | `framework-mode claim` could reject the same actor's still-live temporary framework lock as `ATM_FRAMEWORK_STALE_LOCK_CLEANUP_REQUIRED` when the lock had no linked task context yet, blocking a normal same-session scope refresh or claim widening. | The same actor should be allowed to reuse its active unlabeled temporary framework lock for the same framework session, instead of being forced through false stale-lock cleanup. | Reproduced on 2026-06-28 while widening the staged framework merge scope on `main`: a valid temp lock for `codex-gpt-5.4-mini` existed, but a follow-up claim was blocked as `possibly-stale` only because ATM could not resolve linked task metadata. Landed in `c589f125049ad81adc146f1b78e6a5ee36b8f607` and verified by `packages/cli/src/commands/framework-development/__tests__/temp-claim.spec.ts` plus successful same-actor follow-up claim flow during the `main` repair lane. | Keep regression coverage in `packages/cli/src/commands/framework-development/__tests__/temp-claim.spec.ts` for same-actor unlabeled temp-lock reuse. |
| ATM-BUG-2026-06-28-025 | 2026-06-28 | AI-Atomic-Framework | Bug | High | Fixed in 2026-06-28 repair lane (`c589f1250`, push verified by `f57dbfe0b`) | Git governance / pre-push evidence visibility | `hook pre-push` commit-range inspection only scanned git-head evidence from committed refs, so a valid worktree-local `git-head` backfill for the current `HEAD` was invisible to the guard and protected-branch pushes were still blocked as missing evidence. | Pre-push commit-range inspection should also read worktree-local `git-head` evidence, so a freshly backfilled current `HEAD` can satisfy the push guard without requiring an extra evidence-only commit. | Reproduced on 2026-06-28 after emergency framework delivery: `main` commit `191fd310166054d3fa526b6961351716bb2d489e` could be backfilled locally, but pre-push still reported `ATM_COMMIT_RANGE_GIT_HEAD_EVIDENCE_MISSING` because the guard only read `git show <ref>:.atm/history/evidence/git-head.jsonl`. Landed in `c589f125049ad81adc146f1b78e6a5ee36b8f607`; final verification used `node atm.mjs evidence git-head-backfill ...`, an evidence-only follow-up commit `f57dbfe0bdfdf9f939e35400ec346501f4ccb2f3`, and a successful `git push origin main` with `ATM_HOOK_PRE_PUSH_OK`. | Keep regression coverage in `scripts/validate-git-hooks-enforcement.ts` by backfilling `HEAD` and verifying `hook pre-push` accepts the worktree evidence. |
| ATM-BUG-2026-06-28-026 | 2026-06-28 | AI-Atomic-Framework | Bug | Medium | Fixed in 2026-06-28 repair lane (`c589f1250`) | Validator fixtures / task audit baseline | Temp validator fixtures copied historical `docs/tasks/*.task.md` cards from the framework repo into fresh bootstrap repos without also copying the matching `.atm/history/task-events` ledger, so task audit raised false `ATM_TASK_AUDIT_TRANSITION_EVENT_MISSING` findings unrelated to the hook regression being tested. | Source-first validator fixtures that bootstrap a fresh `.atm` runtime should either copy a consistent historical task/evidence/event set or explicitly drop inherited historical task cards before running task audit sensitive commands. | Reproduced on 2026-06-28 while debugging `scripts/validate-framework-governance-repairs.ts`: after worktree-local `git-head` backfill, `hook pre-push` no longer reported `ATM_COMMIT_RANGE_GIT_HEAD_EVIDENCE_MISSING`, but still failed on orphaned `TASK-AAO-0146` transition history because the temp repo included `docs/tasks/TASK-AAO-0146-*.task.md` without the corresponding `.atm/history/task-events/...` files. The shared fixture layer landed in `c589f125049ad81adc146f1b78e6a5ee36b8f607` via `schemas/validators/validator-fixture.schema.json`, `scripts/lib/validator-fixture.ts`, and fixture-backed validator rewrites. | Keep fixture hygiene in the shared validator fixture schema/helper, preserve the focused regression for worktree-local backfill, and treat future task-audit failures in temp repos as fixture-consistency issues first. |
| ATM-BUG-2026-06-28-027 | 2026-06-28 | AI-Atomic-Framework | Optimization | High | Fixed in 2026-06-28 repair lane (`c589f1250`) | Validator runner / performance budget | The unified validator runner already measured per-validator `durationMs`, but the summary did not flag direct duration budget overruns or compare timings against a baseline, so `validate:*` growth could accumulate until humans noticed that the suite felt slow. | Validator summaries should include a structured performance report, slowest-validator list, direct per-validator budget warnings, optional baseline regression warnings, and profile budget warnings so overweight validators are visible before they become normal. | User concern on 2026-06-28: `validate` is getting larger and slower, and future runs should keep timing data, warn when runs get too long, and point maintainers toward overly broad or complex validator content. Landed in `c589f125049ad81adc146f1b78e6a5ee36b8f607` through `scripts/run-validators.ts`, `scripts/validators.config.json`, and `scripts/validate-test-facade.ts`, including explicit `ATM_VALIDATOR_DURATION_BUDGET_EXCEEDED` reporting and optional `--performance-baseline` comparison. | Keep direct `ATM_VALIDATOR_DURATION_BUDGET_EXCEEDED` warnings plus optional `scripts/run-validators.ts --performance-baseline <summary.json>` comparison covered by `scripts/validate-test-facade.ts`; use `overBudgetMs` and the slowest-validator list before adding new broad validators. |
| ATM-BUG-2026-06-28-028 | 2026-06-28 | AI-Atomic-Framework | Bug | High | Fixed in 2026-06-28 repair lane (`c589f1250`) | Git governance / git-head evidence scope | `hook pre-commit`, `doctor`, and `framework-mode status` treated any staged or latest HEAD change as if it required git-head evidence, so small docs/backlog/fixture edits and emergency repair lanes could be blocked by a commit-boundary evidence check that was not actually needed for low-risk files. | Git-head evidence should remain strict for critical framework commits, governed ledger/closure bundles, protected pre-push, and commit-range checks, but docs-only or low-risk maintenance changes should pass without generating or requiring git-head evidence. | User correction on 2026-06-28: "對任何 staged file 嘗試寫 git-head evidence" is too broad because small docs or emergency maintenance changes repeatedly get stuck on missing evidence. Existing regression `scripts/validate-git-hooks-enforcement.ts` expected docs-only pre-commit to write `.atm/history/evidence/git-head.jsonl`, proving the rule had become over-broad. Landed in `c589f125049ad81adc146f1b78e6a5ee36b8f607` through shared path classification plus updated `hook`, `doctor`, and `git-head-evidence` behavior. | Centralize framework critical path classification, make pre-commit write git-head evidence only for critical or governed ledger-boundary staged files, let `doctor` classify non-critical HEAD as `not-required-non-critical-head`, and keep protected critical commit checks strict. |
| ATM-BUG-2026-06-28-029 | 2026-06-28 | AI-Atomic-Framework | Bug | High | Fixed in 2026-06-28 repair lane (`c589f1250`) | Actor identity / cross-editor handoff | Repo default identity could be silently reused across editors or agents, and commit attribution did not read per-actor runtime identity consistently, so a new agent could accidentally commit or pass governance checks with a prior agent's stale identity. | Governed git boundaries should require an explicit actor, per-actor runtime identity should be resolved before default identity, editor/provider provenance mismatches should block commit attribution, stale repo default identity should be clearable before handoff, and ATM entry skills should teach the identity handoff gate before the agent reaches CLI/hook failure. | User concern on 2026-06-28: agents from different editors can overwrite or inherit each other's identity, causing commits or permissions to use old records. During fixture dry run, `restore-operator` had a valid `.atm/runtime/identity/actors/restore-operator.json` but hook attribution still reported `ATM_COMMIT_IDENTITY_PROFILE_MISSING`, proving the resolver ignored per-actor runtime identity. A follow-up dogfood pass found that only repairing CLI/hook was insufficient: `atm-next`, `atm-governance-router`, dispatch, handoff, and worker entry skills also need the same semantic warning so agents clear stale default identity and set explicit actor identity before claim/commit/report. Landed in `c589f125049ad81adc146f1b78e6a5ee36b8f607`, including `identity clear`, explicit governed git actor enforcement, per-actor identity resolution, editor/provider mismatch checks, and canonical skill-template handoff guidance propagated through integration installers. | Keep `identity clear`, explicit git actor enforcement, per-actor identity resolver, editor/provider mismatch checks, and skill-compiler actor handoff gate covered by hook/git governance and integration-adapter regression. Future handoff docs should say: clear stale default, then set/use explicit actor plus task before claiming authority. |
| ATM-BUG-2026-06-28-030 | 2026-06-28 | AI-Atomic-Framework | Optimization | P0 | Fixed in 2026-06-28 repair lane | Guidance UX / skill-CLI routing | Skill-first guidance and CLI JSON are much better than the old CLI-only flow, but the boundary is still easy to misread: agents can expect a playbook when none exists, treat long CLI JSON as manual prose instead of structured tool output, miss that `artifacts/` evidence requires `git add -f`, or lose track of which dirty files belong to the current task. `guide` can also overgeneralize small evidence backfill work into a heavy `create-atom` route. | ATM should make the skill/CLI contract explicit and low-friction: tell the agent when there is no playbook, surface the one next structured action, distinguish skill authority from CLI evidence, warn when target artifacts are ignored and need force-add, and classify dirty files as in-scope versus unrelated before mutation. | Repaired on 2026-06-28 by hardening prompt-scoped `next` summaries with explicit playbook-absent and structured-output hints, ignored-artifact force-add guidance, and worktree scope classification; by teaching evidence/artifact follow-up prompts to stay in docs-first guidance instead of falling through to `create-atom`; and by aligning stale frozen-runner remediation with the real retained release command (`ATM_RETAIN_RELEASE_ARTIFACTS=1 npm run build`) so shipped runner guidance no longer points to a no-op sync path for onefile refresh. Regression coverage now lives in `scripts/validate-prompt-scoped-next.ts` and `scripts/validate-guide.ts`. | Keep prompt-scoped next hints and evidence-follow-up routing covered in the existing validators, and preserve the retained runner-sync command in runner-mode output so future guidance changes cannot quietly drift back to stale onefile behavior. |
| ATM-BUG-2026-06-28-031 | 2026-06-28 | AI-Atomic-Framework | Bug | P0 | Fixed in 2026-06-28 repair lane | Git governance / pathscoped commit semantics | A path-scoped `git commit -- <paths>` still runs ATM pre-commit against the entire staged index, so an operator cannot safely rescue or land a narrow fix when unrelated staged files already exist in the same worktree. This makes scoped recovery commits behave as though they were full-index commits and can block urgent evidence or hotfix backfills without touching other people's staged work. | ATM hook and governed git lanes should understand the effective commit surface for path-scoped commits. At minimum, the hook should distinguish the requested pathspec surface from unrelated staged entries and validate only the commit-bound file set, or provide a first-class isolated commit lane that preserves other staged state without requiring users to unstage foreign work. | Repaired on 2026-06-28 by keeping `GIT_INDEX_FILE` intact when ATM sanitizes git hook environments, so native path-scoped `git commit -- <paths>` continues to validate only the temporary pathspec index instead of collapsing back to the caller's full staged surface. Focused regression coverage in `scripts/validate-git-hooks-enforcement.ts` now proves the commit lands only the requested pathspec while unrelated staged files remain staged and untouched in the real index. | Keep the pathspec regression in `scripts/validate-git-hooks-enforcement.ts`, and treat any future hook env sanitation touching git index variables as release-critical because it can silently widen commit scope again. |
| ATM-BUG-2026-06-28-032 | 2026-06-28 | AI-Atomic-Framework | Bug | Medium | Fixed in 2026-06-28 repair lane | Git governance / evidence-only follow-up residue | After `node atm.mjs evidence git-head-backfill ...` and a governed evidence-only follow-up commit, the worktree can briefly report `.atm/history/evidence/git-head.jsonl` as modified again even though the evidence commit itself already succeeded. The file settles back to clean, but the transient dirty state is confusing and looks like residue or a second required evidence commit. | Evidence-only follow-up commits should either suppress redundant post-commit git-head rewrites or surface a precise "transient auto-refresh" diagnostic so operators do not misread the worktree as genuinely dirty. | Reproduced and then covered on 2026-06-28 by isolating evidence-only follow-up commit fixtures and sampling git status until the transient rewrite settles. The lane now has deterministic regression coverage in `scripts/validate-governance-commands.ts`, which verifies the evidence-only follow-up commit path and guards against stale mixed-scope assumptions without requiring a human to interpret brief worktree flicker. | Keep the evidence-only follow-up fixture isolated from mixed-scope commit tests, and preserve the settle-check sampling so timing-sensitive residue regressions stay visible without turning the validator into a flaky sleep-heavy lane. |
| ATM-BUG-2026-06-28-033 | 2026-06-28 | AI-Atomic-Framework | Optimization | P0 | Fixed in 2026-06-28 governance-entry-readiness lane | Governance entry / evidence timing | Git-head evidence, protected-push readiness, and framework-claim prerequisites were still easy to discover too late because the main operator route only became painful at hook or push time. Skills told agents to run `next`, but did not also tell them how to prepare for the likely protected-branch blockers returned by that route. | `doctor` and prompt-scoped `next` should surface governance readiness as an explicit early contract, and canonical skill surfaces should teach agents to consume those hints before commit/push. | Repaired on 2026-06-28 by adding `governance-entry-readiness` to `doctor`, by attaching `evidence.nextAction.governanceReadiness` plus `ATM_NEXT_GOVERNANCE_READINESS_HINT` to prompt-scoped `next` routes, and by updating canonical `atm-next` / `atm-governance-router` entry surfaces plus installed editor adapters to teach agents that framework claim, protected-push evidence, `doctor`, and branch queue retry codes must be prepared early rather than discovered at hook/push time. Regression coverage now lives in `scripts/validate-prompt-scoped-next.ts`, `scripts/validate-git-hooks-enforcement.ts`, and `scripts/validate-guide.ts`. | Keep governance-entry-readiness in both CLI and skill entry surfaces; future router or adapter changes must preserve the early warning contract instead of pushing diagnosis back down to hooks only. |
| ATM-BUG-2026-06-28-034 | 2026-06-28 | AI-Atomic-Framework | Optimization | High | Fixed in 2026-06-28 governance-entry-readiness lane | Validator architecture / contract boundary | Focused validators can drift into ad hoc string-anchor checks that mix source-contract verification with behavior-regression expectations, which makes it too easy to "bind the whole spec" when a smaller governance contract would be more stable and faster. | Contract validators should share a small source-anchor helper so they can verify the minimum public governance contract explicitly, while behavior/regression tests stay in their own focused spec lanes. | Repaired on 2026-06-28 by extracting shared source-contract anchor collection into `scripts/lib/validator-envelope.ts` and migrating `scripts/validate-branch-commit-queue.ts` to use that helper for contract anchors instead of open-coded one-off `includes(...)` chains. This keeps the branch queue validator focused on the public governance contract while leaving behavioral regression coverage in the taskflow and git-governance tests. | Reuse the shared source-contract helper for future contract validators instead of binding them to large focused specs or duplicating inline anchor scans. |
| ATM-BUG-2026-06-28-035 | 2026-06-28 | AI-Atomic-Framework | Optimization | High | Fixed in 2026-06-28 governance-entry-readiness lane | Backlog governance / source reality drift | The backlog can lag behind source reality when the source fix lands first and the validator or documentation closeout lands later, but there was no quick repo-level scan to highlight open rows whose prose already sounds effectively fixed. | `doctor` should include a lightweight backlog/source drift scan so maintainers notice when bug-tracking status needs explicit closeout or follow-up evidence. | Repaired on 2026-06-28 by adding a `backlog-sync` doctor check that parses `docs/governance/atm-bug-and-optimization-backlog.md`, looks for open rows whose evidence/follow-up text already implies a landed fix, and emits a focused reminder to update the backlog status or land the missing validator/documentation closeout. | Keep the backlog-sync scan lightweight and heuristic, and tune phrases only when they clearly indicate source reality has outrun backlog state. |
| ATM-BUG-2026-06-28-036 | 2026-06-28 | AI-Atomic-Framework | Optimization | High | Fixed in 2026-06-28 governance-entry-readiness lane | Multi-agent governance / observability | ATM already had branch queue retry codes and protected-push diagnostics, but those signals were spread across hook, taskflow, and git-governance lanes instead of showing up as one early operator summary. That made captain-style巡檢 slow because the agent had to hit several subsystems before it could explain the likely race / retry path. | Governance entry points should surface one compact observability summary for protected branches and multi-agent commit lanes, including ahead-of-upstream context and the branch queue retry codes. | Repaired on 2026-06-28 by teaching `doctor` and prompt-scoped `next` to surface branch queue retry codes, protected-branch context, and ahead-of-upstream readiness as one early governance summary instead of leaving them buried in later hook/git subcommands. `doctor` now exposes the summary through `governance-entry-readiness`, while `next` projects the same preparation model through `evidence.nextAction.governanceReadiness`. | Preserve one compact governance observability summary at the top-level entry points; later hook/taskflow diagnostics can stay richer, but they should no longer be the first time an agent learns the retry model. |
| ATM-BUG-2026-06-28-037 | 2026-06-28 | AI-Atomic-Framework | Product gap | P0 | Fixed in 2026-06-28 governed commit evidence lane | Git governance / commit wrapper evidence timing | Even after `doctor` and `next` surfaced governance readiness early, a critical framework change still commonly landed as one delivery commit plus one evidence-only follow-up commit because the governed `git commit` wrapper relied on later hook/push lanes to discover missing `git-head` evidence for the new commit. | The governed `git commit` wrapper should pre-stage matching future-commit `git-head` evidence before the commit is written, and `hook pre-commit` should treat already-matching staged evidence as idempotent instead of appending duplicates. That lets one governed delivery commit satisfy protected pre-push without a mandatory second evidence-only follow-up commit. | Reproduced on 2026-06-28 immediately after the governance-entry-readiness lane: commit `46b1d9b747e2524fceea670585d88d6727471126` landed through `node atm.mjs git commit`, but protected `git push origin main` still failed with `ATM_COMMIT_RANGE_GIT_HEAD_EVIDENCE_MISSING` until a second `node atm.mjs evidence git-head-backfill ...` follow-up commit was added. Repaired by teaching `packages/cli/src/commands/git-governance.ts` to pre-stage future-commit git-head evidence for governed whole-index commits and by making `packages/cli/src/commands/hook.ts` reuse matching staged future-commit evidence instead of appending duplicate records. Regression coverage now proves a governed critical commit can include `.atm/history/evidence/git-head.jsonl` in the same commit and pass `hook pre-push` immediately without a backfill-only follow-up commit. | Keep the governed wrapper pre-stage lane and hook idempotence together; future git-governance changes must preserve the one-commit protected-push path for critical framework commits instead of reintroducing mandatory evidence-only follow-up commits. |
| ATM-BUG-2026-06-28-038 | 2026-06-28 | AI-Atomic-Framework | Optimization | High | Fixed in 2026-06-28 governance-skill-checklist lane | Governance entry / skill preflight clarity | `doctor` and prompt-scoped `next` already emitted governance-readiness data, but the top-level `atm-next` and `atm-governance-router` skill surfaces still left too much preparation implicit. Agents could see `governanceReadiness`, yet still miss that they should immediately translate it into actor, claim, `doctor`, and `hook pre-push` preflight steps before editing or pushing. | Canonical skill entrypoints should turn `governanceReadiness` into an explicit preflight checklist, including actor resolution, framework claim, proactive `doctor`, proactive `hook pre-push`, and branch-queue retry interpretation. | User feedback on 2026-06-28: "先收斂到 doctor / next 這個上層治理入口 ==> 如果可以在這些的 skill 裡面預先說明, AI 不就可以更早知道要準備嗎?" Repaired by updating `templates/skills/atm-next.skill.md`, `templates/skills/atm-governance-router.skill.md`, and the installed Codex / `.agents` skill surfaces so they now spell out the immediate preparation checklist instead of only mentioning `governanceReadiness` abstractly. `scripts/validate-guide.ts` now also asserts the skill text keeps `doctor --json`, `hook pre-push`, `queueRetryCodes`, and `upstreamRef` visible. | Keep the checklist language in the source templates and installed skill surfaces together; future guidance simplifications must not regress from an explicit preflight checklist back to vague prose that leaves protected-branch preparation to trial and error. |
| ATM-BUG-2026-06-30-040 | 2026-06-30 | AI-Atomic-Framework | Product gap | P0 | Fixed in 2026-06-30 git-head-evidence downgrade lane | Git governance / commit-range git-head evidence lifecycle | Protected-branch push could discover missing commit-range `git-head` evidence too late, after a normal series of local commits already existed. Recovery required historical backfill records and sometimes a separate evidence-only commit, turning ordinary delivery into governance archaeology. | Historical per-critical commit evidence should not be a protected-push hard gate. ATM should keep strict same-commit governed provenance plus closeout/repair cross-checks, while ordinary protected push treats missing historical `git-head` records as diagnostic only. | Reconfirmed on 2026-06-30 when local `main` was ahead by five valid work commits but push still failed on missing commit-range evidence, forcing a repair path through `node atm.mjs evidence git-head-backfill ...` and an evidence-only follow-up commit. Closed by downgrading `hook pre-push`, `guard commit-range`, `doctor`, and `next` so they no longer require per-critical historical `git-head` evidence, while retaining same-commit wrapper provenance plus closure packet / repair / task closeout hard checks. Validation lane: `npm run typecheck`, `npm run lint`, and `node --experimental-strip-types scripts/validate-git-hooks-enforcement.ts --mode validate`. | Keep `node atm.mjs git commit` same-commit provenance strict, preserve closure packet / repair / task closeout hard failures, and retain `evidence git-head-backfill` only as a repair or audit trail tool rather than a normal protected-push requirement. |
| ATM-BUG-2026-07-07-041 | 2026-07-07 | AI-Atomic-Framework | Optimization | Medium | Fixed in next default compact output lane | Guidance UX / next output | Prompt-scoped `next --json` can emit thousands of lines for a normal task route, causing Codex/tool output truncation and forcing the operator to rerun narrower prompts or manually recover the playbook from partial JSON. | `next` should expose a compact/default JSON mode for agent consumption, with large diagnostics behind `--verbose` or a file artifact, while preserving full structured evidence when explicitly requested. | Fixed by `compactNextRouteResult`: default output removes duplicated top-level aliases such as `nextAction`, `taskIntent`, `runnerMode`, `allowedCommands`, and `blockedCommands`; `evidence.nextAction` remains the authoritative route payload; playbook message data points to `evidence.nextAction.playbook`; and `--verbose` is parser-supported to return the full message payload. | Keep `scripts/validate-prompt-scoped-next.ts` coverage that default output omits duplicated aliases and compact playbook message data, while `next --verbose` retains full playbook steps. |
| ATM-BUG-2026-07-07-042 | 2026-07-07 | AI-Atomic-Framework | Optimization | Medium | Fixed in prompt-scoped next validator lane | Validator UX / noisy success logs | `npm run validate:prompt-scoped-next` prints a full `teamPlanResult` JSON on success, producing multi-thousand-line logs and making CI or agent transcripts harder to scan even when the validator passes. | Validators should default to quiet success output and emit large diagnostic payloads only on failure or behind a debug flag. | Fixed in `scripts/validate-prompt-scoped-next.ts`: `teamPlanResult is: ...` is now printed only inside the assertion failure path, and `runTeam failed with error` is printed only when the team command itself throws. Validation: `npm run validate:prompt-scoped-next` completed with quiet success output and no large `teamPlanResult` payload. | Keep the failure-only dump pattern in focused validators; future validator additions should avoid success-path JSON dumps unless explicitly gated by a debug flag. |
| ATM-BUG-2026-07-07-043 | 2026-07-07 | AI-Atomic-Framework | Bug | High | Fixed in this cleanup commit | Claim intent / pre-commit governance | `next --claim --auto-intent` can classify a task as `closeout-only` even when the agent is about to make source or backlog delivery changes, causing pre-commit to fail later with `ATM_PRE_COMMIT_CLOSEOUT_ONLY_CLAIM_MUTATION`. | Auto-intent should infer write intent when the task is open and delivery files are modified or when the prompt asks to implement/fix, or `git commit --task` should surface the re-claim command before staging and hook failure. | During `TASK-AAO-0151`, the first claim reported `claimIntent: closeout-only`; `node atm.mjs git commit --auto-stage` then failed after running hooks because `docs/governance/atm-bug-and-optimization-backlog.md` and `packages/cli/src/commands/next.ts` were real delivery mutations. On 2026-07-09 this was closed by adding `ATM_GIT_COMMIT_CLOSEOUT_ONLY_MUTATION` to task-scoped commit bundle preflight, so source mutation under an active closeout-only claim fails before auto-stage and before long pre-commit hooks. | Keep `tests/cli/git-commit-closeout-only-preflight.test.ts` in `validate:cli`; it asserts the source file is not staged and the operator receives the re-claim/write recovery path. |
| ATM-BUG-2026-07-07-044 | 2026-07-07 | AI-Atomic-Framework | Bug | Medium | Fixed in prompt-scoped next lane | Scope amendment / re-claim lifecycle | Re-claiming an already amended task with `next --claim --claim-intent write` can rebuild the direction lock from original task scope and drop previously added scope-amendment paths, forcing the operator to re-run `tasks scope add`. | Re-claim should preserve active task scope amendments or clearly state that it is replacing the lock and list amendments that must be re-applied. | Fixed in `packages/cli/src/commands/next.ts`: when re-claiming a task that already has an active claim, existing claim files are merged into the next allowed-files set so scope-amended paths survive intent escalation. Regression coverage in `scripts/validate-prompt-scoped-next.ts` creates `TASK-SCOPEAMEND-0001`, reclaims it from `closeout-only` to `write`, and asserts `src/scope-amend-linked.ts` remains in `claim.files`. Validation: `npm run validate:prompt-scoped-next` and `node --strip-types tests/cli/task-scope-amendment.test.ts`. | Keep the prompt-scoped next regression for `TASK-SCOPEAMEND-0001`; future claim-direction changes must preserve scope-amended paths across same-actor re-claim and intent upgrade. |
| ATM-BUG-2026-07-07-045 | 2026-07-07 | AI-Atomic-Framework | Bug | High | Fixed in this cleanup commit | Git governance / commit wrapper observability | `node atm.mjs git commit --auto-stage --json` can exceed the caller timeout after the underlying git commit already succeeds, leaving the operator with a timeout error, a moved `HEAD`, and staged/unstaged residue to reconcile manually. | The wrapper should emit phase progress, bound long hook/validator phases, and make successful commit completion observable before any post-commit work can overrun the caller timeout. A retry/status command should identify whether the commit already landed. | During `TASK-AAO-0151`, the governed commit command timed out after 184 seconds, but `git log -1` showed commit `7eb64669 fix(next): admit unconfirmed broker overlaps` had landed. Residual working-tree changes then had to be inspected and reconciled by diffing staged vs unstaged state. Reconfirmed on 2026-07-09 when wrapper failures returned empty stdout/stderr and required a manual hook pass plus native copyable commit to prove the repo state. | Keep `tests/cli/git-commit-timeout-and-status.test.ts` coverage that timed-out attempts record HEAD before/after, status/retry commands, and copyable host-git diagnostics; keep using `git commit-status` before any retry after timeout. |
| ATM-BUG-2026-07-07-046 | 2026-07-07 | AI-Atomic-Framework | Bug | High | Fixed in taskflow close atomicity lane | Taskflow close / commit attribution | `taskflow close --write` auto-commit can call native `git commit` without propagating the task actor identity, so pre-commit blocks the close bundle with `ATM_COMMIT_AUTHOR_NAME_MISMATCH` / `ATM_COMMIT_AUTHOR_EMAIL_MISMATCH` when repo-local Git config belongs to a previous actor. | Taskflow close auto-commit should either use the same governed git wrapper/env as `node atm.mjs git commit`, or preflight the repo-local Git identity and emit a required identity command before backend close mutates ledger state. | During `TASK-AAO-0151`, close write rolled back after backend close because the commit bundle attempted author `codex-gpt-5.4-mini <codex-gpt-5.4-mini@atm.local>` while the task actor was `codex-captain`; rollback restored ledger state but required manual diagnosis and local git config correction. The source fix in `packages/cli/src/commands/taskflow/commit-bundle-assembly.ts` resolves actor-scoped author/committer env for the raw-git close commit lane, and `packages/cli/src/commands/taskflow/__tests__/commit-bundle-assembly.spec.ts` asserts the close commit uses the actor identity instead of drifted host git config. | Keep `node --strip-types scripts/validate-taskflow-close-atomicity.ts --mode validate` in the close/commit gate; it runs the commit-bundle identity regression alongside atomicity coverage. |
| ATM-BUG-2026-07-07-047 | 2026-07-07 | AI-Atomic-Framework | Bug | High | Fixed in prompt-scoped next batch lane | Prompt routing / batch queue | A broad request to complete all open ATM task cards can expose the open task queue in `importedTaskQueue` but still return `ATM_NEXT_TASK_SCOPE_NOT_FOUND` instead of a batch route, forcing the operator to manually choose the next task and risking a parallel task model. | If the prompt asks for all / remaining / open task cards and open imported tasks exist, `next` should return `recommendedChannel=batch` with a queue head and checkpoint playbook, or explain the exact missing scope without discarding the discovered queue. | During `TASK-AAO-0150` continuation, `node atm.mjs next --prompt "完成所有 open ATM task cards 並提交 push" --json` reported `openTaskCount: 6` and listed the tasks, but hard-blocked with `ATM_NEXT_TASK_SCOPE_NOT_FOUND`. The source fix in `packages/cli/src/commands/next.ts` handles blanket "all/open/remaining task cards" prompts as batch intent, and `scripts/validate-prompt-scoped-next.ts` asserts the route no longer emits `ATM_NEXT_TASK_SCOPE_NOT_FOUND` and recommends `recommendedChannel=batch`. | Keep `npm run validate:prompt-scoped-next` coverage for blanket all/open/remaining task-card prompts. |
| ATM-BUG-2026-07-07-048 | 2026-07-07 | AI-Atomic-Framework | Optimization | High | Fixed in onefile in-process runner lane | Next latency / frozen runner startup | `next --prompt ... --json` had a slow hot path from over-hydrating historical task ledgers, scanning markdown task cards even after a JSON task-id match, and recomputing full framework readiness for advisory fields. After `TASK-AAO-0150`, the dist-internal `ATM_NEXT_PROFILE=1` route is ~0.3s, but `node atm.mjs` on Windows still shows occasional >5s wrapper/import/OS IO spikes. | The prompt-scoped `next` route should keep the semantic routing work under 5s warm, and the frozen wrapper should avoid import-time git/template scans or other startup work unrelated to `next`. | `TASK-AAO-0150` profiling showed `inspectImportedTaskQueue` at ~9.7s, markdown task reads at ~6-8s when JSON had already matched, and `buildGovernanceReadinessHint` at ~2.2s due git subprocesses. The fix lazy-hydrates ledger tasks, skips markdown discovery for JSON task-id matches, fast-reads branch readiness from `.git`, and leaves `ATM_NEXT_PROFILE=1` phase diagnostics. Post-fix onefile warm-run evidence on Windows reports `cliLogicMs: 39`, `p50Ms: 1693.09`, `p95Ms: 5195.47`, and `wrapperOverheadMs: 1998.98`, all within the declared budgets. | Keep the latency validator in the release validation set and investigate future Windows OS scheduling variance only if the declared p95 budget regresses. |
| ATM-BUG-2026-07-07-049 | 2026-07-07 | AI-Atomic-Framework / 3KLife | Bug | High | Fixed in taskflow close atomicity lane | Taskflow close / planning mirror index cleanup | `taskflow close --write` can report a successful planning mirror commit but leave the planning repo index staged with the inverse closeback diff, making the card look reopened/deleted in `git status` even though the just-created commit contains the correct done frontmatter and close event. | A successful taskflow close transaction should leave both target and planning repos with no task-owned staged residue; if cleanup cannot complete, the command should fail closed or emit an explicit recovery command before reporting the transaction committed. | During `TASK-AAO-0150`, close write created ATM close commit `1a9683e2` and 3KLife planning commit `cd57450b`, but `git -C C:\Users\User\3KLife status --short` then showed staged deletion of `.atm/history/task-events/TASK-AAO-0150/...close...json` and mixed `TASK-AAO-0150-next-hot-path-latency.task.md`. The source fix in `commit-bundle-assembly.ts` commits taskflow bundles from a temporary index and then resets only task-owned stage files in the live index to the newly committed HEAD tree, preserving unrelated staged files while removing stale inverse diffs. | Keep `packages/cli/src/commands/taskflow/__tests__/commit-bundle-assembly.spec.ts` and `validate-taskflow-close-atomicity` coverage for post-commit live-index residue cleanup in both target and planning close bundles. |
| ATM-BUG-2026-07-07-050 | 2026-07-07 | AI-Atomic-Framework | Bug | High | Fixed in taskflow close atomicity lane | Taskflow close / write readiness | `taskflow close` dry-run can report `writeReadinessHint.status: ready`, but the identical `--write` command then fails with `ATM_TASKFLOW_CLOSE_PLANNING_PATH_MISSING` for a stale `source.planPath`, even when the task is `closureAuthority: target_repo` and has no `planningMirrorPaths`. | Dry-run readiness must include every blocker that `--write` will enforce, or target-repo-only tasks with no planning mirror should not fail write because a non-authoritative historical planning path is missing. | During `TASK-AAO-0147`, `node atm.mjs taskflow close --task TASK-AAO-0147 --actor codex-captain --historical-delivery cda5705 --json` returned ready, but the `--write` form failed because `atm-agent-first-operability/tasks/TASK-AAO-0147-main-push-audit-baseline-repair.task.md` was missing. The source fix in `packages/cli/src/commands/taskflow/write-readiness.ts` treats stale/unresolvable source plan paths as advisory for target-repo-authority tasks without planning mirrors, so dry-run and write share the same closeback-path policy. | Keep `packages/cli/src/commands/taskflow/__tests__/write-readiness.spec.ts` and `validate-taskflow-close-atomicity` coverage proving dry-run/write closeback-path parity. |
| ATM-BUG-2026-07-07-051 | 2026-07-07 | AI-Atomic-Framework | Optimization | Medium | Fixed in emergency gate lane | Emergency lane UX / flag approvals | `ATM_EMERGENCY_LANE_APPROVAL_REQUIRED` reports an emergency approve command, but omits `--allowed-flag` guidance for required backend flags such as `--historical-delivery`, causing the first lease to fail with `ATM_EMERGENCY_FLAG_NOT_APPROVED`. | When a protected backend command requires an emergency lease and non-default flags, the required command should include the needed `--allowed-flag` entries or explain that flags must be approved explicitly before use. | Fixed in `packages/cli/src/commands/emergency/gate.ts`: the approval-required error dedupes protected flags into `requiredAllowedFlags`, appends matching `--allowed-flag` arguments to `details.requiredCommand`, and warns that missing flag approval will fail with `ATM_EMERGENCY_FLAG_NOT_APPROVED`. Validation: `node --strip-types packages/cli/src/commands/emergency/__tests__/gate.test.ts` and `npm run validate:cli -- --mode validate`. | Keep `packages/cli/src/commands/emergency/__tests__/gate.test.ts`; it proves `--historical-delivery` is pre-approved in the first generated emergency approve command while flag-free commands stay backward compatible. |
| ATM-BUG-2026-07-07-052 | 2026-07-07 | AI-Atomic-Framework | Bug | High | Fixed in protected override audit staging lane | Protected override audit / evidence persistence | A successful emergency backend close can report protected override audit event paths in command output, but the corresponding events are not visible to `node atm.mjs emergency audit --task <id> --json` afterward. | Protected override audit events reported by a backend command should be durable, committed or otherwise discoverable by `emergency audit`, and the audit query should find events for the task immediately after close. | During `TASK-AAO-0147`, backend close output reported `.atm/history/protected-override-audit/2026-07-07T15-25-47-193Z-POA-03e4b36a9140.json` and `...719Z-POA-57c53bb7da90.json`, but `node atm.mjs emergency audit --task TASK-AAO-0147 --json` returned 0 events and the close commit stat did not include those audit files. The source fix keeps this-task audit events out of auto-clean residue and stages them into the task commit bundle while leaving foreign-task audit events untouched. | Keep `tests/cli/protected-override-audit-staging.test.ts` in `validate:cli`; it proves owned audit events land in the commit and foreign events survive without being swept into unrelated commits. |
| ATM-BUG-2026-07-07-053 | 2026-07-07 | AI-Atomic-Framework | Bug | High | Fixed in this cleanup commit | Git wrapper / failed commit residue | A failed `node atm.mjs git commit --auto-stage` can leave staged governance residue, including `.atm/history/evidence/git-head.jsonl`, while the blocking hook also provides no concrete recovery command for the singleton evidence-file context failure. | If the wrapper fails after staging generated provenance, it should roll back its own staged additions or emit an exact cleanup/retry command. Hook failures for evidence-only bundle manifests should name the supported repair path instead of leaving the operator to inspect and restore staged residue manually. | After `TASK-AAO-0147` close, trying to commit the leftover `TASK-AAO-0147.bundle-manifest.json` failed with `ATM_PROTECTED_STATE_EVIDENCE_FILE_MISSING_TASK_CONTEXT`; the failed attempt left both the bundle manifest and a new `git-head.jsonl` record staged. The operator had to run a scoped `git restore --staged --worktree -- .atm/history/evidence/TASK-AAO-0147.bundle-manifest.json .atm/history/evidence/git-head.jsonl` to return to clean state. Reconfirmed on 2026-07-09 when wrapper failures exposed `liveIndexResidueRollback` but still lacked HEAD/retry context. | Keep `tests/cli/git-commit-failure-residue-rollback.test.ts` coverage that failed attempts restore the pre-attempt index, record `headAdvancedDuringAttempt=false`, and expose exact status/retry guidance. |
| ATM-BUG-2026-07-07-054 | 2026-07-07 | AI-Atomic-Framework | Product gap | High | Fixed in validator resume/status lane | Validator evidence / resumable standard profile | Long profile validation through `node atm.mjs evidence run --command "npm run validate:standard"` has no resumable batch/checkpoint model. If the caller times out after many validators have already run, ATM does not expose a batch id, completed-step cache, expected-duration hint, or continuation command, so the operator must either rerun the whole profile or manually decide which evidence is trustworthy. | Standard/full validator profiles should be resumable and time-budget aware: assign a single batch id, persist per-validator command/result/log metadata as each step finishes, surface estimated/observed profile duration before starting, and let a later run continue only unfinished or invalidated steps while preserving one coherent evidence summary. | During `TASK-MAO-0021`, the focused evidence gates passed, but `evidence run` for `npm run validate:standard` hit a 10-minute caller timeout (`exit code 124`). A second run with a 20-minute caller timeout also timed out; process inspection then showed both timed-out evidence runs had left descendant `npm run validate:standard` / `scripts/run-validators.ts standard` processes alive, so retrying created duplicate concurrent standard profiles instead of a clean continuation. The source fix adds validator run status/resume behavior, per-step checkpointing, and timeout-bounded validator execution coverage. | Keep `tests/cli/validator-run-resume-and-status.test.ts` in `validate:cli`; it asserts read-only `--status`, `--validator-timeout-ms`, and resume behavior that skips completed validators and reruns only outstanding work. |
| ATM-BUG-2026-07-07-055 | 2026-07-07 | AI-Atomic-Framework | Bug | Medium | Fixed in taskflow close atomicity lane | Taskflow close / staged residue | `node atm.mjs taskflow close --write` can successfully create the close commit while leaving an older version of the bundle manifest staged in the git index, producing an `MM` state that requires manual diff interpretation. | Successful taskflow close should leave the target repo index clean, or emit an explicit cleanup command when advisory post-close evidence rewrites cannot be auto-reconciled. | Fixed in `packages/cli/src/commands/taskflow/commit-bundle-assembly.ts`: close commits are built from a clean temporary index and committed paths are reset in the live index to the new HEAD, preserving unrelated staged files while removing same-task residue. Regression coverage in `packages/cli/src/commands/taskflow/__tests__/commit-bundle-assembly.spec.ts` now stages an older same-task `.bundle-manifest.json`, refreshes the worktree manifest before close, finalizes the close bundle, and asserts the manifest is not left staged afterward. Validation: `node --strip-types packages/cli/src/commands/taskflow/__tests__/commit-bundle-assembly.spec.ts`. | Keep the refreshed bundle-manifest assertion in the commit-bundle assembly spec; future taskflow close changes must preserve clean live-index state after successful close commits. |
| ATM-BUG-2026-07-08-056 | 2026-07-08 | AI-Atomic-Framework | Bug | High | Fixed in framework lock audit lane | Framework-mode lock audit / protected evidence | `framework-mode claim` can auto-release a same-actor stale-completed temp lock and write `.atm/history/evidence/framework-lock-auto-reconcile.jsonl`, but `node atm.mjs git commit --auto-stage` then stages that audit file and pre-commit blocks it as `ATM_PROTECTED_STATE_EVIDENCE_FILE_MISSING_TASK_CONTEXT` with no recovery command. | Auto-reconcile audit evidence should either be written to a committable governed audit surface with matching context, be treated as runtime-only and never auto-staged, or emit an exact cleanup/retry command that preserves the audit intent without blocking unrelated source/release commits. | During the 2026-07-08 `atm do` removal / release-sync lane, repeated same-actor framework claims correctly auto-released stale locks and appended `framework-lock-auto-reconcile.jsonl`; the governed commit then failed after 196 seconds because the audit singleton was staged without a task ledger or transition event. The source fix writes auto-reconcile audit to `.atm/runtime/framework-lock-auto-reconcile.jsonl` and prevents active unlabeled temp locks from inheriting stale linked-task context from older actor sessions. | Keep `scripts/validate-framework-development-governance.ts` and `scripts/validate-task-ledger-governance.ts` assertions that auto-reconcile evidence reports the runtime audit path and never creates `.atm/history/evidence/framework-lock-auto-reconcile.jsonl`. |
| ATM-BUG-2026-07-08-057 | 2026-07-08 | AI-Atomic-Framework | Optimization | Medium | Fixed in this cleanup commit | Task audit / warning baseline | `tasks audit` returned `ATM_TASKS_AUDIT_WARNINGS` on every clean run because 24 permanent historical warnings were mixed with genuinely actionable new warnings, causing alert fatigue and hiding fresh audit drift. | Known benign historical task-audit warnings should remain visible in JSON evidence but be acknowledged by a repo-local baseline so only baseline-external warnings trigger `ATM_TASKS_AUDIT_WARNINGS`. Errors must never be suppressible by the baseline. | `node atm.mjs tasks audit --json` on 2026-07-08 returned 25 warnings: 4 planning-only done, 20 draft static evidence, and 1 stale framework lock. The stale lock was released; the 24 permanent historical warnings became baseline entries in `docs/governance/tasks-audit-warning-baseline.json`. | Keep `scripts/validate-task-ledger-governance.ts` coverage that acknowledged warnings stay visible with `acknowledged: true` while `activeFindingCount` drops to zero. |
| ATM-BUG-2026-07-08-058 | 2026-07-08 | AI-Atomic-Framework | Optimization | Medium | Fixed in TASK-AAO-0153 | Task ledger / record-only commits | Pure task-ledger or `.atm/history` record updates still often require borrowing a task-bound claim/release/commit dance even when the operator is only syncing import metadata, transition records, or benign governance history. | ATM should provide an official record-commit lane for `tasks import --write` and pure `.atm/history` maintenance that preserves evidence, actor identity, and protected-state checks without requiring a fake delivery session. | `TASK-AAO-0153` adds `node atm.mjs git record-commit`: it requires explicit actor/message, rejects `--task`, `--auto-stage`, `--defer-foreign-staged`, and `--no-verify`, accepts only low-risk `.atm/history` record files, appends `ATM-Record-Commit: true`, and still stages same-commit git-head provenance through the governed commit wrapper. | Keep `tests/cli/git-record-commit.test.ts` wired into `validate:cli`; it proves task ledger records can commit without a task session while source files and closure packets fail closed. |
| ATM-BUG-2026-07-09-059 | 2026-07-09 | AI-Atomic-Framework | Documentation / AI routing | Medium | Fixed in prompt-scoped next audit advisory lane | Prompt routing / retrospective audit scope | `next --prompt` can hard-block retrospective cleanup/audit prompts as `ATM_NEXT_TASK_SCOPE_NOT_FOUND` when the prompt mentions a non-ledger OPT shorthand, even if the requested action is to inspect whether prior cleanup was completed rather than to claim that task. | Audit/review prompts that mention non-ledger historical labels should either route to a read-only audit/orient lane or report the missing task as advisory while preserving safe read-only evidence commands. They should not force the agent into a retry loop when no exact task card exists. | Fixed in `packages/cli/src/commands/next.ts`: read-only audit/analyze prompts with task-like scope misses return `status: task-scope-audit-advisory`, emit `ATM_NEXT_TASK_SCOPE_AUDIT_ADVISORY`, and keep safe read-only guidance instead of hard-blocking. Validation: `node atm.mjs next --prompt "請檢查 OPT-12 後續盤點清單是否已補齊，這不是要 claim OPT-12，只是做 retrospective audit review" --json` with `ATM_PLANNING_REPO_ROOT=C:\Users\User\3KLife\docs\ai_atomic_framework` returned ok with `ATM_NEXT_TASK_SCOPE_AUDIT_ADVISORY`; `npm run validate:prompt-scoped-next` covers the same retrospective OPT-12 lane. | Keep the retrospective audit regression in `scripts/validate-prompt-scoped-next.ts`; mutation-oriented missing task scopes must still fail closed with `ATM_NEXT_TASK_SCOPE_NOT_FOUND`. |
| ATM-BUG-2026-07-09-060 | 2026-07-09 | AI-Atomic-Framework | Bug | High | Fixed in this cleanup commit | Git governance / wrapper timeout budget | Governed `node atm.mjs git commit` defaulted the underlying host `git commit` spawn to 120s, but a normal framework pre-commit path can legitimately take around 277s when task audit and git-head evidence validators run. This makes a green hook look like a wrapper timeout and forces the operator into manual retry/status work. | The wrapper should remain bounded against truly hung hooks while giving the observed green framework pre-commit path enough budget by default; shorter budgets should be an explicit `--timeout-ms` override for tests or emergency operators. | On 2026-07-09, committing `6c9272ee` first failed with `ATM_GIT_COMMIT_TIMEOUT`; `git commit-status` proved `HEAD` did not advance and rollback removed `.atm/history/evidence/git-head.jsonl`. A direct `hook pre-commit --json` then passed after about 277s, and retrying the same wrapper command with `--timeout-ms 420000` succeeded. | Keep `tests/cli/git-commit-timeout-and-status.test.ts` asserting the default recorded timeout is 420000ms while explicit `--timeout-ms 1500` still bounds a deliberately hung hook. |
| ATM-BUG-2026-07-09-061 | 2026-07-09 | AI-Atomic-Framework | Documentation / AI routing | Medium | Fixed in this cleanup commit | Skill preflight / encoding guard | The `encoding-touched-guard` skill tells agents to run `npm run check:encoding:touched`, but this framework repo does not define that npm script, so the recommended preflight fails before checking any touched files. | Repo-local encoding guidance should point to an existing validator or the package should add the documented script; agents should not need to invent a fallback for BOM/U+FFFD checks. | Fixed by adding `scripts/check-encoding-touched.ts`, `check:encoding:touched`, and `check:encoding:staged`. The wrapper delegates the actual scan to `node atm.mjs guard encoding --files ... --json`, supports explicit `--files`, includes untracked text files in touched mode, and keeps staged mode index-only. Validation: `npm run check:encoding:touched -- --files package.json`, `npm run check:encoding:touched`, `npm run check:encoding:staged`, and `npm run validate:script-parity`. | Keep the script-parity assertion that both npm scripts remain wired to the wrapper; future encoding skill changes should prefer this canonical package script instead of ad hoc PowerShell fallbacks. |
| ATM-BUG-2026-07-09-062 | 2026-07-09 | AI-Atomic-Framework | Bug | High | Fixed in this cleanup commit | Prompt routing / framework temp claim playbook | Framework-maintenance prompts without imported tasks returned `status: framework-temp-claim-required` and a correct `framework-mode claim` command, but the shared fast playbook still instructed agents to run `node atm.mjs next --claim ...`; following the playbook immediately failed with `ATM_NEXT_CLAIM_NO_TASK`. | A route that requires framework temp claim should make the embedded playbook first step and command sequence use the same `framework-mode claim` command; fast quickfix routes may still use `next --claim` when they are actual quickfix locks. | Reproduced on 2026-07-09 with a backlog-repair prompt: `node atm.mjs next --prompt ... --json` returned `framework-temp-claim-required`, then the playbook's `next --claim` command failed with `ATM_NEXT_CLAIM_NO_TASK`. Fixed by adding a fast-claim command override to `buildChannelPlaybook` and asserting the framework-maintenance route's playbook uses `framework-mode claim`. Validation: `npm run validate:prompt-scoped-next`. | Keep the framework-maintenance playbook regression in `scripts/validate-prompt-scoped-next.ts`; future playbook refactors must keep `nextAction.command`, `allowedCommands`, and `playbook.steps[0]` semantically aligned. |
| ATM-BUG-2026-07-09-063 | 2026-07-09 | AI-Atomic-Framework | Bug | Medium | Fixed in this cleanup commit | Task opener / template generator | `taskflow open --write` in template-only fallback recommends `node atm.mjs tasks new --template aao-l2-split ...`, but both frozen and source-first `tasks new` fail with `Template not found: aao-l2-split` because the framework repo does not contain the advertised markdown template assets. | The fallback generator should either bundle the documented default templates in both source and release runners, or help text should only recommend templates that actually exist. | Fixed by adding `packages/atm-markdown-task-source/templates/aao-l2-split-template.md`, including `templates` in the package manifest, and extending `tests/cli/tasks-new.test.ts` to prove source package assets plus frozen `node atm.mjs tasks new --template aao-l2-split` generation both work. | Keep the tasks-new regression in `validate:cli`; future default template keys in taskflow/tasks help must resolve before release. |
| ATM-BUG-2026-07-09-064 | 2026-07-09 | AI-Atomic-Framework | Optimization | Medium | Fixed in this cleanup commit | Governed git / pre-commit observability | `node atm.mjs git commit` can time out at the agent/tool layer while its spawned pre-commit hook continues running heavyweight validators, so the operator sees a timeout even though the commit later succeeds. | Governed commit should expose a durable transaction id, phase log, resumable status command, or timeout-aware progress output so agents can distinguish "still validating" from "safe to retry/repair". | Fixed by extending `git commit-status` with `evidence.branchCommitQueueStatus`: current branch queue lock path, lock record, owner PID liveness, age, current HEAD, head-moved signal, and wait-vs-inspect guidance. Regression coverage in `tests/cli/git-commit-timeout-and-status.test.ts` now simulates a live queue owner and requires `status: busy`, `ownerAlive: true`, and wait guidance. | Keep the live branch queue diagnostic in `git commit-status`; future commit transaction work can add richer phase logs, but operators must never have to guess whether a timed-out wrapper is still committing. |
| ATM-BUG-2026-07-09-065 | 2026-07-09 | AI-Atomic-Framework | Optimization | Medium | Fixed in scope-derived validator selection lane | Task card authoring / validator selection | RFT planning cards can over-prescribe broad validators such as `npm run validate:cli` even when the task's atom map and touched paths only require focused specs, a focused atomic-map validator, and typecheck. During TASK-RFT-0017, `validate:cli` exceeded both 124s and 304s agent command timeouts without returning actionable failure output, while the task-specific validator, focused specs, and typecheck passed. | Task card authoring/import should derive or recommend a minimal validator set from scope paths, atom ownership, and risk level, while making broad validators explicit escalation gates instead of default per-card requirements. The close playbook should distinguish required focused evidence from optional full-suite confidence checks. | Fixed by extending scope-derived closure-required classification: `validate:git-head-evidence` is advisory for non-protected scopes and required only for protected git/evidence/framework/taskflow/tasks/core/release surfaces or when explicitly declared by the task card. Verification: `node --strip-types packages/cli/src/commands/evidence/__tests__/validator-classification.spec.ts` and `npm run typecheck` pass. | Keep full-suite validators available as explicit escalation gates; evidence-run timeout/status work remains tracked by `ATM-BUG-2026-07-10-069` / `ATM-BUG-2026-07-09-068` / `ATM-BUG-2026-07-10-072`. |
| ATM-BUG-2026-07-09-066 | 2026-07-09 | AI-Atomic-Framework | Bug | High | Fixed in validator-temp cleanup lane | Validator harness / temp workspace cleanup | `npm run validate:cli` can fail non-deterministically on Windows when temp regression workspaces under `.atm-temp/` are still held by descendant Node processes. During TASK-RFT-0017 this first surfaced as `EPERM` at `scripts/validate-cli.ts:1885` while removing `tasks-invariant-regressions*`, then after zombie-process cleanup the same validator changed mode and hung past a 184s caller timeout instead of exiting cleanly. | `validate:cli` should drain or terminate its owned child processes before deleting temp workspaces, retry bounded Windows `EPERM` / `EBUSY` cleanup, and fail with actionable diagnostics instead of leaving orphaned `.atm-temp/tasks-invariant-regressions*` directories or blocking governed pre-commit indefinitely. | Fixed by adding `safeRmSync()` to `scripts/validate-cli.ts` with bounded retries for `EPERM`, `EBUSY`, and `ENOTEMPTY`, applying it to the `tasks-invariant-regressions` cleanup path and other temp validator workspaces, and keeping child-process EPERM diagnostics actionable with `ATM_TEMP_ROOT` guidance. The validator runner separately tracks/kills spawned validator children on timeout/signals. Verification: `node --strip-types tests/cli/validator-run-resume-and-status.test.ts`, `node --strip-types tests/cli/git-commit-timeout-and-status.test.ts`, and `npm run typecheck` pass. | Keep temp cleanup best-effort after bounded retries; failed cleanup now warns instead of turning a successful validator into a misleading crash. |
| ATM-BUG-2026-07-09-067 | 2026-07-09 | AI-Atomic-Framework | Optimization | Medium | Fixed in foreign governance deferral lane | Git governance / Team Broker parallel lanes | In a valid two-lane hotfile split, `node atm.mjs git commit --task TASK-RFT-0017 --auto-stage --defer-foreign-staged` still fail-closes when another active lane leaves a foreign `.atm/history/evidence/<other-task>.bundle-manifest.json` dirty. The bundle resolver already classifies the foreign file correctly, but it blocks the unrelated lane instead of offering a task-aware deferral or advisory path. | Team Broker style parallel lanes should be able to defer or ignore foreign same-repo generated evidence residue when the owning task is still active, as long as the file is outside the current task scope and is already attributed to a different task id. ATM must still fail closed for user-authored source overlap, but deterministic foreign bundle-manifest byproducts should have an explicit multi-lane policy instead of requiring manual lane-to-lane coordination every time. | Fixed by allowing foreign `.atm/history/evidence/<task>.bundle-manifest.json` files to be deferred under `--defer-foreign-staged`, excluding the deferred byproduct from same-attempt blocked residue, keeping it out of the current commit, and preserving it for the owning task. Closure packets, task events, task JSON, foreign source overlap, and unknown generated artifacts remain fail-closed. Verification: `node --strip-types tests/cli/git-commit-task-scoped-staging.test.ts` passes. | Keep fail-closed behavior for foreign source overlap and unknown generated artifacts; only known foreign bundle-manifest byproducts are deferred. |
| ATM-BUG-2026-07-09-068 | 2026-07-09 | AI-Atomic-Framework | Optimization | Medium | Fixed in validator cache/status lane | Governed git / commit latency | `node atm.mjs git commit --auto-stage --defer-foreign-staged` can spend several minutes inside pre-commit validation even after the task has already recorded fresh focused evidence. This is distinct from `ATM-BUG-2026-07-09-064` observability and `ATM-BUG-2026-07-09-065` task-card validator selection: the wrapper itself still lacks a fast path, validator-result reuse, or phase-level cache for the commit hook. | Governed commit should reuse fresh task evidence or a trusted validator cache when the commit bundle matches the already-validated scope, and it should expose phase timing so operators can see which hook step is consuming time. A normal atomic task commit should not spend four minutes rerunning heavyweight checks unless the touched paths or policy truly require it. | Fixed by the validator runner's manifest/receipt cache and status surface plus commit attempt status. Validator runs now record receipts, support resume/status polling, bound individual validator attempts with `--validator-timeout-ms`, and preserve a durable commit-attempt status packet so repeated heavyweight validation no longer appears as an opaque hung commit. Verification: `node --strip-types tests/cli/validator-run-resume-and-status.test.ts` and `node --strip-types tests/cli/git-commit-timeout-and-status.test.ts` pass. | Hook-level evidence reuse remains conservative for protected git-head provenance; broader skip policies should continue to be explicit and cache-keyed. |
| ATM-BUG-2026-07-10-069 | 2026-07-10 | AI-Atomic-Framework | Bug | High | Fixed in validator timeout/status lane | Evidence validators / closeout baseline | `taskflow pre-close` can require heavyweight global validators such as `validate:git-head-evidence` for a narrow atomized task even when focused specs, atomic-map validation, and typecheck have passed in an isolated Team Broker lane. During `TASK-RFT-0018`, the evidence command for `validate:git-head-evidence` exceeded a 184s agent timeout and left a live Node subprocess chain, forcing manual process inspection and cleanup before closeout could continue. | Closeout should derive the smallest validator set from the task's atom map, scope paths, and Team Broker isolation context, or provide a bounded/resumable validator status path. Heavy global validators should not become mandatory per-card blockers unless the touched paths or protected surface truly require them, and timed-out validator runs must clean up owned child processes. | Fixed by combining scope-derived validator classification with the validator runner's resumable status/timeout lane. `validate:git-head-evidence` is closure-required only for protected surfaces or explicit task declaration, and `scripts/run-validators.ts` tracks spawned children, kills them on timeout/signals, records timeout status, and exposes `--status --run-id` for polling/resume. Verification: `node --strip-types packages/cli/src/commands/evidence/__tests__/validator-classification.spec.ts` and `node --strip-types tests/cli/validator-run-resume-and-status.test.ts` pass. | Keep heavyweight validators available as explicit escalation gates; normal atomized closeout should rely on focused evidence unless protected surfaces are touched. |
| ATM-BUG-2026-07-10-070 | 2026-07-10 | AI-Atomic-Framework | Bug | High | Fixed in broker conflict override lane | Team Broker / emergency governance | A generic `backend.gitHookBypass` emergency lease could be used with `git commit --no-verify` during an active Team Broker file-ownership conflict, which weakens the cross-task mutation guard by treating a collaboration conflict as if it were only a broken local hook. | Hook bypass recovery must not bypass Team Broker cross-task mutation blocks. Active file-ownership conflicts should be forced through the paper-style parallel conflict-resolution lane first: record the competing task ids, shared paths, atom/CID overlap, owner acknowledgement or timeout, chosen serialization/merge order, and validator plan before either lane can commit. | Dogfood review on 2026-07-10 after `TASK-RFT-0019`: `TASK-AAO-0155` had an active claim including `packages/cli/src/commands/tasks.ts`; a `backend.gitHookBypass` lease plus direct `git commit --no-verify` landed `TASK-RFT-0019` without a broker-specific resolution artifact. Later audit found no physical overwrite, but the procedure made Broker authority ambiguous and contradicted the expected early-parallel conflict-resolution model. The repair adds `backend.brokerConflictOverride`, `--broker-conflict-override`, and `--broker-conflict-resolution`, with regression coverage proving ordinary hook bypass is rejected while high-authority override requires a valid `atm.brokerConflictResolution.v1` artifact. | Keep ordinary emergency hook bypass lower authority than Team Broker conflict ownership. Future hardening can add a first-class `tasks parallel resolve` command to generate the resolution artifact instead of requiring operators to author the JSON manually. |
| ATM-BUG-2026-07-10-071 | 2026-07-10 | AI-Atomic-Framework | Bug | High | Fixed in historical scratch-residue lane | Taskflow close / commit bundle scope classification | `taskflow close --historical-delivery` can include unrelated untracked backup files from broad task scope in `targetDeliveryFiles`, even after the real delivery has already landed in a valid historical delivery commit. This can pressure operators to stage or commit another agent's scratch/WIP files just to finish closeout. | Historical-delivery close should restrict delivery-file requirements to files proven to belong to the historical delivery, or classify unrelated untracked backup/scratch files as advisory residue with an exact remediation. Broad scopes such as `packages/**` should not turn arbitrary `.bak` files into required close-bundle delivery. | Fixed by classifying scratch/backup files such as `.bak`, `.backup`, `.orig`, and `.tmp` as advisory residue during historical closeback unless they are explicitly declared as exact deliverables. Verification: `node --strip-types packages/cli/src/commands/taskflow/__tests__/commit-bundle-assembly.spec.ts` covers a broad `packages/**` historical delivery with an unrelated untracked `.bak` and proves it is excluded from `targetDeliveryFiles` and staging. | Keep explicit file deliverables authoritative; only broad-scope historical closeback scratch files are excluded. |
| ATM-BUG-2026-07-10-072 | 2026-07-10 | AI-Atomic-Framework | Optimization | Medium | Fixed in commit-attempt status lane | Governed git / commit timeout recovery | `node atm.mjs git commit --auto-stage` can exceed the agent tool timeout while the nested git commit and pre-commit hook continue running in the background; the commit may succeed later, but the operator must inspect processes and HEAD manually to avoid duplicate commits. Existing commit-status improvements help, but the normal failure surface still leaves too much live-process ambiguity when the caller aborts. | When the outer ATM git wrapper is interrupted or times out, it should leave a recoverable status packet with the live child process ids, hook phase, branch queue lock, staged files, and an exact `git commit-status` / wait command. Ideally the wrapper should stream or poll long pre-commit phases so the agent does not time out silently. | Fixed by writing `atm.gitCommitAttemptStatus.v1` before invoking host git, updating it through `preparing-commit`, `running-git-commit`, `timeout`, `failed`, or `committed`, and including `statusCommand`, `retryCommand`, timeout budget, before/after HEAD, head-advanced signal, and live-index rollback residue. `git commit-status` also reports live branch queue owner PID/liveness and wait guidance. Verification: `node --strip-types tests/cli/git-commit-timeout-and-status.test.ts` passes. | A future enhancement can add finer hook phase streaming, but operators now have a durable status packet and exact recovery command after timeout. |
| ATM-BUG-2026-07-10-073 | 2026-07-10 | AI-Atomic-Framework | Optimization | Medium | Fixed in actor-resolution preview lane | Actor identity / next claim UX | `identity set --actor codex-captain-m8e` did not affect `next --claim --task`, which claimed as the environment-projected `codex-gpt-5.4-mini`; a follow-up explicit claim with `--actor codex-captain-m8e` then failed with `ATM_LOCK_CONFLICT`. This is understandable internally, but confusing in Captain/Team Broker mode where the operator is trying to keep a stable actor label. | `next --claim --task` should either honor the repo default identity, warn that `AGENT_IDENTITY` overrides it before claiming, or expose a pre-claim actor preview. If an explicit actor mismatch is detected immediately after same-session claim, the recovery guidance should explain whether to continue with the projected actor or release/reclaim. | Fixed by adding an `actorResolution` diagnostic shared by `identity show` and `next --claim`, including precedence, winning source, env/default actors, override warning, and lock-conflict recovery hint. Verification: `node --strip-types tests/cli/identity-per-actor-routing.test.ts` covers `AGENT_IDENTITY` overriding repo default and asserts `ATM_NEXT_CLAIMED` exposes `actorSource`. | Keep the diagnostic output stable for Captain/Team Broker UX; a future interactive confirmation can build on the same `actorResolution` payload if needed. |
| ATM-BUG-2026-07-10-074 | 2026-07-10 | AI-Atomic-Framework | Bug | Medium | Fixed in planning-root status triangulation lane | Task status triangulation / planning mirror closeback | After a successful adopter-aware planning closeback commit, `tasks status` can still report `planningFrontmatter.status: null` and classify the task as `stale-import` or `ledger-ahead-of-planning-mirror`, even though live ledger is `done` and the planning repo close commit exists. Target ledger `source.planPath` values imported from `3KLife` are often relative planning-tree paths that do not resolve from the target repo working tree. | Status triangulation should resolve the task's `source.planPath` back to the planning repo via `ATM_PLANNING_REPO_ROOT` (or an absolute stored path) after taskflow closeback and report the actual planning frontmatter status, or explain precisely why the mirror cannot be read. A successful closeback should not immediately look stale. | Fixed by routing `resolvePlanningCardPath()` through `resolvePlanAbsoluteFromStored()`, so planning-root-relative ledger paths resolve against configured planning roots instead of the target repo root. Regression coverage: `node --strip-types packages/cli/src/commands/tasks/__tests__/status-triangulation.spec.ts` creates a temporary `ATM_PLANNING_REPO_ROOT` and proves `source.planPath: rft-hardening/tasks/TASK-RFT-0004.task.md` resolves to the external planning card. | Keep the planning-root-relative resolver regression alongside parity override tests; broader close exit behavior remains tracked by `ATM-BUG-2026-07-10-078`. |
| ATM-BUG-2026-07-10-075 | 2026-07-10 | AI-Atomic-Framework | Bug | Medium | Fixed in encoding touched multi-file parser lane | Encoding guard / touched-file reporting | `npm run check:encoding:touched -- --files <many files>` can pass while reporting only the first file in `evidence.files`, which makes the guard hard to trust for multi-file edits. | The guard should either verify and report every explicit `--files` entry, or clearly distinguish "checked files" from "display sample" so agents know the full touched set was covered. | Fixed by teaching `scripts/check-encoding-touched.ts` to consume every non-option token after `--files`, while preserving CSV and `--files=<csv>` input. Verification: `npm run check:encoding:touched -- --files scripts/check-encoding-touched.ts scripts/validate-script-parity.ts` now reports both files in `evidence.files`; `node --strip-types scripts/validate-script-parity.ts --mode validate` includes a multi-file probe. | Keep the multi-token `--files` regression in script parity so encoding guard evidence cannot silently shrink back to a one-file sample. |
| ATM-BUG-2026-07-10-076 | 2026-07-10 | AI-Atomic-Framework | Bug | Medium | Fixed in cross-file staging-hint lane | Pre-commit / cross-file consistency | Governed `git commit` can fail late with `ATM_PRE_COMMIT_CROSS_FILE_INCONSISTENCY` when a staged file imports or re-exports a sibling module that was created during a split but left unstaged, even though the file is already in task scope. | Commit preflight should detect unstaged in-scope siblings referenced by staged changes and either list them in the failure message with exact `git add` paths, or offer a governed auto-stage suggestion before hooks run. | Fixed by enriching `ATM_PRE_COMMIT_CROSS_FILE_INCONSISTENCY` findings with `files: [<missing sibling>]` and a copyable `git add -- "<path>"` command for untracked, deleted, and unstaged-modified local imports. Verification: `node --strip-types packages/cli/src/commands/__tests__/framework-mode-staged-residue.spec.ts` asserts the missing sibling file and staging command are present. | Keep this as the hook-level final safety net; broader pre-expansion of split task scope remains tracked by `ATM-BUG-2026-07-10-079`. |
| ATM-BUG-2026-07-10-077 | 2026-07-10 | AI-Atomic-Framework | Bug | Medium | Fixed in done-task mirror reconciliation lane | Tasks import / done-task reconciliation | `tasks import --write` on an already `done` task fails with `ATM_TASKS_IMPORT_DONE_REQUIRES_REOPEN`, and `tasks import --write --force` requires an emergency approval lane (`ATM_EMERGENCY_LANE_APPROVAL_REQUIRED`). After cross-repo planning close updates 3KLife frontmatter, target operators cannot cleanly re-import planning mirror metadata to clear residue without reopening or emergency override. | Provide a governed read-only reconciliation import for done tasks that updates planning mirror fields without mutating delivery scope, or let `taskflow close` write authoritative mirror metadata back into the target ledger when planning commits succeed. | Fixed by adding `tasks import --write --reconcile-mirror`, which refreshes planning/source metadata for existing done tasks while preserving `status: done`, closure/provenance fields, and active delivery state. Residue recovery now recommends `--reconcile-mirror` instead of emergency `--force`. Verification: `node --strip-types packages/cli/src/commands/tasks/__tests__/import-orchestrator.spec.ts` covers a done ledger reconciliation, and `node --strip-types packages/cli/src/commands/tasks/__tests__/residue-diagnostics.test.ts` passes with the updated recovery command. | Keep `--reconcile-mirror` separate from `--force`, `--reopen`, and `--reset-open`; broader taskflow close exit semantics remain tracked by `ATM-BUG-2026-07-10-078`. |
| ATM-BUG-2026-07-10-078 | 2026-07-10 | AI-Atomic-Framework | Bug | Medium | Fixed in close residue-advisory lane | Taskflow close / exit semantics | `taskflow close --write` can land delivery and closure commits, set ledger `status: done`, and push planning commits, yet still exit `1` when planning-mirror residue remains classified in target `tasks status`. Operators interpret exit `1` as a failed close and retry closeout unnecessarily. | Close should distinguish hard failure from advisory residue: exit `0` with explicit residue warnings when ledger closure and governed commits succeeded, or return a dedicated residue status code with recovery commands instead of a generic failure exit. | Fixed by adding `atm.taskflowCloseResidueAdvisory.v1` to successful close-write results when the remaining bucket is planning mirror drift (`planning-mirror-only`, `stale-import`, or `ledger-ahead-of-planning-mirror`). The warning includes the recovery command, now routed through `tasks import --write --reconcile-mirror` for done ledgers. Verification: `node --strip-types packages/cli/src/commands/taskflow/__tests__/close-residue-advisory.spec.ts`, `node --strip-types packages/cli/src/commands/tasks/__tests__/residue-diagnostics.test.ts`, and `npm run typecheck` pass. | Keep ambiguous residue fail-closed; only post-close planning mirror drift is downgraded to advisory after backend close and commit bundle success. |
| ATM-BUG-2026-07-10-079 | 2026-07-10 | AI-Atomic-Framework | Optimization | Medium | Fixed in mechanical split scope-checklist lane | Task card authoring / mechanical split scope | RFT atom-map split cards can under-declare deliverable paths relative to the modules a mechanical extraction actually creates, forcing repeated mid-flight `tasks scope add` calls for siblings such as `types.ts`, `shared.ts`, `constants.ts`, `role-registry.ts`, and lane modules. | Import/open playbooks for split tasks should derive or pre-expand scope from the atom map and split recipe, or surface a one-shot scope-amendment checklist before the first delivery commit. | Fixed by adding `ATM_TASK_IMPORT_MECHANICAL_SPLIT_SCOPE_CHECKLIST` to task-card import diagnostics when a split/facade/module/atom-map task declares only one file-shaped scope/deliverable and no broad pattern. The diagnostic tells operators to review sibling `types.ts`, `shared.ts`, `constants.ts`, registry files, and lane modules before first delivery commit, and gives the one-shot `tasks scope add --paths` route. Verification: `node --strip-types packages/cli/src/commands/tasks/__tests__/task-card-writer.spec.ts` and `npm run typecheck` pass. | Keep this as a preflight checklist rather than auto-expanding arbitrary sibling files; atom-map-derived exact expansion can build on the same diagnostic later. |
| ATM-BUG-2026-07-10-080 | 2026-07-10 | AI-Atomic-Framework | Optimization | Low | Fixed in planning-index advisory lane | Cross-repo planning docs / closeout sync | After `taskflow close --write` lands planning commits in `3KLife`, the planning task index (`docs/ai_atomic_framework/rft-hardening/tasks/README.md`) and plan snapshot can still list completed RFT cards as `planned` or in the open-priority queue until an operator manually edits and commits planning docs. | Planning close bundles should either update the index/README as part of governed closeout, or emit an explicit post-close advisory with the exact docs paths and frontmatter fields that still lag ledger reality. | Fixed by adding `atm.taskflowPlanningIndexAdvisory.v1` to successful `taskflow close --write` results whenever a planning roster/index path or roster follow-up command is present. The advisory reports the planning repo root, README/index path, planning card path, closeback frontmatter fields, status (`updated`, `already-current`, or `follow-up-required`), row diff, and exact `tasks roster update` command. Verification: `node --strip-types packages/cli/src/commands/taskflow/__tests__/close-residue-advisory.spec.ts` and `npm run typecheck` pass. | Keep inline roster sync in the close bundle; the advisory is the operator-facing audit trail and fallback when a profile uses follow-up roster sync. |
| ATM-BUG-2026-07-10-081 | 2026-07-10 | AI-Atomic-Framework | Bug | Medium | Fixed in planning-card import-required route lane | Tasks import / next routing / planning-card ledger sync | After a planning card exists in `3KLife`, `next` can remain stuck on the previously imported task because the newer card has not yet entered the target ledger. In Captain/Team Broker mode this makes the official queue look like it is still on the old M9I task, even though the operator intends to advance to the next planning card. | `next --prompt` should either import/register the referenced planning card into the target ledger when enough path/context exists, or emit an explicit `planning-card-not-in-target-ledger` action with the exact `tasks import --from <card> --write` command before recommending or reselecting an already-imported task. | Fixed by adding `atm.planningCardImportRequirement.v1` to prompt-scoped markdown planning-card routes and queue heads. When a route resolves to a planning card that is not an imported target-ledger task, `nextAction.command` / `requiredCommand` point to `node atm.mjs tasks import --from <card> --write --json`, while the eventual task-scoped claim command remains available. Verification: `node --strip-types scripts/validate-prompt-scoped-next.ts` covers shorthand external planning-card routing and import-required evidence. | Keep ledger JSON tasks on normal claim routes; only non-ledger planning cards receive the import-required payload. |
| ATM-BUG-2026-07-10-082 | 2026-07-10 | AI-Atomic-Framework | Product gap | High | Fixed in vendor execution backend lane | Team Agents / vendor runtime execution | `TASK-TEAM-0042`, `TASK-TEAM-0043`, and `TASK-TEAM-0044` were correctly closed for the M9I minimum provider bridge contract, but the task titles and roster wording could make operators think real OpenAI/Azure OpenAI/Claude Code/Gemini/Microsoft Foundry execution backends were production-ready. The provider files previously implemented config validation, permission-broker checks, session ids, artifacts, and observability envelopes without calling vendor SDKs, making network requests, spawning editor/CLI providers, streaming outputs, or executing tool calls. | TEAM status surfaces should distinguish contract bridge completion from real execution readiness, and provider bridges that claim execution readiness should actually call the configured vendor HTTP or command surface while preserving broker authority and redaction. | 2026-07-10 audit found no `fetch`, `https.request`, `child_process`, `spawn`, `execFile`, or vendor SDK calls in `packages/core/src/team-runtime/providers/{openai,azure-openai,claude-code,gemini,microsoft-foundry}.ts`. The repair adds HTTP execution for OpenAI/Azure OpenAI/Microsoft Foundry, command execution for Claude Code/Gemini, executor injection for deterministic validation, artifact execution summaries, and docs in `docs/governance/team-agents/team-vendor-runtime.md`. Validation: `npm run typecheck`; `node --strip-types scripts/validate-team-agents.ts --case openai-azure-openai-bridges`; `--case claude-gemini-bridges`; `--case microsoft-foundry-bridge`. | Keep future TEAM roster wording precise: M9I bridge cards are contract + execution backend when validators prove the HTTP/command executor paths, not merely descriptor readiness. |
| ATM-BUG-2026-07-10-083 | 2026-07-10 | AI-Atomic-Framework | Bug | Medium | Fixed in integration adapter drift repair | Integration adapters / doctor | `node atm.mjs doctor --json` failed on `ATM_DOCTOR_INTEGRATION_DRIFT` after framework source/template changes left installed adapter files and `.atm/integrations/*.manifest.json` out of sync. The drift was unrelated to the Team vendor execution backend but still made doctor look red after a successful guarded push. | Installed integration adapters should be refreshed through `node atm.mjs integration add <adapter> --force --json`, then `doctor` should pass or report only unrelated active worktree residue. | Reproduced 2026-07-10: doctor reported stale/drift for `antigravity`, `claude-code`, `codex`, `copilot`, and `cursor`, with `gemini` installed. Repair refreshed all six installed adapters through the official integration CLI and re-ran verification. | Keep adapter parity refresh as one governed batch when several manifests share the same source snapshot drift; do not hand-edit generated adapter files. |
| ATM-BUG-2026-07-10-084 | 2026-07-10 | AI-Atomic-Framework | Optimization | Medium | Fixed in Team pre-commit compaction lane | Team Agents / pre-commit governance UX | A valid Team hook gate can make commit recovery noisy and slow when several stale active Team runs exist: pre-commit repeats `ATM_TEAM_GIT_OWNER_REQUIRED` for each active run and prints the full staged file list many times, while also reporting the separate framework-claim prerequisite. | Pre-commit should compact duplicate Team git-owner blockers by task/run family, show one staged-file summary, and prioritize the shortest ordered recovery path: framework-mode claim first, then retry with `ATM_COMMIT_ACTOR_ID=coordinator git commit`. Stale active Team runs should be summarized with exact lifecycle cleanup commands when they are not related to the current commit. | Fixed by compacting multiple git-owner Team blockers in `evaluateTeamPreCommitGate()` into one finding with `teamRunIds`, `taskIds`, one staged file summary, and the same coordinator commit recovery command. Verification: `node --strip-types scripts/validate-team-agents.ts --case hook-team-gate` covers two active Team runs and asserts a single `ATM_TEAM_GIT_OWNER_REQUIRED` finding. | Keep the hook-team-gate regression focused on duplicate blocker compaction; lifecycle cleanup hints for unrelated stale runs can be expanded in a later Team runtime UX slice. |
| ATM-BUG-2026-07-10-085 | 2026-07-10 | AI-Atomic-Framework | Bug | Medium | Fixed in team-validator runtime-cleanup lane | Validator harness / Team runtime residue | A successful pre-commit run can leave an untracked active Team run under `.atm/runtime/team-runs/**` in the real repository because `validate:cli` executes a Team start smoke against the working tree instead of an isolated temp repo or guaranteed cleanup lane. | Validators and pre-commit hooks should leave no untracked `.atm/runtime/**` residue in the operator repository; Team runtime smoke tests should run in temp fixtures or clean up through an official CLI lifecycle/cleanup command. | Fixed by wrapping `scripts/validate-team-agents.ts` with a source-repo team-run snapshot/cleanup guard that removes validator-created `.atm/runtime/team-runs/*.json` files while preserving files present before the validator starts. Verification: `node --strip-types scripts/validate-team-agents.ts --case source-runtime-residue-cleanup` and `node --strip-types scripts/validate-team-agents.ts --case hook-team-gate` both pass. | Keep Team start/status smoke tests in temp workspaces; the source-repo cleanup guard is a final safety net for validator-created runtime residue. |
| ATM-BUG-2026-07-11-086 | 2026-07-11 | AI-Atomic-Framework | Product gap | High | Fixed in Team Agents closeback ledger lane | Team Agents / task ledger closeback | The Team Agents completion slice for `TASK-TEAM-0053` through `TASK-TEAM-0065` existed in source history, but those task ids were not present in `.atm/history/tasks/**` or the 3KLife planning-card directory, so operators could not prove the 13-card plan was imported and closed through normal task ledger governance. | A bulk Team completion delivery should have planning cards or an equivalent governed historical closeback path before it is treated as ledger-complete. Source commits can be used as historical delivery evidence, but `.atm/history/tasks/**` must be created by official import/close commands, not hand-authored. | Fixed on 2026-07-11 by importing and closing the missing closeback chain through governed taskflow: `TASK-TEAM-0050` target commit `80f31df2` / planning commit `ffc7b2be`; `TASK-TEAM-0051` target `364ebafe` / planning `27774733`; `TASK-TEAM-0052` target `9f50ccd2` / planning `ff6b385f`; `TASK-TEAM-0053` target `b5f9ff8c` / planning `ce60e2c8`; aggregate `TASK-TEAM-0065` closeback commit `c95490f6`. `tasks status` reports live ledger and planning frontmatter done for 0050/0051/0052/0053/0065. | Keep future Team completion slices from landing as source-only history: import or generate task ledgers before dependency-ordered taskflow close, and use aggregate closeback only when explicit individual cards are unavailable. |
| ATM-BUG-2026-07-11-087 | 2026-07-11 | AI-Atomic-Framework | Bug | High | Fixed in generated-residue defer lane | Taskflow close / lock cleanup / record commit | ATM治理流程/收口UX自身不順：released task locks can retain an embedded `taskDirectionLock.status: active`, and `tasks lock cleanup --task` plus emergency `tasks lock cleanup --all-stale` can report success without clearing the lock. At the same time, `git record-commit --dry-run` accepts a cross-task `.atm/history` record packet that the real pre-commit hook later rejects as `ATM_CROSS_TASK_MUTATION_BLOCKED`, forcing operators into manual lock clearing and temporary generated-residue isolation during historical closeback. This is an ATM workflow bug, not an adopter project feature bug. | Released locks should not be treated as active by broker/pre-commit gates, cleanup commands should either remove or fully deactivate nested direction locks, and record-commit dry-run must match real hook behavior for cross-task history packets. Generated residue from sibling task evidence should have a governed defer/reconcile lane instead of blocking unrelated task-scoped closeback. | Dogfood during 2026-07-11 Team Agents closeback: `TASK-TEAM-0050` was `done`/released in live ledger, but `node atm.mjs lock check --task TASK-TEAM-0050 --owner coordinator --json` still reported active because `.atm/runtime/locks/TASK-TEAM-0050.lock.json` had outer `status: released` and nested `taskDirectionLock.status: active`. `node atm.mjs tasks lock cleanup --all-stale --actor coordinator --emergency-approval EMG-GLOBAL-ebee74b1f4 --json` reported 140 stale locks cleaned with `cleanupActions: []`, yet the lock remained. `node atm.mjs git record-commit --actor coordinator --message ... --dry-run --json` accepted staged low-risk records for `TASK-TEAM-0050/0051/0052/0053/0065`, but the real commit failed with `ATM_CROSS_TASK_MUTATION_BLOCKED`. `TASK-TEAM-0065.bundle-manifest.json` also blocked `TASK-TEAM-0050` task-scoped commit until it was explicitly moved aside and later restored/regenerated. Fixed on 2026-07-11 in stages: lock store now treats release markers with `released: true` or `status: released` as inactive even when an embedded `taskDirectionLock.status: active` remains; regression `packages/cli/src/commands/__tests__/lock.spec.ts` covers `lock check` and reacquire. `tasks lock cleanup` now marks embedded direction locks as released and reports `released-embedded-direction-lock`; regression `packages/cli/src/commands/tasks/__tests__/lock-cleanup.spec.ts` covers stale released-lock cleanup. `git record-commit --dry-run` now rejects multi-task staged record packets with `ATM_CROSS_TASK_MUTATION_BLOCKED`; regression `tests/cli/git-record-commit.test.ts` covers the dry-run parity case while preserving single-task low-risk record commits. `git commit --task ... --defer-foreign-staged` now also treats dirty/untracked foreign bundle manifests, closure packets, and close/reconcile/repair task events as explicit deferred governance residue instead of blocking the current task bundle; regression `tests/cli/git-commit-task-scoped-staging.test.ts` covers deferrable dirty foreign governance residue and requires an explicit warning. | Consider a first-class closeback setup lane for importing sibling ledgers before dependency-ordered taskflow close, but the blocking generated-residue dogfood failure is fixed. |
| ATM-BUG-2026-07-11-088 | 2026-07-11 | AI-Atomic-Framework | Optimization | Medium | Fixed in validate fast/full split lane | Evidence runner / validator performance UX | Routine closeback evidence can spend about five minutes in `npm run validate:cli` during a single task evidence pass, with the operator UI showing a long-running shell step rather than a compact validator phase breakdown, cache hit/miss explanation, or task-scope-based fast path. A related closeback contract mismatch can also mark `validate:cli` and `validate:git-head-evidence` as advisory/out-of-scope in pre-close, then fail `taskflow close --write` because the closure packet still requires those entries. This makes Captain closeback loops feel stalled and undermines trust in pre-close guidance. | ATM evidence runs should expose validator phase timing, reuse fresh task-compatible evidence where safe, and derive the smallest required validator set from task scope and protected-surface impact. Long validators should stream or report progress and offer resumable/status commands so operators can distinguish healthy long work from a stuck run. Pre-close, auto-evidence, closure-packet generation, and close-write gates must agree on which validators are blocking versus advisory. | Dogfood during 2026-07-11 Team Agents closeback: while refreshing evidence for `TASK-TEAM-0051`, the sequential evidence loop reached `RUNNING: npm run validate:cli` and remained active for roughly 4m29s to nearly five minutes before completion. During `TASK-TEAM-0052`, pre-close reported `validate:cli` and `validate:git-head-evidence` as `skipped-out-of-scope` advisory validators, but `taskflow close --write` then failed with `ATM_TASK_CLOSE_CLOSURE_PACKET_INVALID` missing `validationPasses/validate:cli` and `validationPasses/validate:git-head-evidence`. Fixed on 2026-07-11 in stages: `scripts/validate-cli.ts` emits stderr phase markers, 30-second heartbeat progress, per-focused-test timings, and final elapsed time. Independent focused subprocess tests run in parallel with stdout/stderr preserved on failure. Heavy historical-delivery coverage was moved into `tests/cli/validate-cli-historical-delivery.test.ts`, and git/hook-heavy regressions such as `git-commit-failure-residue-rollback.test.ts`, `git-commit-timeout-and-status.test.ts`, `git-record-commit.test.ts`, and `validate-cli-close-gate.test.ts` remain direct targeted tests instead of being run concurrently inside the full integration lane, because local dogfood showed they amplify Windows/Git branch-queue contention when overlapped with the workspace fixture. The default `npm run validate:cli` runs a fast profile that exits before the heavy integration workspace; local verification after the final fast/full split passed in 10s, below the 30s target. `npm run validate:cli:full` retained the integration workspace and passed in 1m19s on 2026-07-11 after replacing repeated onboarding/ATMChart child-process launches with in-process CLI calls and adding full workspace subphase markers for onboarding/ATMChart, integration adapters, spec validation, bootstrap/verify, upgrade/review, and evidence quoting/auto-link. Targeted follow-up verification passed for `node --strip-types tests/cli/git-commit-failure-residue-rollback.test.ts`, `node --strip-types tests/cli/git-commit-timeout-and-status.test.ts`, `node --strip-types tests/cli/git-record-commit.test.ts`, and `node --strip-types tests/cli/validate-cli-close-gate.test.ts`. `docs/testing-strategy.md` sets the default `validate:cli` budget to under 30 seconds and documents that new spawned/temp-workspace regressions must be standalone focused tests registered in the appropriate parallel batch; `scripts/validate-cli.ts` carries the same authoring rule next to the focused-test registration point. `tasks close` closure-packet required gates now use the same changed-file filter as pre-close validator classification through `requiredValidationPassesForClosure(frameworkStatus.requiredGates, closePacketChangedFiles)`, with `tests/cli/closure-required-gates-contract.test.ts` wired into the parallel `validate:cli` batch to prevent regression. The validator resume regression now uses fast deterministic validators instead of the root-wide neutrality scanner, removing a parallel-batch flake. | Keep full-suite optimization as an ordinary future enhancement, not an open blocker for the default closeback lane: further work can cache unchanged full-workspace receipts or split the full integration workspace into additional standalone tests if full runtime becomes release-blocking. |
| ATM-BUG-2026-07-11-089 | 2026-07-11 | AI-Atomic-Framework | Bug | High | Fixed in taskflow target-scope precedence lane | Prompt-scoped next / planning path classification | A target-repo deliverable could be projected into both `targetAllowedFiles` and `planningReadOnlyPaths` during `next --claim`, causing `taskflow pre-close` / commit-bundle preview to treat a valid declared deliverable as outside the active direction lock. This blocked historical closeback even when the task ledger `scopePaths`, `deliverables`, `claim.files`, and runtime lock `files` all included the path. | Prompt-scoped routing must never classify the same target-repo path as planning read-only when it is also a target allowed file or declared deliverable. Planning read-only paths should be limited to external planning-card context or explicitly read-only host documents, and commit-bundle validation should prefer target scope for overlapping paths in the target repository. | Fixed on 2026-07-11 by making taskflow target scope take precedence over planning read-only overlap when resolving declared files and effective deliverables. The repair was built into the frozen runner with `ATM_RETAIN_RELEASE_ARTIFACTS=1 npm run build`, then verified by successfully closing `TASK-TEAM-0053` through `taskflow close --historical-delivery 38fdb423 --waiver-out-of-scope-delivery --write`; the historical delivery report now includes `agent-integrations/vendors/team-secrets.example.json` in `taskMatchedFiles`. | Add focused regression coverage for prompt-scoped planningReadOnly/targetAllowed overlap, and consider a repair command that can recompute direction-lock planning context without hand-editing runtime JSON. |
| ATM-BUG-2026-07-11-090 | 2026-07-11 | AI-Atomic-Framework | Bug | Medium | Fixed in TASK status residue classification lane | Tasks status / planning mirror reconcile | ATM治理流程/收口UX自身不順：`tasks status` could continue classifying a task as `planning-mirror-only` after `tasks import --reconcile-mirror --write` succeeded and both the live ledger plus planning frontmatter reported `done` with no divergence. This left operators with a stale-looking residue recommendation even after following the official reconcile command, and the generated reconcile artifacts could then block later normal commits. This was an ATM workflow/closeout UX bug, not an adopter project feature bug. | After a successful reconcile-mirror write, status should clear the `planning-mirror-only` residue bucket when live ledger, planning mirror, task events, and closeout provenance agree. ATM-generated reconcile/event residue should not be left as a blocker for unrelated framework fixes. | Fixed on 2026-07-11 by making done/done/no-divergence status with complete closeout provenance classify as `no-residue` even when planning and target repo fields describe the same-repo planning authority shape. Verification: `node --strip-types packages/cli/src/commands/tasks/__tests__/residue-diagnostics.test.ts`, `npm run typecheck`, and frozen `node atm.mjs tasks status --task TASK-TEAM-0065 --json`, which now reports `residueBucket: no-residue` and diagnostic-only `nextCommand: node atm.mjs tasks status --task TASK-TEAM-0065 --json`. | Keep the regression for done/done same-repo planning authority. A separate pre-commit adoption/advisory path for generated reconcile event residue remains covered by `ATM-BUG-2026-07-11-087`. |
| ATM-BUG-2026-07-11-091 | 2026-07-11 | AI-Atomic-Framework | Documentation / AI routing | Medium | Fixed in backlog-ID maintenance route lane | Prompt-scoped next / backlog closeout routing | A prompt that names an existing backlog item such as `ATM-BUG-2026-07-07-048` is interpreted as an ATM ledger task id, so `next --prompt` returns `ATM_NEXT_TASK_SCOPE_NOT_FOUND`; falling back to `start` then routes the repair as `create-atom` and asks for unrelated atom evidence. This interrupts a valid bug-closeout workflow and makes the operator recover manually before implementation can begin. | Existing ATM backlog IDs should resolve to their canonical backlog entry and a governed maintenance/closeout route, or `next` should emit one explicit command to open a linked task card. It should not classify the backlog ID as a missing ledger task or route an existing bug fix into atom birth. | Fixed on 2026-07-11 by excluding canonical `ATM-BUG-YYYY-MM-DD-NNN` and `PROJECT-BUG-YYYY-MM-DD-NNN` identifiers from ledger task-id extraction. `npm run validate:prompt-scoped-next` verifies that `Resolve ATM-BUG-2026-07-07-048 and push the fix` routes to `ATM_NEXT_FRAMEWORK_TEMP_CLAIM_REQUIRED`, with no `ATM_NEXT_TASK_SCOPE_NOT_FOUND` and `taskScopeMentioned: false`. | Keep the backlog-ID routing regression beside prompt-scoped next coverage; a future linked task-card workflow may add richer backlog metadata, but maintenance routing must remain unblocked. |
| ATM-BUG-2026-07-11-092 | 2026-07-11 | AI-Atomic-Framework | Documentation / AI routing | Medium | Fixed in Team commit relevance gate lane | Team runtime commit ownership / closeout UX | After all implementation and validation gates passed, governed commit was blocked because five stale or unrelated active Team runs required Coordinator/git.write ownership, even though the current framework claim belonged to the implementing actor. The recovery hint was an environment-prefixed native Git command rather than a governed ATM actor handoff. | A framework closeout should identify the owning Team runs, provide a governed actor handoff or cleanup command, and make the required commit route reproducible through ATM without asking an agent to impersonate another actor or manually translate an environment hint. | Fixed on 2026-07-11 by making Team git-owner enforcement apply only when staged paths intersect an active run's `file.write` lease. `node --strip-types scripts/validate-team-agents.ts --case hook-team-gate` verifies an implementer is still blocked for `packages/cli/src/commands/team.ts` but is allowed to commit unrelated `scripts/validate-team-agents.ts`. | Keep Team git.write ownership strict for matching file.write scopes and retain the focused unrelated-run regression so stale or unrelated runs cannot block framework maintenance. |
| ATM-BUG-2026-07-11-093 | 2026-07-11 | AI-Atomic-Framework | Bug | High | Fixed in Team overlap diagnostics lane | Frozen release dependency sync | A scope-aware Team git-owner gate correctly blocks commits that overlap an active run's `file.write` lease, but its aggregated failure previously omitted the overlapping staged files. That made a valid Coordinator requirement look like unrelated stale-run residue. | When Team ownership blocks a commit, ATM should show the exact staged files that overlap active `file.write` leases so the operator can distinguish a real scoped handoff from unrelated run residue. | Reproduced on 2026-07-11 while committing the 091/092 repair: `packages/cli/src/commands/next.ts` and `scripts/validate-prompt-scoped-next.ts` overlap the active `TASK-AAO-0043` lease. Fixed by returning `relevantFiles` and adding them to `ATM_TEAM_GIT_OWNER_REQUIRED` details. `node --strip-types scripts/validate-team-agents.ts --case hook-team-gate` asserts the exact overlap. | Keep the overlap list in the pre-commit envelope and preserve both related-scope block and unrelated-scope allow regressions. |
| ATM-BUG-2026-07-11-094 | 2026-07-11 | AI-Atomic-Framework | Bug | High | Fixed in TASK-AAO-0198 | Team Agents / runtime selection precedence | A global `--runtime-mode real-agent` is ignored for the Coordinator because `buildTeamRuntimeContract` always resolves the merged selection config first; the implicit repo default has `runtimeMode: broker-only`, so a CLI-requested paid run silently becomes broker-only. | Explicit global CLI runtime/provider/model options should override implicit defaults. Role-specific CLI overrides may then override the global selection for their own roles. | Fixed in TASK-AAO-0198: `explicitRuntimeMode` / related flags override repo-default broker-only; regression `team-runtime-precedence.spec.ts`. | Keep role-specific `--role-provider` above global CLI selection. |
| ATM-BUG-2026-07-11-095 | 2026-07-11 | AI-Atomic-Framework | Bug | High | Fixed in TASK-AAO-0196 | Team Agents / execute result contract | `team start --execute` can return exit code 0 and `ATM_TEAM_STARTED` when provider execution is blocked and no provider bot ran. The success envelope can be mistaken for a completed paid live test. | An explicit execute request must fail closed, or at least return a blocking/non-success result, when `providerExecutionCount` is zero. The message must distinguish a state-only Team run from executed provider orchestration. | Fixed in TASK-AAO-0196: `resolveTeamStartExecutionLane` fail-closes zero-execution `--execute`; regression `team-execute-fail-closed.spec.ts`. | Keep state-only `team start` (no `--execute`) as non-blocking success. |
| ATM-BUG-2026-07-11-096 | 2026-07-11 | AI-Atomic-Framework | Bug | High | Needs task card | Team Agents / direct provider permission scope | Concrete OpenAI and Anthropic Team bridge execution passed `scopedPaths: []`, so the shared permission broker rejected every paid role even though the task had a governed allowed-file scope. | Direct provider roles must receive the normalized task allowed files; the broker must continue rejecting truly empty scopes. | `TASK-TEAM-0066` runs `team-74228cfda35d` and `team-f8ef2357ca7b` reached 10 and 4 provider executions respectively but all roles failed. A direct `launchAnthropicTeamProviderRun` probe exposed `Permission denied by governed broker policy or missing scoped paths`, while the same key/model returned HTTP 200 directly. | Fix in `TASK-TEAM-0068` with deterministic injected-executor coverage, then rerun paid dogfood. |
| ATM-BUG-2026-07-11-097 | 2026-07-11 | AI-Atomic-Framework | Bug | High | Fixed in working tree (cursor-grok-4.5) | Task lock cleanup / canonical direction lock | `tasks lock cleanup --all-stale` reported 151 stale locks cleaned and released the runtime lock's embedded direction lock, but left the terminal task ledger's canonical `taskDirectionLock.status` active. `tasks repair-claim` simultaneously reported no repairable drift, so `taskflow close` remained blocked with `ATM_TASKFLOW_CLOSE_BROKER_TAKEOVER_REQUIRED`. | Cleanup must synchronize terminal stale-lock release across the runtime lock, task ledger direction lock, and sidecar, and report each mutation accurately. | Fixed 2026-07-14: `releaseLock()` now syncs embedded + canonical direction locks and sidecar. Regression: `packages/cli/src/commands/tasks/__tests__/lock-cleanup.spec.ts`. | Governed commit/close still pending after foreign runner-sync wipe recovery. |
| ATM-BUG-2026-07-11-098 | 2026-07-11 | AI-Atomic-Framework | Optimization | High | Needs triage | Taskflow close / broker and commit-bundle recovery | A terminal task's stale broker intent required a separate `broker release` after lock cleanup, and `taskflow close` dry-run reported ready while `--write` first revealed a missing closure-packet validator and then twice rolled back after `ATM_HOOK_PRE_COMMIT_DEFERRED_GOVERNANCE_REQUIRED`, including with `--defer-foreign-state`. Reconfirmed on 2026-07-12 during `TASK-AAO-0163`: `pre-close` reported ready and dry-run treated `validate:cli` / `validate:git-head-evidence` as advisory skipped, but `close --write` failed with `ATM_TASK_CLOSE_CLOSURE_PACKET_INVALID` until both validators were added. | Close preflight should expose every closure-packet requirement; terminal lock cleanup or close should release stale broker intents; and taskflow's own governed bundle must not deadlock with its deferred-governance hook. | TASK-TEAM-0068 close required `broker release TASK-TEAM-0067`, then `validate:git-head-evidence`, then protected backend close after two taskflow write rollbacks. TASK-AAO-0163 close additionally proved pre-close readiness and closure-packet validation can disagree on required validators. | Add focused coverage for ready-to-write parity, terminal broker-intent cleanup, close-window deferred-governance self-ownership, and pre-close versus closure-packet validator parity. |
| ATM-BUG-2026-07-11-099 | 2026-07-11 | AI-Atomic-Framework | Bug | High | Fixed in working tree (cursor-grok-4.5) | Team Agents / OpenAI Responses metadata | Paid OpenAI Coordinator and Review Agent calls reached the Responses API but returned HTTP 400 because `metadata.scopedPathCount` was encoded as an integer; OpenAI metadata values must be strings. | Provider request builders must conform to vendor metadata schemas, with deterministic body-shape assertions before paid dogfood. | Fixed 2026-07-14: `azure-openai.ts` and `microsoft-foundry.ts` now `String(scopedPaths.length)` like `openai.ts`; validator asserts added in `validate-team-agents.ts` (live team-plan preflight may still fail on foreign broker dirt). | Keep metadata values string-only across all OpenAI-family bridges. |
| ATM-BUG-2026-07-11-100 | 2026-07-11 | AI-Atomic-Framework | Optimization | Medium | Needs triage | Team Agents / paid execution preflight | A full 10-role L5 run was needed to discover that OpenAI generation quota was exhausted, even though provider credentials and model listing were valid. Eight Anthropic calls completed before the overall command failed on two OpenAI 429 responses. | Paid multi-provider execution should optionally perform one minimal generation/quota probe per provider before launching the full role roster, report estimated call count, and require explicit continuation after a billing failure. | Final run `team-27b16b429317` reached both vendors; Anthropic was 8/8 HTTP 200 and OpenAI was 2/2 HTTP 429 `insufficient_quota`. | Add a redacted paid-preflight mode that distinguishes authentication, model access, request schema, quota, and role execution readiness. |
| ATM-BUG-2026-07-11-101 | 2026-07-11 | AI-Atomic-Framework | Optimization | High | Needs task card | Team Agents / direct provider artifact handoff | Direct-provider roles currently receive independent generic role prompts. The persisted artifact handoff contract says Review Agent consumes implementation artifacts, but concrete vendor orchestration does not yet pass the Anthropic Implementer output into the OpenAI Review Agent request. | Real-provider orchestration should carry bounded, redacted prior-role artifacts through declared `consumesFrom`/`producesTo` edges and prove that reviewer input references the implementer artifact. | Paid L5 runs invoked the intended providers and roles, but observability proves per-role execution only; it does not prove cross-vendor content handoff. | Author a separate implementation task with deterministic chaining coverage and a paid retry once OpenAI quota is available. |
| ATM-BUG-2026-07-11-102 | 2026-07-11 | AI-Atomic-Framework | Optimization | High | Fixed in working tree (cursor-grok-4.5) | Cross-task mutation incident recovery | `doctor` remains permanently blocked by any JSON file under `.atm/runtime/incidents` even after the conflicting tasks are done and their canonical locks and broker intents are released. Core exports `clearIncidentFlags()` but no CLI surface invokes it or verifies that the incident is resolved. | Provide an audited `incident status/resolve` command that re-evaluates current owners, locks, broker intents, and dirty paths before clearing flags; doctor should distinguish active from resolved incidents. | Fixed 2026-07-14: `readIncidentFlag()` reconciles and auto-archives resolved incidents. Regression: `packages/core/src/broker/__tests__/cross-task-mutation-guard.test.ts`. | Optional follow-up: expose CLI `incident status/resolve` on top of core reconcile. |
| ATM-BUG-2026-07-11-103 | 2026-07-11 | AI-Atomic-Framework | Bug | High | Needs task card | Team Agents / closed-task completion fidelity | TASK-TEAM-0053 closed with a Gemini Direct bridge and deterministic provider tests, but the concrete `team start --execute` dispatcher still accepts only OpenAI and Anthropic, and direct roles do not transfer declared artifacts to downstream reviewers. The closed task therefore did not prove its Captain handoff's live three-vendor cooperation requirements. | Closure and handoff audits must distinguish provider bridge existence from concrete orchestration reachability and cross-role artifact consumption; unproven live/manual requirements should remain explicit follow-up work. | Current `runDirectTeamProviderRole` rejects `gemini-direct`; paid L5 observability shows independent generic role prompts rather than reviewer consumption of implementer output. | `TASK-TEAM-0071` owns three-vendor execution, bounded artifact handoff, conflict continuation, and context benchmark evidence. |
| ATM-BUG-2026-07-11-104 | 2026-07-11 | AI-Atomic-Framework | Bug | High | In progress | Team Agents / historical completion evidence | The TASK-TEAM-0053 handoff and closed-task evidence described a Gemini Direct bridge as implemented, but `packages/core/src/team-runtime/providers/gemini-direct.ts` was absent from the current branch and every Git ref inspected. The closure claim therefore exceeded source reality. | Required deliverables named by a task card should be checked for tracked-file existence and reachable commit evidence before close; manual live requirements must remain explicitly unproven until run artifacts exist. | `git log --all -- packages/core/src/team-runtime/providers/gemini-direct.ts` returned no commits, while the handoff named the file as the preferred and required direct bridge. | TASK-TEAM-0071 restores the missing implementation and adds behavior-level three-vendor coverage; closeout must include source, validator, and live evidence. |
| ATM-BUG-2026-07-11-105 | 2026-07-11 | AI-Atomic-Framework | Bug | High | Fixed in TASK-AAO-0197 | Team Agents / execute CLI defaults | `team start --execute --provider openai --runtime-mode real-agent` wrote an active run but executed zero roles unless every L1 role also received an explicit `--role-provider` override. The command then reported `ATM_TEAM_EXECUTION_BLOCKED` with `providerExecutionCount: 0`. | Top-level provider, SDK, model, and runtime options should become the default role selection when role overrides are absent; zero selected roles should fail before writing an active run. | Fixed in TASK-AAO-0197: `cliGlobalDefault` / `cli-global-default` selection source populates every active roster role; regression `team-execute-defaults.spec.ts`. | Keep explicit `--role-provider` overrides above the global CLI default. |
| ATM-BUG-2026-07-11-106 | 2026-07-11 | AI-Atomic-Framework | Optimization | High | Fixed in TASK-AAO-0195 | Team Agents / plan actor and mutation clarity | `team plan` without `--actor` used stale `AGENT_IDENTITY=codex-gpt-5.4-mini` instead of the active task claim actor `Codex-GPT5.6 Sol`; it also registered a proposal-first provisional write lease during a command operators reasonably read as dry-run planning. | Team plan should prefer the active claim actor, report any identity mismatch before broker evaluation, and state clearly whether plan mutates broker leases; a truly read-only planning option should be available. | Fixed in TASK-AAO-0195: `resolveTeamPlanActorId` prefers active claim; `team plan --read-only` skips registry persist and softens proposal-first. | Covered by TASK-AAO-0195 / ATM-BUG-170. |
| ATM-BUG-2026-07-11-107 | 2026-07-11 | AI-Atomic-Framework | Optimization | Medium | Needs task card | Team Agents / validator discoverability | `npm run validate:team-agents` completed successfully after running only the default lieutenant-escalation lane, while its name suggests the complete Team Agents suite. Focused three-provider coverage had to be invoked separately. | Rename the script to indicate smoke scope or make the default runner enumerate all deterministic Team cases; close evidence should not treat one default case as full-suite proof. | The command output contained only wave-mode setup and `ok (lieutenant-escalation)`; `three-vendor-direct-artifact-handoff` and `heterogeneous-multi-bot-team-run` required explicit `--case`. | Add a manifest-driven all-cases runner with paid/live cases explicitly excluded. |
| ATM-BUG-2026-07-11-108 | 2026-07-11 | AI-Atomic-Framework | Regression | P0 | Fixed in TASK-AAO-0157 | Git governance / framework auto-stage | A framework `node atm.mjs git commit --auto-stage` again returned `ATM_GIT_COMMIT_OK` while committing only `.atm/history/evidence/git-head.jsonl`; 15 tracked release mirror files remained modified. This reproduces the failure class that backlog row 039 records as fixed. | Framework auto-stage success must prove that the intended dirty files were staged and included, or fail before creating an evidence-only commit; release mirrors generated by the canonical build need an explicit recognized staging class. | Commit `057ac25cb755c6e43f5800d26420fac494f9be61` contains one git-head line only, while `git status --short` immediately afterward still listed all generated release mirrors. | Fixed by teaching `isFrameworkGeneratedArtifactAllowed` to honor claim globs via `pathMatchesTaskScope` (so `release/**` admits dirty release mirrors even when absent from release-manifest `generatedFiles`); regression added in `scripts/validate-governance-commands.ts`. |
| ATM-BUG-2026-07-11-109 | 2026-07-11 | AI-Atomic-Framework | Bug | High | Fixed in current working tree | Team Agents / permission hard gate | `team validate/start` checked the permission catalog, but direct `team lease/release` mutation accepted an unknown permission or a missing required scope after bypassing recipe validation. | Every permission lease mutation must pass one fail-closed hard gate that verifies catalog membership and required scope before writing runtime state. | Reproduced by tracing `runTeamLifecycleAction` in `packages/cli/src/commands/team.ts`; the direct lease path only required a non-empty permission string. Added `ATM_TEAM_PERMISSION_HARD_GATE` and direct lease/release checks; broker tests pass. | Keep direct lifecycle mutation and provider permission brokerage covered by one catalog-backed hard-gate regression. |
| ATM-BUG-2026-07-11-110 | 2026-07-11 | AI-Atomic-Framework | Optimization | Medium | Fixed | Validator discoverability / local tooling | The documented `tools_node/compute-gate.js` path is absent in this repository, and the test validator does not accept the common `npm test -- --grep ...` filter, causing avoidable preflight friction. | Documentation and validator entrypoints should expose the actual supported compute-gate command and a supported focused-test selector, or fail with a copyable remediation command. | Fixed by adding a repo-local `tools_node/compute-gate.js` facade that maps common gates such as `ts-syntax` to supported npm scripts, plus a targeted `--grep` migration hint that points operators to `--filter` and `--focus-path`. Validator: `node --strip-types tests/cli/validator-discoverability.test.ts`. | Delivered in current fix commit. |
| ATM-BUG-2026-07-11-111 | 2026-07-11 | AI-Atomic-Framework | Optimization | High | Fixed in TASK-TEAM-0075 | Team Agents / cross-vendor context handoff | Direct Team provider roles needed a durable handoff ledger that remains JSON-authoritative while preserving an auditable Markdown projection. | Implemented canonical role-transition JSON, deterministic Markdown, runtime-to-history promotion, runtime-bound hard gates, continuation verification, tamper regressions, and retention escalation. | TASK-TEAM-0072 through 0075 provide the contract, archive path, permission hard gate, context budget, integrity verifier, and L5 dogfood evidence. | Keep Markdown non-authoritative and maintain the tamper/continuation regressions. |
| ATM-BUG-2026-07-11-112 | 2026-07-11 | AI-Atomic-Framework | Bug | Medium | Fixed in current working tree; validating for commit | Tasks audit / bulk-close mirror false positive | `ATM_TASK_AUDIT_BULK_CLOSE_WITHOUT_MANIFEST` fired on a release-mirror commit (`31b85474`) that only added two planning-card mirrors (`TASK-AAO-0153`, `TASK-TEAM-0065`) whose governed closes and closure packets already existed individually; the audit counts changed done task files without distinguishing mirror-only additions from actual bulk closes. | Mirror-only or planning-card additions whose closure packets pre-exist should not require a bulk closure manifest, or the finding should list the offending files and the exact manifest expectation so the operator is not left reverse-engineering the rule from source. | `inspectLatestCommitForBulkTaskClose` now excludes done task-card changes when the card points to an already-existing closure packet outside the latest commit. Regression `tests/cli/task-audit-bulk-close-mirror.test.ts` creates two pre-existing closure packets, then commits two done planning mirrors and asserts no `ATM_TASK_AUDIT_BULK_CLOSE_WITHOUT_MANIFEST`; `validate-cli --mode surface` includes the focused test. | Commit the mirror-only bulk-close audit repair with frozen release artifacts. |
| ATM-BUG-2026-07-11-113 | 2026-07-11 | AI-Atomic-Framework | Bug | High | Open | Governance attestation / no committable lane | The bulk-closure manifest demanded by `ATM_TASK_AUDIT_BULK_CLOSE_WITHOUT_MANIFEST` has no governed committable home: `.atm/history/evidence/` placement is blocked by protected-state rules (plain commit and `git record-commit` both fail with `evidence-file-missing-task-context`, record-commit additionally with `ATM_COMMIT_SESSION_MISSING`), while the alternative accepted path `atomic_workbench/evidence/` is gitignored. The only working state is an untracked local file, so the attestation that unblocks pre-push is not durable and every fresh clone or teammate hits the same block again. | Provide a governed lane to commit audit-required attestations (tracked manifest location or a record-only lane that accepts them with attribution), so passing the audit leaves a durable, shared artifact. | 2026-07-11 while pushing main: manifest in `.atm/history/evidence/` blocked twice; moving it to `atomic_workbench/evidence/` satisfied `hasBulkClosureManifest` from disk and the push succeeded, but the file remains untracked. | Decide the canonical tracked home for bulk closure manifests and teach pre-commit to accept it with actor attribution. |
| ATM-BUG-2026-07-11-114 | 2026-07-11 | AI-Atomic-Framework | Bug | Medium | Fixed in TASK-AAO-0164 | Pre-commit guidance / requiredCommand accuracy | When a protected evidence file is staged without task context, pre-commit suggested `node atm.mjs git commit --task <basename-of-staged-file>`, deriving a task id from the filename; running the suggested command failed with `ATM_TASK_NOT_FOUND` because no such task existed. | requiredCommand now only references task ids that exist in the ledger or are staged as task ledger files; otherwise it falls back to the generic governed commit wrapper without a fabricated task id. | 2026-07-11: staging `.atm/history/evidence/bulk-closure-manifest-2026-07-11-mirror-sync.json` produced the suggestion `--task bulk-closure-manifest-2026-07-11-mirror-sync`, which then failed `ATM_TASK_NOT_FOUND`. TASK-AAO-0164 adds a regression for a bogus `bulk-closure-manifest-2099-01-01-mirror-sync.json` evidence basename and proves no basename-derived `--task` is emitted. | Keep the hook regression in `scripts/validate-governance-commands.ts`; future pre-commit remediation must validate suggested task ids against the ledger before emitting task-scoped requiredCommand text. |
| ATM-BUG-2026-07-11-115 | 2026-07-11 | AI-Atomic-Framework | Optimization | Medium | Open | Hook failure output UX | Pre-push and pre-commit hook failures surface as one large raw JSON blob (>100 KB observed) on host git stderr; the blocking finding and `requiredCommand` are buried, so both human and AI operators must grep persisted output to find the actual remediation. | Hook failures should print a compact human summary first (top blocking finding + exact requiredCommand), then write the full JSON to a report file path referenced in the summary. | 2026-07-11 `git push origin main` block for `ATM_TASK_AUDIT_BULK_CLOSE_WITHOUT_MANIFEST` produced ~118 KB of JSON through stderr across two attempts before the finding was located. | Add a summary-first formatter to hook stderr with `--json-report <path>` style overflow. |
| ATM-BUG-2026-07-11-116 | 2026-07-11 | AI-Atomic-Framework | Bug | Medium | Fixed in working tree (cursor-grok-4.5) | Team plan / stale broker registry residue | `team plan --task TASK-TEAM-0040` fail-closed with `blocked-broker-cid-conflict` ("Proposal-first lane is active; broker recorded a provisional write lease") caused by leftover `.atm/runtime/broker-parallel-0041-0042/` registry state from a finished lane; the finding named no registry entry, age, owner, or cleanup command. | Broker conflict findings should identify the blocking registry entry (path, owner, age), classify obviously-terminal residue as stale, and emit the governed cleanup command instead of a generic rerun hint. | Fixed 2026-07-14: `cleanupStaleWithEvidence()` + structured owner/age/cleanup guidance; `loadRegistry(..., { persistCleanup })` opt-out preserved. Tests: `registry-stale-cleanup.test.ts`. | Wire findings into team-lane surfaces; keep persistCleanup default true. |
| ATM-BUG-2026-07-11-117 | 2026-07-11 | AI-Atomic-Framework | Optimization | Low | Fixed | Team lifecycle verbs / lease error context | `team lease` on an already-held exclusive permission returns `ATM_TEAM_LEASE_CONFLICT` without naming the current holder, and `team release` with a non-holder actor returns `ATM_TEAM_LEASE_NOT_FOUND` without listing active leases, forcing the operator to read the team-run JSON manually. | Lease conflict and not-found errors should include the current holder agentId, leased paths, and the exact release invocation for the real holder. | Fixed by asserting the live `team lease`/`team release` lifecycle error envelopes include `currentOwner`, `currentOwnerPaths`, active holder summaries, and the copyable holder release command. Validator: `node --strip-types scripts/validate-team-agents.ts --case team-lifecycle-verbs`. | Delivered in current fix commit. |
| ATM-BUG-2026-07-11-118 | 2026-07-11 | AI-Atomic-Framework | Optimization | Medium | Fixed in current working tree; validating for commit | Frozen runner / staleness visibility | The frozen `atm.mjs` runner stayed at its 2026-06-30 build while team-runtime source advanced through M8E/M9I/M10X; `team plan`/`team start` on the stale runner silently omitted new governance fields, so live CLI probes reported features "missing" while source validators passed. Nothing in the runner output indicated staleness. | The runner should self-report build-vs-source drift (advisory build timestamp or source HEAD delta) on startup or in `--version`/`doctor`, so operators know live CLI behavior may lag source before filing false feature-gap findings. | On 2026-07-13, `--version`, `doctor`, and runner-mode evidence gained `atm.runnerSourceDrift.v1` with frozen/source mtimes, advisory text, `syncRequired`, and the retained release sync command. `scripts/validate-cli.ts --mode surface` now asserts the `--version` drift evidence. | Commit the runner drift advisory repair with frozen release artifacts; keep `ATM_RUNNER_SYNC_REQUIRED` as the strict write gate. |
| ATM-BUG-2026-07-11-119 | 2026-07-11 | AI-Atomic-Framework | Bug | High | Fixed in TASK-AAO-0173 | Actor identity / environment precedence | `identity set --actor "Codex-GPT5.6 Terra"` writes a per-actor profile, but `identity show` still resolves the legacy environment value `codex-gpt-5.4-mini` first and reports no repo default identity. This leaves a valid active task session attributed to Terra while generic commands may select a different actor. | Explicit identity must be durable and take precedence over stale legacy editor environment data, or commands must fail closed with an unambiguous mismatch diagnostic. | Fixed in TASK-AAO-0173: repo-default identity now outranks legacy `AGENT_IDENTITY`, while explicit actor and `ATM_ACTOR_ID` remain higher precedence. `node --strip-types tests/cli/identity-per-actor-routing.test.ts` and `npm run typecheck` passed. | Keep the precedence regression and actor-resolution diagnostic stable for Captain/Team Broker UX. |
| ATM-BUG-2026-07-11-120 | 2026-07-11 | AI-Atomic-Framework | Optimization | Medium | Open | Planning route performance | `node atm.mjs next --prompt ... --json` in the 3KLife planning repository timed out while scanning a large task ledger, preventing normal preflight from returning a playbook in an interactive window. | Planning-ledger discovery should use indexed or bounded task lookup and emit progress/timeout diagnostics with a recovery command. | Reproduced while creating Cross-Vendor Team Markdown Handoff cards 0072-0074: 120s and 180s attempts did not return a route. | Benchmark the planner against large ledgers, introduce task-id/path prefiltering, and add a focused timeout regression. |
| ATM-BUG-2026-07-12-114 | 2026-07-12 | AI-Atomic-Framework | Product gap | High | Mitigated in TASK-TEAM-0074; follow-up open | Broker proposal-first / Team execution | `broker runtime activate --proposal-file` validates and opens a safe hot-file admission, but the subsequent `team start --execute` historically rebuilt a fresh provisional admission instead of consuming that approval. | `team start --broker-proposal-file` now validates and consumes a matching proposal in-memory before Team broker planning. A durable activation-to-start binding and automatic terminal intent cleanup remain required. | Reproduced during TASK-TEAM-0074 paid L5 dogfood after a successful broker activation for `packages/cli/src/commands/team.ts`; the Team start route returned to proposal-first rather than launching. | Add a focused regression for activate -> start consumption, stale/mismatched proposal rejection, and automatic terminal intent cleanup. |
| ATM-BUG-2026-07-12-115 | 2026-07-12 | AI-Atomic-Framework | Optimization | Medium | Open | Identity provenance | The active governed claim correctly records `Codex-GPT5.6 Terra`, while generic environment detection still reports legacy `codex-gpt-5.4-mini`; an operator cannot distinguish verified runtime model provenance from a user-authorized actor label. A second dogfood case showed the same defect for the active Codex editor surface: the actor identity should be `Codex-GPT 5.5` from the Codex editor plus the current model code, but `next` integration metadata still exposed stale `AGENT_IDENTITY=codex-gpt-5.4-mini`. This does not mean every agent should be called Codex, and it does not mean every suffix is an editor: for example `Fable 5` is a model code, not an editor surface. | Surface actor label, provider/model provenance, editor/surface id, and confidence/source separately. Never infer a model identity from a legacy editor environment variable. Actor identity should be assembled from the current editor/surface plus current model code when available: current supported editor surfaces are Codex, Claude, Antigravity, Cursor, and Copilot. Codex editor plus GPT 5.5 can produce `Codex-GPT 5.5`, while a Claude surface using Fable 5 should preserve Claude as the surface and Fable 5 as the model code. Stale environment aliases should be reported only as low-confidence legacy hints. | Confirmed while finishing TASK-TEAM-0074; the task ledger claim was Terra and `next` integration metadata exposed the stale environment value. Reconfirmed on 2026-07-14 while operating from the Codex editor as `Codex-GPT 5.5`: `node atm.mjs next --prompt ... --json` failed route selection but still reported `integrationBootstrap.currentEditorDetectedFrom: AGENT_IDENTITY` and `currentEditorRawValue: codex-gpt-5.4-mini`, prompting corrections that Codex is the editor surface for this session, the current editor set is Codex/Claude/Antigravity/Cursor/Copilot, and Fable 5 is a model code, not an editor. | Resolve together with 112 using a precedence regression and an explicit provenance schema; add Codex/Claude/Antigravity/Cursor/Copilot surface regressions proving stale `AGENT_IDENTITY` cannot override the current surface+model actor label, plus a cross-surface test that model codes such as Fable 5 are not misclassified as editor/surface ids. |
| ATM-BUG-2026-07-12-116 | 2026-07-12 | AI-Atomic-Framework | Optimization | P0 | Fixed in TASK-TEAM-0078 | Team Broker / admission ordering | `TASK-TEAM-0076` added a seven-layer CID trace and per-surface queue projection in Broker/Team planning, but `next --claim` does not consume queue state. A waiting task is still rejected by broad shared backlog/map and CID overlap instead of receiving a per-file queued admission. | Every task claim must run the same seven-layer CID decision, use queue state for confirmed shared file/surface conflicts, and return a bounded wait/next-file admission rather than a global overlap deadlock. | Post-0076 dogfood: `TASK-AAO-0158` remains blocked by `next --claim` on shared backlog/map and CID overlap although 0076 is closed and the queue should permit file-ordered progress. | Create follow-up for `next.ts` admission integration, queue-aware claim responses, stale-owner release, and a two-task dogfood regression. Keep 0076's trace/queue modules as the shared source of truth. |
| ATM-BUG-2026-07-12-117 | 2026-07-12 | AI-Atomic-Framework | Bug | Medium | Fixed in TASK-AAO-0204 | Commit bundle / protected override audit | A task-scoped governed commit auto-staged its own generated protected-override audit record, but the active direction lock did not automatically include that record. Pre-commit then failed with `ATM_TASK_DIRECTION_SCOPE_DRIFT` even though every source file was in scope. | When ATM generates a task-attached protected audit artifact, task bundle resolution should include it in the direction lock or omit it until the governed closure lane owns it. | TASK-TEAM-0076 commit attempt was blocked after a clean bundle dry-run; the only blocker was `.atm/history/protected-override-audit/2026-07-12T01-45-43-529Z-POA-c5ec9263d851.json`. | Fixed 2026-07-14: `listTaskOwnedProtectedOverrideAuditFiles` now feeds `resolveTaskDeclaredScope`, taskflow close bundles, and pre-commit `collectTaskGovernedCommitAllowedFiles`; regression `tests/cli/protected-override-audit-direction-lock.test.ts`. |
| ATM-BUG-2026-07-12-119 | 2026-07-12 | AI-Atomic-Framework | Bug | High | Needs task card | Cross-repo planning-source governance | An active target task can continue while its 3KLife planning source card is changed because the imported task/direction lock does not durably enforce source-card identity and hash across repositories. | Import and claim must retain planning source identity/hash; target write/close must detect planning-source drift and fail closed unless a governed amendment or mirror authority exists. | TASK-AAO-0157 planning card was mutable after target claim; the target direction lock had empty persisted planningReadOnlyPaths even though prompt routing could derive the source path. | Add cross-repo source hash/lock persistence, planning-repo enforcement, and drift regressions. |
| ATM-BUG-2026-07-12-120 | 2026-07-12 | AI-Atomic-Framework | Bug | High | Fixed in TASK-AAO-0168 | Runner-mode lifecycle guard | `atm.dev.mjs` presented source-first-only guidance but still accepted lifecycle/claim mutation attempts before failing later in routing. | Source-first runner must fail closed for claim, commit, and close mutations unless an explicit source-first validation authorization is present. | Fixed in TASK-AAO-0168 / commit `0245f6b989d35549390b1c41d11da3cce38aad08`: `assertSourceFirstRunnerReadOnlyAction(...)` now rejects `next --claim` at the command boundary with `ATM_SOURCE_FIRST_WRITE_REFUSED` before claim mutation; `npm run typecheck`, `npm run validate:cli`, and `npm run validate:git-head-evidence` passed. | Extend the same guard to any future lifecycle mutation entrypoint added to the source-first runner, and keep the source-first regression fail-closed. |
| ATM-BUG-2026-07-12-121 | 2026-07-12 | AI-Atomic-Framework | Bug | High | Fixed in current working tree; needs governed closeout | Broker intent schema parity | A file-scoped task claim registered as `parallel-safe`, but effective arbitration reinterpreted the same active intent as malformed because it required non-empty `atomRefs` even though the registry contract permits task-level file-only writes. | Register and effective arbitration must accept the same valid intent shape; file-scoped task claims with non-empty target files must not self-block solely because atom refs are absent. | TASK-AAO-0157 self intent was `parallel-safe` at registration but `blocked-active-lease` at effective status. Releasing and reclaiming reproduced it. Source fix permits file-only intents while retaining fail-closed empty target-file rejection; focused conflict-matrix regression and frozen runner status pass. | Commit the Broker parity repair with release artifacts and retain self-intent effective-status regression coverage. |
| ATM-BUG-2026-07-12-122 | 2026-07-12 | AI-Atomic-Framework | Optimization | High | Fixed in TASK-AAO-0158 | Governance validator performance / cancellation | The focused `node --strip-types scripts/validate-governance-commands.ts` lane exceeded a 124-second interactive command timeout without progress output while validating TASK-AAO-0157. | Focused governance validation should expose phase progress, support safe resume or focused case selection, and complete within an interactive budget where feasible. | TASK-AAO-0158 switched the validator's ATM command calls to in-process `runCli`, added stable phase progress and SIGINT cancellation diagnostics, and measured `node --strip-types scripts/validate-governance-commands.ts --mode validate` at 26.9s on the Windows development host. | Keep `validate:governance-commands` as the default under-30s lane and `validate:governance-commands:full` as the named full integration lane while future fixture splits can add true parallel groups. |
| ATM-BUG-2026-07-12-123 | 2026-07-12 | AI-Atomic-Framework | Bug | High | Fixed in TASK-AAO-0159 | Encoding guard / temporary artifact isolation | `check:encoding:touched` scanned unrelated untracked `tmp/` command transcripts and blocked a governed task when those transcripts contained U+FFFD, even though the claimed source and backlog files were clean. | Touched/staged encoding checks must isolate claimed or staged delivery scope, classify diagnostic temp transcripts separately, and offer a safe cleanup/quarantine command without requiring an agent to delete another owner's files. | During TASK-AAO-0157 closeout, 13 `tmp/aao-0157-*.json` files created by another agent triggered `ATM_GUARD_ENCODING_FAILED`; TASK-AAO-0159 fixed this by isolating temporary artifacts in the touched encoding guard and validating the behavior before close. | Keep the TASK-AAO-0159 scoped touched-encoding regression; future temp transcript handling should remain advisory unless a claimed/staged delivery file is actually dirty. |
| ATM-BUG-2026-07-12-124 | 2026-07-12 | AI-Atomic-Framework | Bug | Critical | Fixed in TASK-AAO-0160 | Governed commit / protected override audit deferral and lease renewal | `git commit --auto-stage --defer-foreign-staged` could resolve a task-scoped bundle successfully while retaining pre-existing protected-override audit files in `commitFiles`; pre-commit then rejected those files as direction-lock scope drift. Long validation and the failed commit attempt could also expire the same actor's claim, forcing repair before retry. | Bundle resolution, index deferral, and hook scope checks must agree on whether pre-existing protected audit is task-owned or foreign; foreign audit must be deferred with a durable snapshot, while owned audit must be admitted by the active direction lock. Long evidence/commit operations must renew the active self-claim or expose an automatic retry-safe renewal. | TASK-AAO-0160 teaches governed commit ownership inference to read `taskId` from protected override audit JSON. Foreign protected audits can now be deferred out of the scoped commit, while current-task audit artifacts are admitted into the bundle. `validate:governance-commands` covers staged foreign audit plus task-owned audit and proves the commit includes only the deliverable and owned audit. | Keep the protected-audit deferral regression beside task-scoped staging coverage; separately retain lease-renewal diagnostics for long-running evidence/commit lanes. |
| ATM-BUG-2026-07-12-125 | 2026-07-12 | AI-Atomic-Framework | Bug | Medium | Fixed in TASK-AAO-0162 | Task scope amendment metadata | `tasks scope add --add` retains shell-level outer quotation marks in the recorded amendment path list when a CSV value is passed as one quoted argument. The effective lock can still contain normalized paths, but the immutable audit record becomes misleading. The backlog also contained duplicate canonical IDs, so numeric identity was not validated. | Scope amendment parsing must normalize quotes before recording audit paths, and backlog validation must reject duplicate canonical IDs. | TASK-TEAM-0075: `node atm.mjs tasks scope add ... --add "docs/...,..." --json` reported `\"docs/...` and `...ts\"` in `amendmentHistory.addedPaths` while the effective `allowedFiles` were correct. TASK-AAO-0162 also reassigned duplicate backlog IDs to unique canonical IDs without changing their row content. | Keep quoted CSV normalization and duplicate-backlog-ID regressions; repair historical event display only through an explicit audited migration. |
| ATM-BUG-2026-07-12-126 | 2026-07-12 | AI-Atomic-Framework | Bug | High | Needs task card | Cross-task closure residue recovery | A completed task's stale close event can block another task's governed commit, while the only offered `repair-closure` path refuses to run until that other task's dirty delivery is committed. This creates a circular close dependency with no governed escape hatch. | A stale completed-task close event must be repairable in a scoped main-worktree lane, or commit bundling must classify it as known stale-import advisory residue without weakening protection for live foreign tasks. | `TASK-TEAM-0075` commit returned `ATM_GIT_COMMIT_GENERATED_RESIDUE_BLOCKED` for `TASK-TEAM-0076/...close...json`; `tasks status --residue` proved 0076 is `done/released` and stale-import. With human-approved `backend.tasks.repairClosure` lease `EMG-TASK-TEAM-0076-bf05b44d7c`, `tasks repair-closure --scope TASK-TEAM-0076` still returned `ATM_CLOSURE_REPAIR_DIRTY_WORKTREE` because active 0075/0079 dirty delivery exists. Its only remediation recommends an isolated worktree, which is prohibited for this main-worktree workflow. | Add a regression for done/released stale-import residue beside an active unrelated task; provide a scoped main-worktree repair or deterministic advisory classification, never manual history deletion or a compulsory worktree fork. |
| ATM-BUG-2026-07-12-127 | 2026-07-12 | AI-Atomic-Framework | Bug | High | Active in TASK-TEAM-0079 | Next claim / Broker lease lifecycle | `next --claim` can create a second overlapping active write intent for the same actor without first advancing, coalescing, or preserving the earlier lease epoch. Broker then treats the earlier valid task as stale and blocks re-arbitration before queue admission. | Claim admission and Broker registration must be one atomic lifecycle: same-actor overlapping tasks either enter explicit per-surface queue order with both valid epochs, or the second claim fails before writing a conflicting intent. | After claiming TASK-TEAM-0079 while TASK-TEAM-0075 remained active, `broker status` reported active intents for both, then `effectiveDecision` for 0079 became `blocked-active-lease` because 0075 epoch `1783822301570` was behind current epoch `1783823610473`. | TASK-TEAM-0079 must add same-actor lease/queue regression and prevent stale-epoch self-conflict before AAO-0158 dogfood. |
| ATM-BUG-2026-07-12-128 | 2026-07-12 | AI-Atomic-Framework | Bug | High | Fixed in TASK-TEAM-0079 | Broker shared-surface queue trigger | `shouldQueueSharedSurface` required a `conflictMatrix.gateResults` block in addition to `needs-physical-split`. Physical file overlaps intentionally route to composer with matrix `allow`, so Broker reported a valid per-file conflict yet created no queue or holder notification. | Any `needs-physical-split` decision with concrete overlapping files must create per-file queue entries and an existing-protocol `FreezeSignal` sidecar; matrix `allow` for the composer route must not suppress queueing. | Live 0075 owner + AAO-0158 waiter transactions both returned `needs-physical-split`, while `broker status` still showed `sharedSurfaceQueues: []`. | Fix trigger condition and retain two-task dogfood with queue entries, freeze/ack evidence, and a governed release that advances the waiter. |
| ATM-BUG-2026-07-12-129 | 2026-07-12 | AI-Atomic-Framework | Bug | High | Fixed in TASK-TEAM-0079 | Broker CID admission | The atom-ID and atom-CID gates treated equal strings as unconditional write/write freezes even after intent shape, lease, and shared-surface gates had established that two transactions wrote no common file or shared surface. This blocked independent work such as TASK-TEAM-0075 and TASK-AAO-0160. | CID write ownership is a semantic refinement of a common writable surface, not a global mutex. Preserve read-set visibility, but require a common target file or shared surface before a CID write conflict can freeze admission. | Source-first import/claim of TASK-AAO-0160 reported no shared paths but froze on `atom-cli-router` / `atom-validator-framework` CID overlap held by active 0075. | Regression now proves broad CID on disjoint files is `parallel-safe`, while same CID on the same target file remains `blocked-cid-conflict`; retain both decision and seven-layer matrix cases. |
| ATM-BUG-2026-07-12-130 | 2026-07-12 | AI-Atomic-Framework | Bug | High | Fixed in TASK-TEAM-0078 | Team L5 Broker admission | `team plan` calls Broker decision directly and treats any shared-file conflict as a global `broker-conflict-blocked`, without consuming the canonical per-file queue or retaining the task's disjoint private paths. | Team plan/start must use the same queue-aware admission projection as `next --claim`: queued shared paths stay blocked, while the role runtime receives only its private allowed files; a team with no private paths remains blocked. | Live `team plan --task TASK-TEAM-0079 --team-size L5` correctly found shared backlog/map files with active 0075, but then denied the whole L5 plan although 0079 has disjoint broker/runtime files. | Add a Team admission regression for queue head, queued-private, and shared-only blocked; dogfood a real L5 run after queue state is consumed. |
| ATM-BUG-2026-07-12-131 | 2026-07-12 | AI-Atomic-Framework | Bug | Critical | Fixed in TASK-TEAM-0079 | Broker CID-to-queue routing | A same-CID write on a known common target file is returned as `blocked-cid-conflict` before the physical file queue is considered. The Broker therefore creates neither a per-file queue nor a freeze/ack sidecar, even though it has enough evidence to serialize the shared paths while admitting private paths. | Retain CID as a hard conflict signal, but route a conflict with concrete common writable paths into the canonical per-file queue/freeze lane. Reserve global `blocked-cid-conflict` for semantic identity conflicts without a bounded queueable surface. | `broker register TASK-TEAM-0079` against active 0075 reported both shared backlog/map paths and same `atm.team-agents-runtime` identity, then returned `blocked-cid-conflict` with `sharedSurfaceQueues: []` and `sharedSurfaceFreezes: []`. | Add a regression proving same CID + bounded shared files yields queued-private admission, while same CID with no bounded surface remains fail-closed/manual escalation. |
| ATM-BUG-2026-07-12-132 | 2026-07-12 | AI-Atomic-Framework | Bug | High | Fixed in TASK-TEAM-0079 | Broker stale queue ownership | A task that remains only `planned` with no live claim or Broker registry intent can still remain queue head, blocking a valid waiter indefinitely. | Queue cleanup/status must identify entries without a live task claim or active Broker intent as stale and provide a governed release/re-arbitration action; planned tasks must never retain write ownership. | `TASK-AAO-0158` was `planned`, had no claim and no active registry intent, but headed both backlog/map queues ahead of `TASK-TEAM-0079`. `broker release --task TASK-AAO-0158` advanced 0079 to queue head. | Add stale queue owner detection and regression; status should name the stale task and a copyable cleanup action without silently discarding active ownership. |
| ATM-BUG-2026-07-12-133 | 2026-07-12 | AI-Atomic-Framework | Bug | High | Fixed in TASK-TEAM-0083 | Team L5 proposal-first usability | `team plan` detects a valid hot-file proposal-first gate but exposes no proposal input/creation route, while only `team start` accepts `--broker-proposal-file`. A Captain therefore cannot turn a blocked L5 plan into a ready plan without reverse-engineering a separate Broker proposal contract. | Team plan/validate must surface the required proposal schema and a copyable create/validate command, and may accept the same proposal file for readiness preview; start remains fail-closed until that validated proposal is supplied. | TASK-TEAM-0083 delivery `31d5faa98` added proposal-first plan/start parity and paid three-provider L5 regression evidence. | Closed by TASK-TEAM-0083. |
| ATM-BUG-2026-07-12-134 | 2026-07-12 | AI-Atomic-Framework | Bug | Critical | Follow-up in TASK-TEAM-0084 | Protected staged bundle / Broker release-order deadlock | A Broker resolution can correctly grant task A the current shared-surface release order, but A's governed commit cannot use `--defer-foreign-staged` when task B's protected `.atm/history` bundle is staged. B cannot commit first because the same resolution blocks B. | A resolution-authorized current task must be able to snapshot and temporarily unstage a complete protected foreign task bundle, commit only its own scope, then return a deterministic restage command; incomplete or non-resolution foreign bundles remain protected. | Closeout audit after TASK-TEAM-0079 and TASK-TEAM-0080 found the stale `Active in TASK-TEAM-0079` status was incorrect: 0079 closed queue/composer projection issues, and 0080 propagated `--defer-foreign-staged` through taskflow plus scope tools, but `git-governance.ts` still blocks protected foreign staged ownership with `ATM_GIT_COMMIT_PROTECTED_FOREIGN_STAGED_OWNERSHIP`. | TASK-TEAM-0084 records the remaining boundary as follow-up: implement or reject a narrow resolution-authorized protected defer lane with regression proving no silent unstage, complete-bundle validation, snapshot/recovery, and denial without currentAllowedTaskId. |
| ATM-BUG-2026-07-12-135 | 2026-07-12 | AI-Atomic-Framework | Bug | Critical | Fixed in TASK-TEAM-0079 | Governed commit / composer projection | `withTaskScopedCommitIndex` built the temporary index from HEAD and re-added bundle files from the working tree, overwriting a composer partial-staged projection with foreign worktree content before pre-commit ran. | The temporary index must plant the exact approved live-index blobs for selected paths instead of re-reading the working tree, preserving partial staging and excluding other tasks' index content. | TASK-TEAM-0079 queue-only projection was repeatedly replaced by 0075 worktree edits inside the wrapper's temporary index, so pre-commit saw handoff imports the composer never staged. | Keep a regression proving a partial-staged blob survives the task-scoped commit wrapper byte-for-byte while unrelated worktree edits stay out. |
| ATM-BUG-2026-07-12-136 | 2026-07-12 | AI-Atomic-Framework | Bug | High | Fixed in TASK-TEAM-0079 | Pre-commit cross-file consistency guard | The cross-file guard treated pre-existing, unmodified handoff imports in a staged validator file as a dependency on another task's uncommitted handoff changes and blocked the commit. | Only imports added or changed by the staged patch may require the dependency to land in the same commit; untouched import contracts must not couple tasks. | TASK-TEAM-0079's queue-only validator staging was blocked because `scripts/validate-team-agents.ts` already imported handoff-ledger symbols owned by in-flight TASK-TEAM-0075. | Retain the tightened staged-patch import-drift detection plus a regression for the legitimate new-import case that must still fail closed. |
| ATM-BUG-2026-07-12-137 | 2026-07-12 | AI-Atomic-Framework | Bug | Medium | Fixed in TASK-TEAM-0081 | Git-head evidence hygiene on failed commits | A failed governed commit attempt leaves its prepared commit record appended to `.atm/history/evidence/git-head.jsonl`; the orphan line (tree never committed) later blocks `taskflow close` as governance-tracked dirty state. | Commit-prep evidence should be finalized only when the commit lands, or failed-attempt records should be auto-reconciled/annotated so close preflight does not require manual `git restore` of the evidence log. | TASK-TEAM-0081 delivery `573ac3cf` added failed commit evidence reconciliation and close-preflight coverage; close packet `.atm/history/evidence/TASK-TEAM-0081.closure-packet.json` marks the task done. | Closed by TASK-TEAM-0081. |
| ATM-BUG-2026-07-12-138 | 2026-07-12 | AI-Atomic-Framework | Bug | High | Fixed in TASK-TEAM-0082 | Task card deliverables path validation | Prose (non-path) deliverables pass `tasks import` with only a mechanical-split warning, then make `taskflow close` fail with `ATM_TASKFLOW_CLOSE_COMMIT_BUNDLE_INCOMPLETE` because the bundle cannot map the deliverable to the direction lock; the only repair lane is an emergency-lease `tasks import --write --force`. | Import should fail closed (or normalize with an explicit diagnostic) when a deliverable is not a repo path, and a governed non-emergency metadata-repair lane should exist for claimed tasks whose card text needs correction. | TASK-TEAM-0082 adds path-shaped deliverable validation, explicit normalization diagnostics when scope paths can repair prose deliverables, strict-mode fail-closed behavior, and regression coverage for CJK prose versus valid CJK paths. | Closed by TASK-TEAM-0082. |
| ATM-BUG-2026-07-12-139 | 2026-07-12 | AI-Atomic-Framework | Bug | High | Fixed in TASK-AAO-0177 | Team Agents / role-provider parser | `--role-provider role=provider:model::real-agent` contains an empty SDK segment, but parser behavior can shift the fields so `real-agent` is consumed as the SDK/mode boundary incorrectly, causing roles to silently downgrade or skip execution. | Role-provider parsing should reject empty required segments with a clear error, or normalize four-segment input without field drift; `team start --execute` should fail before writing a run if requested real-agent roles select no provider execution. | Fixed in TASK-AAO-0177: delivery commit `681c3467a49723c1a46e19accb6966cbb08c56a3` preserves empty SDK segment positions in `provider-selection.ts`; close commit `fdd2f0bd0452963de38c8b621458d46430166d1b` recorded done/released/no-residue. Evidence passed `node --strip-types tests/cli/team-role-provider-parser.test.ts`, `npm run typecheck`, `npm run validate:git-head-evidence`, and `npm run validate:cli`. | Keep parser regressions for two-, three-, four-segment, empty-SDK, malformed, and runtime-mode cases; separately track zero-provider execution semantics under execute-result backlog rows. |
| ATM-BUG-2026-07-12-140 | 2026-07-12 | AI-Atomic-Framework | Optimization | Medium | Needs task card | Next claim / auto-intent UX | `next --claim --auto-intent` can still classify a backlog/doc delivery as `closeout-only`, and the operator only discovers the mismatch when `git commit --auto-stage` rejects source mutations with `ATM_GIT_COMMIT_CLOSEOUT_ONLY_MUTATION`. | Auto-intent should infer `write` for open tasks with declared non-.atm deliverables or prompts that ask to update backlog/source, or `next --claim` should print a stronger warning and copyable `--claim-intent write` recovery before work begins. | During TASK-TEAM-0084, auto-intent claimed `closeout-only`; the commit wrapper correctly failed early and required `node atm.mjs next --claim --actor "Codex-GPT5.6 Terra" --task "TASK-TEAM-0084" --claim-intent write --json`. Earlier preflight guard row `ATM-BUG-2026-07-07-043` remains fixed; this row tracks the remaining UX friction. | Add an auto-intent regression for backlog/document delivery prompts and surface a pre-edit warning or default write intent. |
| ATM-BUG-2026-07-12-141 | 2026-07-12 | AI-Atomic-Framework | Optimization | Medium | Fixed in TASK-AAO-0161 | Broker intent lifecycle / task close | After governed task close, Broker write intents were not automatically released, so captains had to remember a separate `broker release --task <id>` step or later unrelated claims could see stale queue/intent state. | Successful `taskflow close --write` should release the closed task's Broker intent; `broker release` and `broker cleanup` should also prune stale/empty runtime snapshots while preserving live intents. | TASK-AAO-0161 adds shared Broker runtime cleanup for per-task intent snapshots, empty shared queue/freeze snapshots, and taskflow close success. Focused lifecycle validation covers live-vs-stale snapshot preservation, and real repo cleanup removed stale TASK-AAO-0158/TASK-AAO-0159 snapshots while preserving the live TASK-AAO-0161 intent. | Keep `validate:broker-lifecycle` covering stale snapshot cleanup and live snapshot preservation; closeout evidence should show `brokerRuntimeCleanup` after successful `taskflow close --write`. |
| ATM-BUG-2026-07-12-142 | 2026-07-12 | AI-Atomic-Framework | Documentation / AI routing | Medium | Needs task card | Task card atom-map scope authoring | A task card can list `atomic_workbench/atomization-coverage/path-to-atom-map.json` as a deliverable while the validator treats owner shards as the source of truth; editing only the projection makes `validate:atomization-coverage` fail with a shard-merge mismatch. | Task cards that require atom-map updates should name the owner shard path or provide a governed projection rebuild lane; `next`/import should warn when only the generated projection is scoped. | During TASK-AAO-0158, updating only `path-to-atom-map.json` produced `projection mappings=165, merged shards=161` and remediation `path-to-atom-map-shards/merge.js <repo> write-projection`; the change was reverted because the claimed scope did not include shard files. | Add task-card authoring validation for generated atom-map projections versus owner-shard source files, and provide a safe scope amendment or rebuild command. |
| ATM-BUG-2026-07-12-143 | 2026-07-12 | AI-Atomic-Framework | Bug | High | Fixed in TASK-TEAM-0074 | Broker proposal hash normalization | Proposal validation emitted the same SHA-256 digest with and without the `sha256:` prefix, then treated the formatting difference as a stale-file mismatch and blocked a legitimate fail-closed Team start. | Compare normalized SHA-256 digest values while retaining every other proposal validation issue as blocking. | Reproduced by the first formal TASK-TEAM-0074 L5 start: `195ea...8372` matched semantically but gate rejected it because `currentFileHash` included the prefix. | Keep a regression for prefixed/unprefixed equal hashes and a separate mismatch rejection. |
| ATM-BUG-2026-07-12-144 | 2026-07-12 | AI-Atomic-Framework | Bug | High | Fixed in TASK-AAO-0175 | `next` prompt scope resolution / handoff resume | A prompt that explicitly asks to read `WORKSPACE-UNFINISHED-WORK.md` was classified as task-scoped, but `next --prompt` could not resolve the active task from the handoff filename and failed with `ATM_NEXT_TASK_SCOPE_NOT_FOUND` instead of offering the uniquely active governed task. | Handoff/task-root hints should resolve to the active task when the live ledger has exactly one matching running claim, or emit the exact task id and a bounded resume command without falling back to unrelated tasks. | Fixed in TASK-AAO-0175: handoff prompts now read referenced task ids, route to the unique active claim, and return selection-required for multiple active claims; stale completed references do not fall back. `node --strip-types tests/cli/handoff-resume-route.test.ts`, `npm run typecheck`, and `npm run validate:cli` passed. | Keep the handoff filename, stale-reference, unique-active, and multi-claim regressions; row 154 separately tracks stale frozen-runner write preflight. |
| ATM-BUG-2026-07-12-145 | 2026-07-12 | AI-Atomic-Framework | Documentation / AI routing | Medium | Needs task card | Cross-repo planning-root configuration | `.atm/config.json` enables external task-ledger mirroring and contains a broker evidence path under `../3KLife`, but `next --prompt` still fails with `ATM_PLANNING_ROOT_MISSING` unless the operator separately knows to set `ATM_PLANNING_REPO_ROOT`; the required configuration key and expected root are not surfaced by preflight. | Bootstrap/doctor/next should discover or validate the configured planning repository, or provide a repo-local exact configuration command and explain why planning-root resolution is required before task routing. | 2026-07-12: initial `next --prompt` failed; the actual root `C:\Users\User\3KLife\docs\ai_atomic_framework` existed, but `.atm/config.json` had no `taskLedger.planningRoots` and the environment variable was unset. A process-local env override removed this error. | Add planning-root discovery/configuration guidance and a preflight regression for broker-only external paths versus task-ledger planning roots. |
| ATM-BUG-2026-07-12-146 | 2026-07-12 | AI-Atomic-Framework | Optimization | Medium | Fixed in TASK-AAO-0164 | Validator execution / timeout observability | `npm run validate:git-hooks-enforcement` could exceed agent command windows without a resumable status packet or progress checkpoint, and a mixed historical-restore fixture could recurse through the expensive framework-validator lane. | Long-running validators should emit bounded progress, enforce child-process timeouts, and provide actionable evidence when a child hangs; governance blockers should fail before recursively invoking expensive validators. | Fixed in TASK-AAO-0164: `scripts/validate-git-hooks-enforcement.ts` now emits per-step progress/duration, applies `ATM_VALIDATOR_CHILD_TIMEOUT_MS` (default 30s), and reports the timed-out command; `packages/cli/src/commands/hook/pre-commit.ts` skips expensive framework validators after decisive attribution/protected-state blockers. `npm run validate:git-hooks-enforcement` passed in 202s with 168 steps, and `node atm.mjs evidence run --task TASK-AAO-0164 ... --command \"npm run validate:git-hooks-enforcement\" ...` recorded fresh command-backed evidence. | Keep the progress/timeout contract covered; consider a future performance split for the 202s full suite, but do not treat a slow completed run as a hang. |
| ATM-BUG-2026-07-12-147 | 2026-07-12 | AI-Atomic-Framework | Bug | High | Open | Taskflow close / abandoned-task residue | Abandoning malformed `TASK-AAO-0165` left its evidence, task-event records, task ledger, and bundle manifest in the worktree; the next task's pre-close and governed commit were blocked by foreign staged files and then by `ATM_GIT_COMMIT_GENERATED_RESIDUE_BLOCKED`. `--defer-foreign-staged` did not by itself make the subsequent hook commit admissible, so the operator had to identify and remove the orphaned bundle manifest explicitly. | Abandon should produce a governed, complete residue disposition: either commit the abandoned task's audit packet, or remove only disposable generated artifacts and make the remaining history explicitly admissible to later taskflow/commit lanes. The next task should not inherit an unresolvable foreign residue blocker. | Reproduced on 2026-07-12 while closing TASK-AAO-0166: `taskflow pre-close` found `TASK-AAO-0165` foreign staged files; the first governed commit failed on `ATM_GIT_COMMIT_GENERATED_RESIDUE_BLOCKED` for `.atm/history/evidence/TASK-AAO-0165.bundle-manifest.json`; targeted cleanup was required before commit succeeded. | Add abandon -> next-task commit regression covering staged/untracked evidence, bundle-manifest cleanup, hook admission, and preservation of the abandoned task's audit trail. |
| ATM-BUG-2026-07-12-148 | 2026-07-12 | AI-Atomic-Framework | Optimization | Medium | Fixed in TASK-AAO-FABLE-003 (same shared write-side validator set now drives readiness) | Taskflow close readiness / closure packet completeness | `taskflow close` dry-run reported no blocking validator work after the delivery commit, but `taskflow close --write` then failed because the closure packet was missing `validate:cli` and `validate:git-head-evidence`; those validators had to be discovered and run manually. | Dry-run/pre-close should enumerate every closure-required validator and either auto-run them or fail with the exact complete evidence plan before a write attempt. | Reproduced on 2026-07-12 for TASK-AAO-0166: close write returned `ATM_TASK_CLOSE_CLOSURE_PACKET_INVALID` with missing `validationPasses/validate:cli` and `validationPasses/validate:git-head-evidence`; both later passed and close succeeded. | Add a close-readiness regression proving the dry-run and write paths expose the same closure validator set and required commands. |
| ATM-BUG-2026-07-12-149 | 2026-07-12 | AI-Atomic-Framework | Bug | High | Fixed in TASK-AAO-0199 | Task-ledger governance validator fixtures / Broker base state | `validate:task-ledger-governance` fixtures assumed an unborn Git branch or reused one fixed framework fixture directory; after Broker claim admission began requiring a resolvable HEAD, duplicate seed commits and shared queue state caused `ATM_BROKER_TRANSACTION_BASE_MISSING`, `ATM_BROKER_SHARED_QUEUE_BLOCKED`, and empty-commit failures before the intended assertions ran. | Every governance fixture should create an isolated repository with one deterministic seed commit and no inherited Broker queue/claim state; fixture setup must be idempotent and fail only on the behavior under test. | Fixed in TASK-AAO-0199: isolated fixture helpers clear Broker state and seed one HEAD; regression `tests/cli/task-ledger-fixture-isolation.test.ts`. | Keep fixture isolation independent of the long integration suite. |
| ATM-BUG-2026-07-12-150 | 2026-07-12 | AI-Atomic-Framework | Optimization | Medium | Fixed in TASK-AAO-0203 | Task-ledger residue validator contract drift | Several residue fixtures still asserted superseded behavior: `done` without governed closeout expected `planning-mirror-only` instead of fail-closed `source-done-governance-incomplete`, and stale-import guidance expected `--force` instead of governed `--reconcile-mirror`. | Validator fixtures must be updated with the public residue classification contract whenever fail-closed precedence or recovery commands change; stale assertions should identify the current bucket and safe command. | Reproduced during TASK-AAO-0170: after the runner and fixture setup passed, the suite failed at the planning-mirror and stale-import assertions until both were aligned with current CLI behavior. | Fixed 2026-07-14: `suite-impl.ts` residue assertions aligned; focused contract regression `tests/cli/task-ledger-residue-contract.test.ts` pins bucket precedence and recovery templates. |
| ATM-BUG-2026-07-12-151 | 2026-07-12 | AI-Atomic-Framework | Optimization | Medium | Fixed in TASK-AAO-0190 | Taskflow / governed commit operator UX | `taskflow pre-close` suggested a governed commit command without `--auto-stage`, while the wrapper rejected the first attempt with `ATM_GIT_COMMIT_TASK_SCOPED_STAGING_REQUIRED`; separately, passing low-level `--status done` to `taskflow close` produced `ATM_CLI_USAGE` because that flag belongs to `tasks close`. | Close playbooks should emit one copy-paste-ready command sequence with the correct staging mode and taskflow-specific flags; low-level and taskflow option names should be rejected with a targeted migration hint. | Reproduced during TASK-AAO-0170 on 2026-07-12. Adding `--auto-stage` made the governed delivery commit succeed; removing `--status` made taskflow close dry-run succeed. Fixed 2026-07-13 in TASK-AAO-0190: dirty-guard and commit-bundle remediation now include `--auto-stage`; `taskflow --status` returns a migration hint to omit the flag or use `tasks close --status`. | Covered by `packages/cli/src/commands/tasks/__tests__/scope-lock-diagnostics.test.ts` and `tests/cli/taskflow-status-migration-hint.test.ts`. |
| ATM-BUG-2026-07-12-152 | 2026-07-12 | AI-Atomic-Framework | Optimization | Medium | Fixed in TASK-AAO-FABLE-001 | `validate:git-hooks-enforcement` runtime budget | The timeout/progress guard now prevents opaque child hangs, but the full `npm run validate:git-hooks-enforcement` suite still took about 202 seconds in the prior closeout, exceeding common interactive command windows and making a completed run look like a timeout risk. | Split the full hook-enforcement suite into bounded focused lanes with resumable status and retain an explicit full-suite command; report expected duration before execution so agents can choose the correct lane. | TASK-AAO-0164 recorded a successful 202-second run with 168 steps. The current backlog cleanup keeps this as a follow-up rather than treating the slow successful run as fixed timeout behavior. | Add a performance budget/regression for focused hook lanes and a preflight estimate for the full suite; preserve child timeout cleanup from TASK-AAO-0164. |
| ATM-BUG-2026-07-12-153 | 2026-07-12 | AI-Atomic-Framework | Bug | High | Fixed in TASK-AAO-0181 | Task intent routing / claim mode | A source-changing backlog fix with an uncommitted deliverable was classified by `next --claim --auto-intent` as `closeout-only`, forcing release and an explicit `--claim-intent write` reclaim before work could begin. | Auto-intent now requires scoped delivery evidence before treating existing deliverables in `HEAD` as closeout-only; pre-existing deliverables without a task delivery commit resolve to `write` with reason `delivery-evidence-not-found`. | Fixed by delivery commit `973f854025b7c5390e16a51d51ae07543e9449e6` and close commit `551507b7f428d3beef2c3ec99c2c6b359d433a82`. Evidence: `node --strip-types tests/cli/tasks-claim-auto-intent.test.ts`, `npm run typecheck`, `npm run validate:cli`, and `npm run validate:git-head-evidence` passed for TASK-AAO-0181. | Regression now covers pre-existing deliverables without delivery evidence, dirty in-scope source, delivered task with scoped commit evidence, and explicit closeout-only conflict recovery. |
| ATM-BUG-2026-07-12-154 | 2026-07-12 | AI-Atomic-Framework | Optimization | Medium | Fixed in TASK-AAO-FABLE-002 | Frozen runner / write preflight | `taskflow close` dry-run passed, but `taskflow close --write` was rejected late by `ATM_RUNNER_STALE_WRITE_REFUSED` because the frozen runner was older than source; the operator had to infer and run the release build before retrying. | Pre-close and close dry-run should surface stale-runner write readiness and the required sync command before any write attempt; the write path should report this condition as a readiness blocker rather than a late refusal. | Reproduced on TASK-AAO-0173 after a successful delivery commit and close dry-run. Running `ATM_RETAIN_RELEASE_ARTIFACTS=1 npm run build` cleared the guard and allowed close. | Add a stale-runner preflight regression and keep the exact platform-appropriate build/sync command in the close playbook. |
| ATM-BUG-2026-07-12-155 | 2026-07-12 | AI-Atomic-Framework | Bug | High | Fixed in TASK-AAO-FABLE-003 | Taskflow closure packet / validator classification | `taskflow pre-close` and close dry-run treated `validate:git-head-evidence` as advisory, but close write required `validationPasses/validate:git-head-evidence` and failed with `ATM_TASK_CLOSE_CLOSURE_PACKET_INVALID`; the validator had to be discovered and run manually. This extends row 148 from the specific `validate:cli` omission to an inconsistent validator contract. | One shared closure-readiness calculation should drive pre-close, dry-run, and close-write; every closure-required validator must appear in the dry-run blocker/command plan, or none should be required by the write packet. | Reproduced on TASK-AAO-0173: close write reported the missing git-head pass after dry-run showed no blockers; `npm run validate:git-head-evidence` then passed and close succeeded. | Add a regression asserting identical required-validator sets and command-backed evidence requirements across pre-close, dry-run, and write. |
| ATM-BUG-2026-07-12-156 | 2026-07-12 | AI-Atomic-Framework | Optimization | Low | Fixed in TASK-AAO-0183 | Tasks import CLI / operator guidance | Invoking `node atm.mjs tasks import --from plan --write --json` without a path produced `ATM_TASKS_PLAN_NOT_FOUND` for a literal file named `plan`, rather than explaining that `--from` requires a markdown path and showing the copyable command. | Missing or malformed task import paths should produce targeted usage guidance with the expected `--from <path>` form and a valid example; avoid interpreting the subcommand token as a filesystem path when parsing fails. | Fixed by delivery commit `dba70e92a882e33c0a0e7a3b9e706cc9ebbc0288` and close commit `def6460850214e094ed00aabec550bfe64ea62eb`. Evidence: `node --strip-types packages/cli/src/commands/tasks/__tests__/import-orchestrator.spec.ts`, `npm run typecheck`, `npm run validate:cli`, and `npm run validate:git-head-evidence` passed. | Regression now covers missing `--from`, literal `plan`, nonexistent markdown path, and valid reconcile import. |
| ATM-BUG-2026-07-12-157 | 2026-07-12 | AI-Atomic-Framework | Bug | High | Fixed in TASK-AAO-FABLE-004 | Backlog intent routing / Captain continuation | A prompt explicitly asking to continue repairing all backlog items was routed by `next --prompt`/`guide` to the generic `create-atom` route, requiring bucket/title/description/logical-name evidence instead of selecting the existing backlog/task queue. This can send Captain continuation work into an unrelated atom-birth flow. | Backlog-repair and Captain-continuation language should resolve to the existing governed backlog queue or a task-selection route; generic create-atom fallback must not be selected when the prompt names backlog repair and ongoing continuation. | Reproduced on 2026-07-12 after TASK-AAO-0177 close: `node atm.mjs next --prompt "請繼續修復所有 backlog；過程中所有不好用或卡住的地方都回寫 backlog。" --json` returned `prompt-guidance-required`, and `node atm.mjs start --goal ...` selected `recommendedRoute: create-atom` with missing bucket/title/description/logical-name evidence. TASK-AAO-FABLE-004 extends queue-request prompt normalization for zh-TW backlog continuation, English `continue backlog fixes`, and Captain-mode backlog repairs, with `validate:prompt-scoped-next` fixtures asserting batch queue routing instead of prompt-guidance/create-atom fallback. | Keep follow-up row 173 for task-id alias ergonomics; core backlog continuation queue routing is fixed in TASK-AAO-FABLE-004. |
| ATM-BUG-2026-07-12-158 | 2026-07-12 | AI-Atomic-Framework | Optimization | Medium | Fixed | Task claim CLI / option contract | `tasks new`/`tasks claim` help exposes `--files`, but the governed `next --claim` surface rejects the same option as unsupported; an operator following the combined help output cannot attach the intended backlog-file scope through the official claim entry. | Publish one authoritative claim command contract: either let `next --claim` accept/forward scoped files, or remove backend-only flags from the operator-facing guidance and emit an exact governed scope command. | Fixed on 2026-07-13: `next --claim --files <csv>` is accepted by the shared parser, documented in `next --help`, and forwarded as the task claim scope while ATM still includes the task ledger record. | Covered by `packages/cli/src/commands/next/__tests__/claim-files-option.spec.ts`, which proves `next --claim --files src/a.ts` narrows the direction lock and excludes unrequested deliverables. |
| ATM-BUG-2026-07-12-159 | 2026-07-12 | AI-Atomic-Framework | Bug | High | Fixed in working tree (cursor-grok-4.5) | Task import lifecycle / low-level create drift | Creating a placeholder task with `tasks create` before authoring the real markdown plan leaves a ledger entry with a different source hash; `tasks import --write` then refuses with `ATM_TASKS_IMPORT_DRIFT`, while `--force` is protected by `ATM_EMERGENCY_LANE_APPROVAL_REQUIRED`. This traps a normal operator between an unclaimable planned task and an emergency-only overwrite. | A low-level placeholder task should be upgradable through a normal governed import/open lane when no active claim exists, or the create path should emit the exact next command and warn that later markdown import will require emergency overwrite. | Fixed 2026-07-14: `isCreatePlaceholderLedger()` allows normal import overwrite when no active claim. Regression in `import-orchestrator.spec.ts`. | Keep emergency `--force` for active-claim overwrites only. |
| ATM-BUG-2026-07-12-160 | 2026-07-12 | AI-Atomic-Framework | Optimization | Medium | Open | Taskflow open / framework repo profile readiness | `taskflow open --write` is documented as the official operator lane, but in the framework repo it fails in template-only-fallback mode when no opener profile is loaded, then points back to `tasks new` plus `tasks import`; if import is blocked by row 159, the official lane cannot recover. | The framework repo should ship or discover a taskflow opener profile for its own task cards, or `taskflow open` should emit a complete copy-paste-safe sequence that cannot enter create/import drift. | Reproduced on 2026-07-12 for TASK-AAO-0178: `node atm.mjs taskflow open --task-id TASK-AAO-0178 --title "Record backlog continuation routing friction" --output .atm/task-plans/TASK-AAO-0178.md --write --json` failed with `ATM_TASKFLOW_TEMPLATE_ONLY_FALLBACK` and missing `--profile <adopter-repo>/taskflow.profile.json`. | Add framework-repo taskflow-open readiness coverage and ship a repo-local profile or a no-profile safe fallback path. |
| ATM-BUG-2026-07-12-161 | 2026-07-12 | AI-Atomic-Framework | Bug / Product gap | P0 | Fixed in TASK-GIT-0015 | Team Broker / staging index arbitration | Multiple AI captains can hold parallel-safe Broker lanes, but the shared Git index has no owner/lease model. One agent can see another active task's staged files, manually unstage or restore them to make its own commit pass, or be blocked by foreign staged/untracked governance bundles even when Broker says the source scopes are parallel-safe. | ATM should treat the Git index and destructive worktree/index Git commands as governed shared resources: foreign active staged files must be protected from unstage/restage/restore/reset/checkout/clean by other agents unless a highest-authority explicit keyword/lease is supplied, and Broker should expose an index lane/queue so agents can commit in order without hand-editing each other's staged state. | Reproduced on 2026-07-12 while TASK-AAO-0188 attempted to commit a parallel-safe fixture isolation fix while TASK-AAO-FABLE-003 was active. Fable's untracked `.atm/history/evidence/TASK-AAO-FABLE-003.bundle-manifest.json` blocked `node atm.mjs git commit` with `ATM_HOOK_GENERATED_RESIDUE_BLOCKED`; a subsequent operator path suggested unstaging foreign `taskflow.ts`, exposing that agents can mutate another actor's staged index state outside Broker arbitration. User then clarified that `git restore` and similarly severe Git commands must also be prohibited unless a highest-specific authorization keyword is present. Additional live repro while closing TASK-AAO-0188: `taskflow pre-close` recommended `git restore --staged` against foreign staged `packages/cli/src/commands/hook/pre-commit.ts`; `git commit --defer-foreign-staged --dry-run` accepted foreign FABLE bundle manifests as deferred warnings, but the real pre-commit hook re-blocked the same residue because wrapper defer semantics are not transmitted to the hook; `tasks release` left nested `taskDirectionLock.status: active` in released locks, so Broker still reported active-task-scope conflicts; an emergency gitHookBypass lease was consumed by a failed Broker-conflict preflight before any commit was created; and the recovery hint pointed to `backend.brokerConflictOverride`, which would be too dangerous without a paper resolution artifact because it could bypass protection for another agent's staged file. Follow-up repro while trying to safely finish FABLE-005: `next --claim` ignored a freshly written `atm.brokerConflictResolution.v1` artifact (`BCR-9ybygk`) and still reported missing resolution; `--no-verify --dry-run` only surfaced `ATM_EMERGENCY_LANE_APPROVAL_REQUIRED`, but the real commit then surfaced `ATM_GIT_COMMIT_BROKER_CONFLICT_OVERRIDE_REQUIRED` for TASK-AAO-0188/TASK-GIT-0013, so dry-run did not faithfully predict the write gate and can consume an emergency lease on a commit that never reaches Git. 2026-07-13: `TASK-AAO-0189` was formalized as `TASK-GIT-0015` and closed with delivery `329890f7`, close `f8e7a6f`, and planning mirror `cfb724de`. The implementation adds shared git-index ownership evidence, `ATM_INDEX_FOREIGN_ACTIVE_STAGED`, `indexLane`, ATM stage/destructive override lease creation, removes raw `git restore --staged` and dirty-file `git restore --` remediation from core taskflow/commit paths, and keeps TASK-AAO-0188 done/no-residue with `node --strip-types tests/cli/task-ledger-fixture-isolation.test.ts` passing. | Keep follow-up rows 168-170 for remaining UX/performance refinements; core P0 staging-index arbitration is closed by TASK-GIT-0015. |
| ATM-BUG-2026-07-13-159 | 2026-07-13 | AI-Atomic-Framework | Bug | Critical | Fixed in TASK-AAO-FABLE-005 | Pre-commit residue ownership under multi-claim parallelism | `resolveResidueTaskId` ignored `ATM_COMMIT_TASK_ID` and fell back to a single-active-lock heuristic, so with two captains (multiple active locks) every governance artifact resolved to "no active task ownership" and every governed commit was blocked. Foreign in-flight evidence also blocked unconditionally. | The committing task id from the wrapper is the primary ownership context; a foreign governance artifact with a live direction lock or live ledger claim is in-flight parallel work and passes; orphaned artifacts stay fail-closed. | Reproduced live on 2026-07-12/13 with claude-fable-5 and codex-backlog-captain working the same tree; commits deadlocked until the fix. | Keep a two-claim regression: foreign manifest with active owner passes, orphaned manifest blocks. |
| ATM-BUG-2026-07-13-160 | 2026-07-13 | AI-Atomic-Framework | Bug | High | Fixed in TASK-AAO-0200 | Claim admission / broker resolution consumption | `next --claim` freezes on a CID-name collision (`insufficient-mutation-intent`) even when a valid `atm.brokerConflictResolution.v1` artifact authorizes the pair, and the requiredCommand it prints (team broker resolve) therefore cannot unblock the claim. | Claim admission must consume matching broker conflict resolution artifacts the same way the governed commit lane does (`readResolutionAuthorizedForeignTaskIds`). | TASK-AAO-FABLE-004 vs TASK-AAO-FABLE-005: no common writable file, resolution artifact BCR-nvfv9w recorded, claim still froze. | Fixed in TASK-AAO-0200: extracted shared `readResolutionAuthorizedForeignTaskIds` reader, claim admission consumes matching BCR sidecars before freezing; regression `packages/cli/src/commands/next/__tests__/claim-broker-resolution.spec.ts`. |
| ATM-BUG-2026-07-13-161 | 2026-07-13 | AI-Atomic-Framework | Optimization | High | Open | Close-window staged-index contention across captains | Another agent's staged files (source and task-plan) make `taskflow close` fail `ATM_TASKFLOW_CLOSE_INDEX_NOT_ISOLATED`; unattributable staged source cannot be deferred by `--defer-foreign-staged`, so the operator must manually park/restore the foreign staged set. | The close window should snapshot-park unattributable foreign staged entries with an explicit restore contract (or wait on a shared index lease) instead of failing isolation. | TASK-AAO-FABLE-002 close required manually unstaging codex-backlog-captain's stalled staged files and restaging them after the closure commit. | Design with the branch commit queue lease; never silently drop another agent's staged content. |
| ATM-BUG-2026-07-13-162 | 2026-07-13 | AI-Atomic-Framework | Bug | High | Needs task card | Commit wrapper / pre-commit parity | `node atm.mjs git commit --dry-run` and the real pre-commit hook can disagree about active foreign governance residue ownership. | The same ownership proof rules must be shared by the commit bundle builder and pre-commit hook: active direction lock or active ledger claim should protect non-staged foreign governance residue from blocking another active task, while orphan residue stays fail-closed. | During TASK-AAO-FABLE-005 and TASK-AAO-0188, both task-scoped dry-runs became green only after the wrapper was patched, but the real pre-commit still found additional direction-lock scope drift for protected override audit files. FABLE-005 delivery commit `6a535795` and 0188 delivery commit `bb97753b` required extra scope amendments before real commits could pass. | Fold wrapper/hook residue ownership into a shared helper under `TASK-GIT-0013`; add regression for dry-run/write parity with two active claims and foreign bundle manifests. |
| ATM-BUG-2026-07-13-163 | 2026-07-13 | AI-Atomic-Framework | Bug / Product gap | High | Needs task card | Protected override audit scoping | Protected override audit files can be selected into a governed commit bundle as task-owned artifacts, but the active task direction lock may not automatically include them. | Any generated task-owned protected override audit included by the commit bundle should either be automatically allowed by the direction-lock gate or trigger a precise scope-amendment flow before staging. | FABLE-005 commit initially failed on `.atm/history/protected-override-audit/2026-07-12T15-59-52-259Z-POA-0e9840962464.json`; 0188 failed on two protected override audit files. Both were current-task artifacts but outside `taskDirectionLock.allowedFiles`; `tasks scope add` was required before commit. | Add protected-override audit ownership to the direction-lock allowed-file model and cover it in pre-commit + commit-wrapper tests. |
| ATM-BUG-2026-07-13-164 | 2026-07-13 | AI-Atomic-Framework | Optimization | Medium | Fixed | Task scope amendment UX | `tasks scope add` can require a claim, while `next --claim` can require scope expansion; the successful escape hatch is `--claim-first`, but the error path and help text do not lead there clearly enough. | When claim is blocked by scope expansion, ATM should suggest the exact `tasks scope add --claim-first ...` command. Help text should distinguish task-card scope/deliverables from runtime lock repair. | Fixed by making no-lock/released-lock scope amendment failures expose `tasks scope add --claim-first --add <paths>` as the primary `requiredCommand`, while retaining `next --claim` as `claimCommand`; regression asserts both human text and structured data expose the claim-first escape hatch. Validator: `node --strip-types tests/cli/tasks-scope-add-error-clarity.test.ts`. | Delivered in current fix commit. |
| ATM-BUG-2026-07-13-165 | 2026-07-13 | AI-Atomic-Framework | Bug | Medium | Fixed | Scope amendment argument parsing | `tasks scope add --add <csv>` can preserve stray quote characters in amendment history when comma-separated paths are passed through PowerShell/JSON surfaces. | Scope amendment history should store normalized exact paths without shell quote artifacts, regardless of shell. | Fixed by normalizing boundary quote artifacts before splitting `--add` / `--paths` CSV values and again per token; regression covers quoted CSV plus leading-only and trailing-only quote artifacts so amendment history receives clean paths. Validators: `node --strip-types tests/cli/task-scope-amendment.test.ts`; `npm run typecheck`; `npm run check:encoding:touched -- --files docs/governance/atm-bug-and-optimization-backlog.md packages/cli/src/commands/tasks/task-option-parsers.ts tests/cli/task-scope-amendment.test.ts`; `git diff --check -- ...`. | Delivered in current fix commit. |
| ATM-BUG-2026-07-13-166 | 2026-07-13 | AI-Atomic-Framework | Bug / UX | Medium | Fixed | Taskflow pre-close remediation command validity | `taskflow pre-close` can suggest validator remediation commands that are not executable in the current CLI surface. | Every emitted `requiredCommand` should be runnable as printed, or should point to the known package script when no ATM subcommand exists. | Fixed by applying the taskflow auto-evidence package-script mapper to dry-run/pre-close remediation planning, not only execution. Catalog-selected validator hints now surface the mapped executable command such as `npm run check:encoding:touched` when an equivalent package script exists. Validator: `node --strip-types tests/cli/taskflow-auto-evidence.test.ts`. | Delivered in current fix commit. |
| ATM-BUG-2026-07-13-167 | 2026-07-13 | AI-Atomic-Framework | Optimization | Low | Fixed | Task scope amendment UX | `tasks scope add` uses `--add`, but `--paths` is a natural guess and fails with generic `ATM_CLI_USAGE`. | When an operator uses `--paths` with `tasks scope add`, ATM should either alias it to `--add` or print the exact corrected command. | Fixed on 2026-07-13: `tasks scope add --paths <paths>` now aliases to `--add <paths>` through the same CSV/quote normalization, and the missing-add usage text names the alias. | Covered by `tests/cli/task-scope-amendment.test.ts` alias regression. |
| ATM-BUG-2026-07-13-168 | 2026-07-13 | AI-Atomic-Framework | Bug | High | Fixed in TASK-GIT-0015 | Taskflow pre-close / destructive Git remediation | `taskflow pre-close` still emitted raw `git restore -- <files>` as the accidental-drift recovery path for dirty in-scope delivery files even after staging-index remediation had moved to ATM leases. | No taskflow/operator remediation should recommend raw `git restore`, `git reset`, `git checkout`, or `git clean`; destructive worktree or index mutation must route through a governed ATM lease/audit path. | Reproduced during TASK-GIT-0015 pre-close: `ATM_TASKFLOW_PRECLOSE_SCOPE_TRACKED_DIRTY` suggested `git restore -- "packages/cli/src/commands/git-governance.ts" ...`. TASK-GIT-0015 replaced that hint with `node atm.mjs git lease destructive-override ...` and removed a raw `git restore` rollback hint from git-admission proposal generation plus release hygiene guidance. | Keep a regression scanning taskflow/git-governance operator hints for raw destructive Git remediation commands; allow only negative examples such as "do not use". |
| ATM-BUG-2026-07-13-169 | 2026-07-13 | AI-Atomic-Framework | Optimization | Medium | Fixed | Identity CLI / operator discoverability | `identity status --json` is a natural status command but returns generic usage text (`identity supports: set, show, clear`) instead of aliasing to `identity show` or printing the exact corrected command. | Status-style discovery commands should either work as aliases or emit copyable remediation such as `node atm.mjs identity show --json`. | Fixed on 2026-07-13: `identity status` now aliases to `identity show`, emits `ATM_IDENTITY_SHOW`, and is covered by `tests/cli/identity-per-actor-routing.test.ts`; manual `node --strip-types packages/cli/src/atm.ts identity status --json` also returns ok. | Keep an eye on the remaining related discoverability idea: surface active claim actor mismatch earlier when legacy env identity differs from active session actor. |
| ATM-BUG-2026-07-13-170 | 2026-07-13 | AI-Atomic-Framework | Optimization | High | Fixed in TASK-AAO-0195 | Team plan dry-run / proposal-first side effects | Optional `team plan` can fail with `ATM_TEAM_PLAN_INVALID` and register or expose a provisional proposal-first broker lane when the operator only wants a read-only readiness projection. It also used legacy actor `codex-gpt-5.4-mini` while the active task session actor was `codex-backlog-captain`. | Team planning should have an explicitly read-only projection mode for diagnostics such as `indexLane`; if it needs broker admission, it should prefer the active claim actor or require explicit `--actor` before writing/provisioning any lane state. | Fixed in TASK-AAO-0195: `team plan --read-only` skips broker-registry cleanup persistence, downgrades `proposal-first-required` to warning, and prefers active claim actor via `resolveTeamPlanActorId`. Regression: `packages/cli/src/commands/team/__tests__/team-plan-readonly.spec.ts`. | Keep `team start` fail-closed; do not treat read-only plan as admission write. |
| ATM-BUG-2026-07-13-171 | 2026-07-13 | AI-Atomic-Framework | Optimization | High | Fixed in current working tree | Release build hygiene / root-drop mirror residue | `ATM_RETAIN_RELEASE_ARTIFACTS=1 npm run build` and closeout validation can leave a large `release/atm-root-drop/**` tracked mirror diff in the framework worktree, while `scripts/build-release-hygiene.ts --mode cleanup` only restores `release/atm-onefile/atm.mjs`, `release/atm-onefile/release-manifest.json`, and `release/atm-root-drop/release-manifest.json`. Operators then inherit dozens of generated mirror modifications that are not owned by the current task and are unsafe to silently include or raw-restore. | ATM governance actions must be self-cleaning by default: build/runner-sync/validation flows should leave the working tree as clean as they found it unless the workflow is intentionally publishing release artifacts or is forced to pause for human/foreign-owner intervention. When residue is unavoidable, ATM must write an explicit owner/status/next-action packet and provide a governed cleanup/apply command. Cleanup should cover the entire generated root-drop mirror when it was produced only as validation/sync residue. | Reproduced after closing TASK-GIT-0015 on 2026-07-13: source, task ledger, backlog, and remote push were complete, but `git status --short` still showed about 70 modified `release/atm-root-drop/**` files. Running `node --strip-types scripts/build-release-hygiene.ts --mode cleanup` restored only three tracked release files and left the root-drop mirror dirty. Fix evidence: `collectTrackedReleaseArtifactPaths` now includes every tracked path under `release/atm-onefile` and `release/atm-root-drop`; regression `tests/cli/build-release-hygiene.test.ts` covers tracked root-drop files missing from the manifest; `npm run build` without retain restored the generated tracked release tree back to HEAD. | Keep release publication explicit through `ATM_RETAIN_RELEASE_ARTIFACTS=1 npm run build`; validation-only builds should self-clean tracked release mirror changes by default. |
| ATM-BUG-2026-07-13-172 | 2026-07-13 | AI-Atomic-Framework | Product gap | High | Fixed in current working tree; validating for commit | ATM runtime residue reconciler / abandoned or foreign governance artifacts | Governed workflows can leave untracked `.atm/history/**`, `.atm/task-plans/**`, `.atm/runtime/**`, and handoff artifacts from abandoned, released, or foreign tasks. Later agents cannot safely delete, stage, or commit these files because they may be audit evidence from another task, but leaving them in the worktree makes every closeout noisy and can block generated-residue guards. | Every ATM governance action should own its environment cleanup contract: normal completion should either commit/archive its durable evidence or remove disposable runtime files; only forced pauses, active foreign ownership, or explicit human-decision states may leave residue. ATM should provide a first-class residue reconciler that classifies `.atm` artifacts by task id, ledger state, actor/claim ownership, provenance, and disposability, then offers safe actions: attach to owning task, archive as governed evidence, discard only disposable runtime residue, or defer with a clear owner/queue. | Reproduced during the same TASK-GIT-0015 closeout: after the task itself was done/no-residue, `git status --short` still listed untracked `.atm/history/evidence/TASK-AAO-0165.json`, `.atm/history/task-events/TASK-AAO-0165/`, `.atm/history/tasks/TASK-AAO-0171.json`, `.atm/task-plans/TASK-AAO-0189.md`, `.atm/runtime/broker-conflict-resolutions/`, `.atm/runtime/git-push-attempts/`, and related artifacts from older or foreign tasks. Similar residue was previously recorded in rows 147 and 161. On 2026-07-13, `atm residue status --json` correctly separated active-owner TASK-AAO-FABLE-009 artifacts from abandoned/handoff/runtime residue, but it was still read-only; the operator had to remove the non-active residue with a manually curated path list and then rerun status to reach zero residue. The repair adds `residue reconcile`, dry-run by default, plus `--apply` for owner-aware deletion of non-staged disposable residue such as abandoned-task artifacts, runtime push-attempt/broker artifacts, and released broker-intents while deferring active-owner or staged entries. Focused regression: `node --strip-types packages/cli/src/commands/__tests__/residue.spec.ts`; source/frozen smoke: `node atm.dev.mjs residue reconcile --json`, `node atm.mjs residue reconcile --json`, and `node atm.mjs residue reconcile --apply --json` cleaned current safe runtime residue. | Commit the reconciler repair and frozen runner sync, then extend follow-up coverage for incident-specific resolution and release mirror cleanup under rows 102 and 171 rather than treating those as generic disposable residue. |
| ATM-BUG-2026-07-13-173 | 2026-07-13 | AI-Atomic-Framework | Optimization | Medium | Fixed | Task id alias resolution / FABLE shorthand | `next --prompt` does not resolve shorthand task ids such as `FABLE-004` or `FABLE-005` to imported ledger tasks named `TASK-AAO-FABLE-004` / `TASK-AAO-FABLE-005`, even though direct `tasks status --task TASK-AAO-FABLE-004` works. | Task-id alias expansion should understand imported task-family suffixes and map shorthand labels to unique matching ledger task ids; if multiple families match, it should return selection-required rather than scope-not-found. | Fixed on 2026-07-13: prompt scoring now treats a mentioned shorthand as a task-id suffix match, so `FABLE-004` can resolve to `TASK-AAO-FABLE-004` without requiring the full task id. `validate-prompt-scoped-next` covers `FABLE-004` and `AAO-FABLE-005` shorthand routing. | Keep suffix shorthand matching below exact task-id priority so ambiguous same-suffix cases continue to require selection instead of silently choosing a task. |
| ATM-BUG-2026-07-13-174 | 2026-07-13 | AI-Atomic-Framework | Optimization | Medium | Fixed | Next router predicate SSOT drift | Queue-request prompt detection exists in both `packages/cli/src/commands/next/intent-normalizers.ts` and `packages/cli/src/commands/next/route-predicates.ts`; updating only the normalizer did not affect the active `next.ts` route decision, so a new regression kept failing until the second copy was updated. | Queue-request language should have one shared predicate source consumed by both intent normalization and route selection, with tests proving the reported `taskIntent.queueRequested` and actual route decision agree. | Fixed on 2026-07-13: `isQueueRequestedPrompt` now has one source in `intent-normalizers.ts`, while `route-predicates.ts` imports and re-exports that helper for the `next.ts` route decision. `validate-prompt-scoped-next` now asserts backlog continuation prompts set `taskIntent.queueRequested` and route to batch. | Keep the prompt-scoped regression as the SSOT guard whenever queue-request language is expanded. |
| ATM-BUG-2026-07-13-175 | 2026-07-13 | AI-Atomic-Framework | Product gap | High | Fixed in current working tree; needs commit | Active claim radar / Team Agent Broker auto-admission | ATM can store active task claims, direction locks, heartbeats, and staged residue, but `next --prompt`, `next --claim`, and framework temp-claim entrypoints do not present a compact "who is active now" actor map before another AI starts work. This makes concurrent captains depend on the human to notice that another actor such as `claude-fable-5` is already moving. | ATM should automatically surface recent active claims, actor ids, task ids, touched files, heartbeat age, TTL, and staged/index ownership at every work-admission route. If another active actor or conflicting staged residue is present, ATM should emit a clear Team Agent Broker arbitration recommendation or required command without waiting for a human instruction. | Live dogfood on 2026-07-13: Codex attempted to continue backlog work, saw `TASK-AAO-FABLE-008` already claimed by `claude-fable-5`, then `framework-mode claim` reported staged residue outside scope from another lane. The repair adds `activeWorkSummary` to `next` governance readiness, emits `ATM_ACTIVE_WORK_BROKER_RECOMMENDED`, and recommends Team levels L1-L5 from own/foreign scope, staged index state, and framework-foundation risk. Focused regression: `node --strip-types --test packages/cli/src/commands/next/__tests__/active-work-summary.spec.ts`; static gate: `npm run typecheck`. | Commit the active-work summary repair, then expand follow-up coverage to framework-mode status/claim and deeper broker/team planning projections if needed. |
| ATM-BUG-2026-07-13-176 | 2026-07-13 | AI-Atomic-Framework | Optimization / AI routing | High | Fixed in TASK-AAO-0193 | Framework-repo task open / planning-root reminder | When an agent opens or imports a framework-development task from inside `AI-Atomic-Framework`, ATM does not remind them to first author the plan MD and formal task card under the canonical 3KLife planning root (`docs/ai_atomic_framework/**/tasks/`). Agents can therefore claim/import/close using only `.atm/task-plans/` in the target repo, leaving the AAO/TEAM planning roster incomplete until someone notices later. | For framework-repo task creation/import/open routes, ATM should detect missing planning-root authorship and emit a blocking or high-priority notice with the exact 3KLife path pattern and next command (for example write `.../atm-agent-first-operability/tasks/TASK-AAO-NNNN-....task.md`, update the roster README, then import from that path). Target-repo-only task-plans should be treated as incomplete for AAO/TEAM-style work unless an explicit waiver is recorded. | Fixed in TASK-AAO-0193: framework-repo `tasks import` of AAO/TEAM cards from `.atm/task-plans/` now fail-closed on `--write` (and warn on `--dry-run`) unless a matching planning-root card exists or `--waive-planning-root --reason` is explicit. Regression: `packages/cli/src/commands/tasks/__tests__/planning-root-authorship.spec.ts`. | Keep the same preflight visible on taskflow open guidance when AAO/TEAM work starts from target-only mirrors. |
| ATM-BUG-2026-07-13-177 | 2026-07-13 | AI-Atomic-Framework | Bug | High | Fixed in TASK-AAO-0191 | Governed commit / defer-foreign-staged absorbs ordinary-unowned staged residue | `node atm.dev.mjs git commit --auto-stage --defer-foreign-staged` for a framework temp claim scoped only to backlog + task-plan still included a long-lived staged foreign file (`packages/cli/src/commands/tasks/__tests__/import-orchestrator.spec.ts`, ownership `ordinary-unowned`) in the resulting commit `a6aa957a3`. Pre-push then treated that path as a critical changed file even though it was not part of the claim scope. | Task/temp-claim scoped commits must never stage or commit ordinary-unowned / foreign staged files when `--defer-foreign-staged` is set; deferral must cover unattributable staged source, not only governance artifacts with task ids. | Fixed in TASK-AAO-0191: framework-claim commits now fail closed on out-of-claim staged files unless `--defer-foreign-staged` is set, and always commit through an isolated claim-allowed index so ordinary-unowned staged residue stays out of the commit while remaining staged for the original owner. Regression covers both fail-closed and defer paths in `tests/cli/git-commit-task-scoped-staging.test.ts`. | Keep framework-claim commits on the isolated-index path; do not fall back to the live index when a temp framework claim is active. |
| ATM-BUG-2026-07-13-178 | 2026-07-13 | AI-Atomic-Framework | Bug | High | Fixed in TASK-AAO-0192 | Abandoned task re-import still requires emergency --force | After a mistaken `tasks abandon` on a freshly imported ready/in-progress card (`TASK-AAO-0191`), `tasks import --write --reopen` / `--reset-open` still fail with `ATM_TASKS_IMPORT_DRIFT` asking for `--force`, and `--force` always routes through the emergency approval lease even though the ledger is abandoned/inert with no active claim. | Reopening an abandoned/inert task from its planning card when there is no active claim should be a normal operator path (`--reopen` or `--reset-open`) without emergency approval; emergency should remain only for overwriting active claims or protected live work. | Fixed in TASK-AAO-0192: `writeTaskFiles` now allows `--reopen`/`--reset-open` to overwrite hash-drifted inert ledgers without `--force`. Regression covers abandoned reopen in `import-orchestrator.spec.ts`. | Keep active-claim protection; do not soften emergency for live claims. |
| ATM-BUG-2026-07-13-179 | 2026-07-13 | AI-Atomic-Framework | Product gap | High | Fixed | Batch checkpoint / historical-batch closeback | A close-ready `evidence historical-batch` slice can prove that a batch queue-head task's real delivery already landed, but `batch checkpoint` still ignores that slice and fails `ATM_TASK_CLOSE_DELIVERABLE_DIFF_REQUIRED`; the only working checkpoint path is `--historical-delivery`, which routes to protected backend emergency approval. | Batch checkpoint should consume close-ready historical-batch evidence for the current queue head, or accept `--historical-batch <id>` directly, so historical closeback can stay in the governed batch lane without human emergency approval when the slice is complete and validator-backed. | Fixed in `6e7514313`: `batch checkpoint` accepts `--historical-batch`, close-artifact staging understands historical-batch close-ready slices, and related pre-commit/result-contract wiring landed with regression coverage in `batch-skip-resume.spec.ts`. | Keep historical-batch closeback in the governed batch lane; do not route complete slices through emergency `--historical-delivery` only. |
| ATM-BUG-2026-07-13-180 | 2026-07-13 | AI-Atomic-Framework | Bug | High | Fixed in TASK-AAO-0194 | taskflow close / deferred foreign dirty snapshot ENOENT | `taskflow close --write --defer-foreign-state` for `TASK-AAO-0193` still created the target governance close commit (`9658f33e1`) and marked the ledger `done`, but the CLI then failed with `ATM_CLI_UNHANDLED` / `ENOENT` while opening a close-window snapshot under `.atm/runtime/snapshots/close-window-governance-dirty-...TASK-AAO-0192.bundle-manifest.json.json` for a deferred foreign dirty path. Operators cannot tell from the wrapper alone whether close already landed. | Deferred foreign dirty files must never be required as readable snapshot sources during close finalization; if a snapshot is needed, ATM should skip missing/deferred paths or write a recoverable incident packet instead of throwing unhandled ENOENT after the close commit already exists. | Fixed in TASK-AAO-0194: governance-dirty deferral is scoped to `git-head.jsonl` plus the **current task** bundle-manifest only (no foreign-task `git restore`), and `restoreDeferredGovernanceDirtyFiles` skips missing snapshots instead of throwing. Regression coverage lives in `commit-bundle-assembly.spec.ts`. | Keep foreign evidence dirt out of another task's close-window restore path; missing snapshots must remain non-fatal after a successful close commit. |
| ATM-BUG-2026-07-13-181 | 2026-07-13 | AI-Atomic-Framework | Bug / AI routing | High | Fixed in `cf883b8f3` | Next claim / fresh foreign-created task reservation | `next --prompt` can select and `next --claim` can claim a task that was just imported or opened by another captain but not yet actively claimed, because active-work radar only sees live claims/locks and does not treat fresh task creation provenance as a soft reservation. A second captain can therefore steal a newly created task card before the creator starts work. | Fresh task imports/opens should carry creator actor, created/imported timestamp, source planning owner, and a short reservation/grace policy. `next --prompt` and `next --claim` should avoid auto-claiming another actor's fresh unclaimed task unless the creator explicitly handed it off, the reservation TTL expired, or a human/Team Broker override authorizes takeover. The one-glance active-work summary should include these fresh reserved tasks and recommend Team Broker level based on creator/claim/scope overlap; for framework/source compiler work, default at least L3 and escalate if shared foundation files overlap. | Live dogfood on 2026-07-13: `TASK-AAO-FABLE-010` had just been created by captain `claude-fable-5` for skill-template installed-copy drift detection. Codex asked ATM to continue backlog work while avoiding other active actors; activeWorkSummary reported no foreign active work and Team L1, then `next --claim --actor codex-gpt-5.4-mini` claimed `TASK-AAO-FABLE-010`. The user corrected that this was Fable5's freshly opened task; Codex released the claim with `node atm.mjs tasks release --task TASK-AAO-FABLE-010 --actor codex-gpt-5.4-mini --json` and made no deliverable edits. Fix evidence: `node --strip-types packages/cli/src/commands/next/__tests__/active-work-summary.spec.ts`, `node --strip-types packages/cli/src/commands/next/__tests__/fresh-task-reservation.spec.ts`, `npm run typecheck`, pushed commit `cf883b8f3`. | Implemented fresh reservation visibility in active-work summary and a claim-admission guard that blocks different actors from auto-claiming a fresh unclaimed foreign-created task unless `--force` is used after the warning/Team Broker context is shown. |
| ATM-BUG-2026-07-14-182 | 2026-07-14 | AI-Atomic-Framework | Bug | High | Fixed in TASK-AAO-0205 | Governed commit / framework-claim absorbs unstaged release mirror residue | After `integration add --force` refreshed adapter skill mirrors, a framework temp claim scoped only to those skill paths plus `git commit --auto-stage --defer-foreign-staged` still produced commit `abc11c61b` that included unrelated tracked `release/atm-onefile/**` and `release/atm-root-drop/**` files (backlog mirror, closure-packet-schema, validate-cli, release manifests) that were dirty in the worktree but outside the declared claim scope. This is adjacent to ATM-BUG-2026-07-13-177 (ordinary-unowned *staged* absorption) but reproduced with *unstaged* release-mirror dirt and an explicit skill-only claim file list. | Framework-claim commits with `--defer-foreign-staged` must keep the isolated claim-allowed index closed over unstaged out-of-claim dirty files as well as staged ones; release mirrors generated or dirtied by hooks/build hygiene must not be auto-absorbed into an unrelated skill/docs commit. Prefer fail-closed with a copyable defer/widen command, or leave release dirt untouched for the owner. | Reproduced 2026-07-14 by actor `cursor-grok-4.5`: claimed only `.agents/.claude/.cursor/.gemini/.github/integrations` `atm-atom-map-refactor` skill mirrors, then `node atm.dev.mjs git commit --actor cursor-grok-4.5 --auto-stage --defer-foreign-staged --json` returned `ATM_GIT_COMMIT_OK` for `abc11c61b` whose file list mixed the intended skill mirrors with `release/atm-onefile/*` and `release/atm-root-drop/*` paths. Source counterparts (`packages/.../closure-packet-schema.ts`, `scripts/validate-cli.ts`, backlog) remained dirty afterward. | Fixed 2026-07-14: `isFrameworkGeneratedArtifactAllowed` no longer admits every release-manifest path on any active claim; regression extended in `tests/cli/git-commit-task-scoped-staging.test.ts`. |
| ATM-BUG-2026-07-14-183 | 2026-07-14 | AI-Atomic-Framework | Optimization | High | Open | Frozen vs source-first write deadlock under parallel captains | Frozen `node atm.mjs` refuses writes with `ATM_RUNNER_STALE_WRITE_REFUSED` when source is newer; `--allow-stale-runner` needs emergency; `node atm.dev.mjs` refuses lifecycle claim with `ATM_SOURCE_FIRST_WRITE_REFUSED`. Parallel captains told to prefer Frozen cannot sync without dirtying `release/**`. Template-only changes can also leave frozen integration source snapshots stale while `runnerMode.sourceDrift.syncRequired` remains false. | Playbooks should offer a non-emergency source-delivery lane or a scoped runner rebuild that does not absorb foreign release WIP. Runner drift inventory must include integration templates and compiler inputs. | 2026-07-14 cursor-grok-4.5 dogfood on TASK-AAO-0195 before RFT-0020 completed. Reproduced again while updating `atm-dispatch`: frozen `next` reported `syncRequired: false`, frozen/source-first integration verification disagreed, and a full retained-artifact build was required before all adapter manifests could converge. | Add an explicit next-command branch plus template-aware source drift and scoped adapter-sync coverage. |
| ATM-BUG-2026-07-14-184 | 2026-07-14 | AI-Atomic-Framework | Bug / Process | High | Open | Parallel captain runner sync wipes foreign uncommitted source | Another captain's `build(release): sync ... runner artifacts` (`3cd236598`) coincided with disappearance of uncommitted bug-fix WIP (team plan read-only, provider metadata, import/lock/incident edits). | Runner sync must refuse or snapshot-protect foreign dirty non-release paths before mutating the tree. | 2026-07-14 cursor-grok-4.5 lost 0195/099/159/097/102 WIP and had to re-implement after RFT-0020 sync. | Pre-build dirty-owner scan + handoff packet. |
| ATM-BUG-2026-07-14-185 | 2026-07-14 | AI-Atomic-Framework | Bug | Medium | Fixed in TASK-AAO-0202 | Task ledger / claim release transition evidence | Batch checkpoint and governed closeout can leave a post-delivery task ledger mutation that changes `claim.status` from `active` to `released` and adds release metadata after the task delivery commit, but no matching task-event file is generated. The protected-state pre-commit guard then rejects staging that ledger file alone with `ATM_PROTECTED_STATE_TASK_FILE_MISSING_TRANSITION`, leaving durable manual-review residue. | Claim release metadata should be written before the delivery commit, written with a matching transition/audit event, or classified by residue reconcile as a safe post-close claim-release state mutation with an accepted governed cleanup lane. | Reproduced after TASK-RFT-0024 close commit `ee65c657`: `.atm/history/tasks/TASK-RFT-0024.json` changed only claim release fields; `node atm.mjs residue status --json` classified it as manual-review; `node atm.mjs git commit --task TASK-RFT-0024 --actor "Codex-GPT 5.5" --message "chore(tasks): record TASK-RFT-0024 claim release" --json` failed with `ATM_HOOK_PRE_COMMIT_DEFERRED_GOVERNANCE_REQUIRED` / `ATM_PROTECTED_STATE_TASK_FILE_MISSING_TRANSITION` because no matching transition event exists under `.atm/history/task-events/TASK-RFT-0024/`. | Fixed in `packages/cli/src/commands/tasks/legacy-impl.ts`: `tasks lock cleanup` now writes governed `lock-cleanup` task transitions whenever it releases a terminal active claim or canonical direction lock, instead of mutating `.atm/history/tasks/*.json` directly. Validation: `node --strip-types packages/cli/src/commands/tasks/__tests__/claim-release-transition.spec.ts`; `node --strip-types packages/cli/src/commands/tasks/__tests__/lock-cleanup.spec.ts`. |
| ATM-BUG-2026-07-14-186 | 2026-07-14 | AI-Atomic-Framework | Optimization | Medium | Fixed in TASK-AAO-0201 | Multi-task shared delivery close UX | Closing sibling tasks against one intentional shared delivery commit (`--historical-delivery`) fails without `--waiver-out-of-scope-delivery`, and dry-run/write messaging mixes `MIXED_DELIVERY_COMMIT`, `OUT_OF_SCOPE_WAIVER_REQUIRED`, and `DELIVERABLE_DIFF_REQUIRED` until the waiver is present. | When the operator already passes `--historical-delivery` for co-delivered siblings owned by the same actor batch, ATM should offer a first-class multi-task delivery acknowledgment (or auto-suggest the exact waiver command with the sibling task list) instead of looking like a failed delivery. | Dogfood 2026-07-14: TASK-AAO-0197..0200 against delivery `c7e3e81a` after TASK-AAO-0196 scope-amended shared commit; close succeeded only after `--waiver-out-of-scope-delivery --reason`. | Fixed in `packages/cli/src/commands/taskflow/write-readiness.ts` and `historical-close-preflight.ts`: shared `--historical-delivery` close now promotes the exact waiver command as the primary blocker, demotes missing-delivery codes, and surfaces a `multiTaskCloseRecipe` sibling hint. Validation: `node --strip-types packages/cli/src/commands/taskflow/__tests__/mixed-delivery-close-ux.spec.ts`. |
| ATM-BUG-2026-07-14-187 | 2026-07-14 | AI-Atomic-Framework | Documentation / AI routing | Medium | Open | Cross-repository planning authority is remembered but not enforced before authoring | A Captain working in the framework repository created a complete framework-local optimization plan even though the established planning authority was 3KLife and only the imported ATM ledger belonged in the framework. The mistake recurred because handoff memory and a general planning/target distinction did not force an explicit authority declaration before file creation. | Before authoring, ATM guidance must resolve and state planning, target, and closure authority. When they differ, the full plan and source cards stay in the planning repository; the target accepts only CLI-imported ledger records and explicit neutral product deliverables. | Reproduced during the 2026-07-14 governance-optimization planning turn; the local plan was removed and replaced by a Traditional Chinese plan plus `tasks/` under the coordinating planning repository. | Guidance added to source and installed `atm-task-card-authoring` / `atm-dispatch` skills. Add a deterministic authoring/import preflight under `ATM-GOVOPT-008` so source-path authority is machine-checked rather than memory-dependent. |
| ATM-BUG-2026-07-14-188 | 2026-07-14 | AI-Atomic-Framework | Bug / Optimization | Medium | Fixed in ATM-GOV-0124 | Product charter validator / ignored residue isolation | `validate:cli` failed during ATM-GOV-0124 because `validate-product-charter` scanned `templates/skills/atm-memory-consolidate.skill.md`, an ignored/untracked residue excluded by `.git/info/exclude`, and treated its `tools_node/` text as a protected downstream-only term violation. | Product charter validation should check governed tracked files and intentional unignored new files, while ignored/untracked residue remains outside close gates unless explicitly claimed. | Repro: `npm run validate:cli` failed with `[product-charter:validate] templates/skills/atm-memory-consolidate.skill.md contains downstream-only term: tools_node/`; `git check-ignore -v` showed `.git/info/exclude:10:templates/`. | ATM-GOV-0124 updates `scripts/validate-product-charter.ts` to filter protected-scope enumeration through git tracked/ignore state before scanning banned terms. |
| ATM-BUG-2026-07-14-189 | 2026-07-14 | AI-Atomic-Framework | Bug | Medium | Open | Pre-commit / baseline validator isolation | During ATM-GOV-0134 close commit, pre-commit reran validate:cli and failed on existing atm-memory-consolidate skill template prose containing downstream-only tools_node/, even though the active task did not edit that memory-consolidation surface and the handoff explicitly marked it as external residue. | Pre-commit framework validation should distinguish current-task failures from unrelated baseline/template residue, or provide a governed baseline waiver/repair lane that does not force the active task to commit unrelated memory-consolidation files. | node atm.mjs git commit --actor codex-gpt-5-5-captain --task ATM-GOV-0134 --message "feat(governance): complete ATM-GOV-0134" --json failed with ATM_FRAMEWORK_VALIDATOR_FAILED; nested validator-run-resume-and-status reported validate-product-charter failure: templates/skills/atm-memory-consolidate.skill.md contains downstream-only term: tools_node/. | Create a follow-up task for pre-commit baseline/current-task classification around validator-run-resume-and-status and product-charter checks; do not mix the memory-consolidation template repair into ATM-GOV-0134. |
| ATM-BUG-2026-07-15-190 | 2026-07-15 | AI-Atomic-Framework | Optimization | Medium | Open | Batch checkpoint / Team Agents dogfood | TASK-RFT-0026 dogfood showed that batch checkpoint can advance and claim the next queue head before the previous task's post-checkpoint commit lands, causing the active direction lock for TASK-RFT-0027 to block the already-validated TASK-RFT-0026 commit. The same RFT closeout also showed that shared governance files plus another active captain make multi-agent write fan-out more expensive and riskier than a single coordinator for this task family. | Batch checkpoint should preserve a commit window for the validated queue-head bundle or defer next-task claim until the previous task is committed. Team Agents should remain shadow/controlled dogfood until production cost, wall-clock, quality, roster, and pricing evidence prove a net win. | TASK-RFT-0026 required emergency lease EMG-TASK-RFT-0026-a24301fd74 after checkpoint advanced to TASK-RFT-0027; TASK-RFT-0028 controlled dogfood compares incremental, fully-loaded, and list-price-equivalent Team cost against paired single-Agent baseline and keeps token counts diagnostic-only. | Next experiment: add a close-commit-window-aware batch checkpoint or pendingCommitTaskId hook lane, then rerun Team Agents dogfood with canonical provider usage, pricing evidence, cheap worker models, role collapse, prompt caching, mixed-model routing, and stop-loss enabled before promoting Team as default. |
| ATM-BUG-2026-07-15-191 | 2026-07-15 | AI-Atomic-Framework | Bug | P0 | Fixed in 88af0d7b4 and 6c8149a89 | Git governance / governed commit UX | A Codex-side staging helper spawned git add --pathspec-from-file=- --pathspec-file-nul and waited on stdin, making the user-facing flow look like node atm.mjs git commit was hung even though no commit was running and no files were staged. | ATM git commit and git commit-status should fail fast or report a first-class diagnostic when a stdin pathspec git add helper is active, so operators can distinguish a stuck staging helper from a slow governed commit hook. | On 2026-07-15, process inspection showed git.exe -c core.hooksPath=NUL -c core.fsmonitor= add --pathspec-from-file=- --pathspec-file-nul active while git diff --cached was empty and HEAD remained at 9b963b7. | Keep commit preflight detection, commit-status diagnostics, and explicit finite auto-stage coverage. ATM auto-stage must continue to use git add -A -f -- <paths>, never stdin pathspec staging; tests/cli/git-commit-task-scoped-staging.test.ts covers the staging command. |
| ATM-BUG-2026-07-15-192 | 2026-07-15 | AI-Atomic-Framework | Product gap | P0 | Needs task card | CLI error-code knowledge base | ATM has docs/ERROR_CODES.md generated from source occurrences and docs/cli-error-policy.md for envelope policy, but the global error-code surface is not yet a curated knowledge base with canonical category, human description, remediation, retryability, approval requirements, and related recovery commands for every user-visible ATM_* code. | Every user-visible ATM error code should resolve to one canonical registry entry that includes stable meaning, severity/category, short operator-facing explanation, required command or recovery action, retryability, and links to the owning validator or runbook. Skills and CLI help should reference the same registry instead of duplicating partial prose. | docs/ERROR_CODES.md currently lists code, source location, and raw context only; Team Agents has a separate local canonical-code table in docs/governance/team-agents/team-plan-vocabulary-and-roster-drift.md, showing the registry is fragmented by surface. | Open a dedicated task to replace or augment generate-error-code-index with a structured error-code registry/projection and update ATM skills so agents can look up a code and provide the same concise description plus fix path. |
| ATM-BUG-2026-07-15-193 | 2026-07-15 | AI-Atomic-Framework | Optimization | Medium | Open | Task Authoring | The giant-module split wave can author task cards whose acceptance criteria imply one card should reduce a 5,000+ line legacy module all the way to the final 600-line target. That makes safe first-slice extraction look incomplete even when it creates a valid strangler boundary and command-backed evidence. | ATM task-card authoring for oversized modules should generate phased acceptance: first establish or extend an extraction seam, then reduce the source below 2,000 lines, then below 600 lines, with each card carrying a realistic line-budget delta and atom-map coverage requirement. | TASK-RFT-0030 was authored for packages/cli/src/commands/team-legacy.ts after TASK-RFT-0029. The first safe extraction moved 573 lines of Team legacy type/catalog code into packages/cli/src/commands/team/legacy/types.ts, but the source remains above the final 600-line goal. | Add a card-authoring helper or validator hint for giant-module split cards: when source lines exceed 2,000, require phased line-budget acceptance and a follow-up queue instead of a single final 600-line gate. |
| ATM-BUG-2026-07-15-194 | 2026-07-15 | AI-Atomic-Framework | Optimization | Medium | Open | Refactor Tooling | Large next.ts extraction can reach the required line budget faster than it can fully type all newly bounded projection modules, tempting agents to use transitional ts-nocheck surfaces during a governed split. | ATM refactor cards should distinguish temporary extraction seams from fully typed module boundaries, and should automatically open or recommend a follow-up hardening card when transitional ts-nocheck is introduced. | TASK-RFT-0031 split next.ts into route-resolution, prompt-results, and playbook-projection modules. The source and frozen runner validators pass, but prompt-results/playbook-projection were intentionally marked as transitional extraction surfaces to preserve behavior while meeting the <2,000-line source budget. | Add a post-split hardening card to remove ts-nocheck from extracted projection modules, restore explicit imported types, and add a validator that blocks transitional surfaces from surviving beyond the current refactor wave. |
| ATM-BUG-2026-07-15-195 | 2026-07-15 | AI-Atomic-Framework | Optimization | Medium | Needs task card | CLI / Git governance | TASK-RFT-0032 made git-governance.ts a thin facade, but the extracted implementation is a transitional compact carrier instead of semantically typed bounded modules. | Git governance commit, push, status, branch-queue, and evidence-boundary logic should live in readable typed modules that each stay below the line budget without compression. | packages/cli/src/commands/git-governance/implementation.ts created during TASK-RFT-0032 to preserve behavior while satisfying the immediate facade split and line-count gate. | Open a follow-up refactor card to expand implementation.ts into typed bounded modules, remove @ts-nocheck, and keep the git-governance extraction test green. |
| ATM-BUG-2026-07-15-196 | 2026-07-15 | AI-Atomic-Framework | Optimization | Medium | Needs task card | CLI / Tasks command | TASK-RFT-0033 made tasks/legacy-impl.ts a thin facade, but the moved implementation remains a transitional compact carrier instead of readable semantic command modules. | Tasks import, status, close, audit, scope, queue, roster, and parser compatibility logic should live in typed bounded modules that each stay below the line budget without compact carrier compression. | packages/cli/src/commands/tasks/legacy/implementation.ts was created during TASK-RFT-0033 to preserve behavior while satisfying the immediate facade split and line-count gate. | Open follow-up refactor cards to expand tasks/legacy/implementation.ts into typed command modules, keep facade exports stable, and keep tests/cli/tasks-legacy-impl-extraction.test.ts green. |
| ATM-BUG-2026-07-15-197 | 2026-07-15 | AI-Atomic-Framework | Optimization | Medium | Needs task card | CLI / Framework closure packet | TASK-RFT-0034 made framework-development/closure-packet-schema.ts a thin facade, but the moved implementation remains a transitional compact carrier instead of readable semantic schema modules. | Closure packet validation, creation, repair, git-head evidence, and close-window compatibility logic should live in typed bounded modules that each stay below the line budget without compact carrier compression. | packages/cli/src/commands/framework-development/closure-packet-schema/implementation.ts was created during TASK-RFT-0034 to preserve behavior while satisfying the immediate facade split and line-count gate. | Open follow-up refactor cards to expand closure-packet-schema/implementation.ts into typed schema, validation, creation, repair, and evidence modules while keeping tests/cli/closure-packet-schema-extraction.test.ts green. |
| ATM-BUG-2026-07-15-198 | 2026-07-15 | AI-Atomic-Framework | Optimization | Medium | Needs task card | Scripts / Task ledger validators | TASK-RFT-0035 can make scripts/validators/task-ledger/suite-impl.ts bounded with a facade and compact implementation carrier, but the validator concerns should still be expanded into explicit harness, fixture, closeout, residue, import, and emergency-use modules for maintainability. | Task-ledger validator fixtures and assertions should live in semantic bounded modules that each stay below the line budget without compact carrier compression. | scripts/validators/task-ledger/suite-impl/implementation.ts was introduced during TASK-RFT-0035 to preserve behavior while satisfying the immediate facade split and line-count gate. | Open a follow-up RFT card after the current giant-file wave to replace the compact carrier with semantic modules while keeping the public suite exports stable. |
| ATM-BUG-2026-07-15-199 | 2026-07-15 | AI-Atomic-Framework | Optimization | Medium | Needs task card | CLI / Evidence bundle IO | TASK-RFT-0036 can make packages/cli/src/commands/evidence/bundle-io.ts bounded with a facade and compact implementation carrier, but the read, write, manifest, auto-evidence, historical-batch, and git-head evidence concerns should still be expanded into semantic modules for maintainability. | Evidence bundle IO should live in readable typed modules that each stay below the line budget without compact carrier compression. | packages/cli/src/commands/evidence/bundle-io/implementation.ts was introduced during TASK-RFT-0036 to preserve behavior while satisfying the immediate facade split and line-count gate. | Open follow-up refactor cards to expand the implementation carrier into reader, writer, manifest, auto-evidence, historical-batch, and git-head evidence modules while keeping tests/cli/evidence-bundle-io-extraction.test.ts green. |
| ATM-BUG-2026-07-15-200 | 2026-07-15 | AI-Atomic-Framework | Optimization | Medium | Needs task card | CLI / Pre-commit hook guard | TASK-RFT-0037 can make packages/cli/src/commands/hook/pre-commit.ts bounded with a facade plus transitional support carriers, and it extracts cross-file consistency plus failure-envelope helpers first. Protected-state, direction-lock, same-file ownership, identity, and residue checks should still be expanded into semantic guard modules. | Pre-commit guard behavior should live in readable typed modules that each stay below the line budget without compact carrier compression. | packages/cli/src/commands/hook/pre-commit/implementation.ts was introduced during TASK-RFT-0037 to preserve protected framework commit behavior while satisfying the immediate facade split and line-count gate. | Open follow-up refactor cards to expand the implementation and support carriers into protected-state, direction-lock, ownership, identity, and residue modules while keeping tests/cli/pre-commit-hook-extraction.test.ts green. |
| ATM-BUG-2026-07-15-201 | 2026-07-15 | AI-Atomic-Framework | Optimization | High | Needs task card | Governance / Multi-editor session identity | Two agent sessions from Codex or another editor integration can operate through the same actor id and claim surface, making active-work warnings look like self-continuation instead of a distinct concurrent operator. | ATM should surface a distinct session, ticket, and editor identity for each operator instance and warn when another live session touches the same task or shared Git/index lane, even if actor id and task id match. | During TASK-RFT-0037, ATM surfaced active work and branch queue signals, but the shared codex-task-rft-0037 actor/claim made the other session indistinguishable until commit-status showed a separate in-flight owner PID and the user flagged the collision. During TASK-RFT-0038 closeout, Broker reported TASK-RFT-0038 and ATM-GOV-0146 as parallel-safe by file scope while pre-close still blocked on foreign staged governance and the branch commit queue owned by codex-gpt-5-5-captain, showing that cross-editor session identity must also cover shared index and queue resources. | Open a governance task to add editor-agnostic session-key or ticket-level identity to broker status, next active-work summaries, branch commit queue diagnostics, Git index lane diagnostics, and task claim warnings; include regressions where two sessions share actor/task and where distinct editor integrations share a branch/index but must be reported as distinct operators. |
