{
  "schemaId": "atm.governanceBacklogItem.v1",
  "id": "ATM-BUG-2026-07-16-017",
  "date": "2026-07-16",
  "repo": "AI-Atomic-Framework",
  "type": "Bug",
  "severity": "Medium",
  "status": "Closed — skill template inventory and projection parity verified 2026-09-05",
  "area": "Skill templates / Validation",
  "finding": "`npm run validate:skill-templates` fails in a clean worktree because `scripts/validate-skill-templates.ts` requires `atm-memory-consolidate`, but `templates/skills/atm-memory-consolidate.skill.md` is not present in the tracked template set.",
  "expectedBehavior": "The skill-template validator should either ship every required template it enforces or remove stale requiredTemplateIds so unrelated governance work can use the validator as a clean regression gate.",
  "evidenceOrRepro": "The historical failure is no longer reproducible on current main. `templates/skills/atm-memory-consolidate.skill.md` exists, `npm run validate:skill-templates` exits 0, and the validator reports 18 minimum entry templates, 25 sealed source templates, five adapter compilers, and installed-copy parity `{\"sync\":25}` with no untracked or ignored source template paths. The current validator therefore covers the previously missing template and its generated projections.",
  "followUp": "Keep `requiredTemplateIds`, the sealed source template set, and adapter projections covered by `npm run validate:skill-templates`; reopen only if a future clean-worktree run reports missing inventory or parity drift."
}
