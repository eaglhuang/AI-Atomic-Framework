{
  "schemaId": "atm.governanceBacklogItem.v1",
  "id": "ATM-BUG-2026-08-13-007",
  "date": "2026-08-13",
  "repo": "AI-Atomic-Framework",
  "type": "Bug",
  "severity": "High",
  "status": "Open",
  "area": "A scope amendment never reaches the governed delivery bundle",
  "finding": "tasks scope add extends the active direction lock's allowedFiles, but git commit --auto-stage resolves the delivery bundle from the ledger record's scopePaths. The two are never reconciled, so an amended path is editable, passes its validators, and is then silently excluded from the commit with no diagnostic. The only route to land it is tasks import --force, which is a protected emergency surface requiring a lease.",
  "expectedBehavior": "A path the governance layer just authorised for this task must be deliverable by that task. Either the bundle reads the direction lock, or scope add writes both; which one is an implementation choice, but the two views of the task's scope must agree. Silent exclusion is the worst available outcome because the amendment reported success.",
  "evidenceOrRepro": "On ATM-GOV-0360 at HEAD f8f7789a3, two paths (governance-optimization/plan-3x-4x-objective-audit-2026-07-31.json and docs/reports/plan-3x-4x-closeout-blocker-map.json) were added with tasks scope add --write, which returned ATM_SCOPE_AMENDMENT_APPLIED. Both were edited, both passed their validators, and git commit --auto-stage produced a commit containing the six original scopePaths and neither amended path. The ledger scopePaths array still had length 6 after both amendments.",
  "followUp": "Owned by ATM-GOV-0364 (ACC-1) with a focused test that amends a scope path and asserts it appears in the resolved delivery bundle. Do not resolve by documenting the two-step; the amendment already claims to have succeeded."
}
