import assert from'node:assert/strict';import{compileAcceptanceSpecMutation}from'../../packages/core/src/evidence/acceptance-spec-mutation.ts';const a={specId:'s',specDigest:'sha256:d',sealed:true};const r=compileAcceptanceSpecMutation({authority:a,mutationSetId:'m',sourceDigest:'sha256:d',mutations:[{mutationId:'x',operator:'alien',scenarioId:'s',expected:'killed'}]});assert.equal(r.status,'blocked');const s=compileAcceptanceSpecMutation({authority:a,mutationSetId:'m',sourceDigest:'sha256:x',mutations:[{mutationId:'x',operator:'negate',scenarioId:'s',expected:'killed'}]});assert.equal(s.status,'stale');console.log('plan4 acceptance spec mutation negative: ok');
