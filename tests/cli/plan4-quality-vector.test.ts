import assert from'node:assert/strict';import{compileQualityVector}from'../../packages/core/src/evidence/quality-vector.ts';const r=compileQualityVector({authorityDigest:'sha256:d',dimensions:{coverage:1,quality:0.5}});assert.equal(r.status,'proven');assert.ok(r.nonClaims.includes('vector-is-not-a-close-authority'));assert.equal(compileQualityVector({authorityDigest:'sha256:d',dimensions:{quality:2}}).status,'blocked');console.log('plan4 quality vector: ok');
