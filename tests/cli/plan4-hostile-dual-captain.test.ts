import assert from 'node:assert/strict'; import { compileHostileDogfood } from '../../packages/core/src/evidence/hostile-dogfood.ts';
const names = ['shared-index', 'cas-head-moved', 'queue-race', 'close-deferral', 'runner-sync']; const conditions = names.map((condition) => ({ condition, outcome: 'recovered' as const, rollbackPreserved: true, canonicalWorktreeIntact: true })); const green = compileHostileDogfood({ sealedReceiptDigest: 'sha256:receipt', conditions }); assert.equal(green.status, 'proven'); assert.equal(green.saturation.conditionCount, 5); assert.equal(green.rollbackPreserved, true); assert.equal(green.canonicalWorktreeIntact, true);
const unknown = compileHostileDogfood({ sealedReceiptDigest: 'sha256:receipt', conditions: [{ condition: 'unknown-race', outcome: 'unknown', rollbackPreserved: true, canonicalWorktreeIntact: true }] }); assert.equal(unknown.status, 'blocked'); assert.ok(unknown.diagnostics.includes('unknown-outcome:unknown-race'));
const override = compileHostileDogfood({ sealedReceiptDigest: 'sha256:receipt', conditions: [{ condition: 'shared-index', outcome: 'recovered', overrideLeaseUsed: true, rollbackPreserved: true, canonicalWorktreeIntact: true }] }); assert.equal(override.status, 'blocked'); assert.ok(override.diagnostics.includes('override-lease-forbidden:shared-index')); console.log('plan4 hostile dual captain: ok');
