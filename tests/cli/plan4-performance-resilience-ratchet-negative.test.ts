import assert from 'node:assert/strict';
import { compilePerformanceResilienceRatchet, validatePerformanceResilienceRatchet } from '../../packages/core/src/evidence/performance-resilience-ratchet.ts';
const authority = { authorityId: 'bench-1', digest: 'sha256:authority', sealed: true as const }, baseline = { benchmarkId: 'admission', latencyMs: 10, memoryBytes: 100, successRate: 0.99, resilienceRate: 0.99, digest: 'sha256:b' };
const stale = compilePerformanceResilienceRatchet({ ratchetId: 'ratchet-negative', generatedAt: '2026-08-09T00:00:00Z', authority, observedAuthorityDigest: 'sha256:stale', baselines: [baseline], observations: [{ ...baseline, latencyMs: 30, memoryBytes: 200, successRate: 0.5, resilienceRate: 0.5 }], limits: { maxLatencyRegressionMs: 2, maxMemoryRegressionBytes: 10, minSuccessRate: 0.95, minResilienceRate: 0.95 } });
assert.equal(stale.status, 'stale'); assert.match(stale.repairCommand ?? '', /rebuild the sealed benchmark baseline/); assert.ok(stale.diagnostics.includes('ratchet-regression')); assert.equal(validatePerformanceResilienceRatchet(stale).ok, false);
const invalid = compilePerformanceResilienceRatchet({ ratchetId: 'invalid', generatedAt: '2026-08-09T00:00:00Z', authority, observedAuthorityDigest: authority.digest, baselines: [baseline], observations: [baseline], metrics: ['made-up'], limits: { maxLatencyRegressionMs: -1, maxMemoryRegressionBytes: 10, minSuccessRate: 0.95, minResilienceRate: 0.95 } });
assert.equal(invalid.status, 'contradictory'); assert.deepEqual(invalid.diagnostics, ['invalid-limits', 'unsupported-metric:made-up']);
console.log('plan4 performance resilience ratchet negative: ok');
