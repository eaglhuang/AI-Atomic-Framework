import assert from 'node:assert/strict';import{compileComplexityInterfaceSize,replayComplexityInterfaceSize,validateComplexityInterfaceSize}from'../../packages/core/src/evidence/complexity-interface-size.ts';const a={authorityId:'complexity-1',digest:'sha256:a',sealed:true as const};const r=compileComplexityInterfaceSize({runId:'r',generatedAt:'2026-08-09',authority:a,observedAuthorityDigest:a.digest,maxCyclomatic:10,maxCrap:20,maxInterfaceSize:8,modules:[{moduleId:'m',cyclomatic:3,crap:4,interfaceSize:2,digest:'sha256:m'}]});assert.equal(r.status,'proven');assert.equal(validateComplexityInterfaceSize(r).ok,true);assert.deepEqual(replayComplexityInterfaceSize(r),r);console.log('plan4 complexity interface size: ok');
