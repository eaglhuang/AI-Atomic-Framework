import assert from'node:assert/strict';import{readFileSync}from'node:fs';import Ajv2020 from'ajv/dist/2020.js';import{compileAcceptanceSpecMutation,replayAcceptanceSpecMutation,validateAcceptanceSpecMutation}from'../../packages/core/src/evidence/acceptance-spec-mutation.ts';const a={specId:'s',specDigest:'sha256:d',sealed:true};const r=compileAcceptanceSpecMutation({authority:a,mutationSetId:'m',sourceDigest:'sha256:d',mutations:[{mutationId:'b',operator:'negate',scenarioId:'s',expected:'killed',observed:'killed'},{mutationId:'a',operator:'remove-step',scenarioId:'s',expected:'killed',observed:'killed'}],provenance:{producer:'focused-test'}});const schema=JSON.parse(readFileSync(new URL('../../schemas/evidence/acceptance-spec-mutation.schema.json',import.meta.url),'utf8'));const validateSchema=new Ajv2020({allErrors:true,strict:false}).compile(schema);assert.equal(r.status,'proven');assert.equal(validateSchema(r),true,JSON.stringify(validateSchema.errors));assert.deepEqual(replayAcceptanceSpecMutation(r),r);assert.deepEqual(validateAcceptanceSpecMutation(r),{ok:true,diagnostics:[]});assert.deepEqual(validateAcceptanceSpecMutation({...r,survivingMutationIds:['tampered']}),{ok:false,diagnostics:['result-digest-mismatch']});console.log('plan4 acceptance spec mutation: ok');
