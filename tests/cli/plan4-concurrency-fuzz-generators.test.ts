import assert from'node:assert/strict';import{readFileSync}from'node:fs';import Ajv2020 from'ajv/dist/2020.js';import{compileConcurrencyFuzzGenerators,replayConcurrencyFuzzGenerators,validateConcurrencyFuzzGenerators}from'../../packages/core/src/evidence/concurrency-fuzz-generators.ts';const a={authorityId:'a',digest:'sha256:s',sealed:true};const r=compileConcurrencyFuzzGenerators({authority:a,generatorId:'g',sourceDigest:'sha256:s',scenarios:[{scenarioId:'s2',schedule:['b','a'],fault:'reorder',expected:'recover'},{scenarioId:'s1',schedule:['a','b'],fault:'none',expected:'pass'}],provenance:{producer:'focused-test'}});const schema=JSON.parse(readFileSync(new URL('../../schemas/evidence/concurrency-fuzz-generators.schema.json',import.meta.url),'utf8'));const validateSchema=new Ajv2020({allErrors:true,strict:false}).compile(schema);assert.equal(r.status,'proven');assert.equal(validateSchema(r),true,JSON.stringify(validateSchema.errors));assert.equal(r.seeds.length,2);assert.deepEqual(replayConcurrencyFuzzGenerators(r),r);assert.deepEqual(validateConcurrencyFuzzGenerators(r),{ok:true,diagnostics:[]});assert.deepEqual(validateConcurrencyFuzzGenerators({...r,seeds:['seed:tampered']}),{ok:false,diagnostics:['result-digest-mismatch']});console.log('plan4 concurrency fuzz generators: ok');
