import assert from'node:assert/strict';import{compileExampleBranchGenerators}from'../../packages/core/src/evidence/example-branch-generators.ts';const a={authorityId:'a',digest:'sha256:s',sealed:true};const r=compileExampleBranchGenerators({authority:a,generatorId:'g',sourceDigest:'sha256:s',branches:[{branchId:'b',predicate:'x',outcome:'unsupported'}]});assert.equal(r.status,'blocked');assert.match(r.repairCommand??'',/restore/);const s=compileExampleBranchGenerators({authority:a,generatorId:'g',sourceDigest:'sha256:x',branches:[{branchId:'b',predicate:'x',outcome:'covered'}]});assert.equal(s.status,'stale');console.log('plan4 example branch generators negative: ok');
