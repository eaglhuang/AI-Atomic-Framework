import assert from'node:assert/strict';import{compileComplexityInterfaceSize,validateComplexityInterfaceSize}from'../../packages/core/src/evidence/complexity-interface-size.ts';const a={authorityId:'complexity-1',digest:'sha256:a',sealed:true as const};const r=compileComplexityInterfaceSize({runId:'r',generatedAt:'2026-08-09',authority:a,observedAuthorityDigest:'sha256:stale',maxCyclomatic:1,maxCrap:1,maxInterfaceSize:1,modules:[{moduleId:'m',cyclomatic:3,crap:4,interfaceSize:2,digest:'sha256:m'}]});assert.equal(r.status,'stale');assert.equal(validateComplexityInterfaceSize(r).ok,false);console.log('plan4 complexity interface size negative: ok');
