import assert from 'node:assert/strict';import{compileQualityAuthority,validateQualityAuthority}from'../../packages/core/src/evidence/quality-authority.ts';const r=compileQualityAuthority({authorityId:'qa-1',policyEpoch:'epoch-1',expectedPolicyEpoch:'epoch-1',policyDigest:'sha256:p',oracleDigest:'sha256:o',denominatorDigest:'sha256:d',verdictDigest:'sha256:v',protectedSurfaces:['exam-a','exam-b'],observedProtectedSurfaces:['exam-a','exam-b'],roleCapabilities:{writer:['evidence'],oracle:['oracle'],policy:['policy']},writerRole:'writer'});assert.equal(r.status,'proven');assert.equal(validateQualityAuthority(r).ok,true);assert.equal(r.authority.sealed,true);console.log('plan4 quality authority: ok');
