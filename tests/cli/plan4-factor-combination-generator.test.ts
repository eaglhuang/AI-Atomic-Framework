import assert from 'node:assert/strict'; import { generateFactorCombinations } from '../../packages/core/src/evidence/causal-neighborhood.ts'; const result = generateFactorCombinations({ runId: 'fixture', authority: { authorityId: 'sealed', sealed: true, digest: 'sha256:x' }, fingerprint: 'fault-x', publicSeam: 'seam', impactEdges: [], validatorRefs: [], changedFiles: [], factors: ['z', 'a', 'm'], maxCombinations: 1 }); assert.deepEqual(result.combinations, [['a', 'm']]); console.log('plan4 factor combinations: PASS');
