import assert from'node:assert/strict';import{compileAntiGamingControl,replayAntiGamingControl,validateAntiGamingControl}from'../../packages/core/src/evidence/anti-gaming-control.ts';const a={authorityId:'a',digest:'sha256:d',sealed:true};const r=compileAntiGamingControl({authority:a,seedCommitment:'sha256:seed',sourceDigest:'sha256:d',controls:[{controlId:'c',seed:'s',expected:'red',observed:'red'}]});assert.equal(r.status,'proven');assert.deepEqual(replayAntiGamingControl(r),r);assert.deepEqual(validateAntiGamingControl(r),{ok:true,diagnostics:[]});console.log('plan4 anti-gaming: ok');
