import assert from'node:assert/strict';import{compileCoverageCertificate}from'../../packages/core/src/evidence/coverage-certificate.ts';const a={authorityId:'a',digest:'sha256:d',sealed:true};const r=compileCoverageCertificate({authority:a,certificateId:'c',sourceDigest:'sha256:d',obligations:[{obligationId:'o',covered:true,sourceDigest:'sha256:d'}]});assert.equal(r.status,'proven');assert.equal(r.covered,1);assert.ok(r.nonClaims.length>0);console.log('plan4 coverage certificate: ok');
