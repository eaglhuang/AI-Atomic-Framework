---
policy_version: 0.1
framework_version_range: ">=2.0.0 <3.0.0"
---

# Upstream Versioning Policy (wrong-range fixture)

> This file is a TEST FIXTURE only — framework_version_range is intentionally set
> to ">=2.0.0 <3.0.0" which does NOT match the active 0.x framework train.
> Used by tests/policy-version/policy-self-version.test.ts (TC-6).

## 11. Policy Self-Versioning

This section exists so the §11 section check passes; the range rejection happens first.
