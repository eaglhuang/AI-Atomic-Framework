# Patrol Report

Mode: close-preflight

Findings:

- No stale active run.
- No broker-conflict-blocked violation.
- No worker git.write authority.

Suggested command:

```shell
node atm.mjs team status --compact --json
```
