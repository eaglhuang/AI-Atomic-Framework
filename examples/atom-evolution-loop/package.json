{
  "name": "@ai-atomic-framework/example-atom-evolution-loop",
  "version": "0.1.0",
  "private": false,
  "description": "End-to-end ATM atom evolution loop demo: evidence → proposal → review → decision → registry.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "test": "node --strip-types test/run.ts",
    "typecheck": "node --strip-types test/run.ts --typecheck",
    "lint": "node --strip-types test/run.ts --lint"
  }
}
