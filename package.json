{
  "name": "ai-atomic-framework",
  "version": "0.0.0",
  "private": true,
  "description": "Governance framework for AI-assisted atomic engineering work.",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "packages:list": "node scripts/list-packages.mjs",
    "test": "node scripts/validate-product-charter.mjs --mode test && node scripts/validate-package-skeleton.mjs --mode test",
    "typecheck": "node scripts/validate-product-charter.mjs --mode typecheck && node scripts/validate-package-skeleton.mjs --mode typecheck",
    "lint": "node scripts/validate-product-charter.mjs --mode lint && node scripts/validate-package-skeleton.mjs --mode lint"
  }
}