{
  "description": "Reject police finding that attempts to use payload as current contract field. Payload is reserved for future additive proposal only.",
  "finding": {
    "findingId": "police-family.invalid.payload-misuse",
    "policeFamily": "dedup",
    "severity": "advisory",
    "message": "Dedup candidate detected with inline payload",
    "payload": {
      "operation": "merge",
      "targetAtomId": "ATM-CORE-0001",
      "sourceAtomId": "ATM-CORE-0002"
    }
  },
  "expected": {
    "payloadMustBeRejected": true,
    "reason": "payload is reserved for future additive proposal; current contract uses evidenceRefs and readModel only"
  }
}
