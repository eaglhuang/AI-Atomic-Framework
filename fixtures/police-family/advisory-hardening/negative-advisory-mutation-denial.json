{
  "description": "Advisory-only hardening: advisory scanner attempting registry mutation must be rejected.",
  "input": {
    "probes": [
      {
        "probeId": "probe.advisory.dedup.mutation-attempt",
        "scannerSourceValidator": "runDedupPolice",
        "attemptedAction": "registry-mutation",
        "attemptedBy": "advisory-family",
        "description": "Hypothetical scanner attempting to write dedup-merge result directly into registry."
      }
    ]
  },
  "expected": {
    "ok": true,
    "rejected": true,
    "attemptedAction": "registry-mutation",
    "reasonContains": "cannot directly mutate registry"
  }
}
