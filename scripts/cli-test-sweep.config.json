{
  "schemaId": "atm.cliTestSweep.v1",
  "testDir": "tests/cli",
  "timeoutMs": 180000,
  "concurrency": 4,
  "quarantine": [
    {
      "test": "atm-2-1-final-closure.test.ts",
      "reason": "failing-on-main",
      "detail": "Failed on main at 0b986834d (Windows, Node 24.12): ION_PASSED=1` only after standard passes.\\n',\r expected: /420-cell paired A\\/B v4 met rollout metrics and safety controller \\| pass/,\r operator: 'match',\r diff: 'simple'\r } Also rewrites docs/reports/atm-2-1-final-closure.md when it runs, so it cannot join the sweep until it generates into a temporary root.",
      "disposition": "triage"
    },
    {
      "test": "backlog-deferred-waiver-register.test.ts",
      "reason": "stale-assertion",
      "detail": "Asserts a fixed count (133) over the live docs/reports/plan-3x-4x-backlog-deferred-waiver-register.json, which now holds 50. The register changes as waivers are granted and retired.",
      "disposition": "fix"
    },
    {
      "test": "backlog-release-blocking-subset.test.ts",
      "reason": "stale-assertion",
      "detail": "Asserts a fixed count (391) over the live docs/reports/plan-3x-4x-backlog-release-blocking-subset.json, which now holds 477. The subset changes as backlog items are opened and closed.",
      "disposition": "fix"
    },
    {
      "test": "broker-replay-command-surface.test.ts",
      "reason": "stale-assertion",
      "detail": "Snapshots live repository state: it asserts broker replay status stays remain-open \"while closure evidence is incomplete\". The evidence is now complete, so the command correctly reports ready-to-close and exits 0. The test needs a fixture instead of the live repository.",
      "disposition": "fix"
    },
    {
      "test": "captain-parallel-ledger-analysis.test.ts",
      "reason": "stale-assertion",
      "detail": "Runs scripts/analyze-captain-parallel-ledger.ts over the live .atm ledger, so its counts follow current work.",
      "disposition": "fix"
    },
    {
      "test": "cli-published-command-smoke.test.ts",
      "reason": "environment-dependent",
      "detail": "Passes standalone on Windows and Linux, including twice in a row, but fails inside the sweep on Linux with \"candidate install did not expose atm bin\". Running it serially did not help, so the interaction with earlier sweep batches is not yet identified. The bin mode defect it exposed (a rebuild dropped the executable bit) is fixed separately.",
      "disposition": "fix"
    },
    {
      "test": "cross-editor-parallel-proof.test.ts",
      "reason": "environment-dependent",
      "detail": "Needs full git history: acc5 depends on whether a recorded SHA is reachable, and the depth-1 CI checkout cannot answer that, so the status is unmet. Passes in a full clone. Enabling it would require fetch-depth: 0 in Product CI.",
      "disposition": "fix"
    },
    {
      "test": "four-plan-closeout-blocker-map.test.ts",
      "reason": "stale-assertion",
      "detail": "Asserts a closeout verdict over the live docs/reports/plan-3x-4x-closeout-blocker-map.json: expects complete-closeout-certified, reads actionable-not-complete. The report tracks work in progress, so the verdict moves; the test needs a fixture report.",
      "disposition": "fix"
    },
    {
      "test": "four-plan-independent-certificate.test.ts",
      "reason": "environment-dependent",
      "detail": "Needs full git history: it asserts that origin/main descends from the SHA the certificate is bound to, which a depth-1 CI checkout cannot establish. Enabling it would require fetch-depth: 0 in Product CI.",
      "disposition": "fix"
    },
    {
      "test": "four-plan-objective-authority-review.test.ts",
      "reason": "stale-assertion",
      "detail": "Reviews the live docs/reports/reviews/plan-3x-4x-objective-authority-review.json, which is regenerated as objectives are certified.",
      "disposition": "fix"
    },
    {
      "test": "git-record-commit.test.ts",
      "reason": "stale-assertion",
      "detail": "Expects .atm/history/evidence/git-head.jsonl, but the append-only log was replaced by git-head.json (the .jsonl file was deleted in 0feeb741c). The command behaves as designed; the assertion names the retired filename.",
      "disposition": "fix"
    },
    {
      "test": "historical-residue-cleanup.test.ts",
      "reason": "failing-on-main",
      "detail": "Failed on main at 0b986834d (Windows, Node 24.12): Loader (node:internal/modules/run_main:101:5) {\r generatedMessage: false,\r code: 'ERR_ASSERTION',\r actual: false,\r expected: true,\r operator: '==',\r diff: 'simple'\r } Also rewrites docs/reports/historical-governance-residue-cleanup.md when it runs, so it cannot join the sweep until it generates into a temporary root.",
      "disposition": "triage"
    },
    {
      "test": "next-prompt-results-contract-extraction.test.ts",
      "reason": "failing-on-main",
      "detail": "Asserts the CLI owner shard stays at or below 600 physical lines; the file is 1256 lines, so the budget was already exceeded more than twofold before this session added ten mappings (~70 lines) for the extracted team-legacy modules. Splitting the shard is the fix, not relaxing the budget.",
      "disposition": "fix"
    },
    {
      "test": "next-route-selector.test.ts",
      "reason": "failing-on-main",
      "detail": "Runs scripts/validate-prompt-scoped-next.ts over many routing scenarios. Two real routing defects it exposed are fixed (journaling vs repair intent, 17a27270f). It now stops at the batch scenario: asking for an exact task id inside an active batch reports task-queue-head-claim-blocked instead of ATM_NEXT_TASK_QUEUE_READY, because the prompt carries no actor and the queue head already holds the batch’s own active claim, so diagnoseClaimReadinessForTasks treats it as a blocker. Whether an actor-less query should warn instead of block is an owner decision; do not relax the gate just to make the test pass.",
      "disposition": "fix"
    },
    {
      "test": "plan32-dashboard-consumption.test.ts",
      "reason": "stale-assertion",
      "detail": "Runs scripts/validate-plan32-dashboard-consumption.ts against live governance data and asserts 9 rows; the dashboard now reports 29. The row count follows current work.",
      "disposition": "fix"
    },
    {
      "test": "plan32-terminal-verdict-fail-closed.test.ts",
      "reason": "stale-assertion",
      "detail": "Runs scripts/validate-plan32-terminal-verdict-fail-closed.ts against live data and expects a terminal verdict that the current state no longer produces (verdict not-complete, failClosed false).",
      "disposition": "fix"
    },
    {
      "test": "plan4-successor-wave-consumption.test.ts",
      "reason": "stale-assertion",
      "detail": "Runs scripts/validate-plan4-successor-wave-consumption.ts against live reports; it fails on a source digest mismatch for docs/reports/plan-3x-4x-backlog-disposition-census.json, which is regenerated as the census changes.",
      "disposition": "fix"
    },
    {
      "test": "plan4-successor-wave-objective-map.test.ts",
      "reason": "stale-assertion",
      "detail": "Asserts over the live docs/reports/plan4-successor-wave-objective-map.json, which is regenerated as the wave progresses.",
      "disposition": "fix"
    },
    {
      "test": "public-npm-install-contract.test.ts",
      "reason": "failing-on-main",
      "detail": "Failed on main at 0b986834d (Windows, Node 24.12): cli/public-npm-install-contract.test.ts:98:10 {\r generatedMessage: false,\r code: 'ERR_ASSERTION',\r actual: false,\r expected: true,\r operator: '==',\r diff: 'simple'\r }",
      "disposition": "triage"
    },
    {
      "test": "real-paired-ab-analyzer-v3.test.ts",
      "reason": "stale-assertion",
      "detail": "Analyzes live paired A/B artifacts and expects the verdict improved; the current data yields inconclusive. The analyzer is correct; the expectation is a snapshot.",
      "disposition": "fix"
    },
    {
      "test": "root-drop-release-incremental-overlay.test.ts",
      "reason": "writes-tracked-files",
      "detail": "Passes, but on Linux leaves packages/cli/dist/atm.mjs modified; on Windows it also failed when run next to other tests.",
      "disposition": "fix"
    },
    {
      "test": "root-drop-release-manifest-integrity.test.ts",
      "reason": "writes-tracked-files",
      "detail": "Passes, but on Linux leaves packages/cli/dist/atm.mjs modified.",
      "disposition": "fix"
    },
    {
      "test": "runbook-completion-evidence.test.ts",
      "reason": "stale-assertion",
      "detail": "Compiles runbook completion evidence from live docs/reports/plan-3x-4x-runbook-completion-evidence.json and the live evidence ledger, so the verdict follows current state.",
      "disposition": "fix"
    },
    {
      "test": "runbook-release-authority-review.test.ts",
      "reason": "stale-assertion",
      "detail": "Reviews release authority from the live docs/reports/plan-3x-4x-runbook-completion-evidence.json, so its verdict follows current state.",
      "disposition": "fix"
    },
    {
      "test": "task-dependency-semantics-census.test.ts",
      "reason": "environment-dependent",
      "detail": "Reads the planning repository through resolvePlanningRoot(), which defaults to a sibling 3KLife checkout (override: ATM_PLANNING_REPO_ROOT). A CI clone has no such sibling, so the census denominator is 0. It needs a fixture planning root instead of the live one.",
      "disposition": "fix"
    },
    {
      "test": "task-view-dashboard.test.ts",
      "reason": "environment-dependent",
      "detail": "Asserts on live repository state for the historical TASK-MAO-0043: it expects a complete close, but the dashboard reports a partial close because the planning-mirror field cannot be confirmed without the planning repository checked out beside this one. Reporting partial when the mirror is unverifiable is fail-closed and correct; the test needs a fixture instead of the live ledger.",
      "disposition": "fix"
    },
    {
      "test": "taskflow-close-orchestration.test.ts",
      "reason": "failing-on-main",
      "detail": "Failed on main at 0b986834d (Windows, Node 24.12): n.test.ts:109:10 {\r generatedMessage: true,\r code: 'ERR_ASSERTION',\r actual: 'tasks-reconcile',\r expected: 'tasks-close',\r operator: 'strictEqual',\r diff: 'simple'\r }",
      "disposition": "triage"
    },
    {
      "test": "taskflow-host-policy.test.ts",
      "reason": "environment-dependent",
      "detail": "Same root cause as taskflow-open-orchestration: host policy allocates the next task id by scanning the profile repository’s docs/tasks, not the --cwd fixture, so the expected TASK-GOVERNED-0002 depends on whether a 0001 card is left in this repository. Whether a profile may redirect reads and writes outside --cwd is an owner decision.",
      "disposition": "triage"
    },
    {
      "test": "taskflow-open-orchestration.test.ts",
      "reason": "writes-tracked-files",
      "detail": "Root-caused 2026-09-20: taskflow open resolves its output root from the profile path (resolveTaskflowOpenOutputRoot returns the profile repo git root), not from --cwd. The test passes a profile that lives in this repository, so the generated card lands in the framework repo docs/tasks instead of the requested cwd, and the run dirties the worktree. Whether a profile may redirect writes outside --cwd is an owner decision; do not change the resolver to make the test pass.",
      "disposition": "triage"
    }
  ],
  "serial": []
}
