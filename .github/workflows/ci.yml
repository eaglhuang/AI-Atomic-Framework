name: ci

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  test-and-validate:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: ATM Doctor
        run: node atm.mjs doctor --json

      - name: ATM Task Audit
        run: node atm.mjs tasks audit --json

      - name: ATM Framework Development Gate
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            changed_files="$(git diff --name-only "${{ github.event.pull_request.base.sha }}"...HEAD)"
          elif [ "${{ github.event.before }}" != "0000000000000000000000000000000000000000" ]; then
            changed_files="$(git diff --name-only "${{ github.event.before }}"...HEAD)"
          else
            changed_files="$(git diff --name-only HEAD 2>/dev/null || true)"
          fi
          if [ -n "$changed_files" ]; then
            changed_csv="$(printf '%s\n' "$changed_files" | paste -sd, -)"
            node atm.mjs guard framework-development --files "$changed_csv" --json
          else
            node atm.mjs guard framework-development --json
          fi

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm test

      - name: Validate Standard
        run: npm run validate:standard
