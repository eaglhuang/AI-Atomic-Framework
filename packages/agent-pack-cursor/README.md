# @ai-atomic-framework/agent-pack-cursor

Cursor agent pack for ATM entry skills.

The pack installs the shared ATM entry skill set under `.cursor/rules/skills/`, using the same source templates as the Cursor integration adapter. Generated files route agents through `node atm.mjs next --json` or another deterministic ATM command.