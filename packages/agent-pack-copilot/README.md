# @ai-atomic-framework/agent-pack-copilot

GitHub Copilot agent pack for ATM instructions and prompts.

The pack installs the shared ATM instruction and prompt set under `.github/`, using the same source templates as the Copilot integration adapter. Generated files route agents through `node atm.mjs next --json` or another deterministic ATM command.