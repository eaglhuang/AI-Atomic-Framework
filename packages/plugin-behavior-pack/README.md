# @ai-atomic-framework/plugin-behavior-pack

Consolidated reference behavior pack for ATM action ids such as `behavior.split`, `behavior.merge`, and `behavior.evolve`. The action ids remain stable; the implementation surface is one package instead of many thin packages.
