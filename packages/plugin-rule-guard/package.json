{
  "name": "@ai-atomic-framework/plugin-rule-guard",
  "version": "0.1.0",
  "private": false,
  "description": "Deterministic rule guards for protected framework surfaces.",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "node --strip-types ../../scripts/build-package-dist.ts --package packages/plugin-rule-guard",
    "test": "node --strip-types ../../scripts/validate-package-skeleton.ts --mode test",
    "typecheck": "tsc -p ../../tsconfig.json --noEmit",
    "lint": "eslint ."
  },
  "types": "./dist/index.d.ts",
  "dependencies": {
    "@ai-atomic-framework/plugin-sdk": "0.1.0"
  }
}
