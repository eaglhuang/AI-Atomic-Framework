{
  "description": "Protected upstream fixture must not contain adopter-specific or private paths in finding evidence refs.",
  "finding": {
    "findingId": "police-family.boundary.adopter-leak",
    "policeFamily": "boundary",
    "severity": "blocker",
    "message": "Test finding with adopter-local path leaked into upstream fixture",
    "evidenceRefs": [
      "adopter-local/private-registry/atoms.json",
      "adopter-local/.host-config/governance.json"
    ]
  },
  "expected": {
    "privatePathMustBeRejected": true,
    "bannedPatterns": ["adopter-local", "private-registry", ".host-config"],
    "reason": "Upstream fixtures must not reference adopter-specific or host-local private paths"
  }
}
