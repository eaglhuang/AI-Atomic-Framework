# @ai-atomic-framework/agent-pack-sdk

SDK types and helpers for ATM agent packs.

The package defines the shared `AgentPack`, `TargetFile`, `RenderContext`, and `RenderedManifest` contracts used by built-in packs and CLI install flows.