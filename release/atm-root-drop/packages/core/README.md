# @ai-atomic-framework/core

Core defines portable governance contracts for atomic work items, scope locks, artifacts, evidence, and context summaries.

This package must stay independent from default plugins and host adapters.