# @ai-atomic-framework/agent-pack-gemini

Gemini agent pack for ATM command files.

The pack installs the shared ATM command set under `.gemini/commands/`, using the same source templates as the Gemini integration adapter. Generated files route agents through `node atm.mjs next --json` or another deterministic ATM command.