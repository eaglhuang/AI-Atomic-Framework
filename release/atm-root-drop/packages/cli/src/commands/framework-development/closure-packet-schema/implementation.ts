import { createHash } from 'node:crypto'; import { spawnSync } from 'node:child_process'; import { appendFileSync, existsSync, mkdirSync, mkdtempSync, readFileSync, readdirSync, rmSync, statSync, unlinkSync, writeFileSync } from 'node:fs'; import os from 'node:os'; import path from 'node:path'; import type { WorkItemRef } from '@ai-atomic-framework/core'; import { runnerAffectingPatterns, type RunnerBuildScopeManifest } from '../../../../../core/src/broker/atm-core-scope.ts'; import { createLocalGovernanceAdapter } from '../../../../../plugin-governance-local/src/index.ts'; import { CliError, makeResult, message, readFrameworkVersion, relativePathFrom, resolveValue } from '../../shared.ts'; import { createGitHeadEvidenceCheck, gitHeadEvidencePath, gitHeadEvidencePaths } from '../../git-head-evidence.ts'; import { inspectGitWorktreeReadiness, type GitWorktreeReadinessReport } from '../../git-worktree-readiness.ts'; import { bootstrapTaskId, detectGovernanceRuntime } from '../../governance-runtime.ts'; import { listActorWorkSessions } from '../../actor-session.ts'; import { readActiveTaskDirectionLocks } from '../../task-direction.ts'; import { externalTaskKey, readTaskLedgerPolicy, resolveTaskLedgerMode, transitionEventExists, type TaskLedgerMode, type TaskLedgerPolicy } from '../../task-ledger.ts'; import { isClaimExpired, parseClaimRecord } from '../../tasks/task-ledger-readers.ts'; import { isAtmCriticalNonDocSurface, isDocOnlyPath } from '../path-classification.ts'; import { describeBuildReleaseHygienePolicy } from '../../build-release-hygiene.ts'; import { pushSha256ValidationIssue } from '../closure-packet/diagnostics.ts'; import type { ClosurePacket, ClosurePacketCommandRun, ClosurePacketReconcileAttestation, ClosurePacketRepairMetadata, ClosurePacketRequiredGatesSnapshot, ClosurePacketTargetCommitDelta, ClosurePacketTeamSummary, HistoricalDeliveryProvenance } from '../closure-packet/schema-fragments.ts'; import type { ClosurePacketValidationIssue } from '../closure-packet/validator-contract.ts'; export type FrameworkMode = 'inactive' | 'suspected' | 'required' | 'cross-repo-target-required'; export type ClosureAuthority = 'local' | 'target_repo' | 'none'; export type FrameworkStaleLockKind = 'stale-completed' | 'stale-ttl-expired' | 'possibly-stale' | 'still-active'; export interface FrameworkStaleLockInfo { readonly kind: FrameworkStaleLockKind; readonly lockTaskId: string; readonly lockPath: string; readonly actorId: string; readonly lockedAt: string | null; readonly linkedTaskId: string | null; readonly currentTaskId: string | null; readonly requiredCommand: string; readonly releaseCommand: string | null; readonly detail: string; } export interface FrameworkLockAutoReconcileEvidence { readonly schemaId: 'atm.frameworkLockAutoReconcile.v1'; readonly actorId: string; readonly staleLock: FrameworkStaleLockInfo; readonly releasedAt: string; readonly releaseResult: unknown; readonly reclaimTaskId: string; readonly scopedFiles: readonly string[]; readonly reason: string | null; readonly outcome: 'reclaimed' | 'released-but-reclaim-failed'; readonly claimFailure?: { readonly code: string; readonly message: string; } | null; readonly auditPath: string; } export interface FrameworkRepoIdentity { readonly isFrameworkRepo: boolean; readonly score: number; readonly root: string; readonly name: string | null; readonly signals: readonly string[]; } export interface FrameworkModeStatusReport { readonly schemaId: 'atm.frameworkDevelopmentStatus'; readonly specVersion: '0.1.0'; readonly generatedAt: string; readonly repoRole: 'framework' | 'host'; readonly repoIdentity: FrameworkRepoIdentity; readonly targetRepo: string | null; readonly targetRepoIdentity: FrameworkRepoIdentity | null; readonly mode: FrameworkMode; readonly closureAuthority: ClosureAuthority; readonly taskLedgerMode: TaskLedgerMode; readonly taskLedger: TaskLedgerPolicy; readonly changedFiles: readonly string[]; readonly criticalChangedFiles: readonly string[]; readonly docsOnlyChangedFiles: readonly string[]; readonly requiredGates: readonly string[]; readonly activeLocks: readonly string[]; readonly staleLocks: readonly FrameworkStaleLockInfo[]; readonly pinnedRunner: PinnedRunnerStatus; readonly gitWorktreeReadiness: GitWorktreeReadinessReport; readonly blockers: readonly string[]; readonly warnings: readonly string[]; } export interface PinnedRunnerStatus { readonly status: 'available' | 'missing' | 'source-unavailable'; readonly metadataPath: string; readonly sourcePath: string | null; readonly runnerPath: string | null; readonly reason: string | null; } export type { ClosurePacket, ClosurePacketCommandRun, ClosurePacketReconcileAttestation, ClosurePacketRepairMetadata, ClosurePacketRequiredGatesSnapshot, ClosurePacketTargetCommitDelta, ClosurePacketTeamSummary, HistoricalDeliveryProvenance } from '../closure-packet/schema-fragments.ts'; export interface FrameworkCloseWorktreeReport { readonly ok: boolean; readonly trackedDirtyFiles: readonly string[]; readonly unstagedFiles: readonly string[]; readonly stagedFiles: readonly string[]; readonly untrackedFiles: readonly string[]; readonly ignoredUntrackedFiles: readonly string[]; } export type AutoGeneratedResidueVerdict = 'auto-clean-safe' | 'block-and-explain' | 'manual-review'; export interface AutoGeneratedResidueFinding { readonly path: string; readonly verdict: AutoGeneratedResidueVerdict; readonly reason: string; readonly ownerTaskId: string | null; readonly cleanupAction: 'remove' | 'restore' | null; } export interface AutoGeneratedResidueReport { readonly autoCleanSafe: readonly AutoGeneratedResidueFinding[]; readonly blockAndExplain: readonly AutoGeneratedResidueFinding[]; readonly manualReview: readonly AutoGeneratedResidueFinding[]; } interface GovernanceHistoryArtifactInfo { readonly ownerTaskId: string; readonly artifactKind: 'bundle-manifest' | 'closure-packet' | 'close-task-event'; } export function isTaskCloseGovernanceCriticalPath(filePath: string, taskId: string): boolean { const relativePath = normalizeRelativePath(filePath); const normalizedTaskId = taskId.trim(); if (!relativePath || !normalizedTaskId) { return false; } return relativePath === gitHeadEvidencePath || relativePath === `.atm/history/tasks/${normalizedTaskId}.json` || relativePath === `.atm/history/evidence/${normalizedTaskId}.json` || relativePath === `.atm/history/evidence/${normalizedTaskId}.closure-packet.json` || relativePath.startsWith(`.atm/history/task-events/${normalizedTaskId}/`) || relativePath === `.atm/runtime/locks/${normalizedTaskId}.lock.json`; } export function inspectAutoGeneratedResidue(taskId: string | null, candidatePaths: readonly string[]): AutoGeneratedResidueReport { const normalizedTaskId = taskId?.trim().toUpperCase() ?? null; const findings = uniqueSorted(candidatePaths.map(normalizeRelativePath).filter(Boolean)) .map((filePath) => classifyAutoGeneratedResidue(normalizedTaskId, filePath)) .filter((entry): entry is AutoGeneratedResidueFinding => entry !== null); return { autoCleanSafe: findings.filter((entry) => entry.verdict === 'auto-clean-safe'), blockAndExplain: findings.filter((entry) => entry.verdict === 'block-and-explain'), manualReview: findings.filter((entry) => entry.verdict === 'manual-review') }; } function classifyAutoGeneratedResidue(taskId: string | null, filePath: string): AutoGeneratedResidueFinding | null { const normalized = normalizeRelativePath(filePath); const lower = normalized.toLowerCase(); const normalizedTaskId = taskId?.trim().toUpperCase() ?? null; if (lower.startsWith('.atm/history/reports/task-import/') && lower.endsWith('.json')) { return { path: normalized, verdict: 'auto-clean-safe', reason: 'task-import report is an ATM-generated transient report and may be removed automatically after the import/claim lane completes.', ownerTaskId: normalizedTaskId, cleanupAction: 'remove' }; } if (lower.startsWith('.atm/history/protected-override-audit/') && lower.endsWith('.json')) { return null; } const governanceArtifact = classifyGovernanceHistoryArtifact(normalized); if (governanceArtifact) { if (normalizedTaskId && governanceArtifact.ownerTaskId === normalizedTaskId) { return null; } return { path: normalized, verdict: 'block-and-explain', reason: normalizedTaskId ? `${governanceArtifact.artifactKind} belongs to another task (${governanceArtifact.ownerTaskId}) and must be reconciled explicitly instead of silently skipped.` : `${governanceArtifact.artifactKind} is an ATM-managed governance artifact (${governanceArtifact.ownerTaskId}) but no active task ownership is available to prove it is safe residue.`, ownerTaskId: governanceArtifact.ownerTaskId, cleanupAction: null }; } if (lower === gitHeadEvidencePaths.legacyJson || lower === gitHeadEvidencePaths.jsonl) { return { path: normalized, verdict: 'auto-clean-safe', reason: 'hook-generated git-head evidence residue is reproducible and may be restored automatically.', ownerTaskId: taskId, cleanupAction: 'restore' }; } const snapshotMatch = lower.match(/^\.atm\/runtime\/snapshots\/(?:(close-window-)?foreign-staged)-([a-z0-9-]+)-\d+\.json$/); if (snapshotMatch) { const ownerTaskId = snapshotMatch[2]?.toUpperCase() ?? null; return { path: normalized, verdict: 'auto-clean-safe', reason: ownerTaskId === 'GIT-HEAD' || (normalizedTaskId && ownerTaskId === normalizedTaskId) ? 'current-operation foreign-staged snapshot is an ATM byproduct and may be removed automatically after the lane completes.' : `foreign-staged snapshot belongs to another task/run (${ownerTaskId}) but is a disposable ATM byproduct and may be removed automatically before commit.`, ownerTaskId, cleanupAction: 'remove' }; } const governanceDirtyMatch = lower.match(/^\.atm\/runtime\/snapshots\/close-window-governance-dirty-[^.]+-\.atm__history__evidence__([a-z0-9-]+)(?:\.[^.]+)?\.json\.json$/); if (governanceDirtyMatch) { const ownerTaskId = governanceDirtyMatch[1]?.toUpperCase() ?? null; return { path: normalized, verdict: 'auto-clean-safe', reason: ownerTaskId === 'GIT-HEAD' || (normalizedTaskId && ownerTaskId === normalizedTaskId) ? 'close-window governance-dirty snapshot belongs to the current operation and may be removed automatically after the lane completes.' : `close-window governance-dirty snapshot belongs to another task/run (${ownerTaskId}) but is a disposable ATM byproduct and may be removed automatically before commit.`, ownerTaskId, cleanupAction: 'remove' }; } if (lower.match(/^\.atm\/runtime\/snapshots\/close-window-governance-dirty-[^.]+-\.atm__history__evidence__git-head\.jsonl\.json$/)) { return { path: normalized, verdict: 'auto-clean-safe', reason: 'close-window governance-dirty snapshot for git-head evidence is hook-generated and may be removed automatically after the lane completes.', ownerTaskId: 'GIT-HEAD', cleanupAction: 'remove' }; } const lockCleanupReportMatch = lower.match(/^\.atm\/history\/reports\/lock-cleanup\/[^/]+-([a-z0-9-]+)\.json$/); if (lockCleanupReportMatch) { const ownerTaskId = lockCleanupReportMatch[1]?.toUpperCase() ?? null; return { path: normalized, verdict: 'auto-clean-safe', reason: ownerTaskId === normalizedTaskId ? 'lock-cleanup report belongs to the current task checkpoint and may be removed automatically after commit.' : `lock-cleanup report belongs to another task/run (${ownerTaskId}) but is a disposable ATM byproduct and may be removed automatically before commit.`, ownerTaskId, cleanupAction: 'remove' }; } if (lower.startsWith('.atm/runtime/snapshots/') && lower.endsWith('.json')) { return { path: normalized, verdict: 'manual-review', reason: 'snapshot looks generated but ATM cannot prove it belongs to the current operation.', ownerTaskId: null, cleanupAction: null }; } return { path: normalized, verdict: 'manual-review', reason: 'path is not recognized as a known ATM auto-generated residue shape.', ownerTaskId: null, cleanupAction: null }; } function classifyGovernanceHistoryArtifact(filePath: string): GovernanceHistoryArtifactInfo | null { const normalized = normalizeRelativePath(filePath); const bundleManifestMatch = normalized.match(/^\.atm\/history\/evidence\/([^/.]+)\.bundle-manifest\.json$/i); if (bundleManifestMatch) { return { ownerTaskId: bundleManifestMatch[1].toUpperCase(), artifactKind: 'bundle-manifest' }; } const closurePacketMatch = normalized.match(/^\.atm\/history\/evidence\/([^/.]+)\.closure-packet\.json$/i); if (closurePacketMatch) { return { ownerTaskId: closurePacketMatch[1].toUpperCase(), artifactKind: 'closure-packet' }; } const taskEventMatch = normalized.match(/^\.atm\/history\/task-events\/([^/]+)\/.+(?:close|reconcile|repair-closure).+\.json$/i); if (taskEventMatch) { return { ownerTaskId: taskEventMatch[1].toUpperCase(), artifactKind: 'close-task-event' }; } return null; } export type ClosureRepairUpstreamStatus = 'detached-head' | 'no-upstream' | 'ahead-of-upstream' | 'published-head-blocked'; export type { ClosurePacketValidationIssue, ClosurePacketValidationResult } from '../closure-packet/validator-contract.ts'; export interface ClosurePacketRepairResult { readonly taskId: string; readonly packetPath: string; readonly taskPath: string | null; readonly previousHead: string | null; readonly repairedHead: string | null; readonly targetCommit: string | null; readonly governedTreeSha: string | null; readonly changedFiles: readonly string[]; readonly gitHeadEvidencePath: string; readonly amended: boolean; readonly dryRun: boolean; readonly changed: boolean; readonly upstreamStatus: ClosureRepairUpstreamStatus | null; readonly nextActionCommand: string | null; readonly commitMessage: string | null; readonly remediation: string | null; readonly scopeWarnings?: readonly string[]; readonly upstreamEvidenceNormalized?: boolean; } const SHA256_DIGEST_PATTERN = /^sha256:([a-fA-F0-9]{64})$/; export function normalizeSha256DigestValue(value: string): string { const trimmed = value.trim(); const match = SHA256_DIGEST_PATTERN.exec(trimmed); if (!match) return trimmed; return `sha256:${match[1].toLowerCase()}`; } export function normalizeSha256FieldsDeep<T>(value: T): T { if (Array.isArray(value)) { return value.map((entry) => normalizeSha256FieldsDeep(entry)) as T; } if (value && typeof value === 'object') { const next: Record<string, unknown> = {}; for (const [key, entry] of Object.entries(value as Record<string, unknown>)) { next[key] = normalizeSha256FieldsDeep(entry); } return next as T; } if (typeof value === 'string' && SHA256_DIGEST_PATTERN.test(value.trim())) { return normalizeSha256DigestValue(value) as T; } return value; } export interface TaskAuditFinding { readonly level: 'error' | 'warning'; readonly code: string; readonly path: string; readonly taskId?: string; readonly detail: string; readonly acknowledged?: boolean; } export interface TaskAuditReport { readonly schemaId: 'atm.taskAuditReport'; readonly specVersion: '0.1.0'; readonly generatedAt: string; readonly repoIdentity: FrameworkRepoIdentity; readonly inspectedTaskCount: number; readonly inspectedEvidenceCount: number; readonly findings: readonly TaskAuditFinding[]; readonly acknowledgedFindingCount?: number; readonly activeFindingCount?: number; readonly ok: boolean; } interface TaskAuditWarningBaseline { readonly acknowledgedFindings: ReadonlySet<string>; } interface AuditedTaskDocument { readonly relativePath: string; readonly taskId: string; readonly status: string; readonly document: Record<string, unknown>; readonly format: 'json' | 'markdown'; readonly parseError: string | null; } interface FrameworkModeOptions { readonly cwd: string; readonly files?: readonly string[]; readonly targetRepo?: string | null; } export interface InferredFrameworkTargetRepo { readonly taskId: string; readonly taskPath: string; readonly field: string; readonly rawTargetRepo: string; readonly targetRepo: string; readonly targetRepoIdentity: FrameworkRepoIdentity; } interface ParsedFrameworkModeArgs { readonly cwd: string; readonly action: 'status' | 'claim' | 'release'; readonly files: readonly string[]; readonly targetRepo: string | null; readonly actor: string | null; readonly reason: string | null; } const frameworkModeSpecVersion = '0.1.0'; const defaultRequiredGates = [ 'framework-development', 'tasks-audit', 'doctor', 'git-head-evidence', 'typecheck', 'validate:cli', 'validate:git-head-evidence' ] as const; const defaultIgnoredDirs = new Set([ '.git', 'node_modules', 'dist', 'build', 'release', '.atm-temp', '.cache', 'local', 'scratch', 'tmp', 'temp' ]); const markdownCompletionPatterns = [ /^\s*status:\s*\*\*(?:all\s+)?completed\*\*\s*$/im, /^\s*ALL\s+COMPLETED\s*$/m, /^\s*(?:progress|status|completion):\s*16\s*\/\s*16\s*$/im, /^\s*(?:progress|status|completion):\s*100%\s*\(?\s*completed\s*\)?\s*$/im ]; const markdownCompletionReportNamePatterns = [ /(?:^|[-_.])closeout[-_.]?report\.md$/i, /(?:^|[-_.])completion[-_.]?report\.md$/i ]; export function runFrameworkMode(argv: string[]) { const options = parseFrameworkModeArgs(argv); if (options.action === 'claim') { return runFrameworkTempClaim(options.cwd, options.actor, options.files, options.reason); } if (options.action === 'release') { return runFrameworkTempRelease(options.cwd, options.actor); } const report = createFrameworkModeStatus({ cwd: options.cwd, files: options.files, targetRepo: options.targetRepo }); return makeResult({ ok: true, command: 'framework-mode', cwd: options.cwd, messages: [ message('info', 'ATM_FRAMEWORK_MODE_STATUS', `Framework development mode is ${report.mode}.`, { mode: report.mode, repoRole: report.repoRole, criticalChangedFileCount: report.criticalChangedFiles.length, closureAuthority: report.closureAuthority }) ], evidence: { action: options.action, report } }); } export async function runFrameworkTempClaim(cwd: string, actor: string | null, files: readonly string[], reason: string | null, linkedTaskId?: string | null) { const actorId = normalizeOptionalString(actor ?? process.env.ATM_ACTOR_ID ?? process.env.AGENT_IDENTITY); if (!actorId) { throw new CliError('ATM_ACTOR_ID_MISSING', 'framework-mode claim requires --actor or ATM_ACTOR_ID (legacy alias: AGENT_IDENTITY).', { exitCode: 2 }); } const scopedFiles = uniqueSorted(files.map(normalizeRelativePath).filter(Boolean)); if (scopedFiles.length === 0) { throw new CliError('ATM_CLI_USAGE', 'framework-mode claim requires --files <csv> for the intended framework edit scope.', { exitCode: 2 }); } const root = path.resolve(cwd); assertFrameworkGitWorktreeReady(root); const currentTaskId = resolveCurrentFrameworkTaskId(root, actorId, linkedTaskId); const staleLock = classifyFrameworkStaleLock(root, actorId, { currentTaskId }); if (staleLock) { if (canAutoReconcileCompletedSelfTempLock(staleLock, actorId)) { return await runFrameworkTempClaimWithAutoReconcile({ root, actorId, scopedFiles, reason, currentTaskId, staleLock }); } const details = isFrameworkStaleLockReleasable(staleLock) ? { ...staleLock, requiredCommand: buildFrameworkStaleCleanupCommand(staleLock, scopedFiles, reason) } : staleLock; if (staleLock.kind === 'still-active') { throw new CliError('ATM_FRAMEWORK_LOCK_OCCUPIED', staleLock.detail, { exitCode: 1, details: details as unknown as Record<string, unknown> }); } throw new CliError('ATM_FRAMEWORK_STALE_LOCK_CLEANUP_REQUIRED', staleLock.detail, { exitCode: 1, details: details as unknown as Record<string, unknown> }); } const taskId = frameworkTempTaskId(actorId); const adapter = createLocalGovernanceAdapter({ repositoryRoot: root }); const task: WorkItemRef = { workItemId: taskId, title: reason?.trim() || 'Temporary ATM framework-development claim', status: 'running' }; const lock = await resolveValue(adapter.stores.lockStore.acquireLock(task, scopedFiles, actorId)); const resolvedLinkedTaskId = currentTaskId; if (resolvedLinkedTaskId) { try { const lockPath = path.join(root, '.atm', 'runtime', 'locks', `${taskId}.lock.json`); const existing = readJsonIfExists(lockPath); if (existing) { writeFileSync(lockPath, `${JSON.stringify({ ...existing, linkedTaskId: resolvedLinkedTaskId }, null, 2)}\n`, 'utf8'); } } catch { } } const stagedFiles = runGitLines(root, ['diff', '--cached', '--name-only']).map((f) => normalizeRelativePath(f)).filter(Boolean); const stagedResidues = stagedFiles.filter(f => !scopedFiles.includes(f)); const claimMessages = [ message('info', 'ATM_FRAMEWORK_TEMP_CLAIM_ACQUIRED', 'Temporary framework-development runtime lock acquired.', { taskId, actorId, files: scopedFiles, ...(resolvedLinkedTaskId ? { linkedTaskId: resolvedLinkedTaskId } : {}) }) ]; if (stagedResidues.length > 0) { claimMessages.push(message('warn', 'ATM_FRAMEWORK_STAGED_RESIDUE_DETECTED', `Warning: Staged residue detected outside claimed scope: ${stagedResidues.join(', ')}. Please adopt, stash, or widen the scope.`, { stagedResidues })); } return makeResult({ ok: true, command: 'framework-mode', cwd: root, messages: claimMessages, evidence: { action: 'claim', taskId, actorId, reason: reason ?? null, linkedTaskId: resolvedLinkedTaskId, files: scopedFiles, lock } }); } async function runFrameworkTempClaimWithAutoReconcile(input: { readonly root: string; readonly actorId: string; readonly scopedFiles: readonly string[]; readonly reason: string | null; readonly currentTaskId: string | null; readonly staleLock: FrameworkStaleLockInfo; }) { const adapter = createLocalGovernanceAdapter({ repositoryRoot: input.root }); const releaseResult = await resolveValue(adapter.stores.lockStore.releaseLock(input.staleLock.lockTaskId, input.actorId)); const taskId = frameworkTempTaskId(input.actorId); const task: WorkItemRef = { workItemId: taskId, title: input.reason?.trim() || 'Temporary ATM framework-development claim', status: 'running' }; let lock: unknown; let autoReconcileEvidence: FrameworkLockAutoReconcileEvidence | null = null; try { lock = await resolveValue(adapter.stores.lockStore.acquireLock(task, input.scopedFiles, input.actorId)); autoReconcileEvidence = writeFrameworkLockAutoReconcileEvidence(input.root, { actorId: input.actorId, staleLock: input.staleLock, releasedAt: new Date().toISOString(), releaseResult, reclaimTaskId: taskId, scopedFiles: input.scopedFiles, reason: input.reason ?? null, outcome: 'reclaimed', claimFailure: null }); } catch (error) { const code = error instanceof CliError ? error.code : 'ATM_FRAMEWORK_TEMP_CLAIM_REACQUIRE_FAILED'; const messageText = error instanceof Error ? error.message : String(error); autoReconcileEvidence = writeFrameworkLockAutoReconcileEvidence(input.root, { actorId: input.actorId, staleLock: input.staleLock, releasedAt: new Date().toISOString(), releaseResult, reclaimTaskId: taskId, scopedFiles: input.scopedFiles, reason: input.reason ?? null, outcome: 'released-but-reclaim-failed', claimFailure: { code, message: messageText } }); throw error; } const resolvedLinkedTaskId = input.currentTaskId; if (resolvedLinkedTaskId) { try { const lockPath = path.join(input.root, '.atm', 'runtime', 'locks', `${taskId}.lock.json`); const existing = readJsonIfExists(lockPath); if (existing) { writeFileSync(lockPath, `${JSON.stringify({ ...existing, linkedTaskId: resolvedLinkedTaskId }, null, 2)}\n`, 'utf8'); } } catch { } } const stagedFiles = runGitLines(input.root, ['diff', '--cached', '--name-only']).map((f) => normalizeRelativePath(f)).filter(Boolean); const stagedResidues = stagedFiles.filter(f => !input.scopedFiles.includes(f)); const claimMessages = [ message('info', 'ATM_FRAMEWORK_STALE_SELF_LOCK_AUTO_RELEASED', 'ATM automatically released the same-actor stale-completed framework temp lock before reclaiming scope.', { actorId: input.actorId, staleLockTaskId: input.staleLock.lockTaskId, linkedTaskId: input.staleLock.linkedTaskId, auditPath: autoReconcileEvidence.auditPath }), message('info', 'ATM_FRAMEWORK_TEMP_CLAIM_ACQUIRED', 'Temporary framework-development runtime lock acquired.', { taskId, actorId: input.actorId, files: input.scopedFiles, ...(resolvedLinkedTaskId ? { linkedTaskId: resolvedLinkedTaskId } : {}) }) ]; if (stagedResidues.length > 0) { claimMessages.push(message('warn', 'ATM_FRAMEWORK_STAGED_RESIDUE_DETECTED', `Warning: Staged residue detected outside claimed scope: ${stagedResidues.join(', ')}. Please adopt, stash, or widen the scope.`, { stagedResidues })); } return makeResult({ ok: true, command: 'framework-mode', cwd: input.root, messages: claimMessages, evidence: { action: 'claim', taskId, actorId: input.actorId, reason: input.reason ?? null, linkedTaskId: resolvedLinkedTaskId, files: input.scopedFiles, autoReconcile: autoReconcileEvidence, lock } }); } export async function runFrameworkTempRelease(cwd: string, actor: string | null) { const actorId = normalizeOptionalString(actor ?? process.env.ATM_ACTOR_ID ?? process.env.AGENT_IDENTITY); if (!actorId) { throw new CliError('ATM_ACTOR_ID_MISSING', 'framework-mode release requires --actor or ATM_ACTOR_ID (legacy alias: AGENT_IDENTITY).', { exitCode: 2 }); } const root = path.resolve(cwd); const adapter = createLocalGovernanceAdapter({ repositoryRoot: root }); const taskId = frameworkTempTaskId(actorId); const result = await resolveValue(adapter.stores.lockStore.releaseLock(taskId, actorId)); const stagedFiles = runGitLines(root, ['diff', '--cached', '--name-only']).map((f) => normalizeRelativePath(f)).filter(Boolean); const releaseMessages = [ message('info', 'ATM_FRAMEWORK_TEMP_CLAIM_RELEASED', 'Temporary framework-development runtime lock released.', { taskId, actorId }) ]; if (stagedFiles.length > 0) { releaseMessages.push(message('warn', 'ATM_FRAMEWORK_STAGED_RESIDUE_AT_RELEASE', `Warning: Staged files remain in index at session end: ${stagedFiles.join(', ')}. Please commit, stash, or reset them.`, { stagedFiles })); } return makeResult({ ok: true, command: 'framework-mode', cwd: root, messages: releaseMessages, evidence: { action: 'release', taskId, actorId, result } }); } export function buildFrameworkTempClaimCommand(files: readonly string[] = [], reason: string | null = null, actorId: string | null = null) { const normalizedFiles = uniqueSorted(files.map(normalizeRelativePath).filter(Boolean)); const filesValue = normalizedFiles.length > 0 ? normalizedFiles.join(',') : '<files>'; const reasonValue = reason?.trim() || 'temporary framework maintenance'; const actorValue = actorId?.trim() || '<id>'; return `node atm.mjs framework-mode claim --actor ${actorValue === '<id>' ? actorValue : quoteCliValue(actorValue)} --files ${quoteCliValue(filesValue)} --reason ${quoteCliValue(reasonValue)} --json`; } export function classifyFrameworkStaleLock(cwd: string, actorId: string, options: { readonly currentTaskId?: string | null; } = {}): FrameworkStaleLockInfo | null { const root = path.resolve(cwd); const lockId = frameworkTempTaskId(actorId); const lockPath = path.join(root, '.atm', 'runtime', 'locks', `${lockId}.lock.json`); const document = readJsonIfExists(lockPath); if (!document) return null; if (document.released === true) return null; const lockedAt = normalizeOptionalString(document.lockedAt ?? null); const explicitLinkedTaskId = normalizeOptionalString(document.linkedTaskId ?? null); const currentTaskId = resolveCurrentFrameworkTaskId(root, actorId, options.currentTaskId ?? null); const releaseCommand = buildFrameworkStaleReleaseCommand(actorId); const lockActorId = normalizeOptionalString(document.actorId ?? document.lockedBy); const lockWorkItemId = normalizeOptionalString(document.workItemId ?? null); if (!explicitLinkedTaskId && !currentTaskId && lockActorId === actorId && lockWorkItemId === lockId && isRuntimeLockActive(lockPath)) { return null; } const linkedTaskId = explicitLinkedTaskId ?? inferLinkedTaskIdFromActorSessions(root, actorId, lockedAt); const base = { lockTaskId: lockId, lockPath: relativePathFrom(root, lockPath), actorId, lockedAt, linkedTaskId, currentTaskId }; if (linkedTaskId && currentTaskId && linkedTaskId === currentTaskId) { return null; } if (linkedTaskId) { const linkedStatus = readTaskStatusFromLedger(root, linkedTaskId); if (linkedStatus !== null && isTerminalTaskStatus(linkedStatus)) { return { kind: 'stale-completed', ...base, requiredCommand: releaseCommand, releaseCommand, detail: `Framework-mode lock for actor ${actorId} was not released after task ${linkedTaskId} reached status "${linkedStatus}". Release the stale lock, then claim fresh for ${currentTaskId ?? 'the current task'}.` }; } if (linkedStatus !== null) { return { kind: 'still-active', ...base, requiredCommand: `node atm.mjs tasks handoff --task ${linkedTaskId} --actor ${quoteCliValue(actorId)} --to <new-actor> --reason "transferring framework lock" --json`, releaseCommand: null, detail: `Framework-mode lock for actor ${actorId} is linked to non-terminal task ${linkedTaskId} (${linkedStatus}). Use handoff or takeover before releasing.` }; } } if (!isRuntimeLockActive(lockPath)) { return { kind: 'stale-ttl-expired', ...base, requiredCommand: releaseCommand, releaseCommand, detail: `Framework-mode lock for actor ${actorId} exceeded its TTL without heartbeat renewal. Release the stale lock, then claim fresh.` }; } if (lockActorId === actorId && lockWorkItemId === lockId && !linkedTaskId && !currentTaskId) { return null; } return { kind: 'possibly-stale', ...base, requiredCommand: `node atm.mjs framework-mode status --json`, releaseCommand: null, detail: `Framework-mode lock for actor ${actorId} is active but ATM cannot verify the original task context. Review the prior session before releasing.` }; } export function detectFrameworkStaleLocks(cwd: string): readonly FrameworkStaleLockInfo[] { const root = path.resolve(cwd); const lockDir = path.join(root, '.atm', 'runtime', 'locks'); if (!existsSync(lockDir)) return []; return readdirSync(lockDir) .filter((name) => name.startsWith('ATM-FRAMEWORK-TEMP-') && name.endsWith('.lock.json')) .flatMap((name) => { const lockPath = path.join(lockDir, name); const document = readJsonIfExists(lockPath); const actor = normalizeOptionalString(document?.actorId ?? document?.lockedBy) ?? name.replace(/^ATM-FRAMEWORK-TEMP-/, '').replace(/\.lock\.json$/, ''); const info = classifyFrameworkStaleLock(root, actor); return info ? [info] : []; }); } export function buildFrameworkStaleCleanupCommand(staleLock: FrameworkStaleLockInfo, files: readonly string[] = [], reason: string | null = null) { if (!isFrameworkStaleLockReleasable(staleLock) || !staleLock.releaseCommand) return staleLock.requiredCommand; return `${staleLock.releaseCommand} && ${buildFrameworkTempClaimCommand(files, reason, staleLock.actorId)}`; } function canAutoReconcileCompletedSelfTempLock(staleLock: FrameworkStaleLockInfo, actorId: string) { return staleLock.kind === 'stale-completed' && staleLock.actorId === actorId && staleLock.lockTaskId === frameworkTempTaskId(actorId) && staleLock.linkedTaskId !== null; } export function isFrameworkStaleLockReleasable(staleLock: FrameworkStaleLockInfo) { return staleLock.kind === 'stale-completed' || staleLock.kind === 'stale-ttl-expired'; } function writeFrameworkLockAutoReconcileEvidence(root: string, input: Omit<FrameworkLockAutoReconcileEvidence, 'schemaId' | 'auditPath'>): FrameworkLockAutoReconcileEvidence { const auditPath = path.join(root, '.atm', 'runtime', 'framework-lock-auto-reconcile.jsonl'); const record: FrameworkLockAutoReconcileEvidence = { schemaId: 'atm.frameworkLockAutoReconcile.v1', ...input, auditPath: relativePathFrom(root, auditPath) }; mkdirSync(path.dirname(auditPath), { recursive: true }); appendFileSync(auditPath, `${JSON.stringify(record)}\n`, 'utf8'); return record; } export function runFrameworkDevelopmentGuard(cwd: string, files: readonly string[] = [], targetRepo: string | null = null) { const report = createFrameworkModeStatus({ cwd, files, targetRepo }); const ok = report.blockers.length === 0; return makeResult({ ok, command: 'guard', cwd, messages: [ ok ? message('info', 'ATM_GUARD_FRAMEWORK_DEVELOPMENT_OK', 'Framework development guard passed.', { mode: report.mode, criticalChangedFileCount: report.criticalChangedFiles.length }) : message('error', 'ATM_GUARD_FRAMEWORK_DEVELOPMENT_FAILED', 'Framework development guard found blocking issues.', { mode: report.mode, blockers: report.blockers }) ], evidence: { guard: 'framework-development', report, ...(report.staleLocks.length > 0 ? { staleLocks: report.staleLocks } : {}) } }); } export function runFrameworkDevelopmentValidation(cwd: string, files: readonly string[] = [], targetRepo: string | null = null) { const report = createFrameworkModeStatus({ cwd, files, targetRepo }); const taskAudit = auditTasks(cwd); const ok = report.blockers.length === 0 && taskAudit.ok; return makeResult({ ok, command: 'validate', cwd, messages: [ ok ? message('info', 'ATM_VALIDATE_FRAMEWORK_DEVELOPMENT_OK', 'Framework development validation passed.', { mode: report.mode, criticalChangedFileCount: report.criticalChangedFiles.length }) : message('error', 'ATM_VALIDATE_FRAMEWORK_DEVELOPMENT_FAILED', 'Framework development validation failed.', { mode: report.mode, blockers: report.blockers, taskAuditFindings: taskAudit.findings.length }) ], evidence: { validation: 'framework-development', report, taskAudit } }); } export function createFrameworkModeStatus(input: FrameworkModeOptions): FrameworkModeStatusReport { const cwd = path.resolve(input.cwd); const generatedAt = new Date().toISOString(); const repoIdentity = detectFrameworkRepoIdentity(cwd); const gitWorktreeReadiness = inspectGitWorktreeReadiness(cwd); const declaredFiles = (input.files ?? []).map((entry) => normalizeRelativePath(entry)).filter(Boolean); const changedFiles = declaredFiles.length > 0 ? uniqueSorted(declaredFiles) : readChangedFiles(cwd); const adopterInfrastructureSyncOnly = !repoIdentity.isFrameworkRepo && isAdopterInfrastructureSyncCommit(changedFiles); const inferredTarget = input.targetRepo || adopterInfrastructureSyncOnly ? null : inferFrameworkTargetRepoFromTasks(cwd); const targetRepo = input.targetRepo ? resolveTargetRepoReference(cwd, input.targetRepo) : inferredTarget?.targetRepo ?? null; const targetRepoIdentity = targetRepo ? detectFrameworkRepoIdentity(targetRepo) : null; const taskLedger = readTaskLedgerPolicy(cwd); const criticalChangedFiles = repoIdentity.isFrameworkRepo ? changedFiles.filter(isAtmCriticalNonDocSurface) : []; const docsOnlyChangedFiles = repoIdentity.isFrameworkRepo ? changedFiles.filter((entry) => !isAtmCriticalNonDocSurface(entry)) : []; const activeLocks = readActiveLockPaths(cwd); const staleLocks = classifyCurrentFrameworkStaleLocks(cwd); const pinnedRunner = inspectPinnedRunner(cwd); const requiredGates = buildRequiredGates(criticalChangedFiles); let mode: FrameworkMode = 'inactive'; let closureAuthority: ClosureAuthority = 'none'; const blockers: string[] = []; const warnings: string[] = []; if (adopterInfrastructureSyncOnly) { mode = 'inactive'; closureAuthority = 'none'; warnings.push('adopter-infrastructure-sync'); } else if (targetRepoIdentity?.isFrameworkRepo && !sameRepo(cwd, targetRepo)) { mode = 'cross-repo-target-required'; closureAuthority = 'target_repo'; blockers.push('closure-authority-belongs-to-target-repo'); } else if (repoIdentity.isFrameworkRepo && criticalChangedFiles.length > 0) { mode = 'required'; closureAuthority = 'local'; } else if (repoIdentity.isFrameworkRepo && changedFiles.length > 0) { mode = 'suspected'; closureAuthority = 'local'; } if ((mode === 'required' || mode === 'cross-repo-target-required') && pinnedRunner.status !== 'available') { blockers.push('pinned-runner-missing'); } if (!gitWorktreeReadiness.ok) { blockers.push('git-worktree-readiness-failed'); } const blockingStaleLocks = staleLocks.filter((entry) => entry.kind !== 'possibly-stale' || entry.currentTaskId !== null); if (mode === 'required' && blockingStaleLocks.length > 0) { blockers.push('framework-stale-lock-cleanup-required'); } if (mode === 'required' && blockingStaleLocks.length === 0 && !hasActiveFrameworkTaskLock(activeLocks)) { blockers.push('active-framework-claim-required'); } if (mode === 'required') { const gitHead = createGitHeadEvidenceCheck(cwd, detectGovernanceRuntime(cwd, bootstrapTaskId)); if (!gitHead.ok) { warnings.push('git-head-evidence-missing'); } } const taskLedgerMode = resolveTaskLedgerMode({ policy: taskLedger, frameworkMode: mode, repoRole: repoIdentity.isFrameworkRepo ? 'framework' : 'host', closureAuthority }); return { schemaId: 'atm.frameworkDevelopmentStatus', specVersion: frameworkModeSpecVersion, generatedAt, repoRole: repoIdentity.isFrameworkRepo ? 'framework' : 'host', repoIdentity, targetRepo, targetRepoIdentity, mode, closureAuthority, taskLedgerMode, taskLedger, changedFiles, criticalChangedFiles, docsOnlyChangedFiles, requiredGates, activeLocks, staleLocks, pinnedRunner, gitWorktreeReadiness, blockers, warnings }; } function assertFrameworkGitWorktreeReady(cwd: string) { const readiness = inspectGitWorktreeReadiness(cwd); if (readiness.ok) { return; } throw new CliError('ATM_GIT_WORKTREE_READY_REQUIRED', 'Git local config marks this checked-out repository as bare, so framework-mode cannot safely continue until the local worktree setting is repaired.', { exitCode: 1, details: readiness as unknown as Record<string, unknown> }); } export function detectFrameworkRepoIdentity(repositoryRoot: string): FrameworkRepoIdentity { const root = path.resolve(repositoryRoot); const packageJson = readJsonIfExists(path.join(root, 'package.json')); const signals: string[] = []; const packageName = typeof packageJson?.name === 'string' ? packageJson.name : null; if (packageName === 'ai-atomic-framework') { signals.push('package-name:ai-atomic-framework'); } if (existsSync(path.join(root, 'packages', 'core', 'src', 'index.ts'))) { signals.push('packages/core/src/index.ts'); } if (existsSync(path.join(root, 'packages', 'cli', 'src', 'atm.ts'))) { signals.push('packages/cli/src/atm.ts'); } if (existsSync(path.join(root, 'atomic-registry.json'))) { signals.push('atomic-registry.json'); } const workspaces = Array.isArray(packageJson?.workspaces) ? packageJson.workspaces.map((entry: unknown) => String(entry)) : []; if (workspaces.includes('packages/*')) { signals.push('workspace:packages/*'); } return { isFrameworkRepo: signals.length >= 2, score: signals.length, root, name: packageName, signals }; } export function inferFrameworkTargetRepoFromTasks(cwd: string): InferredFrameworkTargetRepo | null { const root = path.resolve(cwd); const currentIdentity = detectFrameworkRepoIdentity(root); if (currentIdentity.isFrameworkRepo) return null; for (const task of readTaskDocuments(root)) { if (!isOpenTaskStatus(task.status)) continue; const targetField = firstTargetRepoField(task.document); if (!targetField) continue; const targetRepo = resolveTargetRepoReference(root, targetField.value); const targetRepoIdentity = detectFrameworkRepoIdentity(targetRepo); if (!targetRepoIdentity.isFrameworkRepo || sameRepo(root, targetRepo)) continue; return { taskId: task.taskId, taskPath: task.relativePath, field: targetField.field, rawTargetRepo: targetField.value, targetRepo, targetRepoIdentity }; } return null; } export function isAdopterInfrastructureSyncCommit(files: readonly string[]): boolean { if (files.length === 0) return false; return files.every((entry) => isAdopterInfrastructureSyncPath(entry)); } export function isAdopterInfrastructureSyncPath(value: string): boolean { const normalized = normalizeRelativePath(value); return normalized === 'atm.mjs' || normalized === '.atm/runtime/pinned-runner.json' || normalized === '.atm/integrations/copilot.manifest.json' || normalized === '.atm/integrations/codex.manifest.json' || normalized === '.atm/integrations/claude-code.manifest.json' || normalized === '.github/hooks/atm-framework-development.json' || normalized.startsWith('.github/instructions/atm-') || normalized.startsWith('.github/prompts/atm-') || normalized.startsWith('integrations/codex-skills/atm-') || normalized.startsWith('.claude/skills/atm-') || normalized.startsWith('.cursor/rules/skills/atm-') || normalized.startsWith('.gemini/commands/atm-'); } export function auditTasks(cwd: string): TaskAuditReport { const root = path.resolve(cwd); const generatedAt = new Date().toISOString(); const repoIdentity = detectFrameworkRepoIdentity(root); const taskLedger = readTaskLedgerPolicy(root); const baseline = readTaskAuditWarningBaseline(root); const findings: TaskAuditFinding[] = []; const taskDocs = readTaskDocuments(root); const evidenceDocs = readEvidenceDocuments(root); const mirrorKeys = new Set<string>(); for (const task of taskDocs) { if (task.parseError) { findings.push({ level: 'error', code: 'ATM_TASK_AUDIT_TASK_JSON_MALFORMED', path: task.relativePath, taskId: task.taskId, detail: `Task ${task.taskId} ledger JSON is malformed and could not be parsed: ${task.parseError}` }); continue; } const planningOnlyTask = isPlanningOnlyAuthorityTask(root, task.document); const manualTaskActor = firstAiIssuedManualTaskActor(task.document); if (manualTaskActor && task.taskId !== bootstrapTaskId) { findings.push({ level: 'error', code: 'ATM_TASK_AUDIT_AI_MANUAL_TASK_IN_LEDGER', path: task.relativePath, taskId: task.taskId, detail: `AI-issued manual task ${task.taskId} by ${manualTaskActor} must not live in .atm/history/tasks; use an ephemeral runtime lock or a human-authored/imported task card.` }); } const originProvider = normalizeOptionalString(task.document.originProvider ?? task.document.origin_provider); const originTaskId = normalizeOptionalString(task.document.originTaskId ?? task.document.origin_task_id); const syncStatus = normalizeOptionalString(task.document.syncStatus ?? task.document.sync_status); if (originProvider && originTaskId) { mirrorKeys.add(externalTaskKey(originProvider, originTaskId)); if (!syncStatus) { findings.push({ level: 'error', code: 'ATM_TASK_AUDIT_MIRROR_SYNC_STATUS_MISSING', path: task.relativePath, taskId: task.taskId, detail: `Mirror task ${task.taskId} must declare syncStatus.` }); } } else if (originProvider || originTaskId) { findings.push({ level: 'error', code: 'ATM_TASK_AUDIT_MIRROR_ORIGIN_INCOMPLETE', path: task.relativePath, taskId: task.taskId, detail: `Mirror task ${task.taskId} must declare both originProvider and originTaskId.` }); } const lastTransitionId = normalizeOptionalString(task.document.lastTransitionId ?? task.document.last_transition_id); const hasTransitionEvent = Boolean(lastTransitionId && transitionEventExists(root, task.taskId, lastTransitionId)); const targetRepo = normalizeOptionalString(task.document.target_repo ?? task.document.targetRepo ?? task.document.upstream_repo ?? task.document.upstreamRepo); const isExternalTask = Boolean(targetRepo && !matchesCurrentRepoIdentity(root, targetRepo)); const transitionRequired = taskLedger.enabled && taskLedger.provider === 'atm-local' && taskLedger.requireCliTransitions && !planningOnlyTask && !isExternalTask && (task.status === 'done' || Boolean(originProvider || originTaskId)); if (transitionRequired && !lastTransitionId) { findings.push({ level: 'error', code: 'ATM_TASK_AUDIT_TRANSITION_EVIDENCE_MISSING', path: task.relativePath, taskId: task.taskId, detail: `Task ${task.taskId} is missing lastTransitionId; status transitions must use ATM CLI.` }); } else if (lastTransitionId && !hasTransitionEvent) { findings.push({ level: 'error', code: 'ATM_TASK_AUDIT_TRANSITION_EVENT_MISSING', path: task.relativePath, taskId: task.taskId, detail: `Task ${task.taskId} references missing transition event ${lastTransitionId}.` }); } const claimStatus = normalizeOptionalString(task.status)?.toLowerCase().replace(/-/g, '_') ?? ''; if (claimStatus === 'running' || claimStatus === 'review') { const claim = parseClaimRecord(task.document.claim); const staleClaimDetail = !claim ? 'no parseable claim record exists' : claim.state !== 'active' ? `claim state is "${claim.state}"` : isClaimExpired(claim, generatedAt) ? `claim lease ${claim.leaseId} for actor ${claim.actorId} expired (ttl ${claim.ttlSeconds}s, last heartbeat ${claim.heartbeatAt})` : null; if (staleClaimDetail) { findings.push({ level: 'warning', code: 'ATM_TASK_AUDIT_STALE_CLAIM', path: task.relativePath, taskId: task.taskId, detail: `Task ${task.taskId} is ${claimStatus} but ${staleClaimDetail}; the backlog entry is stalled. Run "node atm.mjs tasks repair-claim --task ${task.taskId} --actor <id> --json" to diagnose and clear it.` }); } } if (task.status !== 'done') continue; const closureAuthority = normalizeClosureAuthority(task.document.closure_authority ?? task.document.closureAuthority); const closurePacketRef = normalizeOptionalString(task.document.closure_packet ?? task.document.closurePacket); const hasCliClosure = Boolean(task.document.closedByActor || task.document.closedByCommand === 'atm tasks close'); const hasLegacyBaseline = normalizeOptionalString(task.document.ledgerBaselineKind ?? task.document.ledger_baseline_kind) === 'legacy-transition-backfill' && hasTransitionEvent; if (planningOnlyTask) { findings.push({ level: 'warning', code: 'ATM_TASK_AUDIT_PLANNING_ONLY_DONE', path: task.relativePath, taskId: task.taskId, detail: `[planning-only] Task ${task.taskId} is marked done under planning authority and is not enforced as target-repo closure evidence in this repository. Run "node atm.mjs tasks import" if you need to synchronize it as target-repo work.` }); continue; } if (closureAuthority === 'target_repo' && targetRepo && !matchesCurrentRepoIdentity(root, targetRepo)) { if (!closurePacketRef) { findings.push({ level: 'warning', code: 'ATM_TASK_AUDIT_CROSS_REPO_DONE_WITHOUT_PACKET', path: task.relativePath, taskId: task.taskId, detail: `[external-planning] Task ${task.taskId} is done in a non-target repo without a target closure packet. Run "node atm.mjs tasks import" if you need to synchronize it as target-repo work.` }); } continue; } if (!hasCliClosure && !closurePacketRef && !hasLegacyBaseline) { findings.push({ level: 'error', code: 'ATM_TASK_AUDIT_MANUAL_DONE', path: task.relativePath, taskId: task.taskId, detail: `[target-authority] Task ${task.taskId} is marked done without ATM CLI closure metadata.` }); } else if (hasLegacyBaseline && !hasCliClosure && !closurePacketRef) { findings.push({ level: 'warning', code: 'ATM_TASK_AUDIT_LEGACY_BASELINE_DONE', path: task.relativePath, taskId: task.taskId, detail: `[target-authority] Task ${task.taskId} is done via a legacy baseline transition; it is traceable but not equivalent to a fresh ATM CLI close.` }); } } if (taskLedger.enabled && taskLedger.mirrorExternalTasks) { for (const externalTask of taskLedger.externalTasks) { if (!mirrorKeys.has(externalTaskKey(externalTask.provider, externalTask.taskId))) { findings.push({ level: 'error', code: 'ATM_TASK_AUDIT_EXTERNAL_TASK_NOT_MIRRORED', path: '.atm/config.json', detail: `External task ${externalTask.provider}:${externalTask.taskId} must have a visible ATM mirror task.` }); } } } for (const staleLock of detectFrameworkStaleLocks(root)) { if (staleLock.kind === 'still-active') continue; findings.push({ level: 'warning', code: 'ATM_TASK_AUDIT_STALE_FRAMEWORK_LOCK', path: staleLock.lockPath, detail: `Framework-mode lock for actor ${staleLock.actorId} is ${staleLock.kind}${staleLock.lockedAt ? ` (locked at ${staleLock.lockedAt})` : ''}; run "${staleLock.requiredCommand}" to clear the stalled lock.` }); } const latestBulk = inspectLatestCommitForBulkTaskClose(root); if (latestBulk.changedDoneTaskFiles.length > 1 && !hasBulkClosureManifest(root)) { findings.push({ level: 'error', code: 'ATM_TASK_AUDIT_BULK_CLOSE_WITHOUT_MANIFEST', path: latestBulk.commitSha ?? 'HEAD', detail: `Latest commit closes ${latestBulk.changedDoneTaskFiles.length} task files without a bulk closure manifest.` }); } for (const reportPath of findMarkdownCompletionReports(root)) { findings.push({ level: 'error', code: 'ATM_TASK_AUDIT_COMPLETION_REPORT_UNVERIFIED', path: reportPath, detail: 'Completion report claims all work is complete; tasks audit requires matching task states, command evidence, and closure packets.' }); } for (const evidence of evidenceDocs) { if (!hasCommandRunEvidence(evidence.document)) { findings.push({ level: 'warning', code: 'ATM_TASK_AUDIT_DRAFT_STATIC_EVIDENCE', path: evidence.relativePath, detail: 'Static evidence has no commandRuns/exitCode/stdout hash and is treated as draft evidence only.' }); } } const errorCount = findings.filter((entry) => entry.level === 'error').length; const annotatedFindings = findings.map((entry) => { if (entry.level !== 'warning') return entry; if (!baseline.acknowledgedFindings.has(taskAuditFindingKey(entry))) return entry; return { ...entry, acknowledged: true }; }); const acknowledgedFindingCount = annotatedFindings.filter((entry) => entry.acknowledged).length; const activeFindingCount = annotatedFindings.filter((entry) => !entry.acknowledged).length; return { schemaId: 'atm.taskAuditReport', specVersion: frameworkModeSpecVersion, generatedAt, repoIdentity, inspectedTaskCount: taskDocs.length, inspectedEvidenceCount: evidenceDocs.length, findings: annotatedFindings, acknowledgedFindingCount, activeFindingCount, ok: errorCount === 0 }; } export function validateClosurePacket(value: unknown): { ok: boolean; missing: readonly string[]; invalidFormat: readonly ClosurePacketValidationIssue[]; } { const packet = value as Partial<ClosurePacket> | null; const missing: string[] = []; const invalidFormat: ClosurePacketValidationIssue[] = []; if (!packet || typeof packet !== 'object' || Array.isArray(packet)) { return { ok: false, missing: ['object'], invalidFormat }; } if (packet.schemaId !== 'atm.closurePacket.v1') missing.push('schemaId'); if (packet.closedByCommand !== 'atm tasks close') missing.push('closedByCommand'); if (!normalizeOptionalString(packet.taskId)) missing.push('taskId'); if (!normalizeOptionalString(packet.closedByActor)) missing.push('closedByActor'); if (!normalizeOptionalString(packet.evidencePath)) missing.push('evidencePath'); if (!packet.targetRepoIdentity?.isFrameworkRepo) missing.push('targetRepoIdentity'); if (!packet.targetCommitDelta || typeof packet.targetCommitDelta !== 'object' || Array.isArray(packet.targetCommitDelta)) { missing.push('targetCommitDelta'); } else { if (!Array.isArray(packet.targetCommitDelta.parentCommitShas)) missing.push('targetCommitDelta/parentCommitShas'); if (!Array.isArray(packet.targetCommitDelta.changedFiles)) missing.push('targetCommitDelta/changedFiles'); } if (!Array.isArray(packet.commandRuns) || packet.commandRuns.length === 0) { missing.push('commandRuns'); } else { for (const [index, run] of packet.commandRuns.entries()) { if (typeof run.command !== 'string' || run.command.trim().length === 0) missing.push(`commandRuns/${index}/command`); if (typeof run.exitCode !== 'number') missing.push(`commandRuns/${index}/exitCode`); pushSha256ValidationIssue({ missing, invalidFormat }, `commandRuns/${index}/stdoutSha256`, run.stdoutSha256); pushSha256ValidationIssue({ missing, invalidFormat }, `commandRuns/${index}/stderrSha256`, run.stderrSha256); } } if (!Array.isArray(packet.validationPasses) || packet.validationPasses.length === 0) { missing.push('validationPasses'); } else { const recorded = new Set(packet.validationPasses.filter((entry): entry is string => typeof entry === 'string' && entry.trim().length > 0).map((entry) => entry.trim())); for (const gate of requiredValidationPassesForClosure(packet.requiredGates ?? [], packet.targetCommitDelta?.changedFiles ?? [])) { if (!recorded.has(gate)) { missing.push(`validationPasses/${gate}`); } } } if (!packet.requiredGatesSnapshot || typeof packet.requiredGatesSnapshot !== 'object' || Array.isArray(packet.requiredGatesSnapshot)) { missing.push('requiredGatesSnapshot'); } else { const snapshot = packet.requiredGatesSnapshot as Partial<ClosurePacketRequiredGatesSnapshot>; if (snapshot.schemaId !== 'atm.requiredGatesSnapshot.v1') missing.push('requiredGatesSnapshot/schemaId'); if (snapshot.source !== 'frameworkStatus.requiredGates') missing.push('requiredGatesSnapshot/source'); if (!normalizeOptionalString(snapshot.ruleVersion)) missing.push('requiredGatesSnapshot/ruleVersion'); if (snapshot.frameworkMode !== 'required' && snapshot.frameworkMode !== 'cross-repo-target-required' && snapshot.frameworkMode !== 'suspected' && snapshot.frameworkMode !== 'inactive') { missing.push('requiredGatesSnapshot/frameworkMode'); } if (snapshot.repoRole !== 'framework' && snapshot.repoRole !== 'host') missing.push('requiredGatesSnapshot/repoRole'); if (!Array.isArray(snapshot.requiredGates)) { missing.push('requiredGatesSnapshot/requiredGates'); } else if (!sameStringSet(snapshot.requiredGates, packet.requiredGates ?? [])) { missing.push('requiredGatesSnapshot/requiredGates-mismatch'); } } if (packet.attestation !== undefined && packet.attestation !== null) { const att = packet.attestation as Partial<ClosurePacketReconcileAttestation>; if (typeof att !== 'object' || Array.isArray(att)) { missing.push('attestation'); } else { if (att.schemaId !== 'atm.reconcileAttestation.v1') missing.push('attestation/schemaId'); if (typeof att.deliveryCommit !== 'string' || att.deliveryCommit.trim().length === 0) missing.push('attestation/deliveryCommit'); if (typeof att.reconciledAt !== 'string' || att.reconciledAt.trim().length === 0) missing.push('attestation/reconciledAt'); if (typeof att.reconciledByActor !== 'string' || att.reconciledByActor.trim().length === 0) missing.push('attestation/reconciledByActor'); if (typeof att.reason !== 'string') missing.push('attestation/reason'); } } if (packet.teamSummary !== undefined && packet.teamSummary !== null) { const summary = packet.teamSummary as Partial<ClosurePacketTeamSummary> & Record<string, unknown>; if (typeof summary !== 'object' || Array.isArray(summary)) { missing.push('teamSummary'); } else { if (summary.schemaId !== 'atm.closurePacketTeamSummary.v1') missing.push('teamSummary/schemaId'); if (!normalizeOptionalString(summary.capturedAt)) missing.push('teamSummary/capturedAt'); if (!normalizeOptionalString(summary.teamRunId)) missing.push('teamSummary/teamRunId'); if (!summary.source || typeof summary.source !== 'object' || Array.isArray(summary.source)) { missing.push('teamSummary/source'); } else { const source = summary.source as Record<string, unknown>; if (source.kind !== 'team-run') missing.push('teamSummary/source/kind'); if (!normalizeOptionalString(source.teamRunPath)) missing.push('teamSummary/source/teamRunPath'); } if (!Object.prototype.hasOwnProperty.call(summary, 'captainDecision')) missing.push('teamSummary/captainDecision'); if (!Array.isArray(summary.agentReports)) missing.push('teamSummary/agentReports'); if (!Array.isArray(summary.patrolFindings)) missing.push('teamSummary/patrolFindings'); if (!Object.prototype.hasOwnProperty.call(summary, 'evidenceCuratorSummary')) missing.push('teamSummary/evidenceCuratorSummary'); if (!Object.prototype.hasOwnProperty.call(summary, 'teamSummary')) missing.push('teamSummary/teamSummary'); if (Object.prototype.hasOwnProperty.call(summary, 'validationPasses')) { invalidFormat.push({ path: 'teamSummary/validationPasses', kind: 'invalidFormat', formatExpected: 'teamSummary must not declare validationPasses; only command-backed closure packet evidence can pass validators.', actualValue: 'present' }); } } } if (packet.evidenceFreshness !== 'fresh') { missing.push('evidenceFreshness'); } return { ok: missing.length === 0 && invalidFormat.length === 0, missing, invalidFormat }; } export function normalizeUpstreamEvidenceForTask(cwd: string, taskId: string): { evidencePath: string; changed: boolean; } { const evidenceAbsolute = path.join(cwd, '.atm', 'history', 'evidence', `${taskId}.json`); const evidencePath = relativePathFrom(cwd, evidenceAbsolute); if (!existsSync(evidenceAbsolute)) { return { evidencePath, changed: false }; } const parsed = readJsonIfExists(evidenceAbsolute); if (!parsed) return { evidencePath, changed: false }; const normalized = normalizeSha256FieldsDeep(parsed); const before = `${JSON.stringify(parsed, null, 2)}\n`; const after = `${JSON.stringify(normalized, null, 2)}\n`; if (before !== after) { writeFileSync(evidenceAbsolute, after, 'utf8'); return { evidencePath, changed: true }; } return { evidencePath, changed: false }; } function collectTaskScopeFiles(cwd: string, scopeTaskId: string): Set<string> { const scopeFiles = new Set<string>(); const taskPath = path.join(cwd, '.atm', 'history', 'tasks', `${scopeTaskId}.json`); const taskDocument = readJsonIfExists(taskPath); if (!taskDocument) return scopeFiles; const pushPath = (value: unknown) => { if (typeof value !== 'string' || value.trim().length === 0) return; const normalized = normalizeProjectRelativePath(cwd, value); if (normalized) scopeFiles.add(normalized); }; for (const entry of ['scopePaths', 'deliverables', 'targetAllowedFiles'] as const) { if (Array.isArray(taskDocument[entry])) { for (const value of taskDocument[entry]) pushPath(value); } } const claim = taskDocument.claim; if (claim && typeof claim === 'object' && !Array.isArray(claim) && Array.isArray((claim as Record<string, unknown>).files)) { for (const value of (claim as Record<string, unknown>).files as unknown[]) pushPath(value); } const directionLock = taskDocument.taskDirectionLock; if (directionLock && typeof directionLock === 'object' && !Array.isArray(directionLock) && Array.isArray((directionLock as Record<string, unknown>).allowedFiles)) { for (const value of (directionLock as Record<string, unknown>).allowedFiles as unknown[]) pushPath(value); } return scopeFiles; } function pathOverlapsTaskScope(filePath: string, scopeFiles: Set<string>): boolean { const normalized = normalizeRelativePath(filePath); if (!normalized || scopeFiles.size === 0) return false; for (const scopePath of scopeFiles) { if (normalized === scopePath) return true; if (scopePath.endsWith('/**') && normalized.startsWith(scopePath.slice(0, -3))) return true; if (normalized.startsWith(`${scopePath}/`)) return true; } return false; } function refreshPacketFromEvidenceContext(cwd: string, taskId: string, packet: ClosurePacket): ClosurePacket { const context = readClosureEvidenceContext(cwd, taskId); if (context.commandRuns.length === 0) { return normalizeSha256FieldsDeep(packet); } return normalizeSha256FieldsDeep({ ...packet, commandRuns: context.commandRuns, validationPasses: context.validationPasses.length > 0 ? context.validationPasses : packet.validationPasses, evidenceFreshness: context.evidenceFreshness }); } export function requiredValidationPassesForClosure(requiredGates: readonly string[], changedFiles: readonly string[] = []) { const concreteChangedFiles = changedFiles.filter((entry) => !looksLikeScopePattern(entry)); const touchedIntegrations = concreteChangedFiles.some((entry) => /^(templates|integrations)\//.test(entry)); const touchedRelease = concreteChangedFiles.some((entry) => /^(release|templates\/root-drop|scripts\/build-)/.test(entry) || entry === 'atm.mjs'); return uniqueSorted(requiredGates.filter((gate) => { if (gate === 'typecheck') return true; if (!gate.startsWith('validate:')) return false; if (gate === 'validate:integration-adapter' || gate === 'validate:skill-templates') { return touchedIntegrations; } if (gate === 'validate:root-drop-release' || gate === 'validate:onefile-release') { return touchedRelease; } return true; })); } export function createClosurePacket(input: { readonly cwd: string; readonly taskId: string; readonly actorId: string; readonly sessionId?: string | null; readonly evidencePath: string; readonly requiredGates?: readonly string[]; readonly changedFiles?: readonly string[]; readonly frameworkStatus?: Pick<FrameworkModeStatusReport, 'mode' | 'repoRole' | 'changedFiles' | 'criticalChangedFiles' | 'requiredGates'> | null; readonly attestation?: ClosurePacketReconcileAttestation | null; readonly historicalDeliveryProvenance?: HistoricalDeliveryProvenance | null; readonly teamSummary?: ClosurePacketTeamSummary | null; readonly validationPasses?: readonly string[]; readonly evidenceFreshness?: 'fresh' | 'historical-reference' | 'draft'; }): ClosurePacket { const cwd = path.resolve(input.cwd); const targetCommitDelta = readFutureTargetCommitDelta(cwd, input.changedFiles, input.taskId); const evidenceContext = readClosureEvidenceContext(cwd, input.taskId); const requiredGates = uniqueSorted((input.requiredGates ?? defaultRequiredGates).map((entry) => String(entry).trim()).filter(Boolean)); const requiredGatesSnapshot = createRequiredGatesSnapshot({ cwd, changedFiles: input.changedFiles ?? [], frameworkStatus: input.frameworkStatus ?? null, requiredGates }); return { schemaId: 'atm.closurePacket.v1', specVersion: frameworkModeSpecVersion, taskId: input.taskId, targetRepoIdentity: detectFrameworkRepoIdentity(cwd), targetCommit: targetCommitDelta.currentCommitSha, governedTreeSha: targetCommitDelta.governedTreeSha, targetCommitDelta, closedByCommand: 'atm tasks close', commandRuns: evidenceContext.commandRuns, validationPasses: input.validationPasses ? uniqueSorted(input.validationPasses.map((entry) => String(entry).trim()).filter(Boolean)) : evidenceContext.validationPasses, evidenceFreshness: input.evidenceFreshness ?? evidenceContext.evidenceFreshness, requiredGates, requiredGatesSnapshot, evidencePath: input.evidencePath, closedAt: new Date().toISOString(), closedByActor: input.actorId, sessionId: normalizeOptionalString(input.sessionId) ?? null, attestation: input.attestation ?? null, historicalDeliveryProvenance: input.historicalDeliveryProvenance ?? null, teamSummary: input.teamSummary === undefined ? readLatestClosurePacketTeamSummary(cwd, input.taskId) : input.teamSummary }; } function readLatestClosurePacketTeamSummary(cwd: string, taskId: string): ClosurePacketTeamSummary | null { const directory = path.join(cwd, '.atm', 'runtime', 'team-runs'); if (!existsSync(directory)) return null; const candidates = readdirSync(directory) .filter((entry) => entry.endsWith('.json')) .map((entry) => { const absolutePath = path.join(directory, entry); const run = readJsonIfExists(absolutePath); return run && String(run.taskId ?? '').trim() === taskId ? { run, absolutePath } : null; }) .filter((entry): entry is { run: Record<string, unknown>; absolutePath: string; } => Boolean(entry)) .sort((left, right) => readTeamRunTimestamp(right.run).localeCompare(readTeamRunTimestamp(left.run))); const latest = candidates[0]; if (!latest) return null; const run = latest.run; const teamSummary = isRecordValue(run.teamSummary) ? run.teamSummary : null; return { schemaId: 'atm.closurePacketTeamSummary.v1', capturedAt: new Date().toISOString(), source: { kind: 'team-run', teamRunPath: normalizeRelativePath(relativePathFrom(cwd, latest.absolutePath)) }, teamRunId: String((run as { teamRunId?: unknown; }).teamRunId ?? path.basename(latest.absolutePath, '.json')), captainDecision: (run as { captainDecision?: unknown; }).captainDecision ?? readNestedRecordValue(run, ['teamPlan', 'captainDecision']) ?? readNestedRecordValue(teamSummary, ['captainDecision']) ?? null, agentReports: normalizeUnknownArray((run as { agentReports?: unknown; agent_reports?: unknown; }).agentReports ?? (run as { agentReports?: unknown; agent_reports?: unknown; }).agent_reports ?? readNestedRecordValue(teamSummary, ['agentReports'])), patrolFindings: normalizeUnknownArray((run as { patrolFindings?: unknown; patrol_findings?: unknown; }).patrolFindings ?? (run as { patrolFindings?: unknown; patrol_findings?: unknown; }).patrol_findings ?? readNestedRecordValue(teamSummary, ['patrolFindings'])), evidenceCuratorSummary: (run as { evidenceCuratorSummary?: unknown; evidence_curator_summary?: unknown; }).evidenceCuratorSummary ?? (run as { evidenceCuratorSummary?: unknown; evidence_curator_summary?: unknown; }).evidence_curator_summary ?? readNestedRecordValue(teamSummary, ['evidenceCuratorSummary']) ?? null, teamSummary }; } function readTeamRunTimestamp(run: Record<string, unknown>): string { return normalizeOptionalString((run as { updatedAt?: unknown; }).updatedAt) ?? normalizeOptionalString((run as { createdAt?: unknown; }).createdAt) ?? ''; } function isRecordValue(value: unknown): value is Record<string, unknown> { return Boolean(value) && typeof value === 'object' && !Array.isArray(value); } function readNestedRecordValue(value: unknown, pathParts: readonly string[]): unknown { let cursor: unknown = value; for (const part of pathParts) { if (!isRecordValue(cursor)) return null; cursor = cursor[part]; } return cursor ?? null; } function normalizeUnknownArray(value: unknown): readonly unknown[] { return Array.isArray(value) ? value : []; } export function writeClosurePacket(cwd: string, taskId: string, packet: ClosurePacket) { const packetPath = path.join(cwd, '.atm', 'history', 'evidence', `${taskId}.closure-packet.json`); mkdirSync(path.dirname(packetPath), { recursive: true }); const normalizedPacket = normalizeSha256FieldsDeep(packet); writeFileSync(packetPath, `${JSON.stringify(normalizedPacket, null, 2)}\n`, 'utf8'); return relativePathFrom(cwd, packetPath); } export type AtmTasksWriteAction = 'tasks-close' | 'tasks-reconcile' | 'tasks-import-write' | 'tasks-repair-closure-write'; const FROZEN_RUNNER_ENTRYPOINTS = new Set([ 'atm.mjs', 'release/atm-onefile/atm.mjs', 'packages/cli/dist/atm.js', 'release/atm-root-drop/atm.mjs' ]); function newestFrameworkSourceMtime(rootDir: string): number { let newest = 0; for (const entryPath of runnerAffectingMtimeRoots(rootDir)) { newest = Math.max(newest, newestMtimeInTree(path.join(rootDir, entryPath))); } return newest; } function runnerAffectingMtimeRoots(rootDir: string): string[] { const manifest = readRunnerBuildScopeManifest(rootDir); if (!manifest) return ['packages/cli/src', 'scripts']; const roots = runnerAffectingPatterns(manifest) .filter((pattern) => !pattern.startsWith('release/')) .map((pattern) => pattern.includes('*') ? pattern.slice(0, pattern.indexOf('*')) : pattern) .map((pattern) => pattern.replace(/\/$/, '')) .filter((pattern) => pattern.length > 0); return [...new Set(roots)]; } function readRunnerBuildScopeManifest(rootDir: string): RunnerBuildScopeManifest | null { const manifestPath = path.join(rootDir, 'scripts', 'AtmCore', 'runner-build-scope.json'); try { return JSON.parse(readFileSync(manifestPath, 'utf8')) as RunnerBuildScopeManifest; } catch { return null; } } function newestMtimeInTree(entryPath: string): number { let stat: ReturnType<typeof statSync> | null = null; try { stat = statSync(entryPath); } catch { return 0; } if (stat.isFile()) return stat.mtimeMs; if (!stat.isDirectory()) return 0; let newest = 0; for (const child of readdirSync(entryPath, { withFileTypes: true })) { if (child.name === 'dist' || child.name === 'node_modules') continue; newest = Math.max(newest, newestMtimeInTree(path.join(entryPath, child.name))); } return newest; } function resolveFrozenRunnerPath(rootDir: string): string | null { const onefile = path.join(rootDir, 'release', 'atm-onefile', 'atm.mjs'); if (existsSync(onefile)) return onefile; const dist = path.join(rootDir, 'packages', 'cli', 'dist', 'atm.js'); return existsSync(dist) ? dist : null; } function classifyCliEntrypoint(cwd: string): string | null { const entrypointPath = process.argv[1] ? path.resolve(process.argv[1]) : null; if (!entrypointPath) return null; const relative = path.relative(path.resolve(cwd), entrypointPath).replace(/\\/g, '/'); if (relative && !relative.startsWith('..')) return relative; return entrypointPath.replace(/\\/g, '/'); } export function isRunnerSyncRequired(cwd: string): boolean { return inspectRunnerSourceDrift(cwd).syncRequired; } export function inspectRunnerSourceDrift(cwd: string) { const rootDir = path.resolve(cwd); const entrypoint = classifyCliEntrypoint(rootDir); const releaseHygienePolicy = describeBuildReleaseHygienePolicy(); const nonFrozen = !entrypoint || !FROZEN_RUNNER_ENTRYPOINTS.has(entrypoint); const runnerPath = resolveFrozenRunnerPath(rootDir); const newestSourceMtimeMs = newestFrameworkSourceMtime(rootDir); const runnerMtimeMs = runnerPath ? statSync(runnerPath).mtimeMs : 0; const syncRequired = !nonFrozen && Boolean(runnerPath) && newestSourceMtimeMs > runnerMtimeMs; return { schemaId: 'atm.runnerSourceDrift.v1', entrypoint, frozenEntrypoint: !nonFrozen, runnerPath: runnerPath ? relativePathFrom(rootDir, runnerPath) : null, runnerMtime: runnerMtimeMs > 0 ? new Date(runnerMtimeMs).toISOString() : null, newestSourceMtime: newestSourceMtimeMs > 0 ? new Date(newestSourceMtimeMs).toISOString() : null, syncRequired, advisory: syncRequired ? 'Frozen runner is older than framework source files; live CLI behavior may lag source until the runner is rebuilt.' : nonFrozen ? 'Current entrypoint is not the frozen runner; source-first/source-import diagnostics may differ from node atm.mjs.' : 'Frozen runner is not older than framework source files.', syncCommand: releaseHygienePolicy.runnerSyncCommand }; } export function runnerStaleWarningMessage(): string { const releaseHygienePolicy = describeBuildReleaseHygienePolicy(); return `ATM_RUNNER_SYNC_REQUIRED: stable atm.mjs is older than framework source files. Run \`${releaseHygienePolicy.runnerSyncCommand}\` before using the frozen runner, or use \`node atm.dev.mjs ...\` for source-first framework validation.`; } export function assertSourceFirstRunnerReadOnlyAction(input: { readonly cwd: string; readonly action: string; }): void { const entrypoint = classifyCliEntrypoint(path.resolve(input.cwd))?.toLowerCase().replace(/\\/g, '/'); if (entrypoint !== 'atm.dev.mjs' && !entrypoint?.endsWith('/atm.dev.mjs')) return; throw new CliError('ATM_SOURCE_FIRST_WRITE_REFUSED', `ATM refused ${input.action} through the source-first runner.`, { exitCode: 1, details: { action: input.action, sourceFirstOnly: true, guidance: 'Use node atm.dev.mjs only for explicit source-first validation; use node atm.mjs after rebuilding for governed lifecycle mutations.', sourceFirstCommand: 'node atm.dev.mjs ...', governedCommand: 'node atm.mjs ...' } }); } export function assertRunnerFreshForWriteAction(input: { readonly cwd: string; readonly action: AtmTasksWriteAction; readonly allowStaleRunner: boolean; }): { readonly warning: string | null; } { const releaseHygienePolicy = describeBuildReleaseHygienePolicy(); if (!isRunnerSyncRequired(input.cwd)) return { warning: null }; if (input.allowStaleRunner) { return { warning: runnerStaleWarningMessage() }; } throw new CliError('ATM_RUNNER_STALE_WRITE_REFUSED', `ATM refused ${input.action} because the frozen runner is older than framework source files.`, { exitCode: 1, details: { action: input.action, syncCommand: releaseHygienePolicy.runnerSyncCommand, sourceFirstCommand: 'node atm.dev.mjs ...', remediation: `Run ${releaseHygienePolicy.runnerSyncCommand} before write actions through node atm.mjs, or pass --allow-stale-runner only for disaster recovery.` } }); } export function closeJournalPath(cwd: string, taskId: string): string { return path.join(cwd, '.atm-temp', `close-journal-${taskId}.json`); } export interface TaskCloseTransactionResult { readonly transitionPath: string; readonly closurePacketPath: string | null; } export async function executeTaskCloseTransaction(input: { readonly cwd: string; readonly taskId: string; readonly taskPath: string; readonly phase: 'close' | 'reconcile'; readonly previousTaskContent: string; readonly createdClosurePacketAbsolute: string | null; readonly runWrites: () => TaskCloseTransactionResult | Promise<TaskCloseTransactionResult>; }): Promise<TaskCloseTransactionResult> { const journalPath = closeJournalPath(input.cwd, input.taskId); mkdirSync(path.dirname(journalPath), { recursive: true }); writeFileSync(journalPath, `${JSON.stringify({ schemaId: 'atm.closeJournal.v1', taskId: input.taskId, phase: input.phase, status: 'staging', startedAt: new Date().toISOString() }, null, 2)}\n`, 'utf8'); const rolledBackArtifacts: string[] = []; let transitionAbsolute: string | null = null; try { const result = await input.runWrites(); transitionAbsolute = path.resolve(input.cwd, result.transitionPath); writeFileSync(journalPath, `${JSON.stringify({ schemaId: 'atm.closeJournal.v1', taskId: input.taskId, phase: input.phase, status: 'committed', committedAt: new Date().toISOString(), transitionPath: result.transitionPath, closurePacketPath: result.closurePacketPath }, null, 2)}\n`, 'utf8'); unlinkSync(journalPath); return result; } catch (error) { if (transitionAbsolute && existsSync(transitionAbsolute)) { unlinkSync(transitionAbsolute); rolledBackArtifacts.push(relativePathFrom(input.cwd, transitionAbsolute)); } if (input.createdClosurePacketAbsolute && existsSync(input.createdClosurePacketAbsolute)) { unlinkSync(input.createdClosurePacketAbsolute); rolledBackArtifacts.push(relativePathFrom(input.cwd, input.createdClosurePacketAbsolute)); } writeFileSync(input.taskPath, input.previousTaskContent, 'utf8'); rolledBackArtifacts.push(relativePathFrom(input.cwd, input.taskPath)); try { if (existsSync(journalPath)) unlinkSync(journalPath); } catch { } const reason = error instanceof Error ? error.message : String(error); throw new CliError('ATM_TASK_CLOSE_TRANSACTION_FAILED', `Task ${input.taskId} close transaction failed and was rolled back.`, { exitCode: 1, details: { taskId: input.taskId, phase: input.phase, rolledBackArtifacts, reason } }); } } export const CLOSE_COMMIT_WINDOW_TTL_SECONDS = 30; export const CLOSE_COMMIT_WINDOW_SCHEMA_ID = 'atm.closeCommitWindow.v1'; export interface CloseCommitWindowRecord { readonly schemaId: typeof CLOSE_COMMIT_WINDOW_SCHEMA_ID; readonly specVersion: '0.1.0'; readonly taskId: string; readonly actorId: string; readonly createdAt: string; readonly expiresAt: string; readonly ttlSeconds: number; readonly allowedFiles: readonly string[]; readonly transitionId: string | null; readonly transitionAction: 'close' | 'reconcile' | 'abandon'; } function closeCommitWindowPath(cwd: string, taskId: string): string { return path.join(cwd, '.atm', 'runtime', 'locks', `close-commit-window-${taskId}.lock.json`); } export function registerCloseCommitWindow(input: { readonly cwd: string; readonly taskId: string; readonly actorId: string; readonly allowedFiles: readonly string[]; readonly transitionId: string | null; readonly action: 'close' | 'reconcile' | 'abandon'; }): string { const now = new Date(); const record: CloseCommitWindowRecord = { schemaId: CLOSE_COMMIT_WINDOW_SCHEMA_ID, specVersion: '0.1.0', taskId: input.taskId, actorId: input.actorId, createdAt: now.toISOString(), expiresAt: new Date(now.getTime() + CLOSE_COMMIT_WINDOW_TTL_SECONDS * 1000).toISOString(), ttlSeconds: CLOSE_COMMIT_WINDOW_TTL_SECONDS, allowedFiles: uniqueSorted(input.allowedFiles.map(normalizeRelativePath).filter(Boolean)), transitionId: input.transitionId, transitionAction: input.action }; const lockPath = closeCommitWindowPath(input.cwd, input.taskId); mkdirSync(path.dirname(lockPath), { recursive: true }); writeFileSync(lockPath, `${JSON.stringify(record, null, 2)}\n`, 'utf8'); return relativePathFrom(input.cwd, lockPath); } export function readActiveCloseCommitWindows(cwd: string): readonly CloseCommitWindowRecord[] { const locksDir = path.join(cwd, '.atm', 'runtime', 'locks'); if (!existsSync(locksDir)) return []; const now = new Date(); const records: CloseCommitWindowRecord[] = []; for (const entry of readdirSync(locksDir)) { if (!entry.startsWith('close-commit-window-') || !entry.endsWith('.lock.json')) continue; const filePath = path.join(locksDir, entry); try { const raw = JSON.parse(readFileSync(filePath, 'utf8')); if (raw?.schemaId !== CLOSE_COMMIT_WINDOW_SCHEMA_ID) continue; const expiresAt = new Date(String(raw.expiresAt ?? '')); if (Number.isNaN(expiresAt.getTime()) || expiresAt.getTime() <= now.getTime()) { try { unlinkSync(filePath); } catch { } continue; } records.push(raw as CloseCommitWindowRecord); } catch { } } return records; } export function findCloseCommitWindowCoveringPaths(cwd: string, candidatePaths: readonly string[]): CloseCommitWindowRecord | null { if (candidatePaths.length === 0) return null; const windows = readActiveCloseCommitWindows(cwd); const normalizedPaths = candidatePaths.map(normalizeRelativePath).filter(Boolean); for (const win of windows) { const allowed = new Set(win.allowedFiles); const covers = normalizedPaths.every((p) => allowed.has(p)); if (covers) return win; } return null; } export function inspectFrameworkCloseWorktree(cwd: string, taskId: string | null = null): FrameworkCloseWorktreeReport { const unstagedFiles = uniqueSorted(runGitLines(cwd, ['diff', '--name-only']).map(normalizeRelativePath).filter(Boolean)); const stagedFiles = uniqueSorted(runGitLines(cwd, ['diff', '--cached', '--name-only']).map(normalizeRelativePath).filter(Boolean)); const rawUntrackedFiles = uniqueSorted(runGitLines(cwd, ['ls-files', '--others', '--exclude-standard']).map(normalizeRelativePath).filter(Boolean)); let allowedSet: Set<string> | null = null; if (taskId) { const taskPath = path.join(cwd, '.atm', 'history', 'tasks', `${taskId}.json`); if (existsSync(taskPath)) { try { const taskDoc = JSON.parse(readFileSync(taskPath, 'utf8')); const allowedFiles: string[] = []; if (Array.isArray(taskDoc.targetAllowedFiles)) { allowedFiles.push(...taskDoc.targetAllowedFiles); } if (Array.isArray(taskDoc.scopePaths)) { allowedFiles.push(...taskDoc.scopePaths); } if (allowedFiles.length > 0) { allowedSet = new Set(allowedFiles.map((entry) => normalizeProjectRelativePath(cwd, entry)).filter(Boolean)); } } catch { } } } const untrackedFiles: string[] = []; const ignoredUntrackedFiles: string[] = []; for (const f of rawUntrackedFiles) { const isDeliverable = !allowedSet || allowedSet.has(f) || isTaskCloseGovernanceCriticalPath(f, taskId || ''); if (isDeliverable) { untrackedFiles.push(f); } else { ignoredUntrackedFiles.push(f); } } const trackedDirtyFiles = uniqueSorted([...unstagedFiles, ...stagedFiles]); return { ok: trackedDirtyFiles.length === 0, trackedDirtyFiles, unstagedFiles, stagedFiles, untrackedFiles: uniqueSorted(untrackedFiles), ignoredUntrackedFiles: uniqueSorted(ignoredUntrackedFiles) }; } export function repairClosurePacketForTask(input: { readonly cwd: string; readonly taskId: string; readonly actorId?: string | null; readonly dryRun?: boolean; readonly amend?: boolean; readonly scopeTaskId?: string | null; }): ClosurePacketRepairResult { const cwd = path.resolve(input.cwd); const taskId = input.taskId.trim(); if (!taskId) { throw new CliError('ATM_CLOSURE_REPAIR_TASK_REQUIRED', 'tasks repair-closure requires --task <id>.', { exitCode: 2 }); } const packetPath = resolveClosurePacketPath(cwd, taskId); const packetAbsolutePath = path.join(cwd, packetPath); let parsed = readJsonIfExists(packetAbsolutePath) as ClosurePacket | null; const taskDocPath = path.join(cwd, '.atm', 'history', 'tasks', `${taskId}.json`); const taskDocument = readJsonIfExists(taskDocPath) as Record<string, unknown> | null; if (parsed && taskDocument && (taskDocument as { status?: unknown; }).status !== 'done') { throw new CliError('ATM_REPAIR_CLOSURE_NOT_CLOSE', `Task ${taskId} has an existing closure packet but its ledger status is '${(taskDocument as { status?: unknown; }).status}' (not done). Use taskflow close to finalize this task.`, { exitCode: 1, details: { taskId, ledgerStatus: (taskDocument as { status?: unknown; }).status, remediation: 'Run taskflow close to perform closeback finalization instead of repairing.' } }); } if (!parsed) { const taskDocPath = path.join(cwd, '.atm', 'history', 'tasks', `${taskId}.json`); const taskDocument = readJsonIfExists(taskDocPath); if (!taskDocument) { throw new CliError('ATM_CLOSURE_REPAIR_PACKET_NOT_FOUND', `Closure packet not found and task ledger is missing for ${taskId}.`, { exitCode: 1, details: { taskId, expectedPath: packetPath } }); } const lastTransitionId = normalizeOptionalString(taskDocument.lastTransitionId ?? taskDocument.last_transition_id); let reconstructedPacket: ClosurePacket | null = null; const missingSegments: string[] = ['closure-packet']; if (lastTransitionId) { const eventPath = path.join(cwd, '.atm', 'history', 'task-events', taskId, `${lastTransitionId}.json`); const eventDoc = readJsonIfExists(eventPath); const closureRecord = eventDoc?.closure && typeof eventDoc.closure === 'object' && !Array.isArray(eventDoc.closure) ? eventDoc.closure as Record<string, unknown> : null; if (closureRecord?.schemaId === 'atm.taskClosureTransition.v1') { const closureInfo = closureRecord; const headDetails = readGitHeadDetails(cwd); const targetCommit = readLatestCommitChangingPath(cwd, path.join('.atm', 'history', 'tasks', `${taskId}.json`)) || readLatestCommitChangingPath(cwd, path.join('.atm', 'history', 'task-events', taskId, `${lastTransitionId}.json`)) || headDetails.commitSha; if (targetCommit) { const commitDetails = readGitCommitDetails(cwd, targetCommit); const governedTreeSha = commitDetails.treeSha || headDetails.treeSha; const parentCommitShas = commitDetails.parentCommitShas; const changedFiles = readRepairCommitChangedFiles(cwd, targetCommit); const repoStatus = createFrameworkModeStatus({ cwd, files: changedFiles.length > 0 ? changedFiles : undefined }); reconstructedPacket = { schemaId: 'atm.closurePacket.v1', specVersion: '0.1.0', taskId, targetRepoIdentity: detectFrameworkRepoIdentity(cwd), targetCommit, governedTreeSha: governedTreeSha ?? '', targetCommitDelta: { currentCommitSha: targetCommit, parentCommitShas, governedTreeSha: governedTreeSha ?? '', changedFiles: [...changedFiles] }, closedByCommand: 'atm tasks close', commandRuns: [], validationPasses: Array.isArray(closureInfo.validationPasses) ? closureInfo.validationPasses.filter((entry): entry is string => typeof entry === 'string') : [], evidenceFreshness: closureInfo.evidenceFreshness === 'draft' || closureInfo.evidenceFreshness === 'historical-reference' ? closureInfo.evidenceFreshness : 'fresh', requiredGates: Array.isArray(closureInfo.requiredGates) ? closureInfo.requiredGates.filter((entry): entry is string => typeof entry === 'string') : [], requiredGatesSnapshot: closureInfo.requiredGatesSnapshot && typeof closureInfo.requiredGatesSnapshot === 'object' && !Array.isArray(closureInfo.requiredGatesSnapshot) ? closureInfo.requiredGatesSnapshot as ClosurePacketRequiredGatesSnapshot : createRequiredGatesSnapshot({ cwd, changedFiles, frameworkStatus: repoStatus, requiredGates: Array.isArray(closureInfo.requiredGates) ? closureInfo.requiredGates.filter((entry): entry is string => typeof entry === 'string') : [] }), evidencePath: `.atm/history/evidence/${taskId}.json`, closedAt: (eventDoc?.createdAt as string) ?? new Date().toISOString(), closedByActor: (eventDoc?.actorId as string) ?? 'unknown', sessionId: (eventDoc?.sessionId as string) || null, attestation: null, historicalDeliveryProvenance: null, recoveredFromMissingPacket: true }; } } else { missingSegments.push('close-transition-metadata'); } } else { missingSegments.push('close-transition-metadata'); } if (reconstructedPacket) { parsed = reconstructedPacket; if (!input.dryRun) { mkdirSync(path.dirname(packetAbsolutePath), { recursive: true }); writeFileSync(packetAbsolutePath, `${JSON.stringify(reconstructedPacket, null, 2)}\n`, 'utf8'); } } else { const deliveryCommit = readLatestCommitChangingPath(cwd, path.join('.atm', 'history', 'tasks', `${taskId}.json`)) || '<commit-sha>'; const actorId = input.actorId || '<actor>'; const recoveryCommand = `node atm.mjs tasks reconcile --task ${taskId} --actor ${actorId} --delivery-commit ${deliveryCommit} --json`; throw new CliError('ATM_CLOSURE_REPAIR_IMPOSSIBLE', `Closure packet not found for ${taskId} and cannot be reconstructed automatically because close transition metadata is missing.`, { exitCode: 1, details: { taskId, missingSegments, requiredCommand: recoveryCommand, remediation: 'Use the reconcile command to sync the historical commit and build closeout provenance.' } }); } } const upstreamEvidence = normalizeUpstreamEvidenceForTask(cwd, taskId); const scopeTaskId = normalizeOptionalString(input.scopeTaskId); const scopeFiles = scopeTaskId ? collectTaskScopeFiles(cwd, scopeTaskId) : null; const terminalTargetScopedRepair = scopeTaskId === taskId && taskDocument?.status === 'done'; const trackedDirtyFiles = readTrackedChangedFiles(cwd).filter((entry) => entry !== packetPath && entry !== gitHeadEvidencePath && !entry.startsWith('.atm/runtime/')); const trackedChangeStatuses = readTrackedChangeStatuses(cwd); const terminalCloseEventDeletions = trackedDirtyFiles.filter((entry) => isScopedTerminalCloseEventDeletion({ entry, taskId, scopeTaskId, taskDocument, status: trackedChangeStatuses.get(entry) ?? null })); const scopeWarnings: string[] = []; const blockingDirtyFiles = scopeFiles ? trackedDirtyFiles.filter((entry) => { if (terminalCloseEventDeletions.includes(entry)) { return false; } if (isTaskCloseGovernanceCriticalPath(entry, taskId) || (!terminalTargetScopedRepair && pathOverlapsTaskScope(entry, scopeFiles))) { return true; } scopeWarnings.push(entry); return false; }) : trackedDirtyFiles; if (blockingDirtyFiles.length > 0) { throw new CliError('ATM_CLOSURE_REPAIR_DIRTY_WORKTREE', `Cannot repair closure packet for ${taskId} while unrelated tracked worktree changes are present.`, { exitCode: 1, details: { taskId, trackedDirtyFiles: blockingDirtyFiles, scopeWarnings: scopeWarnings.length > 0 ? scopeWarnings : undefined, remediation: scopeTaskId ? 'Use --scope to downgrade unrelated dirty files to warnings, or run repair in an isolated worktree.' : 'Run repair in an isolated worktree at the broken commit, or commit/stash unrelated changes first.' } }); } if (!parsed) { parsed = readJsonIfExists(packetAbsolutePath) as ClosurePacket | null; } if (parsed) { parsed = refreshPacketFromEvidenceContext(cwd, taskId, parsed); if (!input.dryRun) { writeFileSync(packetAbsolutePath, `${JSON.stringify(parsed, null, 2)}\n`, 'utf8'); } } let validation = validateClosurePacket(parsed); if ((!validation.ok || !parsed) && parsed) { parsed = normalizeSha256FieldsDeep(parsed); validation = validateClosurePacket(parsed); } if (!validation.ok || !parsed) { throw new CliError('ATM_CLOSURE_REPAIR_PACKET_INVALID', `Closure packet for ${taskId} is invalid and cannot be repaired automatically.`, { exitCode: 1, details: { taskId, packetPath, missing: validation.missing, invalidFormat: validation.invalidFormat, upstreamEvidenceNormalized: upstreamEvidence.changed } }); } const commitMessage = buildRepairClosureCommitMessage(taskId); const nextActionCommand = buildRepairClosureNextActionCommand(taskId, input.actorId ?? null); const remediation = 'ATM staged the repaired closure packet, required git-head evidence, and any missing task ledger close fields. Finish by running the governed git commit wrapper; ATM does not provide an amend-capable wrapper for this flow yet.'; if (input.amend === true) { throw new CliError('ATM_CLOSURE_REPAIR_AMEND_WRAPPER_UNAVAILABLE', 'tasks repair-closure --amend is not available because ATM only exposes a governed git commit wrapper without amend support.', { exitCode: 1, details: { taskId, remediation, requiredCommand: nextActionCommand, commitMessage } }); } const headDetails = readGitHeadDetails(cwd); const closureCommitSha = readLatestCommitChangingPath(cwd, packetPath) ?? headDetails.commitSha; const closureCommitDetails = closureCommitSha ? readGitCommitDetails(cwd, closureCommitSha) : headDetails; const targetCommit = closureCommitDetails.parentCommitShas[0] ?? normalizeOptionalString(parsed.targetCommit); const governedTreeSha = closureCommitSha ? readCommitTreeWithoutEvidence(cwd, closureCommitSha) : headDetails.treeSha; const targetChangedFiles = closureCommitSha ? readRepairCommitChangedFiles(cwd, closureCommitSha) : parsed.targetCommitDelta.changedFiles.map(normalizeRelativePath).filter(Boolean); const repairedAt = new Date().toISOString(); const repairedPacket: ClosurePacket = { ...parsed, targetCommit, governedTreeSha, targetCommitDelta: { ...parsed.targetCommitDelta, currentCommitSha: closureCommitSha ?? targetCommit, parentCommitShas: closureCommitDetails.parentCommitShas, governedTreeSha, changedFiles: uniqueSorted(targetChangedFiles) }, repair: { schemaId: 'atm.closurePacketRepair.v1', repairedAt, repairedByCommand: 'atm tasks repair-closure', originalPacketCommitSha: closureCommitSha, repairedTargetCommitSha: targetCommit, evidencePath: gitHeadEvidencePath } }; const before = `${JSON.stringify(parsed, null, 2)}\n`; const after = `${JSON.stringify(repairedPacket, null, 2)}\n`; const changed = before !== after; const upstreamStatus = classifyRepairUpstreamStatus(cwd); const repairExtras = { scopeWarnings: scopeWarnings.length > 0 ? uniqueSorted(scopeWarnings) : undefined, upstreamEvidenceNormalized: upstreamEvidence.changed }; if (input.dryRun) { const normalizedTaskStatus = String(taskDocument?.status ?? '').trim().toLowerCase().replace(/-/g, '_'); const dryRunTaskPath = taskDocument && (normalizeOptionalString(taskDocument.closedAt ?? taskDocument.closed_at) === null || normalizeOptionalString(taskDocument.closedByActor ?? taskDocument.closed_by_actor) === null || normalizeOptionalString(taskDocument.closurePacket ?? taskDocument.closure_packet) === null || normalizedTaskStatus !== 'done') ? normalizeRelativePath(path.relative(cwd, taskDocPath)) : null; return { taskId, packetPath, taskPath: dryRunTaskPath, previousHead: headDetails.commitSha, repairedHead: headDetails.commitSha, targetCommit, governedTreeSha, changedFiles: repairedPacket.targetCommitDelta.changedFiles, gitHeadEvidencePath, amended: false, dryRun: true, changed, upstreamStatus, nextActionCommand, commitMessage, remediation, ...repairExtras }; } writeFileSync(packetAbsolutePath, after, 'utf8'); stageGitPath(cwd, packetPath); for (const staleEventPath of terminalCloseEventDeletions) { stageGitPath(cwd, staleEventPath); } let repairedTaskPath: string | null = null; if (taskDocument) { let taskChanged = false; const normalizedTaskStatus = String(taskDocument.status ?? '').trim().toLowerCase().replace(/-/g, '_'); if (normalizedTaskStatus !== 'done') { taskDocument.status = 'done'; taskChanged = true; } if (normalizeOptionalString(taskDocument.closedAt ?? taskDocument.closed_at) === null) { taskDocument.closedAt = repairedPacket.closedAt; taskChanged = true; } if (normalizeOptionalString(taskDocument.closedByActor ?? taskDocument.closed_by_actor) === null) { taskDocument.closedByActor = repairedPacket.closedByActor; taskChanged = true; } if (normalizeOptionalString(taskDocument.closedBySessionId ?? taskDocument.closed_by_session_id) === null && repairedPacket.sessionId) { taskDocument.closedBySessionId = repairedPacket.sessionId; taskChanged = true; } if (normalizeOptionalString(taskDocument.closurePacket ?? taskDocument.closure_packet) === null) { taskDocument.closurePacket = packetPath; taskChanged = true; } if (taskChanged) { writeFileSync(taskDocPath, `${JSON.stringify(taskDocument, null, 2)}\n`, 'utf8'); repairedTaskPath = normalizeRelativePath(path.relative(cwd, taskDocPath)); stageGitPath(cwd, repairedTaskPath); } } appendRepairGitHeadEvidence(cwd, { commitSha: closureCommitSha, treeSha: governedTreeSha, parentCommitShas: closureCommitDetails.parentCommitShas }, repairedPacket.commandRuns); stageGitPath(cwd, gitHeadEvidencePath); return { taskId, packetPath, taskPath: repairedTaskPath, previousHead: headDetails.commitSha, repairedHead: headDetails.commitSha, targetCommit, governedTreeSha, changedFiles: uniqueSorted([...repairedPacket.targetCommitDelta.changedFiles, ...terminalCloseEventDeletions]), gitHeadEvidencePath, amended: false, dryRun: false, changed, upstreamStatus, nextActionCommand, commitMessage, remediation, ...repairExtras }; } function isScopedTerminalCloseEventDeletion(input: { readonly entry: string; readonly taskId: string; readonly scopeTaskId: string | null; readonly taskDocument: Record<string, unknown> | null; readonly status: string | null; }): boolean { if (input.scopeTaskId !== input.taskId || input.taskDocument?.status !== 'done' || input.status !== 'D') { return false; } return normalizeRelativePath(input.entry).startsWith(`.atm/history/task-events/${input.taskId}/`); } export function requireTargetRepoClosureAuthority(input: { readonly cwd: string; readonly taskDocument: Record<string, unknown>; readonly taskId: string; readonly status: string; }) { if (input.status !== 'done') return null; const closureAuthority = normalizeClosureAuthority(input.taskDocument.closure_authority ?? input.taskDocument.closureAuthority); const targetRepo = normalizeOptionalString(input.taskDocument.target_repo ?? input.taskDocument.targetRepo ?? input.taskDocument.upstream_repo ?? input.taskDocument.upstreamRepo); if (closureAuthority !== 'target_repo' || !targetRepo) return null; if (matchesCurrentRepoIdentity(input.cwd, targetRepo)) return null; throw new CliError('ATM_TASK_CLOSE_TARGET_REPO_REQUIRED', `Task ${input.taskId} must be closed by its target repo, not the planning repo.`, { details: { taskId: input.taskId, targetRepo, closureAuthority } }); } function parseFrameworkModeArgs(argv: string[]): ParsedFrameworkModeArgs { const state = { cwd: process.cwd(), action: null as 'status' | 'claim' | 'release' | null, files: [] as string[], targetRepo: null as string | null, actor: null as string | null, reason: null as string | null }; for (let index = 0; index < argv.length; index += 1) { const arg = argv[index]; if (arg === '--cwd' || arg === '--repo') { state.cwd = requireValue(argv, index, arg); index += 1; continue; } if (arg === '--files') { state.files = requireValue(argv, index, '--files').split(',').map((entry) => normalizeRelativePath(entry)).filter(Boolean); index += 1; continue; } if (arg === '--target-repo') { state.targetRepo = requireValue(argv, index, '--target-repo'); index += 1; continue; } if (arg === '--actor') { state.actor = requireValue(argv, index, '--actor'); index += 1; continue; } if (arg === '--reason') { state.reason = requireValue(argv, index, '--reason'); index += 1; continue; } if (arg === '--json' || arg === '--pretty') { continue; } if (arg.startsWith('--')) { throw new CliError('ATM_CLI_USAGE', `framework-mode does not support option ${arg}`, { exitCode: 2 }); } if (state.action) { throw new CliError('ATM_CLI_USAGE', 'framework-mode accepts only one action.', { exitCode: 2 }); } if (arg !== 'status' && arg !== 'claim' && arg !== 'release') { throw new CliError('ATM_CLI_USAGE', 'framework-mode supports only: status, claim, release.', { exitCode: 2 }); } state.action = arg; } if (!state.action) { throw new CliError('ATM_CLI_USAGE', 'framework-mode requires an action: status | claim | release.', { exitCode: 2 }); } return { cwd: path.resolve(state.cwd), action: state.action, files: state.files, targetRepo: state.targetRepo, actor: state.actor, reason: state.reason }; } function readChangedFiles(cwd: string): readonly string[] { const changed = [ ...runGitLines(cwd, ['diff', '--name-only']), ...runGitLines(cwd, ['diff', '--cached', '--name-only']), ...runGitLines(cwd, ['ls-files', '--others', '--exclude-standard']) ]; return uniqueSorted(changed.map(normalizeRelativePath).filter(Boolean)); } function runGitLines(cwd: string, args: readonly string[]): readonly string[] { const result = spawnSync('git', args, { cwd, encoding: 'utf8' }); if (result.error || result.status !== 0) return []; return String(result.stdout || '').split(/\r?\n/).map((entry) => entry.trim()).filter(Boolean); } function readActiveLockPaths(cwd: string): readonly string[] { const lockDir = path.join(cwd, '.atm', 'runtime', 'locks'); if (!existsSync(lockDir)) return []; return readdirSync(lockDir) .filter((entry) => entry.endsWith('.lock.json')) .filter((entry) => isRuntimeLockActive(path.join(lockDir, entry))) .map((entry) => normalizeRelativePath(path.join('.atm', 'runtime', 'locks', entry))) .sort((left, right) => left.localeCompare(right)); } function isRuntimeLockActive(lockPath: string): boolean { const document = readJsonIfExists(lockPath); if (!document) return false; if (document.released === true) return false; const status = normalizeOptionalString(document.status)?.toLowerCase(); if (status && status !== 'active') return false; const heartbeatAt = normalizeOptionalString(document.heartbeatAt ?? document.lockedAt); const ttlSeconds = Number(document.ttlSeconds ?? 0); if (heartbeatAt && Number.isFinite(ttlSeconds) && ttlSeconds > 0) { const heartbeatMs = Date.parse(heartbeatAt); if (Number.isFinite(heartbeatMs) && heartbeatMs + ttlSeconds * 1000 < Date.now()) { return false; } } return true; } function inspectPinnedRunner(cwd: string): PinnedRunnerStatus { const metadataPath = path.join(cwd, '.atm', 'runtime', 'pinned-runner.json'); if (!existsSync(metadataPath)) { return { status: 'missing', metadataPath: relativePathFrom(cwd, metadataPath), sourcePath: null, runnerPath: null, reason: 'pinned runner metadata is missing' }; } const metadata = readJsonIfExists(metadataPath) ?? {}; const sourcePath = normalizeOptionalString(metadata.sourcePath); const runnerPath = normalizeOptionalString(metadata.runnerPath); const sourceAvailable = sourcePath ? existsSync(path.join(cwd, sourcePath)) : false; const runnerAvailable = runnerPath ? existsSync(path.join(cwd, runnerPath)) : false; const available = sourceAvailable || runnerAvailable; return { status: available ? 'available' : 'source-unavailable', metadataPath: relativePathFrom(cwd, metadataPath), sourcePath, runnerPath, reason: available ? normalizeOptionalString(metadata.reason) : 'pinned runner source and runner paths are unavailable' }; } function buildRequiredGates(criticalChangedFiles: readonly string[]): readonly string[] { const gates = new Set<string>(defaultRequiredGates); const concreteChangedFiles = criticalChangedFiles.filter((entry) => !looksLikeScopePattern(entry)); if (concreteChangedFiles.some((entry) => /^(templates|integrations)\//.test(entry))) { gates.add('validate:integration-adapter'); gates.add('validate:skill-templates'); } if (concreteChangedFiles.some((entry) => /^(release|templates\/root-drop|scripts\/build-)/.test(entry) || entry === 'atm.mjs')) { gates.add('validate:root-drop-release'); gates.add('validate:onefile-release'); } return [...gates]; } function looksLikeScopePattern(relativePath: string): boolean { return relativePath.includes('*'); } function buildRepairClosureCommitMessage(taskId: string): string { return `chore(${taskId}): repair closure packet`; } function buildRepairClosureNextActionCommand(taskId: string, actorId: string | null): string { const resolvedActor = normalizeOptionalString(actorId) ?? '<actor-id>'; const messageText = buildRepairClosureCommitMessage(taskId).replace(/"/g, '\\"'); return `node atm.mjs git commit --actor ${resolvedActor} --task ${taskId} --message "${messageText}" --json`; } function readTaskDocuments(root: string): ReadonlyArray<AuditedTaskDocument> { const taskLedger = readTaskLedgerPolicy(root); const jsonTasks = listFiles(path.join(root, taskLedger.taskRoot), (filePath) => filePath.endsWith('.json')) .map((absolutePath) => { const parsed = readJsonDocumentWithParseError(absolutePath); const document = parsed.document ?? {}; const taskId = normalizeOptionalString(document.workItemId ?? document.id ?? document.task_id ?? document.taskId) ?? path.basename(absolutePath, '.json'); return { relativePath: relativePathFrom(root, absolutePath), taskId, status: normalizeStatus(document.status), document, format: 'json' as const, parseError: parsed.parseError }; }); const markdownTasks = listFiles(root, (filePath) => filePath.endsWith('.task.md')) .map((absolutePath) => { const frontmatter = parseMarkdownFrontmatter(readFileSync(absolutePath, 'utf8')); const taskId = normalizeOptionalString(frontmatter.task_id ?? frontmatter.taskId) ?? path.basename(absolutePath).replace(/\.task\.md$/, ''); return { relativePath: relativePathFrom(root, absolutePath), taskId, status: normalizeStatus(frontmatter.status), document: frontmatter, format: 'markdown' as const, parseError: null }; }); return [...jsonTasks, ...markdownTasks].sort((left, right) => left.relativePath.localeCompare(right.relativePath)); } function readJsonDocumentWithParseError(filePath: string): { readonly document: Record<string, unknown> | null; readonly parseError: string | null; } { if (!existsSync(filePath)) { return { document: null, parseError: null }; } try { const parsed = JSON.parse(readFileSync(filePath, 'utf8')) as unknown; if (!parsed || typeof parsed !== 'object' || Array.isArray(parsed)) { return { document: {}, parseError: 'expected top-level JSON object' }; } return { document: parsed as Record<string, unknown>, parseError: null }; } catch (error) { const message = error instanceof Error ? error.message : String(error); return { document: {}, parseError: message }; } } function readEvidenceDocuments(root: string): ReadonlyArray<{ relativePath: string; document: unknown; }> { const evidenceRoots = [ path.join(root, 'atomic_workbench', 'evidence'), path.join(root, 'atomic_workbench', 'atomization-coverage'), path.join(root, 'atomic_workbench', 'graduation-gate') ]; return evidenceRoots.flatMap((evidenceRoot) => listFiles(evidenceRoot, (filePath) => filePath.endsWith('.json')) .map((absolutePath) => ({ relativePath: relativePathFrom(root, absolutePath), document: readJsonIfExists(absolutePath) }))); } function inspectLatestCommitForBulkTaskClose(root: string) { const commitSha = runGitLines(root, ['rev-parse', '--verify', 'HEAD'])[0] ?? null; const changed = runGitLines(root, ['show', '--name-only', '--format=', 'HEAD']) .filter(Boolean) .map((entry) => normalizeRelativePath(entry)); const changedSet = new Set(changed); const changedDoneTaskFiles = changed.filter((relativePath) => { if (!relativePath.endsWith('.task.md')) return false; const absolutePath = path.join(root, relativePath); if (!existsSync(absolutePath)) return false; const frontmatter = parseMarkdownFrontmatter(readFileSync(absolutePath, 'utf8')); if (normalizeStatus(frontmatter.status) !== 'done') return false; return !hasPreExistingClosurePacketForTaskCard(root, frontmatter, changedSet); }); return { commitSha, changedDoneTaskFiles }; } function hasPreExistingClosurePacketForTaskCard(root: string, frontmatter: Record<string, unknown>, changedSet: ReadonlySet<string>): boolean { const candidates = closurePacketCandidatesForTaskCard(frontmatter); return candidates.some((candidate) => { const relativePath = normalizeRelativePath(candidate); return existsSync(path.join(root, relativePath)) && !changedSet.has(relativePath); }); } function closurePacketCandidatesForTaskCard(frontmatter: Record<string, unknown>): readonly string[] { const explicit = normalizeOptionalString(frontmatter.closure_packet ?? frontmatter.closurePacket); const taskId = normalizeOptionalString(frontmatter.task_id ?? frontmatter.taskId ?? frontmatter.id); const candidates: string[] = []; if (explicit) candidates.push(explicit); if (taskId) candidates.push(`.atm/history/evidence/${taskId}.closure-packet.json`); return [...new Set(candidates)]; } function hasBulkClosureManifest(root: string): boolean { const candidates = [ path.join(root, '.atm', 'history', 'evidence'), path.join(root, 'atomic_workbench', 'evidence') ]; return candidates.some((candidate) => existsSync(candidate) && listFiles(candidate, (filePath) => /bulk.*closure.*\.json$/.test(normalizeRelativePath(filePath))).length > 0); } function findMarkdownCompletionReports(root: string): readonly string[] { return listFiles(root, (filePath) => filePath.endsWith('.md')) .filter((absolutePath) => !absolutePath.includes(`${path.sep}node_modules${path.sep}`)) .filter((absolutePath) => markdownCompletionReportNamePatterns.some((pattern) => pattern.test(path.basename(absolutePath)))) .filter((absolutePath) => { const text = readFileSync(absolutePath, 'utf8'); if (text.includes('ATM_GOVERNANCE_AUDIT_SUPERSEDED')) return false; return markdownCompletionPatterns.some((pattern) => pattern.test(text)); }) .map((absolutePath) => relativePathFrom(root, absolutePath)); } function hasCommandRunEvidence(value: unknown): boolean { if (!value || typeof value !== 'object') return false; const candidate = value as Record<string, unknown>; if (Array.isArray(candidate.commandRuns)) return candidate.commandRuns.length > 0; if (Array.isArray(candidate.commands)) { return candidate.commands.some((entry) => entry && typeof entry === 'object' && 'exitCode' in entry); } if (Array.isArray(candidate.evidence)) { return candidate.evidence.some(hasCommandRunEvidence); } return typeof candidate.command === 'string' && ('exitCode' in candidate || 'status' in candidate) && ('stdoutSha256' in candidate || 'outputSha256' in candidate); } function listFiles(directoryPath: string, predicate: (filePath: string) => boolean): readonly string[] { if (!existsSync(directoryPath)) return []; let stats; try { stats = statSync(directoryPath); } catch { return []; } if (stats.isFile()) return predicate(directoryPath) ? [directoryPath] : []; const output: string[] = []; let entries; try { entries = readdirSync(directoryPath, { withFileTypes: true }); } catch { return []; } for (const entry of entries) { if (defaultIgnoredDirs.has(entry.name)) continue; const absolutePath = path.join(directoryPath, entry.name); if (entry.isDirectory()) { output.push(...listFiles(absolutePath, predicate)); } else if (entry.isFile() && predicate(absolutePath)) { output.push(absolutePath); } } return output; } function parseMarkdownFrontmatter(text: string): Record<string, unknown> { const match = text.match(/^---\r?\n([\s\S]*?)\r?\n---/); if (!match) return {}; const result: Record<string, unknown> = {}; for (const rawLine of match[1].split(/\r?\n/)) { const separatorIndex = rawLine.indexOf(':'); if (separatorIndex === -1) continue; const key = rawLine.slice(0, separatorIndex).trim(); const value = rawLine.slice(separatorIndex + 1).trim().replace(/^"(.*)"$/, '$1'); result[key] = value; } return result; } function normalizeClosureAuthority(value: unknown): ClosureAuthority { const normalized = String(value ?? '').trim().toLowerCase().replace(/-/g, '_'); return normalized === 'target_repo' ? 'target_repo' : normalized === 'local' ? 'local' : 'none'; } function isPlanningOnlyAuthorityTask(root: string, document: Record<string, unknown>): boolean { const explicitPlanningOnly = String(document.planningOnly ?? document.planning_only ?? '').trim().toLowerCase(); if (explicitPlanningOnly === 'true' || explicitPlanningOnly === 'yes') return true; const closureAuthorityRaw = String(document.closure_authority ?? document.closureAuthority ?? '').trim().toLowerCase().replace(/-/g, '_'); if (closureAuthorityRaw === 'planning_repo') return true; const targetRepo = normalizeOptionalString(document.target_repo ?? document.targetRepo ?? document.upstream_repo ?? document.upstreamRepo); const planningRepo = normalizeOptionalString(document.planning_repo ?? document.planningRepo); return Boolean(planningRepo && targetRepo && !matchesCurrentRepoIdentity(root, targetRepo) && closureAuthorityRaw !== 'target_repo'); } function normalizeStatus(value: unknown): string { return String(value ?? '').trim().toLowerCase().replace(/-/g, '_'); } function classifyCurrentFrameworkStaleLocks(cwd: string): readonly FrameworkStaleLockInfo[] { const root = path.resolve(cwd); const locks = readActiveTaskDirectionLocks(root); const seenActors = new Set<string>(); const output: FrameworkStaleLockInfo[] = []; for (const lock of locks) { if (seenActors.has(lock.actorId)) continue; seenActors.add(lock.actorId); const stale = classifyFrameworkStaleLock(root, lock.actorId, { currentTaskId: lock.taskId }); if (stale) output.push(stale); } return output; } function resolveCurrentFrameworkTaskId(cwd: string, actorId: string, explicitTaskId?: string | null): string | null { const explicit = normalizeOptionalString(explicitTaskId); if (explicit) return explicit; const actorKey = actorId.toLowerCase(); const locks = readActiveTaskDirectionLocks(cwd) .filter((lock) => lock.actorId.toLowerCase() === actorKey) .sort((left, right) => right.createdAt.localeCompare(left.createdAt)); return locks[0]?.taskId ?? null; } function inferLinkedTaskIdFromActorSessions(cwd: string, actorId: string, lockedAt: string | null): string | null { const actorKey = actorId.toLowerCase(); const lockedAtMs = lockedAt ? Date.parse(lockedAt) : Number.NaN; const sessions = listActorWorkSessions(cwd) .filter((session) => session.actorId.toLowerCase() === actorKey) .filter((session) => { if (!Number.isFinite(lockedAtMs)) return true; const createdMs = Date.parse(session.createdAt); return Number.isFinite(createdMs) && createdMs <= lockedAtMs; }) .sort((left, right) => { const leftTime = Date.parse(left.updatedAt ?? left.createdAt); const rightTime = Date.parse(right.updatedAt ?? right.createdAt); return rightTime - leftTime; }); return sessions[0]?.taskId ?? null; } function readTaskStatusFromLedger(cwd: string, taskId: string): string | null { const taskLedger = readTaskLedgerPolicy(cwd); const taskPath = path.join(cwd, taskLedger.taskRoot, `${taskId}.json`); const document = readJsonIfExists(taskPath); if (!document) return null; return normalizeOptionalString(document.status) ?? null; } function isTerminalTaskStatus(status: string): boolean { const s = status.toLowerCase(); return s === 'done' || s === 'abandoned' || s === 'review'; } function buildFrameworkStaleReleaseCommand(actorId: string): string { return `node atm.mjs framework-mode release --actor ${quoteCliValue(actorId)} --json`; } function firstAiIssuedManualTaskActor(document: Record<string, unknown>): string | null { if (!isManualTaskSource(document)) return null; for (const actor of taskActorCandidates(document)) { if (isAiIssuedManualTaskActor(actor)) return actor; } return null; } function isManualTaskSource(document: Record<string, unknown>): boolean { const source = document.source; if (source && typeof source === 'object' && !Array.isArray(source)) { const sourceDocument = source as Record<string, unknown>; return normalizeOptionalString(sourceDocument.planPath) === 'manual' || normalizeOptionalString(sourceDocument.plan_path) === 'manual'; } return normalizeOptionalString(document.sourcePlanPath ?? document.source_plan_path) === 'manual'; } function taskActorCandidates(document: Record<string, unknown>): readonly string[] { const candidates = [ normalizeOptionalString(document.owner), normalizeOptionalString(document.reservedByActor ?? document.reserved_by_actor), normalizeOptionalString(document.startedByActor ?? document.started_by_actor), normalizeOptionalString(document.closedByActor ?? document.closed_by_actor), normalizeOptionalString(document.createdByActor ?? document.created_by_actor), normalizeOptionalString(document.updatedByActor ?? document.updated_by_actor) ]; const claim = document.claim; if (claim && typeof claim === 'object' && !Array.isArray(claim)) { const claimDocument = claim as Record<string, unknown>; candidates.push(normalizeOptionalString(claimDocument.actorId ?? claimDocument.actor_id)); } return candidates.filter((entry): entry is string => Boolean(entry)); } function isAiIssuedManualTaskActor(actor: string | null): boolean { const normalized = actor?.trim().toLowerCase(); if (!normalized) return false; return normalized === 'codex' || normalized === 'codex-main' || normalized.startsWith('copilot') || normalized.startsWith('claude') || normalized.startsWith('gemini') || normalized.startsWith('cursor'); } function matchesCurrentRepoIdentity(root: string, targetRepo: string): boolean { const identity = detectFrameworkRepoIdentity(root); const target = targetRepo.trim().toLowerCase().replace(/\\/g, '/'); const rootNormalized = root.replace(/\\/g, '/').toLowerCase(); const basename = path.basename(root).toLowerCase(); if (target === rootNormalized || target === identity.name?.toLowerCase() || target === basename || target.endsWith(`/${basename}`)) { return true; } const mainRepoPath = resolveMainRepoPath(root); if (mainRepoPath && mainRepoPath !== root) { const mainNormalized = mainRepoPath.replace(/\\/g, '/').toLowerCase(); const mainBasename = path.basename(mainRepoPath).toLowerCase(); if (target === mainNormalized || target === mainBasename || target.endsWith(`/${mainBasename}`)) { return true; } } return false; } function resolveMainRepoPath(cwd: string): string | null { try { const result = spawnSync('git', ['rev-parse', '--git-common-dir'], { cwd, encoding: 'utf8' }); if (result.status !== 0) return null; const gitCommonDir = result.stdout.trim(); if (!gitCommonDir) return null; const absoluteGitCommonDir = path.isAbsolute(gitCommonDir) ? gitCommonDir : path.resolve(cwd, gitCommonDir); const mainRepoPath = path.dirname(absoluteGitCommonDir); if (path.resolve(mainRepoPath) === path.resolve(cwd)) return null; return mainRepoPath; } catch { return null; } } function hasActiveFrameworkTaskLock(activeLocks: readonly string[]): boolean { return activeLocks.some((entry) => { const normalized = normalizeRelativePath(entry); return normalized.endsWith('.lock.json') && !normalized.includes('/BOOTSTRAP-'); }); } function frameworkTempTaskId(actorId: string) { const normalized = actorId.trim().replace(/[^A-Za-z0-9_-]+/g, '-').replace(/^-+|-+$/g, ''); return `ATM-FRAMEWORK-TEMP-${normalized || 'actor'}`; } function quoteCliValue(value: string) { return `"${value.replace(/\\/g, '\\\\').replace(/"/g, '\\"')}"`; } function firstTargetRepoField(document: Record<string, unknown>): { field: string; value: string; } | null { for (const field of ['target_repo', 'targetRepo', 'upstream_repo', 'upstreamRepo']) { const value = normalizeOptionalString(document[field]); if (value) return { field, value }; } return null; } function isOpenTaskStatus(status: string): boolean { return status === 'planned' || status === 'ready' || status === 'open' || status === 'active' || status === 'in_progress' || status === 'waiting_target_evidence' || status === 'blocked'; } function resolveTargetRepoReference(cwd: string, targetRepo: string): string { const trimmed = targetRepo.trim(); const candidates = targetRepoCandidates(cwd, trimmed); for (const candidate of candidates) { if (existsSync(candidate) && detectFrameworkRepoIdentity(candidate).isFrameworkRepo) { return path.resolve(candidate); } } return path.isAbsolute(trimmed) ? path.resolve(trimmed) : path.resolve(cwd, trimmed); } function targetRepoCandidates(cwd: string, targetRepo: string): readonly string[] { const candidates: string[] = []; const add = (candidate: string) => { const resolved = path.resolve(candidate); if (!candidates.includes(resolved)) candidates.push(resolved); }; if (path.isAbsolute(targetRepo)) { add(targetRepo); } else { add(path.join(cwd, targetRepo)); add(path.join(cwd, '..', targetRepo)); } const repoName = targetRepo .replace(/\\/g, '/') .split('/') .filter(Boolean) .pop() ?.replace(/\.git$/i, ''); if (repoName && repoName !== targetRepo) { add(path.join(cwd, repoName)); add(path.join(cwd, '..', repoName)); } return candidates; } function sameRepo(left: string, right: string | null): boolean { if (!right) return false; return path.resolve(left).toLowerCase() === path.resolve(right).toLowerCase(); } function readGitHeadDetails(cwd: string): { commitSha: string | null; treeSha: string | null; parentCommitShas: readonly string[]; } { const commitSha = runGitLines(cwd, ['rev-parse', '--verify', 'HEAD'])[0] ?? null; const treeSha = commitSha ? runGitLines(cwd, ['rev-parse', `${commitSha}^{tree}`])[0] ?? null : null; const parentCommitShas = commitSha ? runGitLines(cwd, ['rev-list', '--parents', '-n', '1', commitSha])[0]?.split(/\s+/).slice(1).filter(Boolean) ?? [] : []; return { commitSha, treeSha, parentCommitShas }; } function readGitCommitDetails(cwd: string, commitSha: string): { commitSha: string; treeSha: string | null; parentCommitShas: readonly string[]; } { const treeSha = runGitLines(cwd, ['rev-parse', `${commitSha}^{tree}`])[0] ?? null; const parentCommitShas = runGitLines(cwd, ['rev-list', '--parents', '-n', '1', commitSha])[0]?.split(/\s+/).slice(1).filter(Boolean) ?? []; return { commitSha, treeSha, parentCommitShas }; } function readLatestCommitChangingPath(cwd: string, relativePath: string): string | null { const normalized = normalizeRelativePath(relativePath); return runGitLines(cwd, ['log', '-n', '1', '--format=%H', '--', normalized])[0] ?? null; } function createRequiredGatesSnapshot(input: { readonly cwd: string; readonly changedFiles: readonly string[]; readonly frameworkStatus: Pick<FrameworkModeStatusReport, 'mode' | 'repoRole' | 'changedFiles' | 'criticalChangedFiles' | 'requiredGates'> | null; readonly requiredGates: readonly string[]; }): ClosurePacketRequiredGatesSnapshot { const status = input.frameworkStatus ?? createFrameworkModeStatus({ cwd: input.cwd, files: input.changedFiles.length > 0 ? input.changedFiles : undefined }); return { schemaId: 'atm.requiredGatesSnapshot.v1', generatedAt: new Date().toISOString(), source: 'frameworkStatus.requiredGates', ruleVersion: frameworkModeSpecVersion, frameworkMode: status.mode, repoRole: status.repoRole, changedFiles: uniqueSorted((status.changedFiles ?? []).map(normalizeRelativePath).filter(Boolean)), criticalChangedFiles: uniqueSorted((status.criticalChangedFiles ?? []).map(normalizeRelativePath).filter(Boolean)), requiredGates: uniqueSorted(input.requiredGates.map((entry) => String(entry).trim()).filter(Boolean)) }; } function readFutureTargetCommitDelta(cwd: string, preferredChangedFiles: readonly string[] = [], taskId: string | null = null): ClosurePacketTargetCommitDelta { const gitDetails = readGitHeadDetails(cwd); let allowedSet: Set<string> | null = null; if (taskId) { const taskPath = path.join(cwd, '.atm', 'history', 'tasks', `${taskId}.json`); if (existsSync(taskPath)) { try { const taskDoc = JSON.parse(readFileSync(taskPath, 'utf8')); const allowedFiles: string[] = []; if (Array.isArray(taskDoc.targetAllowedFiles)) { allowedFiles.push(...taskDoc.targetAllowedFiles); } if (Array.isArray(taskDoc.scopePaths)) { allowedFiles.push(...taskDoc.scopePaths); } if (allowedFiles.length > 0) { allowedSet = new Set(allowedFiles.map((entry) => normalizeProjectRelativePath(cwd, entry)).filter(Boolean)); } } catch { } } } const diffFiles = runGitLines(cwd, ['diff', '--name-only']).map(normalizeRelativePath).filter(Boolean); const cachedFiles = runGitLines(cwd, ['diff', '--cached', '--name-only']).map(normalizeRelativePath).filter(Boolean); const untrackedFiles = runGitLines(cwd, ['ls-files', '--others', '--exclude-standard']).map(normalizeRelativePath).filter(Boolean); const filteredUntrackedFiles = untrackedFiles.filter((f) => { if (!allowedSet) return true; if (allowedSet.has(f)) return true; if (isTaskCloseGovernanceCriticalPath(f, taskId!)) return true; return false; }); const worktreeChangedFiles = uniqueSorted([ ...diffFiles, ...cachedFiles, ...filteredUntrackedFiles ]); return { currentCommitSha: gitDetails.commitSha, parentCommitShas: gitDetails.commitSha ? [gitDetails.commitSha] : gitDetails.parentCommitShas, governedTreeSha: readFutureCommitTreeWithoutEvidence(cwd) ?? gitDetails.treeSha, changedFiles: uniqueSorted([ ...preferredChangedFiles.map(normalizeRelativePath).filter(Boolean), ...worktreeChangedFiles ]) }; } function readFutureCommitTreeWithoutEvidence(cwd: string): string | null { const tempDir = mkdtempSync(path.join(os.tmpdir(), 'atm-framework-closure-index-')); const tempIndex = path.join(tempDir, 'index'); try { const head = runGitLines(cwd, ['rev-parse', '--verify', 'HEAD'])[0] ?? null; if (head) { runGitWithEnv(cwd, ['read-tree', head], { GIT_INDEX_FILE: tempIndex }); } const addResult = runGitWithEnv(cwd, ['add', '-A', '--', '.'], { GIT_INDEX_FILE: tempIndex }); if (addResult.status !== 0) return null; runGitWithEnv(cwd, ['rm', '--cached', '--quiet', '--ignore-unmatch', '--', '.atm/history/evidence/git-head.json', '.atm/history/evidence/git-head.jsonl'], { GIT_INDEX_FILE: tempIndex }); const tree = runGitWithEnv(cwd, ['write-tree'], { GIT_INDEX_FILE: tempIndex }); return tree.status === 0 ? String(tree.stdout ?? '').trim() : null; } finally { rmSync(tempDir, { recursive: true, force: true }); } } function readCommitTreeWithoutEvidence(cwd: string, commitSha: string): string | null { const tempDir = mkdtempSync(path.join(os.tmpdir(), 'atm-framework-commit-index-')); const tempIndex = path.join(tempDir, 'index'); try { const readTree = runGitWithEnv(cwd, ['read-tree', commitSha], { GIT_INDEX_FILE: tempIndex }); if (readTree.status !== 0) return null; runGitWithEnv(cwd, ['rm', '--cached', '--quiet', '--ignore-unmatch', '--force', '--', gitHeadEvidencePaths.legacyJson, gitHeadEvidencePaths.jsonl], { GIT_INDEX_FILE: tempIndex }); const tree = runGitWithEnv(cwd, ['write-tree'], { GIT_INDEX_FILE: tempIndex }); return tree.status === 0 ? String(tree.stdout ?? '').trim() : null; } finally { rmSync(tempDir, { recursive: true, force: true }); } } function readRepairCommitChangedFiles(cwd: string, commitSha: string): readonly string[] { const parentRow = runGitLines(cwd, ['rev-list', '--parents', '-n', '1', commitSha])[0] ?? ''; const hasParent = parentRow.split(/\s+/).length > 1; const args = hasParent ? ['diff-tree', '--no-commit-id', '--name-only', '-r', commitSha] : ['show', '--name-only', '--format=', '--root', commitSha]; return uniqueSorted(runGitLines(cwd, args).map(normalizeRelativePath).filter(Boolean)); } function readJsonIfExists(filePath: string): Record<string, unknown> | null { if (!existsSync(filePath)) return null; try { return JSON.parse(readFileSync(filePath, 'utf8')) as Record<string, unknown>; } catch { return null; } } function readTaskAuditWarningBaseline(root: string): TaskAuditWarningBaseline { const baselinePath = path.join(root, 'docs', 'governance', 'tasks-audit-warning-baseline.json'); const document = readJsonIfExists(baselinePath); const acknowledged = Array.isArray(document?.acknowledgedFindings) ? document.acknowledgedFindings : []; const keys = new Set<string>(); for (const entry of acknowledged) { if (!entry || typeof entry !== 'object' || Array.isArray(entry)) continue; const record = entry as Record<string, unknown>; const code = normalizeOptionalString(record.code); const findingPath = normalizeOptionalString(record.path); if (!code || !findingPath) continue; const taskId = normalizeOptionalString(record.taskId); keys.add(taskAuditFindingKey({ code, path: findingPath, taskId: taskId ?? undefined })); } return { acknowledgedFindings: keys }; } function taskAuditFindingKey(finding: Pick<TaskAuditFinding, 'code' | 'path' | 'taskId'>): string { return `${finding.code}\u0000${normalizeRelativePath(finding.path)}\u0000${finding.taskId ?? ''}`; } function normalizeOptionalString(value: unknown): string | null { if (typeof value !== 'string') return null; const trimmed = value.trim(); if (!trimmed) return null; return trimmed.replace(/^"(.*)"$/, '$1'); } function normalizeRelativePath(value: string): string { return String(value).replace(/\\/g, '/').replace(/^\.\//, '').trim(); } function normalizeProjectRelativePath(cwd: string, value: string): string { const normalized = normalizeRelativePath(value); if (!normalized) return ''; return path.isAbsolute(normalized) ? normalizeRelativePath(relativePathFrom(cwd, normalized)) : normalized; } function uniqueSorted(values: readonly string[]): readonly string[] { return [...new Set(values)].sort((left, right) => left.localeCompare(right)); } function sha256(value: string): string { return `sha256:${createHash('sha256').update(value).digest('hex')}`; } function runGitWithEnv(cwd: string, args: readonly string[], env: Record<string, string>) { return spawnSync('git', args, { cwd, encoding: 'utf8', env: { ...process.env, ...env } }); } function readTrackedChangedFiles(cwd: string): readonly string[] { const changed = [ ...runGitLines(cwd, ['diff', '--name-only']), ...runGitLines(cwd, ['diff', '--cached', '--name-only']) ]; return uniqueSorted(changed.map(normalizeRelativePath).filter(Boolean)); } function readTrackedChangeStatuses(cwd: string): ReadonlyMap<string, string> { const statuses = new Map<string, string>(); for (const args of [['diff', '--name-status'], ['diff', '--cached', '--name-status']] as const) { for (const line of runGitLines(cwd, args)) { const [status, rawPath] = line.split('\t', 2); const filePath = normalizeRelativePath(rawPath ?? ''); if (status && filePath) statuses.set(filePath, status.charAt(0)); } } return statuses; } function runGitProcess(cwd: string, args: readonly string[]) { const result = spawnSync('git', args, { cwd, encoding: 'utf8' }); return { status: result.status ?? 1, stdout: String(result.stdout ?? ''), stderr: String(result.stderr ?? ''), error: result.error ?? null }; } function stageGitPath(cwd: string, relativePath: string) { const result = runGitProcess(cwd, ['add', '--', relativePath]); if (result.status !== 0) { throw new CliError('ATM_CLOSURE_REPAIR_STAGE_FAILED', `Failed to stage ${relativePath}.`, { exitCode: 1, details: { path: relativePath, stdout: result.stdout, stderr: result.stderr } }); } } function appendRepairGitHeadEvidence(cwd: string, gitDetails: { readonly commitSha: string | null; readonly treeSha: string | null; readonly parentCommitShas: readonly string[]; }, commandRuns: readonly ClosurePacketCommandRun[]) { const generatedAt = new Date().toISOString(); const evidenceAbsolute = path.join(cwd, gitHeadEvidencePath); mkdirSync(path.dirname(evidenceAbsolute), { recursive: true }); const payload = { schemaId: 'atm.gitHeadEvidence.v1', specVersion: '0.1.0', generatedAt, checks: [ { checkId: 'closure-packet-repair', ok: true, severity: 'info', summary: 'Closure packet repair restaged git-head evidence for follow-up governed commit.', details: { git: { commitSha: gitDetails.commitSha, treeSha: gitDetails.treeSha, parentCommitShas: gitDetails.parentCommitShas, stagedPathCount: runGitLines(cwd, ['diff', '--cached', '--name-only']).length, evidencePath: gitHeadEvidencePath, generatedAt }, hookContractVersion: 'atm.integration-hooks/v1', runnerVersion: readFrameworkVersion(cwd) }, commandRuns } ] }; appendFileSync(evidenceAbsolute, `${JSON.stringify(payload)}\n`, 'utf8'); } function classifyRepairUpstreamStatus(cwd: string): ClosureRepairUpstreamStatus { const branch = runGitLines(cwd, ['rev-parse', '--abbrev-ref', 'HEAD'])[0] ?? null; if (!branch || branch === 'HEAD') return 'detached-head'; const upstream = runGitLines(cwd, ['rev-parse', '--abbrev-ref', '--symbolic-full-name', '@{upstream}'])[0] ?? null; if (!upstream) return 'no-upstream'; const mergeBase = runGitProcess(cwd, ['merge-base', '--is-ancestor', 'HEAD', upstream]); if (mergeBase.status === 0) return 'published-head-blocked'; return 'ahead-of-upstream'; } function resolveClosurePacketPath(cwd: string, taskId: string): string { const taskPath = path.join(cwd, '.atm', 'history', 'tasks', `${taskId}.json`); const taskDocument = readJsonIfExists(taskPath); const declaredPath = normalizeOptionalString(taskDocument?.closurePacket ?? taskDocument?.closure_packet); if (declaredPath) return normalizeRelativePath(declaredPath); return normalizeRelativePath(path.join('.atm', 'history', 'evidence', `${taskId}.closure-packet.json`)); } function sameStringSet(left: readonly string[], right: readonly string[]) { const normalize = (values: readonly string[]) => [...new Set(values.map((value) => String(value).trim()).filter(Boolean))].sort(); return JSON.stringify(normalize(left)) === JSON.stringify(normalize(right)); } function readClosureEvidenceContext(cwd: string, taskId: string): { readonly commandRuns: readonly ClosurePacketCommandRun[]; readonly validationPasses: readonly string[]; readonly evidenceFreshness: 'fresh' | 'historical-reference' | 'draft'; } { const evidencePath = path.join(cwd, '.atm', 'history', 'evidence', `${taskId}.json`); const records = flattenEvidenceRecords(readJsonIfExists(evidencePath)); const commandRuns = dedupeCommandRuns(records.flatMap((record) => extractEvidenceCommandRuns(record, cwd))); const validationPasses = uniqueSorted(records.flatMap(extractValidationPasses)); const evidenceFreshness = resolveEvidenceFreshness(records); return { commandRuns, validationPasses, evidenceFreshness }; } function flattenEvidenceRecords(value: Record<string, unknown> | null): readonly Record<string, unknown>[] { if (!value) return []; if (Array.isArray((value as { evidence?: unknown; }).evidence)) { return (value as { evidence: Record<string, unknown>[]; }).evidence.filter((entry): entry is Record<string, unknown> => Boolean(entry) && typeof entry === 'object' && !Array.isArray(entry)); } return [value]; } function extractEvidenceCommandRuns(record: Record<string, unknown>, cwd: string): readonly ClosurePacketCommandRun[] { const runs = [ ...readCommandRunArray(record.commandRuns), ...readCommandRunArray(isObjectRecord(record.details) ? (record.details as { commandRuns?: unknown; }).commandRuns : undefined) ]; return runs.map((run) => ({ command: String(run.command), cwd: typeof run.cwd === 'string' && run.cwd.trim().length > 0 ? run.cwd : (relativePathFrom(cwd, cwd) || '.'), exitCode: Number(run.exitCode), stdoutSha256: String(run.stdoutSha256), stderrSha256: String(run.stderrSha256), runnerVersion: typeof run.runnerVersion === 'string' && run.runnerVersion.trim().length > 0 ? run.runnerVersion : readFrameworkVersion(cwd) })); } function readCommandRunArray(value: unknown): readonly Record<string, unknown>[] { if (!Array.isArray(value)) return []; return value.filter((entry): entry is Record<string, unknown> => isObjectRecord(entry) && isClosureCommandRunShape(entry)); } function isClosureCommandRunShape(value: Record<string, unknown>) { return typeof value.command === 'string' && typeof value.exitCode === 'number' && /^sha256:[a-f0-9]{64}$/i.test(String(value.stdoutSha256 ?? '')) && /^sha256:[a-f0-9]{64}$/i.test(String(value.stderrSha256 ?? '')); } function extractValidationPasses(record: Record<string, unknown>) { const fromTop = Array.isArray(record.validationPasses) ? record.validationPasses : []; const fromDetails = isObjectRecord(record.details) && Array.isArray(record.details.validationPasses) ? record.details.validationPasses : []; return [...fromTop, ...fromDetails] .filter((entry): entry is string => typeof entry === 'string' && entry.trim().length > 0) .map((entry) => entry.trim()); } function resolveEvidenceFreshness(records: readonly Record<string, unknown>[]): 'fresh' | 'historical-reference' | 'draft' { let hasFresh = false; let hasHistorical = false; for (const record of records) { const top = typeof record.evidenceFreshness === 'string' ? record.evidenceFreshness : typeof record.freshness === 'string' ? record.freshness : null; const detail = isObjectRecord(record.details) && typeof record.details.freshness === 'string' ? record.details.freshness : null; const normalized = normalizeEvidenceFreshness(top ?? detail ?? null); if (normalized === 'fresh') hasFresh = true; if (normalized === 'historical-reference') hasHistorical = true; } if (hasFresh) return 'fresh'; if (hasHistorical) return 'historical-reference'; return 'draft'; } function normalizeEvidenceFreshness(value: string | null): 'fresh' | 'historical-reference' | 'draft' { const normalized = String(value ?? '').trim().toLowerCase(); if (normalized === 'historical-reference' || normalized === 'historical_reference' || normalized === 'reference-only') { return 'historical-reference'; } if (normalized === 'draft') return 'draft'; return 'fresh'; } function dedupeCommandRuns(runs: readonly ClosurePacketCommandRun[]) { const seen = new Set<string>(); const output: ClosurePacketCommandRun[] = []; for (const run of runs) { const key = `${run.command}|${run.cwd}|${run.exitCode}|${run.stdoutSha256}|${run.stderrSha256}`; if (seen.has(key)) continue; seen.add(key); output.push(run); } return output; } function isObjectRecord(value: unknown): value is Record<string, unknown> { return Boolean(value) && typeof value === 'object' && !Array.isArray(value); } function requireValue(argv: string[], index: number, flag: string): string { const value = argv[index + 1]; if (!value || value.startsWith('--')) { throw new CliError('ATM_CLI_USAGE', `framework development command requires a value for ${flag}`, { exitCode: 2 }); } return value; }
