# Team Memory Shard

domain: team-agents
atoms: team.minimal-example
validators: node --strip-types scripts/validate-team-agents.ts --case minimal-team-agents-example

Use L1 when the task only needs Coordinator, Atomization Planner, Implementer, and Validator.

Promote to L2 or higher when reading, evidence collection, scope guarding, review, or knowledge retrieval needs a distinct role.
