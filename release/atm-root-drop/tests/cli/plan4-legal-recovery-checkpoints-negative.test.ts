import assert from 'node:assert/strict'; import { projectLegalRecoveryCheckpoints } from '../../packages/core/src/evidence/legal-recovery-checkpoints.ts'; const result = projectLegalRecoveryCheckpoints({ runId: 'fixture', authority: { authorityId: 'unsealed', sealed: false, digest: '' }, checkpoints: [{ checkpointId: 'x', phase: 'unknown', state: '' }] }); assert.equal(result.status, 'stale'); assert.ok(result.diagnostics.some((entry) => entry.code === 'ATM_RECOVERY_PHASE_UNSUPPORTED')); console.log('plan4 legal recovery checkpoints negative: PASS');
