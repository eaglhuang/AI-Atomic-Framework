import assert from'node:assert/strict';import{compileAcceptanceSpecMutation,validateAcceptanceSpecMutation}from'../../packages/core/src/evidence/acceptance-spec-mutation.ts';const a={specId:'s',specDigest:'sha256:d',sealed:true};const r=compileAcceptanceSpecMutation({authority:a,mutationSetId:'m',sourceDigest:'sha256:d',mutations:[{mutationId:'x',operator:'alien',scenarioId:'s',expected:'killed'}]});assert.equal(r.status,'blocked');assert.match(r.repairCommand??'',/restore the sealed acceptance specification/);assert.deepEqual(validateAcceptanceSpecMutation(r),{ok:false,diagnostics:['unsupported-operator:x']});const s=compileAcceptanceSpecMutation({authority:a,mutationSetId:'m',sourceDigest:'sha256:x',mutations:[{mutationId:'x',operator:'negate',scenarioId:'s',expected:'killed'}]});assert.equal(s.status,'stale');assert.match(s.repairCommand??'',/restore the sealed acceptance specification/);assert.deepEqual(validateAcceptanceSpecMutation(s),{ok:false,diagnostics:['source-authority-drift','result-digest-mismatch']});console.log('plan4 acceptance spec mutation negative: ok');
