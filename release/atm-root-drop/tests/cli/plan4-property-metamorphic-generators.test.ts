import assert from'node:assert/strict';import{readFileSync}from'node:fs';import Ajv2020 from'ajv/dist/2020.js';import{compilePropertyMetamorphicGenerators,replayPropertyMetamorphicGenerators,validatePropertyMetamorphicGenerators}from'../../packages/core/src/evidence/property-metamorphic-generators.ts';const a={authorityId:'a',digest:'sha256:s',sealed:true};const r=compilePropertyMetamorphicGenerators({authority:a,generatorId:'g',sourceDigest:'sha256:s',relations:[{relationId:'r2',inputDigest:'i',transformedDigest:'t',expectedRelation:'equal'},{relationId:'r1',inputDigest:'i',transformedDigest:'t',expectedRelation:'subset'}],provenance:{producer:'focused-test'}});const schema=JSON.parse(readFileSync(new URL('../../schemas/evidence/property-metamorphic-generators.schema.json',import.meta.url),'utf8'));const validateSchema=new Ajv2020({allErrors:true,strict:false}).compile(schema);assert.equal(r.status,'proven');assert.equal(validateSchema(r),true,JSON.stringify(validateSchema.errors));assert.equal(r.generatedCaseIds.length,2);assert.deepEqual(replayPropertyMetamorphicGenerators(r),r);assert.deepEqual(validatePropertyMetamorphicGenerators(r),{ok:true,diagnostics:[]});assert.deepEqual(validatePropertyMetamorphicGenerators({...r,generatedCaseIds:['case:tampered']}),{ok:false,diagnostics:['result-digest-mismatch']});console.log('plan4 property metamorphic generators: ok');
