import assert from'node:assert/strict';import{compileConcurrencyFuzzGenerators,validateConcurrencyFuzzGenerators}from'../../packages/core/src/evidence/concurrency-fuzz-generators.ts';const a={authorityId:'a',digest:'sha256:s',sealed:true};const r=compileConcurrencyFuzzGenerators({authority:a,generatorId:'g',sourceDigest:'sha256:s',scenarios:[{scenarioId:'s',schedule:['a'],fault:'alien',expected:'x'}]});assert.equal(r.status,'blocked');assert.match(r.repairCommand??'',/restore the sealed schedule authority/);assert.deepEqual(validateConcurrencyFuzzGenerators(r),{ok:false,diagnostics:['unsupported-fault:s']});const s=compileConcurrencyFuzzGenerators({authority:a,generatorId:'g',sourceDigest:'sha256:x',scenarios:[{scenarioId:'s',schedule:['a'],fault:'none',expected:'x'}]});assert.equal(s.status,'stale');assert.match(s.repairCommand??'',/restore the sealed schedule authority/);assert.deepEqual(validateConcurrencyFuzzGenerators(s),{ok:false,diagnostics:['source-authority-drift','result-digest-mismatch']});console.log('plan4 concurrency fuzz generators negative: ok');
