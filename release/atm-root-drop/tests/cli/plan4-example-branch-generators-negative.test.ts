import assert from'node:assert/strict';import{compileExampleBranchGenerators,validateExampleBranchGenerators}from'../../packages/core/src/evidence/example-branch-generators.ts';const a={authorityId:'a',digest:'sha256:s',sealed:true};const r=compileExampleBranchGenerators({authority:a,generatorId:'g',sourceDigest:'sha256:s',branches:[{branchId:'b',predicate:'x',outcome:'unsupported'}]});assert.equal(r.status,'blocked');assert.match(r.repairCommand??'',/restore/);const uncovered=compileExampleBranchGenerators({authority:a,generatorId:'g',sourceDigest:'sha256:s',branches:[{branchId:'b',predicate:'x',outcome:'uncovered'}]});assert.equal(uncovered.status,'blocked');const s=compileExampleBranchGenerators({authority:a,generatorId:'g',sourceDigest:'sha256:x',branches:[{branchId:'b',predicate:'x',outcome:'covered'}]});assert.equal(s.status,'stale');const proven=compileExampleBranchGenerators({authority:a,generatorId:'g',sourceDigest:'sha256:s',branches:[{branchId:'b',predicate:'x',outcome:'covered'}],provenance:{actor:'original'}});assert.deepEqual(validateExampleBranchGenerators({...proven,provenance:{actor:'substituted'}}),{ok:false,diagnostics:['result-digest-mismatch']});console.log('plan4 example branch generators negative: ok');
