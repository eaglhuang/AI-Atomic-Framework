import assert from'node:assert/strict';import{readFileSync}from'node:fs';import Ajv2020 from'ajv/dist/2020.js';import{compileCoverageCertificate,validateCoverageCertificate}from'../../packages/core/src/evidence/coverage-certificate.ts';const a={authorityId:'a',digest:'sha256:d',sealed:true as const};const r=compileCoverageCertificate({authority:a,certificateId:'c',sourceDigest:'sha256:d',obligations:[{obligationId:'o',covered:true,sourceDigest:'sha256:d',evidenceDigest:'sha256:e',disposition:'closed'}],provenance:{reviewer:'independent'}});const schema=JSON.parse(readFileSync(new URL('../../schemas/evidence/coverage-certificate.schema.json',import.meta.url),'utf8')),validateSchema=new Ajv2020({allErrors:true,strict:false}).compile(schema);assert.equal(r.status,'proven');assert.equal(r.covered,1);assert.equal(validateSchema(r),true,JSON.stringify(validateSchema.errors));assert.deepEqual(validateCoverageCertificate(r),{ok:true,diagnostics:[]});assert.equal(validateCoverageCertificate({...r,covered:0}).ok,false);const open=compileCoverageCertificate({authority:a,certificateId:'c',sourceDigest:'sha256:d',obligations:[{obligationId:'o',covered:false,sourceDigest:'sha256:d',evidenceDigest:'sha256:e',disposition:'open'}]});assert.equal(open.status,'blocked');assert.deepEqual(open.nonClaims,['does-not-claim:o']);console.log('plan4 coverage certificate: ok');
