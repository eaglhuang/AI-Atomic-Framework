import assert from'node:assert/strict';import{compileAntiGamingControl}from'../../packages/core/src/evidence/anti-gaming-control.ts';const a={authorityId:'a',digest:'sha256:d',sealed:true};const r=compileAntiGamingControl({authority:a,seedCommitment:'sha256:seed',sourceDigest:'sha256:d',controls:[{controlId:'c',seed:'s',expected:'red'}]});assert.equal(r.verdict,'fail-closed');const s=compileAntiGamingControl({authority:a,seedCommitment:'sha256:seed',sourceDigest:'sha256:x',controls:[{controlId:'c',seed:'s',expected:'red',observed:'red'}]});assert.equal(s.status,'stale');console.log('plan4 hidden negative controls: ok');
