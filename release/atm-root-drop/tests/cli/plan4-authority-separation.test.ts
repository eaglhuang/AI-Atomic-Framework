import assert from'node:assert/strict';import{compileOracleArbitration}from'../../packages/core/src/evidence/oracle-adjudication.ts';const a={authorityId:'a',digest:'sha256:d',sealed:true};const r=compileOracleArbitration({authority:a,arbitrationId:'x',sourceDigest:'sha256:d',writerRole:'same',authorityRole:'same',observations:[{oracleId:'o',verdict:'pass',evidenceDigest:'sha256:e',independent:true}]});assert.equal(r.status,'contradictory');const f=compileOracleArbitration({authority:a,arbitrationId:'x',sourceDigest:'sha256:d',writerRole:'writer',authorityRole:'oracle',observations:[{oracleId:'o',verdict:'pass',evidenceDigest:'sha256:e',independent:true},{oracleId:'p',verdict:'fail',evidenceDigest:'sha256:f',independent:true}]});assert.equal(f.verdict,'fail-closed');console.log('plan4 authority separation: ok');
