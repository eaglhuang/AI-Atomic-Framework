{
  "schemaId": "atm.governanceBacklogItem.v1",
  "id": "ATM-BUG-2026-07-27-245",
  "date": "2026-07-27",
  "repo": "AI-Atomic-Framework",
  "type": "Optimization",
  "severity": "Medium",
  "status": "Closed — source repair and focused validation verified; formal backlog closeout reconciled 2026-09-07",
  "area": "Task import fidelity",
  "finding": "A planning-side scope amendment for an already imported, actively claimed task can only replace the ledger via `tasks import --write --force`. ATM classifies that same-task, source-hash-verified re-import as protected emergency work requiring a human emergency approval lease, even when it only adds declared implementation and test paths.",
  "expectedBehavior": "Provide a normal governed amendment/re-import route that verifies the planning revision, preserves the existing actor, claim, lane, direction lock history, and audit trail, and applies an additive scope change without an emergency bypass. Destructive replacement or ownership-changing amendments should remain protected.",
  "evidenceOrRepro": "ATM-GOV-0266 historically required `tasks import --write --force` for an additive planning amendment. Current governed scope-amendment coverage passes normal audited lane parsing, additive and shrink operations, active-claim admission/reseal, and protected emergency boundaries; the normal path preserves ownership while incompatible changes remain protected.",
  "followUp": "Keep additive-versus-destructive amendment classification and active-claim preservation coverage aligned with future import contract changes."
}
