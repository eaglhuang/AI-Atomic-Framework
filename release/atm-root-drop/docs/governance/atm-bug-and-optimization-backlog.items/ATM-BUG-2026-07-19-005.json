{
  "schemaId": "atm.governanceBacklogItem.v1",
  "id": "ATM-BUG-2026-07-19-005",
  "date": "2026-07-19",
  "repo": "AI-Atomic-Framework",
  "type": "Bug",
  "severity": "Medium",
  "status": "Fixed",
  "area": "Task scope amendment metadata",
  "finding": "`tasks scope add --add <csv>` can still surface quote artifacts in closeback amendmentHistory even when the immediate scope-amendment command reports normalized addedPaths.",
  "expectedBehavior": "Every consumer of scope-amendment history should see the same normalized path list without shell quote artifacts.",
  "evidenceOrRepro": "Fixed and regression-verified. `parseScopeAddOptions` and the persisted-event parser both normalize a quoted CSV argument; `tests/cli/write-ticket-scope-amendment.test.ts` now writes a persisted `scope-amendment` transition event and asserts the closeback/status projection returns the same quote-free paths as the immediate scope-add response.",
  "followUp": "No remaining source repair. Formal backlog closeout and any historical receipt migration remain steward work."
}
