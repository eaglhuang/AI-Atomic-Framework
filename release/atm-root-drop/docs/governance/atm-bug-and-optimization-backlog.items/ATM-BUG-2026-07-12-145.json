{
  "schemaId": "atm.governanceBacklogItem.v1",
  "id": "ATM-BUG-2026-07-12-145",
  "date": "2026-07-12",
  "repo": "AI-Atomic-Framework",
  "type": "Documentation / AI routing",
  "severity": "Medium",
  "status": "Closed — planning-root preference and canonical configuration guidance verified 2026-09-05",
  "area": "Cross-repo planning-root configuration",
  "finding": "`.atm/config.json` enables external task-ledger mirroring and contains a broker evidence path under `../3KLife`, but `next --prompt` still fails with `ATM_PLANNING_ROOT_MISSING` unless the operator separately knows to set `ATM_PLANNING_REPO_ROOT`; the required configuration key and expected root are not surfaced by preflight.",
  "expectedBehavior": "Bootstrap/doctor/next should discover or validate the configured planning repository, or provide a repo-local exact configuration command and explain why planning-root resolution is required before task routing.",
  "evidenceOrRepro": "The historical missing-root report is not reproducible against the current source. `node --strip-types packages/cli/src/commands/next/__tests__/planning-root-preference.test.ts` and `node --strip-types scripts/validate-planning-root-canonical-preference.ts` both exited 0 on 2026-09-05. The regression covers configured roots, explicit `ATM_PLANNING_REPO_ROOT`, canonical preference, ambiguity, and the operator-facing `--planning-root` recovery guidance.",
  "followUp": "Keep the planning-root preference regression and canonical-preference validator in the CLI validation set. A future change that removes configured-root discovery or turns ambiguity into silent guessing must reopen this item."
}
