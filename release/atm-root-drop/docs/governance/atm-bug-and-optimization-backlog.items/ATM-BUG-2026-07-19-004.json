{
  "schemaId": "atm.governanceBacklogItem.v1",
  "id": "ATM-BUG-2026-07-19-004",
  "date": "2026-07-19",
  "repo": "AI-Atomic-Framework",
  "type": "Bug",
  "severity": "High",
  "status": "Resolved — paired-evidence fail-closed repair verified; broader Analyzer v3 product evidence remains separate",
  "area": "Auto-batch analyzer / Performance evidence",
  "finding": "The auto-batch rollout analyzer could classify a run as improved from synthetic broker-ticket events when batchRate was at least 0.70 and build/projection ticket counts were within limits, without requiring paired control/treatment lane evidence. ATM-GOV-0179's focused test exercised this synthetic path rather than a real paired A/B dogfood run.",
  "expectedBehavior": "An improved verdict must require real plan-run control and treatment samples joined through durable task, wave, broker, checkpoint, and lane-session evidence. Missing makespan, throughput, cost, sample-size, or safety evidence must produce inconclusive, never improved.",
  "evidenceOrRepro": "Red/green regression `strict-paired-ab-dogfood.test.ts` now supplies three synthetic broker-ticket lane events and proves they produce `inconclusive`, not `improved`. The analyzer now requires a same-wave control/treatment pair with outcome references in addition to the generated-write thresholds. The paired auto-batch fixture retains `improved`; typecheck, encoding guard, and analyzer failure-matrix regression pass.",
  "followUp": "The larger performance claim remains governed by Analyzer v3 / real paired A/B evidence: makespan, throughput, cost, safety, sample thresholds, deterministic replay, and prospective dogfood are not fabricated or closed by this fail-open repair. The existing v3 overall test currently observes its default sealed cohort as inconclusive; that fixture/rollout inconsistency is outside this bounded analyzer repair."
}
