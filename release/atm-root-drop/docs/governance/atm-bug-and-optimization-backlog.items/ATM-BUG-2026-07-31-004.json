{
  "schemaId": "atm.governanceBacklogItem.v1",
  "id": "ATM-BUG-2026-07-31-004",
  "date": "2026-07-31",
  "repo": "AI-Atomic-Framework",
  "type": "Bug",
  "severity": "High",
  "status": "Closed — governed dry-run non-mutation regression verified",
  "area": "Git governance",
  "finding": "`node atm.mjs git commit --dry-run --auto-stage` reported and created commit `393d65a3b07b2715aa178d3aa546ed503bdbbf9c` during ATM-GOV-0271 closeout instead of remaining non-mutating.",
  "expectedBehavior": "The dry-run path must not create commits, advance HEAD, stage new files, or mutate git-head evidence. It should only resolve and report the task-scoped commit bundle.",
  "evidenceOrRepro": "Historical reproduction: `node atm.mjs git commit --actor codex-skl-captain --task ATM-GOV-0271 --message \"feat(taskflow): add close saga recovery lanes\" --auto-stage --dry-run --json` incorrectly returned ATM_GIT_COMMIT_OK and created 393d65a3b07b2715aa178d3aa546ed503bdbbf9c. Current regression: `node --strip-types scripts/validate-governance-commands.ts` completed exit 0. Its framework auto-stage dry-run fixture records HEAD before the command, requires ATM_GIT_COMMIT_FRAMEWORK_DRY_RUN, then asserts identical HEAD and an empty cached diff afterwards.",
  "followUp": "Closed. Keep the governance-command fixture assertion that dry-run neither creates a commit nor mutates the index; any regression must be recorded with HEAD/index before-and-after evidence."
}
