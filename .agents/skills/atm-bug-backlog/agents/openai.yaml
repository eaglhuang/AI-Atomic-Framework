interface:
  display_name: "ATM Bug Backlog Router"
  short_description: "Record bugs and optimization items in the owning repo backlog."
  default_prompt: "Classify whether this is an ATM bug or project bug, then record it in the correct backlog."
